// LoanServiceSpecTests.cs -- generated xUnit.v3 spec tests from `test("...")` DSL blocks.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
using LoanService.Data;
using LoanService.Enums;
using LoanService.Models.LoanDb;
using Microsoft.EntityFrameworkCore;
using Xunit;

namespace LoanService.Tests.Spec;

public sealed class LoanServiceSpecTests
{
    [Fact]
    public async Task ComputedIsOverdueIsTrueWhenStatusIsOverdue()
    {
        // computed isOverdue is true when status is Overdue
        var options = new DbContextOptionsBuilder<LoanServiceDbContext>()
            .UseInMemoryDatabase(
                databaseName: $"LoanServiceSpecTests_ComputedIsOverdueIsTrueWhenStatusIsOverdue_{Guid.NewGuid()}"
            )
            .Options;
        await using var context = new LoanServiceDbContext(options);
        var loan = new Loan
        {
            BookId = Guid.NewGuid(),
            MemberId = Guid.NewGuid(),
            Status = LoanStatus.Overdue,
            DueDate = DateTimeOffset.UtcNow.AddDays(7),
        };
        context.Loans.Add(loan);
        await context.SaveChangesAsync();
        await context.SaveChangesAsync();
        Assert.True((loan.IsOverdue == true), "Assertion failed: (loan.IsOverdue == true)");
    }

    [Fact]
    public async Task ComputedIsOverdueIsFalseWhenStatusIsReturned()
    {
        // computed isOverdue is false when status is Returned
        var options = new DbContextOptionsBuilder<LoanServiceDbContext>()
            .UseInMemoryDatabase(
                databaseName: $"LoanServiceSpecTests_ComputedIsOverdueIsFalseWhenStatusIsReturned_{Guid.NewGuid()}"
            )
            .Options;
        await using var context = new LoanServiceDbContext(options);
        var loan = new Loan
        {
            BookId = Guid.NewGuid(),
            MemberId = Guid.NewGuid(),
            Status = LoanStatus.Returned,
            DueDate = DateTimeOffset.UtcNow.AddDays(-7),
        };
        context.Loans.Add(loan);
        await context.SaveChangesAsync();
        await context.SaveChangesAsync();
        Assert.True((loan.IsOverdue == false), "Assertion failed: (loan.IsOverdue == false)");
    }
}
