// AppServiceLocator.cs -- static IServiceProvider bridge for DI-free contexts.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
#nullable enable
using System;
using Microsoft.Extensions.DependencyInjection;

namespace BookService.Messaging;

/// <summary>
/// Exposes DI-registered singletons to call sites with no constructor/
/// field-injection surface of their own -- module/service-level `fn` bodies
/// render as `public static` methods on ONE static `Functions` class with no
/// surrounding DI-constructed instance. Initialized once, immediately after
/// the host's IServiceProvider is built (see Program.cs).
/// </summary>
public static class AppServiceLocator
{
    private static IServiceProvider? _provider;

    public static void Initialize(IServiceProvider provider)
    {
        _provider = provider;
    }

    public static T GetRequiredService<T>()
        where T : notnull
    {
        if (_provider is null)
        {
            throw new InvalidOperationException(
                "AppServiceLocator.Initialize(...) has not been called yet -- Program.cs must "
                    + "call it immediately after the host's IServiceProvider is built, before any "
                    + "request, background service, or scheduled job can run."
            );
        }
        return _provider.GetRequiredService<T>();
    }
}
