// UserService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against User's own
// persistence set (Users) on the project's single
// BookServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using System.Collections.Generic;
using System.Threading.Tasks;
using BookService.Data;
using Microsoft.EntityFrameworkCore;

namespace BookService.Services.BookDb;

public sealed class UserService
{
    private readonly BookServiceDbContext _context;

    public UserService(BookServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::BookService.Models.BookDb.User?> GetByIdAsync(Guid id)
    {
        return await _context.Users.FindAsync(id);
    }

    public async Task<List<global::BookService.Models.BookDb.User>> GetAllAsync()
    {
        return await _context.Users.ToListAsync();
    }

    public async Task<global::BookService.Models.BookDb.User> CreateAsync(
        global::BookService.Models.BookDb.User entity
    )
    {
        _context.Users.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::BookService.Models.BookDb.User?> UpdateAsync(
        Guid id,
        global::BookService.Models.BookDb.User updated
    )
    {
        var existing = await _context.Users.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.Users.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.Users.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
