/**
 * Base class for business logic errors.
 * Extend this to create domain-specific errors with RFC 7807 type and title.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { HttpException, HttpStatus } from '@nestjs/common';

export class BusinessError extends HttpException {
  readonly errorType: string;
  readonly errorTitle: string;

  constructor(
    errorType: string,
    errorTitle: string,
    detail: string,
    status: HttpStatus = HttpStatus.UNPROCESSABLE_ENTITY,
  ) {
    super(detail, status);
    this.errorType = errorType;
    this.errorTitle = errorTitle;
  }
}

export class EntityNotFoundError extends BusinessError {
  constructor(entityName: string, identifier: string) {
    super(
      'entity-not-found',
      `${entityName} Not Found`,
      `${entityName} with identifier '${identifier}' was not found.`,
      HttpStatus.NOT_FOUND,
    );
  }
}

export class ConflictError extends BusinessError {
  constructor(detail: string) {
    super('conflict', 'Conflict', detail, HttpStatus.CONFLICT);
  }
}
