package library_member_service.model.member_db;

import static org.junit.jupiter.api.Assertions.*;

import jakarta.validation.ConstraintViolation;
import jakarta.validation.Validation;
import jakarta.validation.Validator;
import java.util.Set;
import library_member_service.dto.member_db.MemberCreateRequest;
import org.hibernate.validator.HibernateValidator;
import org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator;
import org.junit.jupiter.api.Test;

/**
 * Generated tests for Member: field round-trip, update-assertion, relationship navigation, and
 * (when the entity's CreateRequest DTO carries at least one @NotNull field) Bean Validation
 * constraint-violation tests.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
class MemberTest {

  @Test
  void fieldRoundTripAssignsAndReturnsSameValue() {
    Member instance = MemberTestFactory.create();
    instance.setName("test_value");
    assertEquals("test_value", instance.getName());
    instance.setEmail("user@example.com");
    assertEquals("user@example.com", instance.getEmail());
    instance.setEmailVerificationToken("test_value");
    assertEquals("test_value", instance.getEmailVerificationToken());
  }

  @Test
  void fieldUpdateAssignsDistinctValue() {
    Member instance = MemberTestFactory.create();
    instance.setName("test_value");
    instance.setEmail("user@example.com");
    instance.setEmailVerificationToken("test_value");
    instance.setName("alt_value");
    assertEquals("alt_value", instance.getName());
    instance.setEmail("alt@example.com");
    assertEquals("alt@example.com", instance.getEmail());
    instance.setEmailVerificationToken("alt_value");
    assertEquals("alt_value", instance.getEmailVerificationToken());
  }

  private static Validator buildValidator() {
    return Validation.byProvider(HibernateValidator.class)
        .configure()
        .messageInterpolator(new ParameterMessageInterpolator())
        .buildValidatorFactory()
        .getValidator();
  }

  @Test
  void requiredFieldLeftNullFailsBeanValidation() {
    Validator validator = buildValidator();
    MemberCreateRequest invalid = new MemberCreateRequest(null, "user@example.com", "test_value");
    Set<ConstraintViolation<MemberCreateRequest>> violations = validator.validate(invalid);
    assertFalse(violations.isEmpty(), "expected a violation for a null required field");
  }

  @Test
  void allRequiredFieldsPresentPassesBeanValidation() {
    Validator validator = buildValidator();
    MemberCreateRequest valid =
        new MemberCreateRequest("test_value", "user@example.com", "test_value");
    Set<ConstraintViolation<MemberCreateRequest>> violations = validator.validate(valid);
    assertTrue(violations.isEmpty(), "expected no violations, got: " + violations);
  }
}
