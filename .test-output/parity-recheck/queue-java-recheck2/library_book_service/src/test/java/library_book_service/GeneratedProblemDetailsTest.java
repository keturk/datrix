package library_book_service;

import static org.hamcrest.Matchers.containsString;
import static org.hamcrest.Matchers.not;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.content;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.jsonPath;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

import library_book_service.errors.GlobalExceptionHandler;
import org.junit.jupiter.api.Test;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

/**
 * Generated proof (D8): an UNDECLARED exception reaching {@link GlobalExceptionHandler} yields the
 * generated RFC 7807 taxonomy -- never Spring's default whitelabel page or a leaked stack trace --
 * and a DECLARED exception yields its own declared status and type urn. Runs a REAL {@link MockMvc}
 * request through the REAL, generated {@code GlobalExceptionHandler} (standalone setup -- no full
 * application context needed to prove the advice's own behavior).
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
class GeneratedProblemDetailsTest {

  @RestController
  static class ProbeController {

    @GetMapping("/__probe/undeclared")
    public String undeclared() {
      throw new IllegalArgumentException("boom");
    }
  }

  private final MockMvc mockMvc =
      MockMvcBuilders.standaloneSetup(new ProbeController())
          .setControllerAdvice(new GlobalExceptionHandler())
          .build();

  @Test
  void undeclaredExceptionYieldsGeneratedTaxonomyNeverWhitelabel() throws Exception {
    mockMvc
        .perform(get("/__probe/undeclared"))
        .andExpect(status().is(500))
        .andExpect(content().contentType(MediaType.valueOf("application/problem+json")))
        .andExpect(jsonPath("$.title").value("Internal Server Error"))
        .andExpect(jsonPath("$.type").value("urn:datrix:error:internal"))
        .andExpect(content().string(not(containsString("Whitelabel"))))
        .andExpect(content().string(not(containsString("\tat "))));
  }
}
