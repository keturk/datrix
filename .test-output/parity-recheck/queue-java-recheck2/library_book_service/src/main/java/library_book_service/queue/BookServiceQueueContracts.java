package library_book_service.queue;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;
import library_book_service.errors.ContractViolationError;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Event contract validation functions, auto-generated from {@code ensure} clauses in event
 * declarations. Each function validates preconditions before publication.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public final class BookServiceQueueContracts {

  private static final Logger logger = LoggerFactory.getLogger(BookServiceQueueContracts.class);

  private BookServiceQueueContracts() {}

  /** Validate contracts for the IndexBook event. */
  public static void validateIndexBook(UUID bookId, String title, String author) {
    if (!((title.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("title", title);
      logger.error(
          "contract_violation event={} clause={} actual={}", "IndexBook", "ensure_0", actual);
      throw new ContractViolationError("IndexBook", "ensure_0", actual);
    }
    if (!((author.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("author", author);
      logger.error(
          "contract_violation event={} clause={} actual={}", "IndexBook", "ensure_1", actual);
      throw new ContractViolationError("IndexBook", "ensure_1", actual);
    }
  }

  /** Validate contracts for the GenerateBookCover event. */
  public static void validateGenerateBookCover(UUID bookId, String title) {
    if (!((title.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("title", title);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "GenerateBookCover",
          "ensure_0",
          actual);
      throw new ContractViolationError("GenerateBookCover", "ensure_0", actual);
    }
  }

  /** Validate contracts for the SendBookNotification event. */
  public static void validateSendBookNotification(
      UUID bookId, String recipientEmail, String message) {
    if (!((recipientEmail.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("recipientEmail", recipientEmail);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "SendBookNotification",
          "ensure_0",
          actual);
      throw new ContractViolationError("SendBookNotification", "ensure_0", actual);
    }
    if (!((message.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("message", message);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "SendBookNotification",
          "ensure_1",
          actual);
      throw new ContractViolationError("SendBookNotification", "ensure_1", actual);
    }
  }
}
