package library_notification_service.clients;

import io.micrometer.core.instrument.Counter;
import io.micrometer.core.instrument.Metrics;
import org.springframework.stereotype.Component;

/**
 * Micrometer counters for this service's dependency clients' resilience events.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component
public class ResilienceMetrics {

  /** Record a circuit breaker transitioning to {@code toState} for {@code dependency}. */
  public void recordCircuitBreakerStateTransition(String dependency, String toState) {
    Counter.builder("resilience.circuit_breaker.transitions")
        .tag("dependency", dependency)
        .tag("state", toState)
        .register(Metrics.globalRegistry)
        .increment();
  }

  /** Record retry attempt number {@code attempt} for {@code dependency}. */
  public void recordRetryAttempt(String dependency, int attempt) {
    Counter.builder("resilience.retry.attempts")
        .tag("dependency", dependency)
        .register(Metrics.globalRegistry)
        .increment();
  }
}
