package library_member_service.model.member_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import jakarta.persistence.Transient;
import library_member_service.enums.MembershipType;

/**
 * Member JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "members",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_members_first_name_max_length",
          constraint = "length(first_name) <= 100"),
      @CheckConstraint(
          name = "ck_members_last_name_max_length",
          constraint = "length(last_name) <= 100"),
      @CheckConstraint(
          name = "ck_members_membership_number_max_length",
          constraint = "length(membership_number) <= 20")
    })
public class Member extends BaseEntity {

  @Column(name = "firstName", columnDefinition = "varchar(100)", nullable = false)
  private String firstName;

  @Column(name = "lastName", columnDefinition = "varchar(100)", nullable = false)
  private String lastName;

  @Column(name = "email", columnDefinition = "varchar(320)", nullable = false, unique = true)
  private String email;

  @Column(
      name = "membershipNumber",
      columnDefinition = "varchar(20)",
      nullable = false,
      unique = true)
  private String membershipNumber;

  @Column(name = "membershipType", columnDefinition = "varchar(255)", nullable = false)
  private MembershipType membershipType = MembershipType.STANDARD;

  public String getFirstName() {
    return this.firstName;
  }

  public void setFirstName(String firstName) {
    this.firstName = firstName;
  }

  public String getLastName() {
    return this.lastName;
  }

  public void setLastName(String lastName) {
    this.lastName = lastName;
  }

  public String getEmail() {
    return this.email;
  }

  public void setEmail(String email) {
    this.email = email;
  }

  public String getMembershipNumber() {
    return this.membershipNumber;
  }

  public void setMembershipNumber(String membershipNumber) {
    this.membershipNumber = membershipNumber;
  }

  public MembershipType getMembershipType() {
    return this.membershipType;
  }

  public void setMembershipType(MembershipType membershipType) {
    this.membershipType = membershipType;
  }

  /** Computed: "%s %s".formatted(this.getFirstName(), this.getLastName()) */
  @Transient
  public String getFullName() {
    return "%s %s".formatted(this.getFirstName(), this.getLastName());
  }

  /**
   * Build a new {@link Member} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.Member.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new Member(...)`.
   */
  public static Member fromMap(java.util.Map<String, Object> data) {
    Member result = new Member();
    if (data.containsKey("firstName")) {
      Object __v = data.get("firstName");
      result.setFirstName((String) __v);
    }
    if (data.containsKey("lastName")) {
      Object __v = data.get("lastName");
      result.setLastName((String) __v);
    }
    if (data.containsKey("email")) {
      Object __v = data.get("email");
      result.setEmail((String) __v);
    }
    if (data.containsKey("membershipNumber")) {
      Object __v = data.get("membershipNumber");
      result.setMembershipNumber((String) __v);
    }
    if (data.containsKey("membershipType")) {
      Object __v = data.get("membershipType");
      result.setMembershipType((MembershipType) __v);
    }
    return result;
  }
}
