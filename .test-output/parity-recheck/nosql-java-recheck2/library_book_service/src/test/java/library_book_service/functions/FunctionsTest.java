package library_book_service.functions;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

/**
 * Generated tests for BookService's module-level functions.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
class FunctionsTest {

  @Test
  void calculateLateFeeExistsWithExpectedParameterCount() {
    long matches =
        java.util.Arrays.stream(Functions.class.getDeclaredMethods())
            .filter(m -> m.getName().equals("calculateLateFee"))
            .filter(m -> m.getParameterCount() == 1)
            .count();
    assertTrue(matches > 0, "Functions.calculateLateFee with 1 parameter(s) not found");
  }

  @Test
  void formatBookReferenceExistsWithExpectedParameterCount() {
    long matches =
        java.util.Arrays.stream(Functions.class.getDeclaredMethods())
            .filter(m -> m.getName().equals("formatBookReference"))
            .filter(m -> m.getParameterCount() == 3)
            .count();
    assertTrue(matches > 0, "Functions.formatBookReference with 3 parameter(s) not found");
  }
}
