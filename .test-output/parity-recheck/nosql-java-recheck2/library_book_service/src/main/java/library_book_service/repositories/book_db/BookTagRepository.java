package library_book_service.repositories.book_db;

import java.util.List;
import java.util.Optional;
import java.util.UUID;
import library_book_service.model.book_db.BookTag;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

/**
 * BookTag Spring Data JPA repository.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public interface BookTagRepository
    extends JpaRepository<BookTag, UUID>, JpaSpecificationExecutor<BookTag> {

  List<BookTag> findByBookId(UUID bookId);

  List<BookTag> findByTagId(UUID tagId);

  Optional<BookTag> findByBookIdAndTagId(UUID bookId, UUID tagId);

  Page<BookTag> findByBookId(UUID bookId, Pageable pageable);

  Page<BookTag> findByTagId(UUID tagId, Pageable pageable);
}
