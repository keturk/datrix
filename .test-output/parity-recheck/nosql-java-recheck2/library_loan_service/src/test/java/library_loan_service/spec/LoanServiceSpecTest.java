package library_loan_service.spec;

import static library_loan_service.support.MapSupport._datrixOrderedMap;
import static org.junit.jupiter.api.Assertions.assertTrue;

import java.time.LocalDateTime;
import java.util.Objects;
import library_loan_service.enums.LoanStatus;
import library_loan_service.model.loan_db.Loan;
import library_loan_service.repositories.loan_db.LoanRepository;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.transaction.annotation.Transactional;

/**
 * Generated from this service's {@code test("...")} DSL blocks. Exercises the service through the
 * real Spring context -- {@code assert}/{@code throws} lower to JUnit 5 assertions, {@code
 * emitted(...)} asserts against the real, non-mocked {@link EventSpyRecorder}.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@SpringBootTest
@ActiveProfiles("test")
@Transactional
@Rollback
class LoanServiceSpecTest {

  @Autowired private LoanRepository loanRepository;

  /** computed isOverdue is true when status is Overdue */
  @Test
  @DisplayName("computed isOverdue is true when status is Overdue")
  void computedIsoverdueIsTrueWhenStatusIsOverdue() {
    var loan =
        loanRepository.saveAndFlush(
            Loan.fromMap(
                _datrixOrderedMap(
                    "bookId",
                    uuid(),
                    "memberId",
                    uuid(),
                    "status",
                    LoanStatus.OVERDUE,
                    "dueDate",
                    LocalDateTime.now().addDays(7))));
    loanRepository.saveAndFlush(loan);
    assertTrue((Objects.equals(loan.getIsOverdue(), true)));
  }

  /** computed isOverdue is false when status is Returned */
  @Test
  @DisplayName("computed isOverdue is false when status is Returned")
  void computedIsoverdueIsFalseWhenStatusIsReturned() {
    var loan =
        loanRepository.saveAndFlush(
            Loan.fromMap(
                _datrixOrderedMap(
                    "bookId",
                    uuid(),
                    "memberId",
                    uuid(),
                    "status",
                    LoanStatus.RETURNED,
                    "dueDate",
                    LocalDateTime.now().subtractDays(7))));
    loanRepository.saveAndFlush(loan);
    assertTrue((Objects.equals(loan.getIsOverdue(), false)));
  }
}
