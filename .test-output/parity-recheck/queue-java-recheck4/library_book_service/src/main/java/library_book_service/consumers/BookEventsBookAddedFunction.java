package library_book_service.consumers;

import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: book_events_book_added (subscription 'BookEvents', event
 * 'BookAdded').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersBookEventsBookAdded")
public class BookEventsBookAddedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger = LoggerFactory.getLogger(BookEventsBookAddedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.info(
        "subscription_handler_start handler={} event={}", "book_events_book_added", "BookAdded");
    UUID bookId = (UUID) input.get("bookId");
    String title = (String) input.get("title");
    try {
      logger.info("New book added");
      return null;
    } finally {
      logger.info(
          "subscription_handler_complete handler={} event={}",
          "book_events_book_added",
          "BookAdded");
    }
  }
}
