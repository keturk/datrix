package library_notification_service.model.notif_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import java.util.UUID;

/**
 * NotificationDeliveryLog JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "notification_delivery_logs",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_notification_delivery_logs_message_preview_max_length",
          constraint = "length(message_preview) <= 2000")
    })
public class NotificationDeliveryLog extends BaseEntity {

  @Column(name = "bookId", columnDefinition = "uuid", nullable = false)
  private UUID bookId;

  @Column(name = "recipientEmail", columnDefinition = "varchar(320)", nullable = false)
  private String recipientEmail;

  @Column(name = "messagePreview", columnDefinition = "varchar(2000)", nullable = false)
  private String messagePreview;

  public UUID getBookId() {
    return this.bookId;
  }

  public void setBookId(UUID bookId) {
    this.bookId = bookId;
  }

  public String getRecipientEmail() {
    return this.recipientEmail;
  }

  public void setRecipientEmail(String recipientEmail) {
    this.recipientEmail = recipientEmail;
  }

  public String getMessagePreview() {
    return this.messagePreview;
  }

  public void setMessagePreview(String messagePreview) {
    this.messagePreview = messagePreview;
  }

  /**
   * Build a new {@link NotificationDeliveryLog} from a DSL object-literal map, setting only the
   * keys present in {@code data} (a key absent from {@code data} leaves the corresponding field at
   * its normal Java default -- {@code null} unless the field declares its own initializer above).
   * Keys are the AS-AUTHORED DSL field name (Java's own object-literal emission, {@code
   * _datrixOrderedMap(...)}, preserves author-declared spelling -- no case conversion), which for a
   * camelCase-authored DSL field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.NotificationDeliveryLog.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new NotificationDeliveryLog(...)`.
   */
  public static NotificationDeliveryLog fromMap(java.util.Map<String, Object> data) {
    NotificationDeliveryLog result = new NotificationDeliveryLog();
    if (data.containsKey("bookId")) {
      Object __v = data.get("bookId");
      result.setBookId((UUID) __v);
    }
    if (data.containsKey("recipientEmail")) {
      Object __v = data.get("recipientEmail");
      result.setRecipientEmail((String) __v);
    }
    if (data.containsKey("messagePreview")) {
      Object __v = data.get("messagePreview");
      result.setMessagePreview((String) __v);
    }
    return result;
  }
}
