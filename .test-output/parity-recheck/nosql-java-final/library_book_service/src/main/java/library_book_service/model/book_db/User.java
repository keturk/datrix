package library_book_service.model.book_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.EntityListeners;
import jakarta.persistence.Table;
import library_book_service.enums.UserRole;

/**
 * User JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@EntityListeners({UserLifecycleListener.class})
@Entity
@Table(
    name = "users",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(name = "ck_users_email_max_length", constraint = "length(email) <= 255")
    })
public class User extends BaseEntity {

  @Column(name = "email", columnDefinition = "varchar(255)", nullable = false, unique = true)
  private String email;

  @Column(name = "passwordHash", columnDefinition = "varchar(255)", nullable = false)
  private String passwordHash;

  @Column(name = "role", columnDefinition = "varchar(255)", nullable = false)
  private UserRole role = UserRole.MEMBER;

  public String getEmail() {
    return this.email;
  }

  public void setEmail(String email) {
    this.email = email;
  }

  public String getPasswordHash() {
    return this.passwordHash;
  }

  public void setPasswordHash(String passwordHash) {
    this.passwordHash = passwordHash;
  }

  public UserRole getRole() {
    return this.role;
  }

  public void setRole(UserRole role) {
    this.role = role;
  }

  /**
   * Build a new {@link User} from a DSL object-literal map, setting only the keys present in {@code
   * data} (a key absent from {@code data} leaves the corresponding field at its normal Java default
   * -- {@code null} unless the field declares its own initializer above). Keys are the AS-AUTHORED
   * DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)}, preserves
   * author-declared spelling -- no case conversion), which for a camelCase-authored DSL field is
   * identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.User.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new User(...)`.
   */
  public static User fromMap(java.util.Map<String, Object> data) {
    User result = new User();
    if (data.containsKey("email")) {
      Object __v = data.get("email");
      result.setEmail((String) __v);
    }
    if (data.containsKey("passwordHash")) {
      Object __v = data.get("passwordHash");
      result.setPasswordHash((String) __v);
    }
    if (data.containsKey("role")) {
      Object __v = data.get("role");
      result.setRole((UserRole) __v);
    }
    return result;
  }
}
