package library_member_service.spec;

import static library_member_service.support.MapSupport._datrixOrderedMap;
import static org.junit.jupiter.api.Assertions.assertTrue;

import java.util.Objects;
import library_member_service.enums.MembershipType;
import library_member_service.model.member_db.Member;
import library_member_service.repositories.member_db.MemberRepository;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.annotation.Rollback;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.transaction.annotation.Transactional;

/**
 * Generated from this service's {@code test("...")} DSL blocks. Exercises the service through the
 * real Spring context -- {@code assert}/{@code throws} lower to JUnit 5 assertions, {@code
 * emitted(...)} asserts against the real, non-mocked {@link EventSpyRecorder}.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@SpringBootTest
@ActiveProfiles("test")
@Transactional
@Rollback
class MemberServiceSpecTest {

  @Autowired private MemberRepository memberRepository;

  /** computed fullName concatenates first and last name */
  @Test
  @DisplayName("computed fullName concatenates first and last name")
  void computedFullnameConcatenatesFirstAndLastName() {
    var member =
        memberRepository.saveAndFlush(
            Member.fromMap(
                _datrixOrderedMap(
                    "firstName",
                    "Alice",
                    "lastName",
                    "Johnson",
                    "email",
                    "alice.johnson@example.com",
                    "membershipNumber",
                    "MEM001SPECFN",
                    "membershipType",
                    MembershipType.STANDARD)));
    memberRepository.saveAndFlush(member);
    assertTrue((Objects.equals(member.getFullName(), "Alice Johnson")));
  }
}
