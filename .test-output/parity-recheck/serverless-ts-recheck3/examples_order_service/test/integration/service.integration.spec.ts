/**
 * Service integration tests for ExamplesOrderService.
 * Tests the full HTTP → Controller → Service → Repository → DB pipeline
 * using AppModule bootstrapped against a test database.
 *
 * Requires TEST_DATABASE_URL environment variable.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Test, TestingModule } from '@nestjs/testing';
import { INestApplication } from '@nestjs/common';
import * as request from 'supertest';
import { MikroORM } from '@mikro-orm/core';
import { AppModule } from '../../src/app.module';

describe('ExamplesOrderService Service Integration', () => {
  let app: INestApplication;
  let orm: MikroORM;

  beforeAll(async () => {
    process.env['DB_DATABASE_URL'] =
      process.env['TEST_DATABASE_URL'] ||
      'postgresql://test:test@localhost:5432/test_db';
    process.env['JWT_SECRET'] = 'datrix-test-jwt-secret';
    process.env['NODE_ENV'] = 'test';

    const moduleFixture: TestingModule = await Test.createTestingModule({
      imports: [AppModule],
    }).compile();

    app = moduleFixture.createNestApplication();
    await app.init();
    orm = app.get(MikroORM);
  });

  afterEach(async () => {
    const conn = orm.em.getConnection();
    await conn.execute('TRUNCATE TABLE "orders" CASCADE');
  });

  afterAll(async () => {
    await app.close();
  });

  describe('Order CRUD workflow', () => {
  });
});
