{
  "comments": {
    "do_not_remove": "Removing this breaks dependency sharing and creates duplicate node_modules in every service",
    "shared_deps": "ALL platforms (docker, aws, azure) and ALL examples share ONE node_modules",
    "why": "Without this workspace: 47 examples \u00d7 3 platforms = 141 node_modules folders (gigabytes wasted)",
    "with_workspace": "ONE node_modules at typescript/node_modules shared by everything",
    "workspace": "This package.json creates a pnpm workspace at the TypeScript language level"
  },
  "description": "Datrix TypeScript workspace - shared node_modules for all platforms and examples",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "name": "datrix-typescript-workspace",
  "packageManager": "pnpm@9.15.9",
  "private": true,
  "scripts": {
    "build:all": "pnpm -r --workspace-concurrency=4 run build",
    "clean": "pnpm -r exec -- rm -rf dist node_modules",
    "clean:cache": "rm -rf node_modules .pnpm-store",
    "install:all": "pnpm install",
    "test:all": "pnpm -r --workspace-concurrency=4 run test"
  },
  "version": "1.0.0"
}
