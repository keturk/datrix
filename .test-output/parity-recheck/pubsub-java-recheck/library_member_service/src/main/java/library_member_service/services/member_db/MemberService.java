package library_member_service.services.member_db;

import java.util.List;
import java.util.Optional;
import java.util.UUID;
import library_member_service.model.member_db.Member;
import library_member_service.repositories.member_db.MemberRepository;
import library_member_service.services.ServiceBase;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * Member service -- request-scoped unit-of-work wrapper over {@link MemberRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class MemberService extends ServiceBase {

  private final MemberRepository memberRepository;

  public MemberService(MemberRepository memberRepository) {
    this.memberRepository = memberRepository;
  }

  public Optional<Member> get(UUID id) {
    return this.memberRepository.findById(id);
  }

  public List<Member> list() {
    return this.memberRepository.findAll();
  }

  public Optional<Member> getByEmail(String email) {
    return this.memberRepository.findByEmail(email);
  }

  public Optional<Member> getByMembershipNumber(String membershipNumber) {
    return this.memberRepository.findByMembershipNumber(membershipNumber);
  }

  @Transactional
  public Member create(Member entity) {
    return this.memberRepository.save(entity);
  }

  @Transactional
  public Member update(Member entity) {
    return this.memberRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.memberRepository.deleteById(id);
  }
}
