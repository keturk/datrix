// BookServiceApiApiTests.cs -- generated xUnit.v3 API smoke + auth-boundary tests.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Drives the real ASP.NET Core pipeline in-process via WebApplicationFactory<Program>
// -- one smoke [Fact] per endpoint asserting a real HTTP
// status: 401 Unauthorized when the endpoint requires authentication (no principal
// token supplied), otherwise a non-5xx status band (never a 200-only happy-path
// assumption -- an empty test database legitimately 404s a by-id lookup).
using System.Net;
using System.Net.Http;
using Microsoft.AspNetCore.Mvc.Testing;
using Xunit;

namespace BookService.Tests.ApiTests;

public sealed class BookServiceApiApiTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;

    public BookServiceApiApiTests(WebApplicationFactory<Program> factory)
    {
        _factory = factory;
    }

    [Fact]
    public async Task GetListBooksSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/books");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetGetBookSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/books/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/00000000-0000-0000-0000-000000000001, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task PostCreateBookSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Post, "/api/v1/books");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task PutUpdateBookSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Put,
            "/api/v1/books/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task DeleteDeleteBookSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Delete,
            "/api/v1/books/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task GetListCategoriesSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/categories");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/categories, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetGetCategorySmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/categories/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/categories/00000000-0000-0000-0000-000000000001, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task PostCreateCategorySmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Post, "/api/v1/categories");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task PutUpdateCategorySmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Put,
            "/api/v1/categories/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task DeleteDeleteCategorySmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Delete,
            "/api/v1/categories/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task GetBooksSearchSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/books/search");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/search, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetBooksFilterSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/books/filter");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/filter, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetBooksCatalogSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/books/catalog");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/catalog, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetBooksAvailableSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/books/available");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/available, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetBooksCategoryCategoryIdSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/books/category/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/category/00000000-0000-0000-0000-000000000001, got {(int)response.StatusCode}"
        );
    }

    [Fact]
    public async Task GetBooksByTagTagIdSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/books/by-tag/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx status for public endpoint /api/v1/books/by-tag/00000000-0000-0000-0000-000000000001, got {(int)response.StatusCode}"
        );
    }
}
