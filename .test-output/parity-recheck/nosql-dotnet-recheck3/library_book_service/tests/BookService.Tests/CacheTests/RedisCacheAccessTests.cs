// RedisCacheAccessTests.cs -- generated xUnit.v3 cache entry key/TTL/type well-formedness tests.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
using Xunit;

namespace BookService.Tests.CacheTests;

public sealed class RedisCacheAccessTests
{
    [Fact]
    public void BookCacheKeyPrefixIsWellFormed()
    {
        var keyPrefix = "redis:book_cache";
        Assert.False(string.IsNullOrEmpty(keyPrefix));
    }

    [Fact]
    public void BookCacheCacheTypeIsKnown()
    {
        var cacheType = "HASH";
        Assert.True(cacheType is "HASH" or "counter" or "sorted set" or "SET" or "LIST");
    }

    [Fact]
    public void BookViewCountKeyPrefixIsWellFormed()
    {
        var keyPrefix = "book:views";
        Assert.False(string.IsNullOrEmpty(keyPrefix));
    }

    [Fact]
    public void BookViewCountCacheTypeIsKnown()
    {
        var cacheType = "counter";
        Assert.True(cacheType is "HASH" or "counter" or "sorted set" or "SET" or "LIST");
    }
}
