// DocdbNosqlIndexBootstrap.cs -- MongoDB index bootstrap for the "docdb"
// nosql block. Auto-generated by datrix-codegen-dotnet.
// Do not edit manually.

using System.Threading.Tasks;
using MongoDB.Bson;
using MongoDB.Driver;

namespace BookService.Nosql.Docdb;

/// <summary>Creates every declared index for each collection in the
/// "docdb" nosql block. Run once at application startup --
/// idempotent (MongoDB no-ops re-creating an identical index), so calling
/// this more than once is safe but wasteful. Never call this from a
/// repository constructor (startup-time, block-scoped concern, not a
/// per-request/per-instantiation one).</summary>
public sealed class DocdbNosqlIndexBootstrap
{
    private readonly DocdbNosqlClient _client;

    public DocdbNosqlIndexBootstrap(DocdbNosqlClient client)
    {
        _client = client;
    }

    public async Task RunAsync()
    {
        await Task.CompletedTask.ConfigureAwait(false);
        var ReadingActivityCollection = _client.Database.GetCollection<BsonDocument>(
            "reading-activity"
        );
        await ReadingActivityCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("book_id"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        await ReadingActivityCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("member_id"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        await ReadingActivityCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("book_id").Ascending("timestamp"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        await ReadingActivityCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("member_id").Ascending("timestamp"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        var BookRatingCollection = _client.Database.GetCollection<BsonDocument>("book-rating");
        await BookRatingCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("book_id"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        await BookRatingCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("member_id"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
        await BookRatingCollection
            .Indexes.CreateOneAsync(
                new CreateIndexModel<BsonDocument>(
                    Builders<BsonDocument>.IndexKeys.Ascending("book_id").Ascending("created_at"),
                    new CreateIndexOptions { Unique = false }
                )
            )
            .ConfigureAwait(false);
    }
}
