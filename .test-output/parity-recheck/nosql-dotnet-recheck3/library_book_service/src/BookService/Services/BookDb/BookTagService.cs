// BookTagService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against BookTag's own
// persistence set (BookTags) on the project's single
// BookServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using System.Collections.Generic;
using System.Threading.Tasks;
using BookService.Data;
using Microsoft.EntityFrameworkCore;

namespace BookService.Services.BookDb;

public sealed class BookTagService
{
    private readonly BookServiceDbContext _context;

    public BookTagService(BookServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::BookService.Models.BookDb.BookTag?> GetByIdAsync(Guid id)
    {
        return await _context.BookTags.FindAsync(id);
    }

    public async Task<List<global::BookService.Models.BookDb.BookTag>> GetAllAsync()
    {
        return await _context.BookTags.ToListAsync();
    }

    public async Task<global::BookService.Models.BookDb.BookTag> CreateAsync(
        global::BookService.Models.BookDb.BookTag entity
    )
    {
        _context.BookTags.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::BookService.Models.BookDb.BookTag?> UpdateAsync(
        Guid id,
        global::BookService.Models.BookDb.BookTag updated
    )
    {
        var existing = await _context.BookTags.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.BookTags.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.BookTags.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
