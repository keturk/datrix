// MemberApiApiTests.cs -- generated xUnit.v3 API smoke + auth-boundary tests.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Drives the real ASP.NET Core pipeline in-process via WebApplicationFactory<Program>
// -- one smoke [Fact] per endpoint asserting a real HTTP
// status: 401 Unauthorized when the endpoint requires authentication (no principal
// token supplied), otherwise a non-5xx status band (never a 200-only happy-path
// assumption -- an empty test database legitimately 404s a by-id lookup).
using System.Net;
using System.Net.Http;
using Microsoft.AspNetCore.Mvc.Testing;
using Xunit;

namespace MemberService.Tests.ApiTests;

public sealed class MemberApiApiTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;

    public MemberApiApiTests(WebApplicationFactory<Program> factory)
    {
        _factory = factory;
    }

    [Fact]
    public async Task GetListMembersSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Get, "/api/v1/members/members");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task GetGetMemberSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/members/members/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task PostCreateMemberSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(HttpMethod.Post, "/api/v1/members/members");
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task PutUpdateMemberSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Put,
            "/api/v1/members/members/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task DeleteDeleteMemberSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Delete,
            "/api/v1/members/members/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }

    [Fact]
    public async Task GetByMembershipMembershipNumberSmoke()
    {
        HttpClient client = _factory.CreateClient();
        using var request = new HttpRequestMessage(
            HttpMethod.Get,
            "/api/v1/members/by-membership/00000000-0000-0000-0000-000000000001"
        );
        HttpResponseMessage response = await client.SendAsync(request);
        Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);
    }
}
