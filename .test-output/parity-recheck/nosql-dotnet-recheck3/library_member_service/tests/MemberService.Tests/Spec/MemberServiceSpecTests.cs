// MemberServiceSpecTests.cs -- generated xUnit.v3 spec tests from `test("...")` DSL blocks.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
using MemberService.Data;
using MemberService.Enums;
using MemberService.Models.MemberDb;
using Microsoft.EntityFrameworkCore;
using Xunit;

namespace MemberService.Tests.Spec;

public sealed class MemberServiceSpecTests
{
    [Fact]
    public async Task ComputedFullNameConcatenatesFirstAndLastName()
    {
        // computed fullName concatenates first and last name
        var options = new DbContextOptionsBuilder<MemberServiceDbContext>()
            .UseInMemoryDatabase(
                databaseName: $"MemberServiceSpecTests_ComputedFullNameConcatenatesFirstAndLastName_{Guid.NewGuid()}"
            )
            .Options;
        await using var context = new MemberServiceDbContext(options);
        var member = new Member
        {
            FirstName = "Alice",
            LastName = "Johnson",
            Email = "alice.johnson@example.com",
            MembershipNumber = "MEM001SPECFN",
            MembershipType = MembershipType.Standard,
        };
        context.Members.Add(member);
        await context.SaveChangesAsync();
        await context.SaveChangesAsync();
        Assert.True(
            (member.FullName == "Alice Johnson"),
            "Assertion failed: (member.FullName == \"Alice Johnson\")"
        );
    }
}
