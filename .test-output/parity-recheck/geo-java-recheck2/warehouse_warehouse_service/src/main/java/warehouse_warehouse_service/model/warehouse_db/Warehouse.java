package warehouse_warehouse_service.model.warehouse_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import org.locationtech.jts.geom.Geometry;

/**
 * Warehouse JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "warehouses",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(name = "ck_warehouses_name_max_length", constraint = "length(name) <= 200"),
      @CheckConstraint(name = "ck_warehouses_code_max_length", constraint = "length(code) <= 100")
    })
public class Warehouse extends BaseEntity {

  @Column(name = "name", columnDefinition = "varchar(200)", nullable = false)
  private String name;

  @Column(name = "code", columnDefinition = "varchar(100)", nullable = false, unique = true)
  private String code;

  @Column(name = "boundary", columnDefinition = "geometry(Geometry,4326)", nullable = false)
  private Geometry boundary;

  @Column(name = "centerPoint", columnDefinition = "geometry(Geometry,4326)", nullable = false)
  private Geometry centerPoint;

  @Column(name = "capacitySquareMeters", columnDefinition = "integer", nullable = false)
  private Integer capacitySquareMeters;

  public String getName() {
    return this.name;
  }

  public void setName(String name) {
    this.name = name;
  }

  public String getCode() {
    return this.code;
  }

  public void setCode(String code) {
    this.code = code;
  }

  public Geometry getBoundary() {
    return this.boundary;
  }

  public void setBoundary(Geometry boundary) {
    this.boundary = boundary;
  }

  public Geometry getCenterPoint() {
    return this.centerPoint;
  }

  public void setCenterPoint(Geometry centerPoint) {
    this.centerPoint = centerPoint;
  }

  public Integer getCapacitySquareMeters() {
    return this.capacitySquareMeters;
  }

  public void setCapacitySquareMeters(Integer capacitySquareMeters) {
    this.capacitySquareMeters = capacitySquareMeters;
  }

  /**
   * Build a new {@link Warehouse} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.Warehouse.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new Warehouse(...)`.
   */
  public static Warehouse fromMap(java.util.Map<String, Object> data) {
    Warehouse result = new Warehouse();
    if (data.containsKey("name")) {
      Object __v = data.get("name");
      result.setName((String) __v);
    }
    if (data.containsKey("code")) {
      Object __v = data.get("code");
      result.setCode((String) __v);
    }
    if (data.containsKey("boundary")) {
      Object __v = data.get("boundary");
      result.setBoundary((Geometry) __v);
    }
    if (data.containsKey("centerPoint")) {
      Object __v = data.get("centerPoint");
      result.setCenterPoint((Geometry) __v);
    }
    if (data.containsKey("capacitySquareMeters")) {
      Object __v = data.get("capacitySquareMeters");
      result.setCapacitySquareMeters(__v instanceof Integer __n ? __n : ((Number) __v).intValue());
    }
    return result;
  }
}
