# Auto-generated by datrix-codegen-docker. Do not edit manually.
#
# Targets below use Docker Compose service hostnames and resolve only inside
# the application's Docker network. When deploying to an external Prometheus,
# either run Prometheus inside the same network or rewrite each
# 'static_configs.targets' entry to reach the services from your environment.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['warehouse-system-alertmanager:9093']

rule_files:
  - /etc/prometheus/rules/*_alerts.yml

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'warehouse-warehouse-service'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['warehouse-warehouse-service:8000']
        labels:
          service: 'warehouse-warehouse-service'
  - job_name: 'cadvisor'
    static_configs:
      - targets: ['warehouse-system-cadvisor:8080']
