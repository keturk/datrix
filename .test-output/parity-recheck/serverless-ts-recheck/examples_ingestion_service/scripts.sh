#!/usr/bin/env bash
# examples-ingestion-service — development scripts (NestJS / examples_ingestion_service)
# Auto-generated by datrix-codegen-typescript. Do not edit manually.
set -euo pipefail
case "${1:-}" in
  install)
    pnpm install
    ;;
  dev)
    pnpm start:dev -- --host 0.0.0.0 --port 8002
    ;;
  build)
    pnpm run build
    ;;
  test)
    shift
    pnpm test -- "$@"
    ;;
  lint)
    pnpm run lint
    ;;
  help|*)
    echo "Usage: ./scripts.sh <install|dev|build|test|lint|help>"
    ;;
esac
