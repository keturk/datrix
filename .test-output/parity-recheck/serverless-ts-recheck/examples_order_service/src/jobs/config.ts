// Job configuration.
// Auto-generated by datrix-codegen-typescript. Do not edit manually.

export interface JobConfig {
  name: string;
  schedule: string;
  timeoutSeconds: number;
  maxAttempts: number;
  concurrency: number;
  deadLetterQueue?: string;
}

export const JOB_CONFIGS: Record<string, JobConfig> = {
  'ReconcileOrders': {
    name: 'ReconcileOrders',
    schedule: process.env.JOBS_RECONCILE_ORDERS_SCHEDULE || '0 2 * * *',
    timeoutSeconds: parseInt(process.env.JOBS_RECONCILE_ORDERS_TIMEOUT || '300', 10),
    maxAttempts: parseInt(process.env.JOBS_RECONCILE_ORDERS_MAX_ATTEMPTS || '1', 10),
    concurrency: parseInt(process.env.JOBS_RECONCILE_ORDERS_CONCURRENCY || '1', 10),
  },
};
