#!/usr/bin/env bash
# examples-order-service — development scripts (NestJS / examples_order_service)
# Auto-generated by datrix-codegen-typescript. Do not edit manually.
set -euo pipefail
case "${1:-}" in
  install)
    pnpm install
    ;;
  dev)
    pnpm start:dev -- --host 0.0.0.0 --port 8001
    ;;
  build)
    pnpm run build
    ;;
  test)
    shift
    pnpm test -- "$@"
    ;;
  lint)
    pnpm run lint
    ;;
  db-migrate)
    echo "Database migrations are project-specific; configure MikroORM CLI or your migration tool."
    ;;
  help|*)
    echo "Usage: ./scripts.sh <install|dev|build|test|lint|db-migrate|help>"
    ;;
esac
