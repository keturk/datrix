"""Integration tests for Order API endpoints.

Auto-generated by datrix-codegen-python. Do not edit manually.
Tests hit the full HTTP stack via httpx AsyncClient.
"""
from __future__ import annotations

import uuid

import pytest
from httpx import AsyncClient



@pytest.mark.integration
@pytest.mark.asyncio
class TestOrderEndpoints:
    """Test Order CRUD endpoints via HTTP."""


    async def test_list_orders(self, client: AsyncClient) -> None:
        """GET /api/v1/orders returns 200 with array."""
        response = await client.get("/api/v1/orders")
        assert response.status_code == 200, f"Expected 200, got {response.status_code}: {response.text!r}"
        data = response.json()
        assert isinstance(data, list)


    async def test_list_orders_pagination(self, client: AsyncClient) -> None:
        """GET /api/v1/orders?skip=0&limit=10 returns paginated results."""
        response = await client.get("/api/v1/orders?skip=0&limit=10")
        assert response.status_code == 200, f"Expected 200, got {response.status_code}: {response.text!r}"
        data = response.json()
        assert isinstance(data, list)
        assert len(data) <= 10


    async def test_get_order_not_found(self, client: AsyncClient) -> None:
        """GET /api/v1/orders/{non_existent_id} returns 404."""
        _nonexistent_id = uuid.uuid4()
        response = await client.get(f"/api/v1/orders/{_nonexistent_id}")
        assert response.status_code == 404, f"Expected 404, got {response.status_code}: {response.text!r}"




