package library_loan_service.services.loan_db;

import java.util.List;
import java.util.Optional;
import java.util.UUID;
import library_loan_service.model.loan_db.Loan;
import library_loan_service.repositories.loan_db.LoanRepository;
import library_loan_service.services.ServiceBase;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * Loan service -- request-scoped unit-of-work wrapper over {@link LoanRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class LoanService extends ServiceBase {

  private final LoanRepository loanRepository;

  public LoanService(LoanRepository loanRepository) {
    this.loanRepository = loanRepository;
  }

  public Optional<Loan> get(UUID id) {
    return this.loanRepository.findById(id);
  }

  public List<Loan> list() {
    return this.loanRepository.findAll();
  }

  public List<Loan> getByBookId(UUID bookId) {
    return this.loanRepository.findByBookId(bookId);
  }

  public List<Loan> getByMemberId(UUID memberId) {
    return this.loanRepository.findByMemberId(memberId);
  }

  @Transactional
  public Loan create(Loan entity) {
    return this.loanRepository.save(entity);
  }

  @Transactional
  public Loan update(Loan entity) {
    return this.loanRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.loanRepository.deleteById(id);
  }
}
