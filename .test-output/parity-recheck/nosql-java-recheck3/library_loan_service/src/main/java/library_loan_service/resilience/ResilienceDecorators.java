package library_loan_service.resilience;

import io.github.resilience4j.retry.Retry;
import io.github.resilience4j.retry.RetryConfig;
import java.util.function.Supplier;
import org.springframework.stereotype.Component;

/**
 * Generated resilience layer for endpoint-level {@code @retry} decorators.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component
public class ResilienceDecorators {

  /** Execute {@code supplier} under a resilience4j retry policy built from {@code maxAttempts}. */
  public <T> T retry(int maxAttempts, Supplier<T> supplier) {
    RetryConfig config = RetryConfig.custom().maxAttempts(Math.max(1, maxAttempts)).build();
    Retry retry = Retry.of("endpoint-retry-" + maxAttempts, config);
    return retry.executeSupplier(supplier);
  }

  /**
   * Execute {@code action} (a void handler body) under a resilience4j retry policy built from
   * {@code maxAttempts}.
   */
  public void retryVoid(int maxAttempts, Runnable action) {
    RetryConfig config = RetryConfig.custom().maxAttempts(Math.max(1, maxAttempts)).build();
    Retry retry = Retry.of("endpoint-retry-void-" + maxAttempts, config);
    retry.executeRunnable(action);
  }
}
