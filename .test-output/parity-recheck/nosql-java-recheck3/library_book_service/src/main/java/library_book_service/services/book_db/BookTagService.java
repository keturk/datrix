package library_book_service.services.book_db;

import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import library_book_service.model.book_db.Book;
import library_book_service.model.book_db.BookTag;
import library_book_service.model.book_db.Tag;
import library_book_service.repositories.book_db.BookTagRepository;
import library_book_service.services.ServiceBase;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * BookTag service -- request-scoped unit-of-work wrapper over {@link BookTagRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class BookTagService extends ServiceBase {

  private final BookTagRepository bookTagRepository;

  @PersistenceContext private EntityManager entityManager;

  public BookTagService(BookTagRepository bookTagRepository) {
    this.bookTagRepository = bookTagRepository;
  }

  public Optional<BookTag> get(UUID id) {
    return this.bookTagRepository.findById(id);
  }

  public List<BookTag> list() {
    return this.bookTagRepository.findAll();
  }

  public List<BookTag> getByBookId(UUID bookId) {
    return this.bookTagRepository.findByBookId(bookId);
  }

  public List<BookTag> getByTagId(UUID tagId) {
    return this.bookTagRepository.findByTagId(tagId);
  }

  public Optional<BookTag> getByBookIdAndTagId(UUID bookId, UUID tagId) {
    return this.bookTagRepository.findByBookIdAndTagId(bookId, tagId);
  }

  public Page<BookTag> listByBook(UUID bookId, Pageable pageable) {
    return this.bookTagRepository.findByBookId(bookId, pageable);
  }

  public Page<BookTag> listByTag(UUID tagId, Pageable pageable) {
    return this.bookTagRepository.findByTagId(tagId, pageable);
  }

  public Book getBookReference(UUID id) {
    return this.entityManager.getReference(Book.class, id);
  }

  public Tag getTagReference(UUID id) {
    return this.entityManager.getReference(Tag.class, id);
  }

  @Transactional
  public BookTag create(BookTag entity) {
    return this.bookTagRepository.save(entity);
  }

  @Transactional
  public BookTag update(BookTag entity) {
    return this.bookTagRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.bookTagRepository.deleteById(id);
  }
}
