package library_book_service.services.book_db;

import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import library_book_service.model.book_db.Tag;
import library_book_service.repositories.book_db.TagRepository;
import library_book_service.services.ServiceBase;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * Tag service -- request-scoped unit-of-work wrapper over {@link TagRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class TagService extends ServiceBase {

  private final TagRepository tagRepository;

  @PersistenceContext private EntityManager entityManager;

  public TagService(TagRepository tagRepository) {
    this.tagRepository = tagRepository;
  }

  public Optional<Tag> get(UUID id) {
    return this.tagRepository.findById(id);
  }

  public List<Tag> list() {
    return this.tagRepository.findAll();
  }

  public Optional<Tag> getByName(String name) {
    return this.tagRepository.findByName(name);
  }

  @Transactional
  public Tag create(Tag entity) {
    return this.tagRepository.save(entity);
  }

  @Transactional
  public Tag update(Tag entity) {
    return this.tagRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    if (((Number)
                this.entityManager
                    .createNativeQuery("SELECT COUNT(*) FROM book_tags WHERE tag_id = ?1")
                    .setParameter(1, id)
                    .getSingleResult())
            .longValue()
        > 0) {
      throw new IllegalStateException(
          "Cannot delete Tag " + id + ": referenced by BookTag(s) via bookTags");
    }
    this.tagRepository.deleteById(id);
  }
}
