# Spring Boot test profile for warehouse.WarehouseService (JUnit 5 + Spring Boot Test)
spring:
  threads:
    virtual:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: false
