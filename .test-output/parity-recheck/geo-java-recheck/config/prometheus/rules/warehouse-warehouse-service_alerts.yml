groups:
  - name: warehouse-warehouse-service-standard
    rules:
      - alert: HighErrorRate
        expr: sum(rate(http_errors_total{service="warehouse-warehouse-service",status_code=~"5.."}[5m])) / clamp_min(sum(rate(http_requests_total{service="warehouse-warehouse-service"}[5m])), 1e-9) > 0.05
        for: 5m
        labels:
          severity: critical
          service: warehouse-warehouse-service
        annotations:
          summary: "High error rate on warehouse-warehouse-service"
          description: "5xx error rate above 5% for 5 minutes."
      - alert: HighLatency
        expr: histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{service="warehouse-warehouse-service"}[5m])) by (le)) > 2
        for: 5m
        labels:
          severity: warning
          service: warehouse-warehouse-service
        annotations:
          summary: "High P95 latency on warehouse-warehouse-service"
          description: "P95 latency above 2 seconds for 5 minutes."
      - alert: DatabaseConnectionPoolExhausted
        expr: sum(sqlalchemy_pool_checked_out{service="warehouse-warehouse-service"}) / clamp_min(sum(sqlalchemy_pool_size{service="warehouse-warehouse-service"}), 1) > 0.9
        for: 2m
        labels:
          severity: critical
          service: warehouse-warehouse-service
        annotations:
          summary: "DB pool nearly exhausted on warehouse-warehouse-service"
          description: "Checked-out connections exceed 90% of configured pool capacity."
