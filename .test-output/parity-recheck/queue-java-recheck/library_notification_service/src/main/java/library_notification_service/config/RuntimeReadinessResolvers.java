package library_notification_service.config;

import library_notification_service.security.SecretsResolver;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

/**
 * Binds {@link RuntimeReadiness.SecretResolver} / {@link RuntimeReadiness.ConfigResolver} to the
 * SAME runtime clients the application uses. The readiness route resolves through these beans --
 * never a stand-in that always reports ready or never ready.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Configuration
public class RuntimeReadinessResolvers {

  /** Backed by the always-on {@code RemoteConfig} facade's "connections" profile. */
  @Bean
  public RuntimeReadiness.ConfigResolver configResolver(RemoteConfig remoteConfig) {
    return logicalName -> remoteConfig.getString("connections", logicalName);
  }

  /** Backed by this service's generated fail-closed {@code SecretsResolver}. */
  @Bean
  public RuntimeReadiness.SecretResolver secretResolver(SecretsResolver resolver) {
    return resolver::getSecret;
  }
}
