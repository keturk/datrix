package library_book_service.consumers;

import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import library_book_service.enums.BookStatus;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: book_events_book_status_changed (subscription 'BookEvents',
 * event 'BookStatusChanged').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersBookEventsBookStatusChanged")
public class BookEventsBookStatusChangedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(BookEventsBookStatusChangedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.info(
        "subscription_handler_start handler={} event={}",
        "book_events_book_status_changed",
        "BookStatusChanged");
    UUID bookId = (UUID) input.get("bookId");
    BookStatus oldStatus = (BookStatus) input.get("oldStatus");
    BookStatus newStatus = (BookStatus) input.get("newStatus");
    try {
      logger.info("Book status changed");
      return null;
    } finally {
      logger.info(
          "subscription_handler_complete handler={} event={}",
          "book_events_book_status_changed",
          "BookStatusChanged");
    }
  }
}
