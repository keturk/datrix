package library_loan_service.model.loan_db;

import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import jakarta.persistence.Transient;
import java.time.LocalDateTime;
import java.time.OffsetDateTime;
import java.util.Objects;
import java.util.UUID;
import library_loan_service.enums.LoanStatus;

/**
 * Loan JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "loans",
    indexes = {},
    uniqueConstraints = {},
    check = {})
public class Loan extends BaseEntity {

  @Column(name = "bookId", columnDefinition = "uuid", nullable = false)
  private UUID bookId;

  @Column(name = "memberId", columnDefinition = "uuid", nullable = false)
  private UUID memberId;

  @Column(name = "status", columnDefinition = "varchar(255)", nullable = false)
  private LoanStatus status = LoanStatus.ACTIVE;

  @Column(name = "dueDate", columnDefinition = "timestamptz", nullable = false)
  private OffsetDateTime dueDate;

  @Column(name = "returnedAt", columnDefinition = "timestamptz", nullable = true)
  private OffsetDateTime returnedAt;

  public UUID getBookId() {
    return this.bookId;
  }

  public void setBookId(UUID bookId) {
    this.bookId = bookId;
  }

  public UUID getMemberId() {
    return this.memberId;
  }

  public void setMemberId(UUID memberId) {
    this.memberId = memberId;
  }

  public LoanStatus getStatus() {
    return this.status;
  }

  public void setStatus(LoanStatus status) {
    this.status = status;
  }

  public OffsetDateTime getDueDate() {
    return this.dueDate;
  }

  public void setDueDate(OffsetDateTime dueDate) {
    this.dueDate = dueDate;
  }

  public OffsetDateTime getReturnedAt() {
    return this.returnedAt;
  }

  public void setReturnedAt(OffsetDateTime returnedAt) {
    this.returnedAt = returnedAt;
  }

  /**
   * Computed: ((Objects.equals(this.getStatus(), LoanStatus.OVERDUE)) ||
   * ((Objects.equals(this.getStatus(), LoanStatus.ACTIVE)) && (LocalDateTime.now() >
   * this.getDueDate())))
   */
  @Transient
  public Boolean getIsOverdue() {
    return ((Objects.equals(this.getStatus(), LoanStatus.OVERDUE))
        || ((Objects.equals(this.getStatus(), LoanStatus.ACTIVE))
            && (LocalDateTime.now() > this.getDueDate())));
  }

  /**
   * Build a new {@link Loan} from a DSL object-literal map, setting only the keys present in {@code
   * data} (a key absent from {@code data} leaves the corresponding field at its normal Java default
   * -- {@code null} unless the field declares its own initializer above). Keys are the AS-AUTHORED
   * DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)}, preserves
   * author-declared spelling -- no case conversion), which for a camelCase-authored DSL field is
   * identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.Loan.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new Loan(...)`.
   */
  public static Loan fromMap(java.util.Map<String, Object> data) {
    Loan result = new Loan();
    if (data.containsKey("bookId")) {
      Object __v = data.get("bookId");
      result.setBookId((UUID) __v);
    }
    if (data.containsKey("memberId")) {
      Object __v = data.get("memberId");
      result.setMemberId((UUID) __v);
    }
    if (data.containsKey("status")) {
      Object __v = data.get("status");
      result.setStatus((LoanStatus) __v);
    }
    if (data.containsKey("dueDate")) {
      Object __v = data.get("dueDate");
      result.setDueDate((OffsetDateTime) __v);
    }
    if (data.containsKey("returnedAt")) {
      Object __v = data.get("returnedAt");
      result.setReturnedAt((OffsetDateTime) __v);
    }
    return result;
  }
}
