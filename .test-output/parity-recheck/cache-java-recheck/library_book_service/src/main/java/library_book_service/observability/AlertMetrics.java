package library_book_service.observability;

import io.micrometer.core.instrument.MeterRegistry;
import io.micrometer.core.instrument.Tag;
import io.micrometer.core.instrument.Timer;
import java.util.List;
import java.util.concurrent.TimeUnit;
import java.util.concurrent.atomic.AtomicLong;
import org.springframework.stereotype.Component;

/**
 * Alert metrics for library_book_service.observability. One Micrometer meter per declared {@code
 * alert}.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component
public class AlertMetrics {

  private final MeterRegistry registry;
  private final Timer bookDbQueryDurationTimer;
  private final AtomicLong bookDbConnectionPoolValue = new AtomicLong(0);
  private final AtomicLong redisHitRatioValue = new AtomicLong(0);
  private final AtomicLong mqConsumerLagValue = new AtomicLong(0);

  public AlertMetrics(MeterRegistry registry) {
    this.registry = registry;
    this.bookDbQueryDurationTimer =
        Timer.builder("book_db_query_duration")
            .description("queryDuration metric for bookDb.")
            .tag("service", "library_book_service")
            .publishPercentileHistogram()
            .register(registry);
    registry.gauge(
        "book_db_connection_pool",
        List.of(Tag.of("service", "library_book_service")),
        this.bookDbConnectionPoolValue);
    registry.gauge(
        "redis_hit_ratio",
        List.of(Tag.of("service", "library_book_service")),
        this.redisHitRatioValue);
    registry.gauge(
        "mq_consumer_lag",
        List.of(Tag.of("service", "library_book_service")),
        this.mqConsumerLagValue);
  }

  /** Record an elapsed duration for the {@code book_db_query_duration} timer. */
  public void recordBookDbQueryDuration(long elapsedNanos) {
    bookDbQueryDurationTimer.record(elapsedNanos, TimeUnit.NANOSECONDS);
  }

  /** Set the {@code book_db_connection_pool} gauge value. */
  public void setBookDbConnectionPool(long value) {
    bookDbConnectionPoolValue.set(value);
  }

  /** Set the {@code redis_hit_ratio} gauge value. */
  public void setRedisHitRatio(long value) {
    redisHitRatioValue.set(value);
  }

  /** Set the {@code mq_consumer_lag} gauge value. */
  public void setMqConsumerLag(long value) {
    mqConsumerLagValue.set(value);
  }
}
