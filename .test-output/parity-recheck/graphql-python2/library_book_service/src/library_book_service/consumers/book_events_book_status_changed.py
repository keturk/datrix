"""Serverless subscription handler: book_events_book_status_changed.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def book_events_book_status_changed(payload: object) -> None:
    """Handle BookStatusChanged event from subscription 'BookEvents'.

    Invoked by the platform adapter when a BookStatusChanged event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.info(
        "subscription_handler_start handler=%s event=%s",
        "book_events_book_status_changed",
        "BookStatusChanged",
    )
    book_id = _payload_value(payload, "book_id")
    old_status = _payload_value(payload, "old_status")
    new_status = _payload_value(payload, "new_status")
    try:
        logger.info(
            "book_status_changed book_id=%s old_status=%s new_status=%s",
            book_id,
            old_status,
            new_status,
        )
    finally:
        logger.info(
            "subscription_handler_complete handler=%s event=%s",
            "book_events_book_status_changed",
            "BookStatusChanged",
        )
