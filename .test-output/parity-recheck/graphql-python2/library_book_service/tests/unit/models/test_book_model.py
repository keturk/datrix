"""Unit tests for Book model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

import pytest

from library_book_service.enums.book_status import BookStatus
from library_book_service.models.book_db.book import Book


@pytest.mark.unit
class TestBookModel:
    """Test Book SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a Book instance with required fields."""
        _category_id = uuid.uuid4()
        instance = Book(
            title="world",
            author="hello",
            publication_year=42,
            category_id=_category_id,
        )
        assert instance.title == "world"
        assert instance.author == "hello"
        assert instance.publication_year == 42
        assert instance.category_id == _category_id

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _category_id = uuid.uuid4()
        instance = Book(
            title="world",
            author="hello",
            publication_year=42,
            category_id=_category_id,
        )
        instance.title = "foo_bar"
        assert instance.title == "foo_bar"
        instance.author = "world"
        assert instance.author == "world"
        instance.publication_year = 100
        assert instance.publication_year == 100
        instance.status = BookStatus.checked_out
        assert instance.status == BookStatus.checked_out
        _alt_category_id = uuid.uuid4()
        instance.category_id = _alt_category_id
        assert instance.category_id == _alt_category_id

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(Book, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = Book(
            title="world",
            author="hello",
            publication_year=42,
            category_id=uuid.uuid4(),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_title_max_length_in_column(self) -> None:
        """title column has max_length=200."""
        col = Book.__table__.columns["title"]
        assert col.type.length == 200

    def test_author_max_length_in_column(self) -> None:
        """author column has max_length=100."""
        col = Book.__table__.columns["author"]
        assert col.type.length == 100

    def test_status_enum_round_trip(self) -> None:
        """status enum value survives assign and read."""
        instance = Book(
            title="world",
            author="hello",
            publication_year=42,
            category_id=uuid.uuid4(),
        )
        instance.status = BookStatus.available
        assert instance.status == BookStatus.available
        instance.status = BookStatus.checked_out
        assert instance.status == BookStatus.checked_out
        instance.status = BookStatus.reserved
        assert instance.status == BookStatus.reserved
