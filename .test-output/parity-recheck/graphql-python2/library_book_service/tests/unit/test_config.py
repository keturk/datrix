"""Unit tests for library_book_service configuration.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest

import library_book_service.config.settings as settings_module
from library_book_service.config import get_settings


@pytest.mark.unit
class TestConfig:
    """Test AppSettings guard invariants."""

    def test_get_settings_raises_before_startup(
        self, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        """get_settings() raises RuntimeError when assemble_settings() has not run.

        The conftest's ``_assemble_test_settings`` fixture reproduces the
        lifespan's assembly for every test, so this test restores the
        pre-startup state explicitly rather than relying on its absence.
        """
        monkeypatch.setattr(settings_module, "_settings", None)
        with pytest.raises(RuntimeError, match="assemble_settings"):
            get_settings()
