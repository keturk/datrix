# Generated by Datrix — do not edit by hand.
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.mypy_cache/
*.egg-info/
.coverage
htmlcov/
build/
dist/
.env
.venv/
node_modules/
# Per-service file-backed secrets (generated per build; never commit).
# Top-level dev JWT PEMs under secrets/ stay tracked; per-service dirs do not.
secrets/*/
# Deploy-script build artifacts (run-from-package): per-service pip staging
# dirs, the zip archives uploaded to blob storage, and per-run deploy
# transcripts. Transient — the deploy script recreates them on every run;
# never commit.
.pkg-*/
*.zip
.deploy-logs/
