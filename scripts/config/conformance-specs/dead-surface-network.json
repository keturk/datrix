{
  "target": "../../../examples",
  "negative_control": "_fixtures/_shared/deleted-config-surfaces-control",
  "assertions": [
    {
      "id": "network-block-absent",
      "type": "must_not_contain",
      "pattern": "\\bnetwork\\s*\\{",
      "glob": "**/system.dcfg",
      "description": "The system-level 'network {}' block has zero generator consumers on any platform and was deleted from the config model; no example may reintroduce it."
    }
  ]
}
