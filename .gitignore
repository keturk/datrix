# Byte-compiled / optimized / DLL files
__pycache__/
*.py[codz]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
# Anchored to the repo root and to setuptools' own generated file. The stock
# unanchored `MANIFEST` pattern matches at ANY depth, and git ignore matching is
# case-insensitive here (core.ignorecase=true on Windows), so it silently
# swallows any file or directory named `manifest` anywhere in the tree --
# shipped template directories included. That is invisible locally (the files
# exist and the tests pass) and only surfaces as a package that cannot generate
# after a clone or a wheel install.
/MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock
#poetry.toml

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#   pdm recommends including project-wide configuration in pdm.toml, but excluding .pdm-python.
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
#pdm.lock
#pdm.toml
.pdm-python
.pdm-build/

# pixi
#   Similar to Pipfile.lock, it is generally recommended to include pixi.lock in version control.
#pixi.lock
#   Pixi creates a virtual environment in the .pixi directory, just like venv module creates one
#   in the .venv directory. It is recommended not to include this directory in version control.
.pixi

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore 
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer, 
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
.ruff_cache/
.ruff_check/

# PyPI configuration file
.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Project tasks and test results
.tasks/
.test_results/
.benchmarks/

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Generated project structure metadata
.project-structure.md

# RDBMS migration-ledger state. In a real project `.datrix/rdbms-migrations/` is
# written back into the APPLICATION SOURCE dir and version-controlled by design:
# it is the append-only record of what each deployed database ran. No example
# under this repo is that: an example is generated in every registered language
# and only ever meets a fresh database, so it has nothing to migrate FROM, and a
# ledger would record one language's rendering of each revision and seal the
# store against every other language. Every example that keeps the migration
# lifecycle on therefore declares `migrations { ledger = false; }`, the pipeline
# writes no state for it, and nothing under examples/ is ever re-negated here.
# The blanket ignore is the stray guard: `commit-and-push` runs `git add -A`, so
# state an ad-hoc run left behind would otherwise be committed unlooked-at, and
# ignored-source-gate.ps1 requires a reviewed exemption for anything it hides.
.datrix/

# Crash / stack dump files
*.stackdump

# Claude Code per-session runtime state. The hook CODE under
# claude-config/.claude/hooks/ is versioned on purpose; the state it writes at
# runtime is not. `.state/mandatory-reads-<session-id>.json` records which gated
# docs one session has read (written by session-context.py at every SessionStart,
# appended by track-mandatory-reads.py, consumed by gate-mandatory-reads.py);
# `scheduled_tasks.lock` holds a live pid + session id. Both are machine- and
# session-local and meaningless in any other clone -- 12 of the former were
# committed by stray `git add .` before this rule existed.
claude-config/.claude/hooks/.state/
claude-config/.claude/scheduled_tasks.lock

# Claude Code's per-machine permission grants. `settings.json` is the shared,
# reviewed project configuration; `settings.local.json` is the file Claude Code
# WRITES BY ITSELF whenever a permission prompt is answered "don't ask again",
# so its contents are whatever commands happened to run on one machine --
# absolute paths outside the workspace, session-scoped temp directories, and
# (this is why it is ignored, not merely untidy) grants naming CUSTOMER cloud
# resources and customer checkout paths, which must never enter a framework
# repo. It was tracked by stray `git add .` and carried exactly that. Nothing in
# it is meaningful in another clone.
claude-config/.claude/settings.local.json

# Temp / scratch / test-output directories must never be created inside a
# package repo at all -- they belong outside the repo tree. This is a backstop
# for the rule, not a licence: a stray `.test-output/` was committed here once.
.test-output/
.test_output/
.tmp/
.temp/
.scratch/
.scripts/
.agent_output/
