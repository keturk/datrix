test:
  tags: [api, ecommerce, orders, v1]
  meta: {}
  healthCheck: { type: http, path: /health, interval: 10s }

development:
  tags: [api, ecommerce, orders, v1]
  meta: {}
  healthCheck: { type: http, path: /health, interval: 10s }

production:
  tags: [api, ecommerce, orders, v1]
  meta: {}
  healthCheck: { type: http, path: /health, interval: 10s }
