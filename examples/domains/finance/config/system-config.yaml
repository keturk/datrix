test:
  language: python
  hosting: docker
  defaultTimeout: 30000
  maxPageSize: 100
  env:
    DEBUG: false
    CORS_ORIGINS: '["*"]'
    SECRETS_BACKEND: environment
  secrets:
    provider: env
development:
  language: python
  hosting: docker
  defaultTimeout: 30000
  maxPageSize: 100
  env:
    DEBUG: false
    CORS_ORIGINS: '["*"]'
    SECRETS_BACKEND: environment
  secrets:
    provider: env
production:
  language: python
  hosting: aws
  defaultTimeout: 30000
  maxPageSize: 100
  region: us-east-1
  secrets:
    provider: vault
    path: secret/finance
