"""taskmgmt_user_service FastAPI application.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import logging
from contextlib import asynccontextmanager
from typing import AsyncGenerator

from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

from taskmgmt_user_service.config import settings

from taskmgmt_user_service.routes.user_api import router as user_api_router

from fastapi import Request
from fastapi.responses import JSONResponse
from taskmgmt_user_service.db.connection import engine as db_engine
from taskmgmt_user_service.services._base import (
    EntityNotFoundError,
    CascadeRestrictionError,
    ValidationError,
)

from taskmgmt_user_service.observability.metrics_middleware import setup_metrics
from taskmgmt_user_service.observability.tracing_setup import setup_tracing
from taskmgmt_user_service.observability.structured_logger import setup_logging
from taskmgmt_user_service.middleware.logging_middleware import LoggingMiddleware

from taskmgmt_user_service.observability.health_endpoint import setup_health

logger = logging.getLogger(__name__)


@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncGenerator[None, None]:
    """Application lifespan context manager.

    Handles startup and shutdown logic.
    """
    setup_logging()
    logger.info("application_starting service=%s", "taskmgmt_user_service")
    # Database engine is initialized on import

    yield

    logger.info("application_stopping service=%s", "taskmgmt_user_service")
    await db_engine.dispose()


def create_app() -> FastAPI:
    """Create and configure the FastAPI application."""
    app = FastAPI(
        title="taskmgmt_user_service",
        version="1.0.0",
        lifespan=lifespan,
    )

    # Middleware
    app.add_middleware(
        CORSMiddleware,
        allow_origins=settings.CORS_ORIGINS,
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )

    # Request lifecycle logging
    app.add_middleware(LoggingMiddleware)

    @app.exception_handler(EntityNotFoundError)
    async def entity_not_found_handler(request: Request, exc: EntityNotFoundError) -> JSONResponse:
        return JSONResponse(status_code=404, content={"detail": str(exc)})

    @app.exception_handler(CascadeRestrictionError)
    async def cascade_restriction_handler(
        request: Request, exc: CascadeRestrictionError
    ) -> JSONResponse:
        return JSONResponse(status_code=409, content={"detail": str(exc)})

    @app.exception_handler(ValidationError)
    async def validation_error_handler(request: Request, exc: ValidationError) -> JSONResponse:
        return JSONResponse(status_code=422, content={"detail": exc.errors})

    @app.exception_handler(Exception)
    async def unhandled_exception_handler(request: Request, exc: Exception) -> JSONResponse:
        logger.exception("unhandled_exception request_path=%s", request.url.path)
        detail = str(exc) if settings.DEBUG else "Internal server error"
        return JSONResponse(status_code=500, content={"detail": detail})

    # Prometheus metrics middleware + /metrics endpoint
    setup_metrics(app)

    # OpenTelemetry tracing instrumentation
    setup_tracing(app)

    # Health check endpoint (replaces inline /health)
    setup_health(app)

    # Include routers
    app.include_router(user_api_router)

    return app


app = create_app()
