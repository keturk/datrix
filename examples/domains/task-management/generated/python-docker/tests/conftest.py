"""Project-level test fixtures for deployed service testing.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import os
from collections.abc import Generator

import httpx
import pytest


def _service_base_url(env_key: str) -> str:
    """Get base URL for a service from environment variable (required).

    Raises:
        ValueError: If the environment variable is not set.
    """
    url = os.environ.get(env_key)
    if not url:
        raise ValueError(f"Service URL required: set environment variable {env_key!r}.")
    return url


@pytest.fixture
def http_client() -> Generator[httpx.Client, None, None]:
    """Synchronous HTTP client for deployed service testing."""
    with httpx.Client(timeout=30.0) as client:
        yield client


@pytest.fixture
def taskmgmt_user_service_url() -> str:
    """Base URL for taskmgmt.UserService."""
    return _service_base_url("TASKMGMT_USER_SERVICE_URL")


@pytest.fixture
def taskmgmt_project_service_url() -> str:
    """Base URL for taskmgmt.ProjectService."""
    return _service_base_url("TASKMGMT_PROJECT_SERVICE_URL")


@pytest.fixture
def taskmgmt_task_service_url() -> str:
    """Base URL for taskmgmt.TaskService."""
    return _service_base_url("TASKMGMT_TASK_SERVICE_URL")
