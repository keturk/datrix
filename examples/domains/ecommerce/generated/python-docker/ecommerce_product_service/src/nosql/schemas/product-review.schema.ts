/**
 * Schema/interface for ProductReview.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Prop, Schema, SchemaFactory } from '@nestjs/mongoose';
import { Document } from 'mongoose';

export type ProductReviewDocument = ProductReview & Document;

@Schema({ collection: 'ProductReview', timestamps: false })
export class ProductReview {
  @Prop({ required: true, index: true })
  product_id: string;

  @Prop({ required: true, index: true })
  user_id: string;

  @Prop()
  rating?: unknown;

  @Prop({ required: false })
  title?: string;

  @Prop({ required: false })
  body?: string;

  @Prop({ type: Object, default: {} })
  media: Record<string, unknown>;

  @Prop({ default: false })
  verified: boolean;

  @Prop({ required: true })
  created_at: Date;

}

export const ProductReviewSchema =
  SchemaFactory.createForClass(ProductReview);
