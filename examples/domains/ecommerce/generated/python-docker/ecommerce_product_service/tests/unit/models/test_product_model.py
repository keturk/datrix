"""Unit tests for Product model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import decimal
import uuid

import pytest

from ecommerce_product_service.models.db.product import Product
from ecommerce_product_service.enums.product_status import ProductStatus


@pytest.mark.unit
class TestProductModel:
    """Test Product SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a Product instance with required fields."""
        _slug = f"test-{uuid.uuid4().hex[:8]}"
        _category_id = uuid.uuid4()
        instance = Product(
            slug=_slug,
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=_category_id,
        )
        assert instance.slug == _slug
        assert instance.price == decimal.Decimal("19.99")
        assert instance.name == "sample_text"
        assert instance.description == "The quick brown fox jumps over the lazy dog"
        assert instance.images == {"items": [1, 2, 3]}
        assert instance.tags == {"key": "value"}
        assert instance.category_id == _category_id

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _slug = f"test-{uuid.uuid4().hex[:8]}"
        _category_id = uuid.uuid4()
        instance = Product(
            slug=_slug,
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=_category_id,
        )
        _alt_slug = f"alt-{uuid.uuid4().hex[:8]}"
        instance.slug = _alt_slug
        assert instance.slug == _alt_slug
        instance.price = decimal.Decimal("49.95")
        assert instance.price == decimal.Decimal("49.95")
        instance.compare_at_price = decimal.Decimal("149.00")
        assert instance.compare_at_price == decimal.Decimal("149.00")
        instance.inventory = 1
        assert instance.inventory == 1
        instance.name = "hello"
        assert instance.name == "hello"
        instance.description = "Sample paragraph text for testing"
        assert instance.description == "Sample paragraph text for testing"
        instance.status = ProductStatus.active
        assert instance.status == ProductStatus.active
        instance.product_metadata = {"items": [1, 2, 3]}
        assert instance.product_metadata == {"items": [1, 2, 3]}
        instance.images = {"name": "test", "active": False}
        assert instance.images == {"name": "test", "active": False}
        instance.tags = {"count": 42}
        assert instance.tags == {"count": 42}
        _alt_category_id = uuid.uuid4()
        instance.category_id = _alt_category_id
        assert instance.category_id == _alt_category_id

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(Product, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = Product(
            slug=f"test-{uuid.uuid4().hex[:8]}",
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=uuid.uuid4(),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_name_max_length_in_column(self) -> None:
        """name column has max_length=200."""
        col = Product.__table__.columns["name"]
        assert col.type.length == 200

    def test_slug_unique_constraint(self) -> None:
        """slug column has unique=True."""
        col = Product.__table__.columns["slug"]
        assert col.unique is True

    def test_status_enum_round_trip(self) -> None:
        """status enum value survives assign and read."""
        instance = Product(
            slug=f"test-{uuid.uuid4().hex[:8]}",
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=uuid.uuid4(),
        )
        instance.status = ProductStatus.draft
        assert instance.status == ProductStatus.draft
        instance.status = ProductStatus.active
        assert instance.status == ProductStatus.active
        instance.status = ProductStatus.discontinued
        assert instance.status == ProductStatus.discontinued
