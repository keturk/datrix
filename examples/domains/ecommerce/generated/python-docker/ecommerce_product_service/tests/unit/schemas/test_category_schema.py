"""Unit tests for Category schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

import pytest
from pydantic import ValidationError

from ecommerce_product_service.schemas.db.category import (
    CategoryCreate,
    CategoryResponse,
    CategoryUpdate,
)


@pytest.mark.unit
class TestCategoryCreate:
    """Test CategoryCreate schema validation."""

    def test_valid_data(self) -> None:
        """Create schema accepts valid data."""
        _name = f"test_{uuid.uuid4().hex[:8]}"
        _slug = f"test-{uuid.uuid4().hex[:8]}"
        data = {
            "name": _name,
            "description": "The quick brown fox jumps over the lazy dog",
            "slug": _slug,
        }
        schema = CategoryCreate(**data)
        assert schema.name == _name
        assert schema.description == "The quick brown fox jumps over the lazy dog"
        assert schema.slug == _slug

    def test_name_required(self) -> None:
        """name is required for Create schema."""
        data = {
            "description": "The quick brown fox jumps over the lazy dog",
            "slug": f"test-{uuid.uuid4().hex[:8]}",
        }
        with pytest.raises(ValidationError):
            CategoryCreate(**data)

    def test_slug_required(self) -> None:
        """slug is required for Create schema."""
        data = {
            "name": f"test_{uuid.uuid4().hex[:8]}",
            "description": "The quick brown fox jumps over the lazy dog",
        }
        with pytest.raises(ValidationError):
            CategoryCreate(**data)

    def test_name_rejects_over_max_length(self) -> None:
        """CategoryCreate rejects name exceeding 100 chars."""
        data = {
            "name": f"test_{uuid.uuid4().hex[:8]}",
            "description": "The quick brown fox jumps over the lazy dog",
            "slug": f"test-{uuid.uuid4().hex[:8]}",
        }
        data["name"] = "x" * (100 + 1)
        with pytest.raises(ValidationError):
            CategoryCreate(**data)

    def test_name_accepts_at_max_length(self) -> None:
        """CategoryCreate accepts name at exactly 100 chars."""
        data = {
            "name": f"test_{uuid.uuid4().hex[:8]}",
            "description": "The quick brown fox jumps over the lazy dog",
            "slug": f"test-{uuid.uuid4().hex[:8]}",
        }
        data["name"] = "x" * 100
        schema = CategoryCreate(**data)
        assert len(schema.name) == 100

    def test_name_trim_strips_whitespace(self) -> None:
        """name with leading/trailing whitespace is stripped."""
        data = {
            "name": f"test_{uuid.uuid4().hex[:8]}",
            "description": "The quick brown fox jumps over the lazy dog",
            "slug": f"test-{uuid.uuid4().hex[:8]}",
        }
        data["name"] = "  test value  "
        schema = CategoryCreate(**data)
        assert schema.name == "test value"

    def test_id_excluded_from_create(self) -> None:
        """Server-managed field id is not in CategoryCreate."""
        assert "id" not in CategoryCreate.model_fields

    def test_id_excluded_from_update(self) -> None:
        """Server-managed field id is not in CategoryUpdate."""
        assert "id" not in CategoryUpdate.model_fields

    def test_id_in_response(self) -> None:
        """Server-managed field id is in CategoryResponse."""
        assert "id" in CategoryResponse.model_fields

    def test_created_at_excluded_from_create(self) -> None:
        """Server-managed field created_at is not in CategoryCreate."""
        assert "created_at" not in CategoryCreate.model_fields

    def test_created_at_excluded_from_update(self) -> None:
        """Server-managed field created_at is not in CategoryUpdate."""
        assert "created_at" not in CategoryUpdate.model_fields

    def test_created_at_in_response(self) -> None:
        """Server-managed field created_at is in CategoryResponse."""
        assert "created_at" in CategoryResponse.model_fields

    def test_updated_at_excluded_from_create(self) -> None:
        """Server-managed field updated_at is not in CategoryCreate."""
        assert "updated_at" not in CategoryCreate.model_fields

    def test_updated_at_excluded_from_update(self) -> None:
        """Server-managed field updated_at is not in CategoryUpdate."""
        assert "updated_at" not in CategoryUpdate.model_fields

    def test_updated_at_in_response(self) -> None:
        """Server-managed field updated_at is in CategoryResponse."""
        assert "updated_at" in CategoryResponse.model_fields


@pytest.mark.unit
class TestCategoryUpdate:
    """Test CategoryUpdate schema validation."""

    def test_all_fields_optional(self) -> None:
        """Update schema allows empty payload."""
        schema = CategoryUpdate()
        assert schema.name is None
        assert schema.description is None
        assert schema.slug is None

    def test_partial_update(self) -> None:
        """Update schema accepts partial data."""
        _name = f"alt_{uuid.uuid4().hex[:8]}"
        schema = CategoryUpdate(**{"name": _name})
        assert schema.name == _name


@pytest.mark.unit
class TestCategoryResponse:
    """Test CategoryResponse schema settings."""

    def test_from_attributes(self) -> None:
        """Response schema has ORM mode enabled."""
        config = CategoryResponse.model_config
        assert config.get("from_attributes") is True
