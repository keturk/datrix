/**
 * Deployment test for ecommerce.UserService.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { fetchWithRetry, httpRequest, waitForHealth, httpGetJson } from './helpers';

const SERVICE_PORT = 8000;
const BASE_URL = `http://localhost:${SERVICE_PORT}`;
const HEALTH_TIMEOUT_MS = 120_000;

describe('UserService Deployment', () => {
  it('health check returns 200 with healthy status', async () => {
    const healthy = await waitForHealth(BASE_URL, HEALTH_TIMEOUT_MS);
    expect(healthy).toBe(true);

    const data = await httpGetJson(`${BASE_URL}/health`);
    expect(data).toBeDefined();
    expect(['healthy', 'ok']).toContain(data.status);
  }, HEALTH_TIMEOUT_MS + 10_000);

  it('responds to GET /', async () => {
    const response = await fetchWithRetry(BASE_URL);
    expect(response.status).toBeLessThan(500);
  });

  // --- Endpoint Smoke Tests ---

  it('GET /api/v1/users returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/users`, {
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(200);
  });

  it('GET /api/v1/users/00000000-0000-0000-0000-000000000001 returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(200);
  });

  it('POST /api/v1/users returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/users`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('PUT /api/v1/users/00000000-0000-0000-0000-000000000001 returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(200);
  });

  it('DELETE /api/v1/users/00000000-0000-0000-0000-000000000001 returns 204', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
      method: 'DELETE',
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(204);
  });

  it('POST /api/v1/register returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/register`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('POST /api/v1/login returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/login`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('POST /api/v1/logout returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/logout`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('GET /api/v1/me returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/me`, {
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(200);
  });

  it('PUT /api/v1/me returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/me`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(200);
  });

  it('PUT /api/v1/me/password returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/me/password`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(200);
  });

  it('POST /api/v1/verify-email returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/verify-email`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('POST /api/v1/forgot-password returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/forgot-password`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('POST /api/v1/reset-password returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/reset-password`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('PUT /api/v1/00000000-0000-0000-0000-000000000001/status returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/00000000-0000-0000-0000-000000000001/status`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(200);
  });

  // --- Authentication / Authorization Tests ---

  describe('UserApi Auth', () => {
    it('Public POST /api/v1/register without auth returns 201', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/register`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(201);
    });

    it('Public POST /api/v1/login without auth returns 201', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/login`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(201);
    });

    it('Public POST /api/v1/verify-email without auth returns 201', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/verify-email`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(201);
    });

    it('Public POST /api/v1/forgot-password without auth returns 201', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/forgot-password`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(201);
    });

    it('Public POST /api/v1/reset-password without auth returns 201', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/reset-password`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(201);
    });

    it('GET /api/v1/users without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/users`);
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/users/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/users without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/users/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('DELETE /api/v1/users/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
        method: 'DELETE',
      });
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/logout without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/logout`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/me without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/me`);
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/me without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/me`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/me/password without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/me/password`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/00000000-0000-0000-0000-000000000001/status without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/00000000-0000-0000-0000-000000000001/status`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/users with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users`, {
        headers: { 'Authorization': 'Bearer wrong-role-token' },
      });
      expect(response.status).toBe(403);
    });

    it('GET /api/v1/users/00000000-0000-0000-0000-000000000001 with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
        headers: { 'Authorization': 'Bearer wrong-role-token' },
      });
      expect(response.status).toBe(403);
    });

    it('POST /api/v1/users with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer wrong-role-token' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(403);
    });

    it('PUT /api/v1/users/00000000-0000-0000-0000-000000000001 with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer wrong-role-token' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(403);
    });

    it('DELETE /api/v1/users/00000000-0000-0000-0000-000000000001 with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/users/00000000-0000-0000-0000-000000000001`, {
        method: 'DELETE',
        headers: { 'Authorization': 'Bearer wrong-role-token' },
      });
      expect(response.status).toBe(403);
    });

    it('PUT /api/v1/00000000-0000-0000-0000-000000000001/status with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/00000000-0000-0000-0000-000000000001/status`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer wrong-role-token' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(403);
    });

  });
});
