"""Unit tests for RefundResult struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_payment_service.schemas.refund_result import RefundResult


@pytest.mark.unit
class TestRefundResultStruct:
    """Test RefundResult Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a RefundResult instance with all fields."""
        instance = RefundResult(
            success=False,
            refund_transaction_id="sample_text",
            error="foo_bar",
        )
        assert instance is not None
        assert instance.success == False
        assert instance.refund_transaction_id == "sample_text"
        assert instance.error == "foo_bar"

    def test_success_required(self) -> None:
        """success is required — omitting it raises ValidationError."""
        kwargs = {
            "refund_transaction_id": "sample_text",
            "error": "foo_bar",
        }
        with pytest.raises(ValidationError):
            RefundResult(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        instance = RefundResult(
            success=False,
            refund_transaction_id="sample_text",
            error="foo_bar",
        )
        data = instance.model_dump()
        restored = RefundResult.model_validate(data)
        assert restored == instance
