-- Audit log table for payments.
-- Generated by Datrix -- do not edit.
CREATE TABLE payments_audit_log (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    entity_id UUID NOT NULL,
    action VARCHAR(10) NOT NULL,
    old_values JSONB,
    new_values JSONB,
    user_id VARCHAR(255),
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    trace_id VARCHAR(64));

CREATE INDEX idx_payments_audit_log_entity_id ON payments_audit_log (entity_id);
CREATE INDEX idx_payments_audit_log_timestamp ON payments_audit_log (timestamp);
CREATE INDEX idx_payments_audit_log_action ON payments_audit_log (action);
