#!/usr/bin/env bash
# ecommerce-shipping-service — development scripts (NestJS / ecommerce_shipping_service)
# Auto-generated by datrix-codegen-typescript. Do not edit manually.
set -euo pipefail
case "${1:-}" in
  install)
    npm ci
    ;;
  dev)
    npm run start:dev -- --host 0.0.0.0 --port 8004
    ;;
  build)
    npm run build
    ;;
  test)
    shift
    npm test -- "$@"
    ;;
  lint)
    npm run lint
    ;;
  db-migrate)
    echo "Database migrations are project-specific; configure TypeORM CLI or your migration tool."
    ;;
  help|*)
    echo "Usage: ./scripts.sh <install|dev|build|test|lint|db-migrate|help>"
    ;;
esac
