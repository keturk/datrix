"""Unit tests for AddTrackingEventRequest struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_shipping_service.schemas.add_tracking_event_request import AddTrackingEventRequest
from ecommerce_shipping_service.enums.shipment_status import ShipmentStatus


@pytest.mark.unit
class TestAddTrackingEventRequestStruct:
    """Test AddTrackingEventRequest Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a AddTrackingEventRequest instance with all fields."""
        instance = AddTrackingEventRequest(
            status=ShipmentStatus.pending,
            location="sample_text",
            description="world",
        )
        assert instance is not None
        assert instance.status == ShipmentStatus.pending
        assert instance.location == "sample_text"
        assert instance.description == "world"

    def test_status_required(self) -> None:
        """status is required — omitting it raises ValidationError."""
        kwargs = {
            "location": "sample_text",
            "description": "world",
        }
        with pytest.raises(ValidationError):
            AddTrackingEventRequest(**kwargs)

    def test_location_required(self) -> None:
        """location is required — omitting it raises ValidationError."""
        kwargs = {
            "status": ShipmentStatus.pending,
            "description": "world",
        }
        with pytest.raises(ValidationError):
            AddTrackingEventRequest(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        instance = AddTrackingEventRequest(
            status=ShipmentStatus.pending,
            location="sample_text",
            description="world",
        )
        data = instance.model_dump()
        restored = AddTrackingEventRequest.model_validate(data)
        assert restored == instance
