"""Integration tests for Shipment service CRUD operations.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_shipping_service.models.db.shipment import Shipment
from ecommerce_shipping_service.schemas.db.shipment import ShipmentCreate, ShipmentUpdate
from ecommerce_shipping_service.services._base import EntityNotFoundError

from ecommerce_shipping_service.services.db.shipment_service import ShipmentService
from ecommerce_shipping_service.enums.shipment_status import ShipmentStatus
from ecommerce_shipping_service.enums.shipping_carrier import ShippingCarrier


@pytest.mark.integration
@pytest.mark.asyncio
class TestShipmentRepository:
    """Test Shipment service CRUD operations."""

    async def test_create(self, db_session: AsyncSession) -> None:
        """Create persists entity."""
        service = ShipmentService(db_session)
        _tracking_number = f"test_{uuid.uuid4().hex[:8]}"
        payload = ShipmentCreate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            tracking_number=_tracking_number,
            carrier=ShippingCarrier.fed_ex,
            status=ShipmentStatus.pending,
            destination={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            weight=decimal.Decimal("0.01"),
            estimated_delivery=datetime.datetime(
                2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
            ),
            actual_delivery=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            failure_reason="hello",
        )
        created = await service.create(payload)
        await db_session.refresh(created)
        assert created.order_id == uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7")
        assert created.tracking_number == _tracking_number
        assert created.carrier == ShippingCarrier.fed_ex
        assert created.status == ShipmentStatus.pending
        assert created.destination == {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
        assert created.weight == decimal.Decimal(str(decimal.Decimal("0.01")))
        assert created.estimated_delivery == datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )
        assert created.actual_delivery == datetime.datetime(
            2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc
        )
        assert created.failure_reason == "hello"

    async def test_get_by_id_not_found(self, db_session: AsyncSession) -> None:
        """Get by ID raises not found for unknown row."""
        service = ShipmentService(db_session)
        _nonexistent_id = uuid.uuid4()
        with pytest.raises(EntityNotFoundError):
            await service.get(_nonexistent_id)

    async def test_update_not_found(self, db_session: AsyncSession) -> None:
        """Update raises not found for unknown row."""
        service = ShipmentService(db_session)
        _nonexistent_id = uuid.uuid4()
        payload = ShipmentUpdate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
        )
        with pytest.raises(EntityNotFoundError):
            await service.update(_nonexistent_id, payload)

    async def test_delete_not_found(self, db_session: AsyncSession) -> None:
        """Delete raises not found for unknown row."""
        service = ShipmentService(db_session)
        _nonexistent_id = uuid.uuid4()
        with pytest.raises(EntityNotFoundError):
            await service.delete(_nonexistent_id)

    async def test_get_by_tracking_number(self, db_session: AsyncSession) -> None:
        """get_by_tracking_number returns entity with matching tracking_number."""
        service = ShipmentService(db_session)
        _tracking_number = f"test_{uuid.uuid4().hex[:8]}"
        payload = ShipmentCreate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            tracking_number=_tracking_number,
            carrier=ShippingCarrier.fed_ex,
            status=ShipmentStatus.pending,
            destination={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            weight=decimal.Decimal("0.01"),
            estimated_delivery=datetime.datetime(
                2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
            ),
            actual_delivery=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            failure_reason="hello",
        )
        created = await service.create(payload)
        found = await service.get_by_tracking_number(created.tracking_number)
        assert found.id == created.id

    async def test_get_by_tracking_number_not_found(self, db_session: AsyncSession) -> None:
        """get_by_tracking_number raises EntityNotFoundError for unknown value."""
        service = ShipmentService(db_session)
        with pytest.raises(EntityNotFoundError):
            await service.get_by_tracking_number("nonexistent_value")

    async def test_find_by_order_id(self, db_session: AsyncSession) -> None:
        """find_by_order_id returns matching entities."""
        service = ShipmentService(db_session)
        _tracking_number = f"test_{uuid.uuid4().hex[:8]}"
        payload = ShipmentCreate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            tracking_number=_tracking_number,
            carrier=ShippingCarrier.fed_ex,
            status=ShipmentStatus.pending,
            destination={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            weight=decimal.Decimal("0.01"),
            estimated_delivery=datetime.datetime(
                2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
            ),
            actual_delivery=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            failure_reason="hello",
        )
        created = await service.create(payload)
        results = await service.find_by_order_id(created.order_id)
        assert len(results) >= 1
        assert any(r.id == created.id for r in results)

    async def test_find_by_order_id_empty(self, db_session: AsyncSession) -> None:
        """find_by_order_id returns empty list for no matches."""
        service = ShipmentService(db_session)
        results = await service.find_by_order_id(uuid.uuid4())
        assert results == []


@pytest.mark.integration
@pytest.mark.asyncio
class TestShipmentLifecycleHooks:
    """Test Shipment lifecycle hook behavior."""

    async def test_after_update_detects_status_change(self, db_session: AsyncSession) -> None:
        """afterUpdate detects when status changes."""
        service = ShipmentService(db_session)
        _tracking_number = f"test_{uuid.uuid4().hex[:8]}"
        payload = ShipmentCreate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            tracking_number=_tracking_number,
            carrier=ShippingCarrier.fed_ex,
            status=ShipmentStatus.pending,
            destination={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            weight=decimal.Decimal("0.01"),
            estimated_delivery=datetime.datetime(
                2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
            ),
            actual_delivery=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            failure_reason="hello",
        )
        created = await service.create(payload)
        update_payload = ShipmentUpdate(status=ShipmentStatus.picked_up)
        updated = await service.update(created.id, update_payload)
        assert updated is not None

    async def test_after_update_no_change_when_status_unchanged(
        self, db_session: AsyncSession
    ) -> None:
        """afterUpdate does not detect change when status is not modified."""
        service = ShipmentService(db_session)
        _tracking_number = f"test_{uuid.uuid4().hex[:8]}"
        payload = ShipmentCreate(
            order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            tracking_number=_tracking_number,
            carrier=ShippingCarrier.fed_ex,
            status=ShipmentStatus.pending,
            destination={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            weight=decimal.Decimal("0.01"),
            estimated_delivery=datetime.datetime(
                2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
            ),
            actual_delivery=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            failure_reason="hello",
        )
        created = await service.create(payload)
        update_payload = ShipmentUpdate(order_id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"))
        updated = await service.update(created.id, update_payload)
        assert updated is not None
