/**
 * Configuration unit test for EcommerceShippingService.
 * Verifies the application configuration module loads correctly.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import configuration from '../src/config/configuration';

describe('Application Configuration', () => {
  beforeAll(() => {
    process.env['JWT_SECRET'] = 'test-secret-for-config-test';
  });

  it('returns a configuration object', () => {
    const config = configuration();
    expect(config).toBeDefined();
    expect(typeof config).toBe('object');
  });

  it('provides a port number', () => {
    const config = configuration();
    expect(typeof config.port).toBe('number');
    expect(config.port).toBeGreaterThan(0);
  });

  it('includes JWT secret', () => {
    const config = configuration();
    expect(config.jwtSecret).toBeDefined();
    expect(typeof config.jwtSecret).toBe('string');
  });
});
