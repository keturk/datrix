"""Unit tests for User model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest

from ecommerce_user_service.models.db.user import User
from ecommerce_user_service.enums.user_role import UserRole
from ecommerce_user_service.enums.user_status import UserStatus


@pytest.mark.unit
class TestUserModel:
    """Test User SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a User instance with required fields."""
        instance = User(
            email="test@example.com",
            password_hash="T3st!ng#789",
            first_name="TestName",
            last_name="foo_bar",
        )
        assert instance.email == "test@example.com"
        assert instance.password_hash == "T3st!ng#789"
        assert instance.first_name == "TestName"
        assert instance.last_name == "foo_bar"

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        instance = User(
            email="test@example.com",
            password_hash="T3st!ng#789",
            first_name="TestName",
            last_name="foo_bar",
        )
        _alt_email = f"alt_{uuid.uuid4().hex[:8]}@example.com"
        instance.email = _alt_email
        assert instance.email == _alt_email
        instance.password_hash = "C0mpl3x$Pwd"
        assert instance.password_hash == "C0mpl3x$Pwd"
        instance.first_name = "AltValue"
        assert instance.first_name == "AltValue"
        instance.last_name = "test_string"
        assert instance.last_name == "test_string"
        instance.phone_number = "15552223333"
        assert instance.phone_number == "15552223333"
        instance.role = UserRole.admin
        assert instance.role == UserRole.admin
        instance.status = UserStatus.inactive
        assert instance.status == UserStatus.inactive
        instance.last_login_at = datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.last_login_at == datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )
        instance.email_verified_at = datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.email_verified_at == datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        instance.email_verification_token = "hello"
        assert instance.email_verification_token == "hello"
        instance.password_reset_token = "foo_bar"
        assert instance.password_reset_token == "foo_bar"
        instance.password_reset_expiry = datetime.datetime(
            2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.password_reset_expiry == datetime.datetime(
            2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc
        )
        instance.shipping_address = {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
        assert instance.shipping_address == {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
        instance.billing_address = {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
        assert instance.billing_address == {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(User, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = User(
            email="test@example.com",
            password_hash="T3st!ng#789",
            first_name="TestName",
            last_name="foo_bar",
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_first_name_max_length_in_column(self) -> None:
        """first_name column has max_length=100."""
        col = User.__table__.columns["first_name"]
        assert col.type.length == 100

    def test_last_name_max_length_in_column(self) -> None:
        """last_name column has max_length=100."""
        col = User.__table__.columns["last_name"]
        assert col.type.length == 100

    def test_email_unique_constraint(self) -> None:
        """email column has unique=True."""
        col = User.__table__.columns["email"]
        assert col.unique is True

    def test_role_enum_round_trip(self) -> None:
        """role enum value survives assign and read."""
        instance = User(
            email="test@example.com",
            password_hash="T3st!ng#789",
            first_name="TestName",
            last_name="foo_bar",
        )
        instance.role = UserRole.customer
        assert instance.role == UserRole.customer
        instance.role = UserRole.admin
        assert instance.role == UserRole.admin
        instance.role = UserRole.support
        assert instance.role == UserRole.support

    def test_status_enum_round_trip(self) -> None:
        """status enum value survives assign and read."""
        instance = User(
            email="test@example.com",
            password_hash="T3st!ng#789",
            first_name="TestName",
            last_name="foo_bar",
        )
        instance.status = UserStatus.active
        assert instance.status == UserStatus.active
        instance.status = UserStatus.inactive
        assert instance.status == UserStatus.inactive
        instance.status = UserStatus.suspended
        assert instance.status == UserStatus.suspended
        instance.status = UserStatus.pending
        assert instance.status == UserStatus.pending
