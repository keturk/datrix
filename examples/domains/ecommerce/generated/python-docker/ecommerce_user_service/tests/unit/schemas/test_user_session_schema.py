"""Unit tests for UserSession schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_user_service.schemas.db.user_session import (
    UserSessionCreate,
    UserSessionResponse,
    UserSessionUpdate,
)


@pytest.mark.unit
class TestUserSessionCreate:
    """Test UserSessionCreate schema validation."""

    def test_valid_data(self) -> None:
        """Create schema accepts valid data."""
        _token = f"test_{uuid.uuid4().hex[:8]}"
        _user_id = uuid.uuid4()
        data = {
            "token": _token,
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": _user_id,
        }
        schema = UserSessionCreate(**data)
        assert schema.token == _token
        assert schema.device_name == "sample_text"
        assert str(schema.ip_address) == "203.0.113.50"
        assert schema.user_agent == "world"
        assert schema.expires_at == datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc)
        assert schema.last_activity_at == datetime.datetime(
            2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
        )
        assert schema.user_id == _user_id

    def test_token_required(self) -> None:
        """token is required for Create schema."""
        data = {
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_expires_at_required(self) -> None:
        """expires_at is required for Create schema."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_user_id_required(self) -> None:
        """user_id is required for Create schema."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
        }
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_token_rejects_over_max_length(self) -> None:
        """UserSessionCreate rejects token exceeding 255 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["token"] = "x" * (255 + 1)
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_token_accepts_at_max_length(self) -> None:
        """UserSessionCreate accepts token at exactly 255 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["token"] = "x" * 255
        schema = UserSessionCreate(**data)
        assert len(schema.token) == 255

    def test_device_name_rejects_over_max_length(self) -> None:
        """UserSessionCreate rejects device_name exceeding 500 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["device_name"] = "x" * (500 + 1)
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_device_name_accepts_at_max_length(self) -> None:
        """UserSessionCreate accepts device_name at exactly 500 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["device_name"] = "x" * 500
        schema = UserSessionCreate(**data)
        assert len(schema.device_name) == 500

    def test_user_agent_rejects_over_max_length(self) -> None:
        """UserSessionCreate rejects user_agent exceeding 255 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["user_agent"] = "x" * (255 + 1)
        with pytest.raises(ValidationError):
            UserSessionCreate(**data)

    def test_user_agent_accepts_at_max_length(self) -> None:
        """UserSessionCreate accepts user_agent at exactly 255 chars."""
        data = {
            "token": f"test_{uuid.uuid4().hex[:8]}",
            "device_name": "sample_text",
            "ip_address": "203.0.113.50",
            "user_agent": "world",
            "expires_at": datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            "last_activity_at": datetime.datetime(
                2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
            ),
            "user_id": uuid.uuid4(),
        }
        data["user_agent"] = "x" * 255
        schema = UserSessionCreate(**data)
        assert len(schema.user_agent) == 255

    def test_id_excluded_from_create(self) -> None:
        """Server-managed field id is not in UserSessionCreate."""
        assert "id" not in UserSessionCreate.model_fields

    def test_id_excluded_from_update(self) -> None:
        """Server-managed field id is not in UserSessionUpdate."""
        assert "id" not in UserSessionUpdate.model_fields

    def test_id_in_response(self) -> None:
        """Server-managed field id is in UserSessionResponse."""
        assert "id" in UserSessionResponse.model_fields

    def test_created_at_excluded_from_create(self) -> None:
        """Server-managed field created_at is not in UserSessionCreate."""
        assert "created_at" not in UserSessionCreate.model_fields

    def test_created_at_excluded_from_update(self) -> None:
        """Server-managed field created_at is not in UserSessionUpdate."""
        assert "created_at" not in UserSessionUpdate.model_fields

    def test_created_at_in_response(self) -> None:
        """Server-managed field created_at is in UserSessionResponse."""
        assert "created_at" in UserSessionResponse.model_fields

    def test_updated_at_excluded_from_create(self) -> None:
        """Server-managed field updated_at is not in UserSessionCreate."""
        assert "updated_at" not in UserSessionCreate.model_fields

    def test_updated_at_excluded_from_update(self) -> None:
        """Server-managed field updated_at is not in UserSessionUpdate."""
        assert "updated_at" not in UserSessionUpdate.model_fields

    def test_updated_at_in_response(self) -> None:
        """Server-managed field updated_at is in UserSessionResponse."""
        assert "updated_at" in UserSessionResponse.model_fields


@pytest.mark.unit
class TestUserSessionUpdate:
    """Test UserSessionUpdate schema validation."""

    def test_all_fields_optional(self) -> None:
        """Update schema allows empty payload."""
        schema = UserSessionUpdate()
        assert schema.token is None
        assert schema.device_name is None
        assert schema.ip_address is None
        assert schema.user_agent is None
        assert schema.expires_at is None
        assert schema.last_activity_at is None
        assert schema.user_id is None

    def test_partial_update(self) -> None:
        """Update schema accepts partial data."""
        _token = f"alt_{uuid.uuid4().hex[:8]}"
        schema = UserSessionUpdate(**{"token": _token})
        assert schema.token == _token


@pytest.mark.unit
class TestUserSessionResponse:
    """Test UserSessionResponse schema settings."""

    def test_from_attributes(self) -> None:
        """Response schema has ORM mode enabled."""
        config = UserSessionResponse.model_config
        assert config.get("from_attributes") is True
