test:
  defaults:
    timeout: 10s
    retry:
      maxAttempts: 2
      backoff:
        type: exponential
        initial: 100ms
        multiplier: 2
        max: 2s
        jitter: 0.1
      retryableErrors: [ConnectionError, TimeoutError, ServiceUnavailable]
      nonRetryableErrors: [ValidationError, AuthenticationError]

development:
  defaults:
    timeout: 10s
    retry:
      maxAttempts: 2
      backoff:
        type: exponential
        initial: 100ms
        multiplier: 2
        max: 2s
        jitter: 0.1
      retryableErrors: [ConnectionError, TimeoutError, ServiceUnavailable]
      nonRetryableErrors: [ValidationError, AuthenticationError]

production:
  defaults:
    timeout: 10s
    retry:
      maxAttempts: 2
      backoff:
        type: exponential
        initial: 100ms
        multiplier: 2
        max: 2s
        jitter: 0.1
      retryableErrors: [ConnectionError, TimeoutError, ServiceUnavailable]
      nonRetryableErrors: [ValidationError, AuthenticationError]
