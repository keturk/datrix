/**
 * Initial schema migration for catalog-db.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 *
 * Syncs the database schema to match MikroORM entity metadata via
 * SqlSchemaGenerator.updateSchema() (MikroORM v6: schema helpers live
 * on the SQL stack, not EntityManager).
 *
 * Uses updateSchema() instead of createSchema() because the init-db SQL
 * may have already created enum types — updateSchema() diffs the current
 * DB state and only creates objects that are missing (tables, indexes).
 * Subsequent schema changes should use dedicated migrations.
 */
import { Migration } from '@mikro-orm/migrations';
import { SqlSchemaGenerator } from '@mikro-orm/postgresql';

export class InitialSchema1700000000000 extends Migration {
  override async up(): Promise<void> {
    const generator = new SqlSchemaGenerator(this.getEntityManager());
    await generator.updateSchema();
  }

  override async down(): Promise<void> {
    const generator = new SqlSchemaGenerator(this.getEntityManager());
    await generator.dropSchema();
  }
}
