base:
  platform: compose
  port: 8002
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
  healthCheck:
    path: /health
    initialDelay: 10s
  rdbms:
    loanDb:
      engine: postgres
      host: localhost
      database: library_loans
      poolSize: 20
      platform: container
  registration:
    tags:
    - api
    - library
    - loans
    - v1
    meta: {}
    healthCheck:
      type: http
      path: /health
      interval: 10s
  resilience:
    defaults:
      timeout: 10s
      retry:
        maxAttempts: 2
        backoff:
          type: exponential
          initial: 100ms
          multiplier: 2
production:
  platform: ecs-fargate
  replicas: 2
  rdbms:
    loanDb:
      engine: postgres
      host: ${LOAN_DB_HOST}
      database: library_loans
      poolSize: 40
      platform: rds
      ssl: true
      _replace: true
  strategy:
    rolling:
      maxUnavailable: 25%
