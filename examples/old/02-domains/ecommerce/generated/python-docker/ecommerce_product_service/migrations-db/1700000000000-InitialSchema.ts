/**
 * Initial schema migration for db.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 *
 * Creates all tables from TypeORM entity metadata via synchronize().
 * Subsequent schema changes should use dedicated migrations.
 */
import { MigrationInterface, QueryRunner } from 'typeorm';
import { Category } from '../src/src/entities/category.entity';
import { Product } from '../src/src/entities/product.entity';
import { InventoryReservation } from '../src/src/entities/inventory_reservation.entity';

const _entities = [Category, Product, InventoryReservation];

export class InitialSchema1700000000000 implements MigrationInterface {
  name = 'InitialSchema1700000000000';

  public async up(queryRunner: QueryRunner): Promise<void> {
    await queryRunner.connection.synchronize();
  }

  public async down(queryRunner: QueryRunner): Promise<void> {
    const entities = queryRunner.connection.entityMetadatas;
    for (const entity of [...entities].reverse()) {
      const tableName = entity.tableName;
      const tableExists = await queryRunner.hasTable(tableName);
      if (tableExists) {
        await queryRunner.dropTable(tableName, true, true);
      }
    }
  }
}
