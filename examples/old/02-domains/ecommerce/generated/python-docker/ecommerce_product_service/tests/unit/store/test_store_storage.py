"""Unit tests for store storage configuration.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest


@pytest.mark.unit
class TestStoreStorageConfig:
    """Test store storage folder configuration."""

    def test_images_path_non_empty(self) -> None:
        """images folder path is non-empty."""
        path = "products/images/"
        assert path, "Storage folder path must be non-empty"

    def test_images_allowed_types(self) -> None:
        """images has allowed MIME types defined."""
        allowed = ["image/jpeg", "image/png", "image/webp"]
        assert isinstance(allowed, list)

    def test_images_max_size_positive(self) -> None:
        """images max size is positive."""
        max_bytes = 10485760
        assert max_bytes > 0

    def test_thumbnails_path_non_empty(self) -> None:
        """thumbnails folder path is non-empty."""
        path = "products/thumbnails/"
        assert path, "Storage folder path must be non-empty"

    def test_thumbnails_allowed_types(self) -> None:
        """thumbnails has allowed MIME types defined."""
        allowed = ["image/jpeg", "image/png", "image/webp"]
        assert isinstance(allowed, list)

    def test_thumbnails_max_size_positive(self) -> None:
        """thumbnails max size is positive."""
        max_bytes = 2097152
        assert max_bytes > 0
