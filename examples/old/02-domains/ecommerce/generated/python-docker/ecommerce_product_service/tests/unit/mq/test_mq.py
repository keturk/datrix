"""Unit tests for pubsub schemas and handler dispatch.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import decimal
import json
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_product_service.mq.schemas import (
    ProductCreatedPayload,
    InventoryUpdatedPayload,
    InventoryReservedPayload,
    InventoryReleasedPayload,
    OrderConfirmedPayload,
    OrderCancelledPayload,
)


class TestProductCreatedPayload:
    """Tests for ProductCreated event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = ["product_id", "name", "price"]
        assert list(ProductCreatedPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "name": "test",
            "price": decimal.Decimal("0"),
        }
        instance = ProductCreatedPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "name": "test",
            "price": decimal.Decimal("0"),
        }
        instance = ProductCreatedPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = ProductCreatedPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """ProductCreatedPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            ProductCreatedPayload()


class TestInventoryUpdatedPayload:
    """Tests for InventoryUpdated event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = ["product_id", "old_quantity", "new_quantity"]
        assert list(InventoryUpdatedPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "old_quantity": 1,
            "new_quantity": 1,
        }
        instance = InventoryUpdatedPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "old_quantity": 1,
            "new_quantity": 1,
        }
        instance = InventoryUpdatedPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = InventoryUpdatedPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """InventoryUpdatedPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            InventoryUpdatedPayload()


class TestInventoryReservedPayload:
    """Tests for InventoryReserved event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = ["reservation_id", "product_ids"]
        assert list(InventoryReservedPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "product_ids": [uuid.uuid4()],
        }
        instance = InventoryReservedPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "product_ids": [uuid.uuid4()],
        }
        instance = InventoryReservedPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = InventoryReservedPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """InventoryReservedPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            InventoryReservedPayload()


class TestInventoryReleasedPayload:
    """Tests for InventoryReleased event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = ["reservation_id", "reason"]
        assert list(InventoryReleasedPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "reason": "test",
        }
        instance = InventoryReleasedPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "reason": "test",
        }
        instance = InventoryReleasedPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = InventoryReleasedPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """InventoryReleasedPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            InventoryReleasedPayload()


class TestOrderConfirmedPayload:
    """Tests for OrderConfirmed event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = [
            "order_id",
            "payment_id",
            "reservation_id",
            "shipping_address",
            "items",
            "estimated_weight",
        ]
        assert list(OrderConfirmedPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "payment_id": uuid.uuid4(),
            "reservation_id": uuid.uuid4(),
            "shipping_address": {
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "15551234567",
            },
            "items": [{"key": "value"}],
            "estimated_weight": decimal.Decimal("0"),
        }
        instance = OrderConfirmedPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "payment_id": uuid.uuid4(),
            "reservation_id": uuid.uuid4(),
            "shipping_address": {
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "15551234567",
            },
            "items": [{"key": "value"}],
            "estimated_weight": decimal.Decimal("0"),
        }
        instance = OrderConfirmedPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = OrderConfirmedPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """OrderConfirmedPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            OrderConfirmedPayload()


class TestOrderCancelledPayload:
    """Tests for OrderCancelled event payload schema."""

    def test_has_expected_field_names(self) -> None:
        """Schema has expected field names."""
        expected = ["order_id", "reason", "reservation_id"]
        assert list(OrderCancelledPayload.model_fields.keys()) == expected

    def test_valid_data_roundtrip(self) -> None:
        """Valid data round-trips through model."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "reason": "test",
            "reservation_id": uuid.uuid4(),
        }
        instance = OrderCancelledPayload(**valid_data)
        dumped = instance.model_dump()
        for key in valid_data:
            assert key in dumped

    def test_json_serialization(self) -> None:
        """Model serializes to JSON and back."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "reason": "test",
            "reservation_id": uuid.uuid4(),
        }
        instance = OrderCancelledPayload(**valid_data)
        json_str = instance.model_dump_json()
        assert isinstance(json_str, str)
        restored = OrderCancelledPayload.model_validate_json(json_str)
        assert restored.model_dump() == instance.model_dump()

    def test_rejects_missing_required_fields(self) -> None:
        """OrderCancelledPayload rejects when required fields are missing."""
        with pytest.raises(ValidationError):
            OrderCancelledPayload()


@pytest.mark.unit
class TestProducerEnvelope:
    """Producer envelope format: event_type and payload keys, payload validates."""

    def test_envelope_ProductCreated_has_event_type_and_payload(self) -> None:
        """Envelope for ProductCreated has event_type and payload; payload validates."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "name": "test",
            "price": decimal.Decimal("0"),
        }
        payload = ProductCreatedPayload(**valid_data)
        envelope = {
            "event_type": "ProductCreated",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "ProductCreated"
        assert "payload" in parsed
        ProductCreatedPayload.model_validate(parsed["payload"])

    def test_envelope_InventoryUpdated_has_event_type_and_payload(self) -> None:
        """Envelope for InventoryUpdated has event_type and payload; payload validates."""
        valid_data = {
            "product_id": uuid.uuid4(),
            "old_quantity": 1,
            "new_quantity": 1,
        }
        payload = InventoryUpdatedPayload(**valid_data)
        envelope = {
            "event_type": "InventoryUpdated",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "InventoryUpdated"
        assert "payload" in parsed
        InventoryUpdatedPayload.model_validate(parsed["payload"])

    def test_envelope_InventoryReserved_has_event_type_and_payload(self) -> None:
        """Envelope for InventoryReserved has event_type and payload; payload validates."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "product_ids": [uuid.uuid4()],
        }
        payload = InventoryReservedPayload(**valid_data)
        envelope = {
            "event_type": "InventoryReserved",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "InventoryReserved"
        assert "payload" in parsed
        InventoryReservedPayload.model_validate(parsed["payload"])

    def test_envelope_InventoryReleased_has_event_type_and_payload(self) -> None:
        """Envelope for InventoryReleased has event_type and payload; payload validates."""
        valid_data = {
            "reservation_id": uuid.uuid4(),
            "reason": "test",
        }
        payload = InventoryReleasedPayload(**valid_data)
        envelope = {
            "event_type": "InventoryReleased",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "InventoryReleased"
        assert "payload" in parsed
        InventoryReleasedPayload.model_validate(parsed["payload"])

    def test_envelope_OrderConfirmed_has_event_type_and_payload(self) -> None:
        """Envelope for OrderConfirmed has event_type and payload; payload validates."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "payment_id": uuid.uuid4(),
            "reservation_id": uuid.uuid4(),
            "shipping_address": {
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "15551234567",
            },
            "items": [{"key": "value"}],
            "estimated_weight": decimal.Decimal("0"),
        }
        payload = OrderConfirmedPayload(**valid_data)
        envelope = {
            "event_type": "OrderConfirmed",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "OrderConfirmed"
        assert "payload" in parsed
        OrderConfirmedPayload.model_validate(parsed["payload"])

    def test_envelope_OrderCancelled_has_event_type_and_payload(self) -> None:
        """Envelope for OrderCancelled has event_type and payload; payload validates."""
        valid_data = {
            "order_id": uuid.uuid4(),
            "reason": "test",
            "reservation_id": uuid.uuid4(),
        }
        payload = OrderCancelledPayload(**valid_data)
        envelope = {
            "event_type": "OrderCancelled",
            "payload": payload.model_dump(mode="json"),
        }
        json_str = json.dumps(envelope)
        parsed = json.loads(json_str)
        assert "event_type" in parsed
        assert parsed["event_type"] == "OrderCancelled"
        assert "payload" in parsed
        OrderCancelledPayload.model_validate(parsed["payload"])


@pytest.mark.integration
class TestProducerMethods:
    """Test producer payload construction and method invocation for each event."""

    def test_publish_product_created_payload_structure(self) -> None:
        """ProductCreatedPayload builds from valid data and serializes."""
        from ecommerce_product_service.mq.schemas import ProductCreatedPayload

        payload = ProductCreatedPayload(
            product_id=uuid.uuid4(),
            name="test",
            price=decimal.Decimal("0"),
        )
        dumped = payload.model_dump()
        assert list(dumped.keys())
        assert payload.model_dump_json()

    @pytest.mark.kafka
    @pytest.mark.asyncio
    async def test_publish_product_created_invokes_without_error(self) -> None:
        """publish_product_created() accepts valid payload without raising.

        Requires a running Kafka broker — skipped unless -m kafka is passed.
        """
        from ecommerce_product_service.mq.connection import KafkaConnection
        from ecommerce_product_service.mq.producer import KafkaEventProducer
        from ecommerce_product_service.mq.schemas import ProductCreatedPayload

        payload = ProductCreatedPayload(
            product_id=uuid.uuid4(),
            name="test",
            price=decimal.Decimal("0"),
        )
        connection = KafkaConnection()
        producer = KafkaEventProducer(connection)
        await producer.publish_product_created(**payload.model_dump())

    def test_publish_inventory_updated_payload_structure(self) -> None:
        """InventoryUpdatedPayload builds from valid data and serializes."""
        from ecommerce_product_service.mq.schemas import InventoryUpdatedPayload

        payload = InventoryUpdatedPayload(
            product_id=uuid.uuid4(),
            old_quantity=1,
            new_quantity=1,
        )
        dumped = payload.model_dump()
        assert list(dumped.keys())
        assert payload.model_dump_json()

    @pytest.mark.kafka
    @pytest.mark.asyncio
    async def test_publish_inventory_updated_invokes_without_error(self) -> None:
        """publish_inventory_updated() accepts valid payload without raising.

        Requires a running Kafka broker — skipped unless -m kafka is passed.
        """
        from ecommerce_product_service.mq.connection import KafkaConnection
        from ecommerce_product_service.mq.producer import KafkaEventProducer
        from ecommerce_product_service.mq.schemas import InventoryUpdatedPayload

        payload = InventoryUpdatedPayload(
            product_id=uuid.uuid4(),
            old_quantity=1,
            new_quantity=1,
        )
        connection = KafkaConnection()
        producer = KafkaEventProducer(connection)
        await producer.publish_inventory_updated(**payload.model_dump())

    def test_publish_inventory_reserved_payload_structure(self) -> None:
        """InventoryReservedPayload builds from valid data and serializes."""
        from ecommerce_product_service.mq.schemas import InventoryReservedPayload

        payload = InventoryReservedPayload(
            reservation_id=uuid.uuid4(),
            product_ids=[uuid.uuid4()],
        )
        dumped = payload.model_dump()
        assert list(dumped.keys())
        assert payload.model_dump_json()

    @pytest.mark.kafka
    @pytest.mark.asyncio
    async def test_publish_inventory_reserved_invokes_without_error(self) -> None:
        """publish_inventory_reserved() accepts valid payload without raising.

        Requires a running Kafka broker — skipped unless -m kafka is passed.
        """
        from ecommerce_product_service.mq.connection import KafkaConnection
        from ecommerce_product_service.mq.producer import KafkaEventProducer
        from ecommerce_product_service.mq.schemas import InventoryReservedPayload

        payload = InventoryReservedPayload(
            reservation_id=uuid.uuid4(),
            product_ids=[uuid.uuid4()],
        )
        connection = KafkaConnection()
        producer = KafkaEventProducer(connection)
        await producer.publish_inventory_reserved(**payload.model_dump())

    def test_publish_inventory_released_payload_structure(self) -> None:
        """InventoryReleasedPayload builds from valid data and serializes."""
        from ecommerce_product_service.mq.schemas import InventoryReleasedPayload

        payload = InventoryReleasedPayload(
            reservation_id=uuid.uuid4(),
            reason="test",
        )
        dumped = payload.model_dump()
        assert list(dumped.keys())
        assert payload.model_dump_json()

    @pytest.mark.kafka
    @pytest.mark.asyncio
    async def test_publish_inventory_released_invokes_without_error(self) -> None:
        """publish_inventory_released() accepts valid payload without raising.

        Requires a running Kafka broker — skipped unless -m kafka is passed.
        """
        from ecommerce_product_service.mq.connection import KafkaConnection
        from ecommerce_product_service.mq.producer import KafkaEventProducer
        from ecommerce_product_service.mq.schemas import InventoryReleasedPayload

        payload = InventoryReleasedPayload(
            reservation_id=uuid.uuid4(),
            reason="test",
        )
        connection = KafkaConnection()
        producer = KafkaEventProducer(connection)
        await producer.publish_inventory_released(**payload.model_dump())


from ecommerce_product_service.mq.consumer import HANDLER_DISPATCH


class TestHandlerDispatch:
    """Tests for HANDLER_DISPATCH completeness and shape."""

    def test_all_events_in_dispatch(self) -> None:
        """Every expected event is in HANDLER_DISPATCH."""
        expected_events = [
            "ProductCreated",
            "InventoryUpdated",
            "InventoryReserved",
            "InventoryReleased",
            "OrderConfirmed",
            "OrderCancelled",
        ]
        for event_name in expected_events:
            assert event_name in HANDLER_DISPATCH, (
                f"Missing HANDLER_DISPATCH entry for event: {event_name!r}"
            )

    def test_dispatch_values_are_two_tuples(self) -> None:
        """Each HANDLER_DISPATCH value is a (schema_cls, handler_fn) 2-tuple."""
        for event_name, value in HANDLER_DISPATCH.items():
            assert isinstance(value, tuple), (
                f"HANDLER_DISPATCH[{event_name!r}] must be a tuple, got {type(value)}"
            )
            assert len(value) == 2, (
                f"HANDLER_DISPATCH[{event_name!r}] must be (schema_cls, handler_fn), "
                f"got length {len(value)}"
            )

    def test_handlers_are_callable(self) -> None:
        """Each handler in HANDLER_DISPATCH is callable."""
        for event_name, (schema_cls, handler_fn) in HANDLER_DISPATCH.items():
            assert callable(handler_fn), (
                f"HANDLER_DISPATCH[{event_name!r}] handler must be callable, got {type(handler_fn)}"
            )


@pytest.mark.unit
@pytest.mark.asyncio
class TestHandlerExecution:
    """Handlers process valid payloads without error (no DB required)."""

    async def test_handle_product_created_processes_payload(self) -> None:
        """handle_product_created processes valid payload without error."""
        from ecommerce_product_service.mq.consumer import handle_product_created

        valid_data = {
            "product_id": uuid.uuid4(),
            "name": "test",
            "price": decimal.Decimal("0"),
        }
        payload = ProductCreatedPayload(**valid_data)
        await handle_product_created(payload)

    async def test_handle_inventory_updated_processes_payload(self) -> None:
        """handle_inventory_updated processes valid payload without error."""
        from ecommerce_product_service.mq.consumer import handle_inventory_updated

        valid_data = {
            "product_id": uuid.uuid4(),
            "old_quantity": 1,
            "new_quantity": 1,
        }
        payload = InventoryUpdatedPayload(**valid_data)
        await handle_inventory_updated(payload)

    async def test_handle_inventory_reserved_processes_payload(self) -> None:
        """handle_inventory_reserved processes valid payload without error."""
        from ecommerce_product_service.mq.consumer import handle_inventory_reserved

        valid_data = {
            "reservation_id": uuid.uuid4(),
            "product_ids": [uuid.uuid4()],
        }
        payload = InventoryReservedPayload(**valid_data)
        await handle_inventory_reserved(payload)

    async def test_handle_inventory_released_processes_payload(self) -> None:
        """handle_inventory_released processes valid payload without error."""
        from ecommerce_product_service.mq.consumer import handle_inventory_released

        valid_data = {
            "reservation_id": uuid.uuid4(),
            "reason": "test",
        }
        payload = InventoryReleasedPayload(**valid_data)
        await handle_inventory_released(payload)
