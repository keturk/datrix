"""Integration tests for InventoryReservation service CRUD operations.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest
import pytest_asyncio
from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_product_service.schemas.db.inventory_reservation import (
    InventoryReservationCreate,
    InventoryReservationUpdate,
)
from ecommerce_product_service.services._base import EntityNotFoundError
from ecommerce_product_service.services._base import ValidationError

from ecommerce_product_service.services.db.inventory_reservation_service import (
    InventoryReservationService,
)
from ecommerce_product_service.models.db.product import Product
from ecommerce_product_service.models.db.category import Category
from ecommerce_product_service.enums.reservation_status import ReservationStatus


@pytest.mark.integration
@pytest.mark.asyncio
class TestInventoryReservationRepository:
    """Test InventoryReservation service CRUD operations."""

    @pytest_asyncio.fixture(autouse=True)
    async def _setup_parents(self, db_session: AsyncSession) -> None:
        """Create parent entities required by FK relationships."""
        _gp_category = Category(
            id=uuid.uuid4(),
            name=f"test_{uuid.uuid4().hex[:8]}",
            slug=f"test-{uuid.uuid4().hex[:8]}",
        )
        db_session.add(_gp_category)
        await db_session.flush()
        _parent_product = Product(
            id=uuid.uuid4(),
            slug=f"test-{uuid.uuid4().hex[:8]}",
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=_gp_category.id,
        )
        db_session.add(_parent_product)
        await db_session.flush()
        self._product_id = _parent_product.id

    async def test_create(self, db_session: AsyncSession) -> None:
        """Create persists entity."""
        service = InventoryReservationService(db_session)
        payload = InventoryReservationCreate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
            quantity=1,
            status=ReservationStatus.reserved,
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
            product_id=self._product_id,
        )
        created = await service.create(payload)
        await db_session.refresh(created)
        assert created.reservation_id == uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890")
        assert created.quantity == 1
        assert created.status == ReservationStatus.reserved
        assert created.expires_at == datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )
        assert created.product_id == self._product_id

    async def test_get_by_id_not_found(self, db_session: AsyncSession) -> None:
        """Get by ID raises not found for unknown row."""
        service = InventoryReservationService(db_session)
        _nonexistent_id = uuid.uuid4()
        with pytest.raises(EntityNotFoundError):
            await service.get(_nonexistent_id)

    async def test_update_not_found(self, db_session: AsyncSession) -> None:
        """Update raises not found for unknown row."""
        service = InventoryReservationService(db_session)
        _nonexistent_id = uuid.uuid4()
        payload = InventoryReservationUpdate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
        )
        with pytest.raises(EntityNotFoundError):
            await service.update(_nonexistent_id, payload)

    async def test_delete_not_found(self, db_session: AsyncSession) -> None:
        """Delete raises not found for unknown row."""
        service = InventoryReservationService(db_session)
        _nonexistent_id = uuid.uuid4()
        with pytest.raises(EntityNotFoundError):
            await service.delete(_nonexistent_id)

    async def test_find_by_reservation_id(self, db_session: AsyncSession) -> None:
        """find_by_reservation_id returns matching entities."""
        service = InventoryReservationService(db_session)
        payload = InventoryReservationCreate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
            quantity=1,
            status=ReservationStatus.reserved,
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
            product_id=self._product_id,
        )
        created = await service.create(payload)
        results = await service.find_by_reservation_id(created.reservation_id)
        assert len(results) >= 1
        assert any(r.id == created.id for r in results)

    async def test_find_by_reservation_id_empty(self, db_session: AsyncSession) -> None:
        """find_by_reservation_id returns empty list for no matches."""
        service = InventoryReservationService(db_session)
        results = await service.find_by_reservation_id(uuid.uuid4())
        assert results == []

    async def test_find_by_expires_at(self, db_session: AsyncSession) -> None:
        """find_by_expires_at returns matching entities."""
        service = InventoryReservationService(db_session)
        payload = InventoryReservationCreate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
            quantity=1,
            status=ReservationStatus.reserved,
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
            product_id=self._product_id,
        )
        created = await service.create(payload)
        results = await service.find_by_expires_at(created.expires_at)
        assert len(results) >= 1
        assert any(r.id == created.id for r in results)

    async def test_find_by_expires_at_empty(self, db_session: AsyncSession) -> None:
        """find_by_expires_at returns empty list for no matches."""
        service = InventoryReservationService(db_session)
        results = await service.find_by_expires_at(
            datetime.datetime(1900, 1, 1, tzinfo=datetime.timezone.utc)
        )
        assert results == []


@pytest.mark.integration
@pytest.mark.asyncio
class TestInventoryReservationValidationRules:
    """Test InventoryReservation business validation rules from validate block."""

    @pytest_asyncio.fixture(autouse=True)
    async def _setup_parents(self, db_session: AsyncSession) -> None:
        """Create parent entities required by FK relationships."""
        _gp_category = Category(
            id=uuid.uuid4(),
            name=f"test_{uuid.uuid4().hex[:8]}",
            slug=f"test-{uuid.uuid4().hex[:8]}",
        )
        db_session.add(_gp_category)
        await db_session.flush()
        _parent_product = Product(
            id=uuid.uuid4(),
            slug=f"test-{uuid.uuid4().hex[:8]}",
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=_gp_category.id,
        )
        db_session.add(_parent_product)
        await db_session.flush()
        self._product_id = _parent_product.id

    async def test_quantity_non_positive_rejected(self, db_session: AsyncSession) -> None:
        """Validation rejects quantity when non_positive."""
        service = InventoryReservationService(db_session)
        data = InventoryReservationCreate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
            quantity=1,
            status=ReservationStatus.reserved,
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
            product_id=self._product_id,
        )
        setattr(data, "quantity", 0)
        with pytest.raises(ValidationError, match=r"Reservation quantity must be positive"):
            await service.create(data)

    async def test_quantity_valid_accepted(self, db_session: AsyncSession) -> None:
        """Validation accepts quantity when valid."""
        service = InventoryReservationService(db_session)
        data = InventoryReservationCreate(
            reservation_id=uuid.UUID("a1b2c3d4-e5f6-7890-abcd-ef1234567890"),
            quantity=1,
            status=ReservationStatus.reserved,
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
            product_id=self._product_id,
        )
        created = await service.create(data)
        assert created is not None
