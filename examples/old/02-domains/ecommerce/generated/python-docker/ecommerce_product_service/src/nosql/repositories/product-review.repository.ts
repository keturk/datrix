/**
 * Repository for ProductReview (mongodb).
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Injectable, Logger } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import {
  ProductReview,
  ProductReviewDocument,
} from '../schemas/product-review.schema';

@Injectable()
export class ProductReviewRepository {
  private readonly logger = new Logger(ProductReviewRepository.name);

  constructor(
    @InjectModel(ProductReview.name)
    private readonly model: Model<ProductReviewDocument>,
  ) {}

  async insert(
    data: Partial<ProductReview>,
  ): Promise<ProductReviewDocument> {
    const doc = new this.model(data);
    const result = await doc.save();
    this.logger.log(
      `Document inserted: ProductReview/${result._id}`,
    );
    return result;
  }

  async findById(
    id: string,
  ): Promise<ProductReviewDocument | null> {
    return this.model.findById(id).exec();
  }

  async findMany(
    filter: Record<string, unknown> = {},
    skip = 0,
    limit = 100,
  ): Promise<ProductReviewDocument[]> {
    return this.model.find(filter).skip(skip).limit(limit).exec();
  }

  async findByProductId(value: unknown): Promise<ProductReviewDocument[]> {
    return this.model.find({ product_id: value }).exec();
  }

  async findByUserId(value: unknown): Promise<ProductReviewDocument[]> {
    return this.model.find({ user_id: value }).exec();
  }

  async update(
    id: string,
    data: Partial<ProductReview>,
  ): Promise<ProductReviewDocument | null> {
    const result = await this.model
      .findByIdAndUpdate(id, { $set: data }, { new: true })
      .exec();
    this.logger.log(`Document updated: ProductReview/${id}`);
    return result;
  }

  async delete(id: string): Promise<boolean> {
    const result = await this.model.findByIdAndDelete(id).exec();
    this.logger.log(`Document deleted: ProductReview/${id}`);
    return result !== null;
  }
}
