"""Event payload schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

from ecommerce_product_service.schemas.address import Address
from pydantic import JsonValue
import decimal
import uuid

from pydantic import BaseModel


class ProductCreatedPayload(BaseModel):
    """Payload schema for ProductCreated event."""

    product_id: uuid.UUID
    name: str
    price: decimal.Decimal


class InventoryUpdatedPayload(BaseModel):
    """Payload schema for InventoryUpdated event."""

    product_id: uuid.UUID
    old_quantity: int
    new_quantity: int


class InventoryReservedPayload(BaseModel):
    """Payload schema for InventoryReserved event."""

    reservation_id: uuid.UUID
    product_ids: list[uuid.UUID]


class InventoryReleasedPayload(BaseModel):
    """Payload schema for InventoryReleased event."""

    reservation_id: uuid.UUID
    reason: str


class OrderConfirmedPayload(BaseModel):
    """Payload schema for OrderConfirmed event."""

    order_id: uuid.UUID
    payment_id: uuid.UUID | None
    reservation_id: uuid.UUID
    shipping_address: Address
    items: list[JsonValue]
    estimated_weight: decimal.Decimal


class OrderCancelledPayload(BaseModel):
    """Payload schema for OrderCancelled event."""

    order_id: uuid.UUID
    reason: str
    reservation_id: uuid.UUID
