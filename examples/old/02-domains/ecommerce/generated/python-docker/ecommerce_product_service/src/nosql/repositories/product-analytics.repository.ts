/**
 * Repository for ProductAnalytics (mongodb).
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Injectable, Logger } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import {
  ProductAnalytics,
  ProductAnalyticsDocument,
} from '../schemas/product-analytics.schema';

@Injectable()
export class ProductAnalyticsRepository {
  private readonly logger = new Logger(ProductAnalyticsRepository.name);

  constructor(
    @InjectModel(ProductAnalytics.name)
    private readonly model: Model<ProductAnalyticsDocument>,
  ) {}

  async insert(
    data: Partial<ProductAnalytics>,
  ): Promise<ProductAnalyticsDocument> {
    const doc = new this.model(data);
    const result = await doc.save();
    this.logger.log(
      `Document inserted: ProductAnalytics/${result._id}`,
    );
    return result;
  }

  async findById(
    id: string,
  ): Promise<ProductAnalyticsDocument | null> {
    return this.model.findById(id).exec();
  }

  async findMany(
    filter: Record<string, unknown> = {},
    skip = 0,
    limit = 100,
  ): Promise<ProductAnalyticsDocument[]> {
    return this.model.find(filter).skip(skip).limit(limit).exec();
  }

  async findByProductId(value: unknown): Promise<ProductAnalyticsDocument[]> {
    return this.model.find({ product_id: value }).exec();
  }

  async update(
    id: string,
    data: Partial<ProductAnalytics>,
  ): Promise<ProductAnalyticsDocument | null> {
    const result = await this.model
      .findByIdAndUpdate(id, { $set: data }, { new: true })
      .exec();
    this.logger.log(`Document updated: ProductAnalytics/${id}`);
    return result;
  }

  async delete(id: string): Promise<boolean> {
    const result = await this.model.findByIdAndDelete(id).exec();
    this.logger.log(`Document deleted: ProductAnalytics/${id}`);
    return result !== null;
  }
}
