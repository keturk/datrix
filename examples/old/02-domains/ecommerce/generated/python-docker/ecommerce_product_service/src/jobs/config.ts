// Job configuration.
// Auto-generated by datrix-codegen-typescript. Do not edit manually.

export interface JobConfig {
  name: string;
  schedule: string;
  timeoutSeconds: number;
  maxAttempts: number;
  concurrency: number;
  deadLetterQueue?: string;
}

export const JOB_CONFIGS: Record<string, JobConfig> = {
  'ReleaseExpiredReservations': {
    name: 'ReleaseExpiredReservations',
    schedule: process.env.JOBS_RELEASE_EXPIRED_RESERVATIONS_SCHEDULE || '* * * * *',
    timeoutSeconds: parseInt(process.env.JOBS_RELEASE_EXPIRED_RESERVATIONS_TIMEOUT || '30', 10),
    maxAttempts: parseInt(process.env.JOBS_RELEASE_EXPIRED_RESERVATIONS_MAX_ATTEMPTS || '1', 10),
    concurrency: parseInt(process.env.JOBS_RELEASE_EXPIRED_RESERVATIONS_CONCURRENCY || '1', 10),
  },
  'UpdatePopularProducts': {
    name: 'UpdatePopularProducts',
    schedule: process.env.JOBS_UPDATE_POPULAR_PRODUCTS_SCHEDULE || '0 * * * *',
    timeoutSeconds: parseInt(process.env.JOBS_UPDATE_POPULAR_PRODUCTS_TIMEOUT || '30', 10),
    maxAttempts: parseInt(process.env.JOBS_UPDATE_POPULAR_PRODUCTS_MAX_ATTEMPTS || '1', 10),
    concurrency: parseInt(process.env.JOBS_UPDATE_POPULAR_PRODUCTS_CONCURRENCY || '1', 10),
  },
};
