"""Integration tests for Payment API endpoints.

Auto-generated by datrix-codegen-python. Do not edit manually.
Tests hit the full HTTP stack via httpx AsyncClient.
"""

from __future__ import annotations

import uuid

import pytest
from httpx import AsyncClient

from ecommerce_payment_service.enums.payment_method import PaymentMethod
from ecommerce_payment_service.enums.payment_status import PaymentStatus


@pytest.mark.integration
@pytest.mark.asyncio
class TestPaymentEndpoints:
    """Test Payment CRUD endpoints via HTTP."""

    async def test_get_payment_not_found(self, client: AsyncClient) -> None:
        """GET /api/v1/payments/{non_existent_id} returns 404."""
        _nonexistent_id = uuid.uuid4()
        response = await client.get(f"/api/v1/payments/{_nonexistent_id}")
        assert response.status_code == 404, (
            f"Expected 404, got {response.status_code}: {response.text!r}"
        )
