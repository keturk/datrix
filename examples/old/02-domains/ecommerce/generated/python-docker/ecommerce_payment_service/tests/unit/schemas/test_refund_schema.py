"""Unit tests for Refund schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_payment_service.schemas.db.refund import (
    RefundCreate,
    RefundResponse,
    RefundUpdate,
)
from ecommerce_payment_service.enums.payment_method import PaymentMethod
from ecommerce_payment_service.enums.payment_status import PaymentStatus


@pytest.mark.unit
class TestRefundCreate:
    """Test RefundCreate schema validation."""

    def test_valid_data(self) -> None:
        """Create schema accepts valid data."""
        _payment_id = uuid.uuid4()
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": _payment_id,
        }
        schema = RefundCreate(**data)
        assert schema.amount == decimal.Decimal("149.00")
        assert schema.reason == "foo_bar"
        assert schema.status == PaymentStatus.pending
        assert schema.refund_transaction_id == "sample_text"
        assert schema.error_message == "foo_bar"
        assert schema.processed_at == datetime.datetime(
            2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
        )
        assert schema.payment_id == _payment_id

    def test_amount_required(self) -> None:
        """amount is required for Create schema."""
        data = {
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_reason_required(self) -> None:
        """reason is required for Create schema."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_payment_id_required(self) -> None:
        """payment_id is required for Create schema."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
        }
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_reason_rejects_over_max_length(self) -> None:
        """RefundCreate rejects reason exceeding 500 chars."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["reason"] = "x" * (500 + 1)
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_reason_accepts_at_max_length(self) -> None:
        """RefundCreate accepts reason at exactly 500 chars."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["reason"] = "x" * 500
        schema = RefundCreate(**data)
        assert len(schema.reason) == 500

    def test_id_excluded_from_create(self) -> None:
        """Server-managed field id is not in RefundCreate."""
        assert "id" not in RefundCreate.model_fields

    def test_id_excluded_from_update(self) -> None:
        """Server-managed field id is not in RefundUpdate."""
        assert "id" not in RefundUpdate.model_fields

    def test_id_in_response(self) -> None:
        """Server-managed field id is in RefundResponse."""
        assert "id" in RefundResponse.model_fields

    def test_created_at_excluded_from_create(self) -> None:
        """Server-managed field created_at is not in RefundCreate."""
        assert "created_at" not in RefundCreate.model_fields

    def test_created_at_excluded_from_update(self) -> None:
        """Server-managed field created_at is not in RefundUpdate."""
        assert "created_at" not in RefundUpdate.model_fields

    def test_created_at_in_response(self) -> None:
        """Server-managed field created_at is in RefundResponse."""
        assert "created_at" in RefundResponse.model_fields

    def test_updated_at_excluded_from_create(self) -> None:
        """Server-managed field updated_at is not in RefundCreate."""
        assert "updated_at" not in RefundCreate.model_fields

    def test_updated_at_excluded_from_update(self) -> None:
        """Server-managed field updated_at is not in RefundUpdate."""
        assert "updated_at" not in RefundUpdate.model_fields

    def test_updated_at_in_response(self) -> None:
        """Server-managed field updated_at is in RefundResponse."""
        assert "updated_at" in RefundResponse.model_fields

    def test_status_default_applied(self) -> None:
        """status defaults to PaymentStatus.pending when omitted."""
        required_data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "payment_id": uuid.uuid4(),
        }
        schema = RefundCreate(**required_data)
        assert schema.status == PaymentStatus.pending


@pytest.mark.unit
class TestRefundStatusEnumValidation:
    """Test enum validation for status field."""

    def test_status_accepts_pending(self) -> None:
        """status accepts PaymentStatus.pending."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = PaymentStatus.pending
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.pending

    def test_status_accepts_processing(self) -> None:
        """status accepts PaymentStatus.processing."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = PaymentStatus.processing
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.processing

    def test_status_accepts_completed(self) -> None:
        """status accepts PaymentStatus.completed."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = PaymentStatus.completed
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.completed

    def test_status_accepts_failed(self) -> None:
        """status accepts PaymentStatus.failed."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = PaymentStatus.failed
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.failed

    def test_status_accepts_refunded(self) -> None:
        """status accepts PaymentStatus.refunded."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = PaymentStatus.refunded
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.refunded

    def test_status_coerces_string_pending(self) -> None:
        """status coerces string "pending" to PaymentStatus.pending."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "pending"
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.pending

    def test_status_coerces_string_processing(self) -> None:
        """status coerces string "processing" to PaymentStatus.processing."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "processing"
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.processing

    def test_status_coerces_string_completed(self) -> None:
        """status coerces string "completed" to PaymentStatus.completed."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "completed"
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.completed

    def test_status_coerces_string_failed(self) -> None:
        """status coerces string "failed" to PaymentStatus.failed."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "failed"
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.failed

    def test_status_coerces_string_refunded(self) -> None:
        """status coerces string "refunded" to PaymentStatus.refunded."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "refunded"
        schema = RefundCreate(**data)
        assert schema.status == PaymentStatus.refunded

    def test_status_rejects_invalid_string(self) -> None:
        """status rejects non-member string value."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = "NotAValidMember"
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_status_rejects_invalid_type(self) -> None:
        """status rejects non-string non-enum value."""
        data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "status": PaymentStatus.pending,
            "refund_transaction_id": "sample_text",
            "error_message": "foo_bar",
            "processed_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            "payment_id": uuid.uuid4(),
        }
        data["status"] = 42
        with pytest.raises(ValidationError):
            RefundCreate(**data)

    def test_status_default_applied_when_omitted(self) -> None:
        """status defaults to PaymentStatus.pending when omitted."""
        required_data = {
            "amount": decimal.Decimal("149.00"),
            "reason": "foo_bar",
            "payment_id": uuid.uuid4(),
        }
        schema = RefundCreate(**required_data)
        assert schema.status == PaymentStatus.pending

    def test_status_in_update_is_optional(self) -> None:
        """status in Update schema is optional (None by default)."""
        schema = RefundUpdate()
        assert schema.status is None


@pytest.mark.unit
class TestRefundUpdate:
    """Test RefundUpdate schema validation."""

    def test_all_fields_optional(self) -> None:
        """Update schema allows empty payload."""
        schema = RefundUpdate()
        assert schema.amount is None
        assert schema.reason is None
        assert schema.status is None
        assert schema.refund_transaction_id is None
        assert schema.error_message is None
        assert schema.processed_at is None
        assert schema.payment_id is None

    def test_partial_update(self) -> None:
        """Update schema accepts partial data."""
        schema = RefundUpdate(**{"amount": decimal.Decimal("4.50")})
        assert schema.amount == decimal.Decimal("4.50")


@pytest.mark.unit
class TestRefundResponse:
    """Test RefundResponse schema settings."""

    def test_from_attributes(self) -> None:
        """Response schema has ORM mode enabled."""
        config = RefundResponse.model_config
        assert config.get("from_attributes") is True

    def test_response_serializes_enum_fields(self) -> None:
        """Response schema correctly serializes enum fields from ORM model."""
        from ecommerce_payment_service.models.db.refund import Refund

        _payment_id = uuid.uuid4()

        instance = Refund(
            amount=decimal.Decimal("149.00"),
            reason="foo_bar",
            payment_id=_payment_id,
            status=PaymentStatus.pending,
            refund_transaction_id="sample_text",
            error_message="foo_bar",
            processed_at=datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
            id=uuid.UUID("6ba7b810-9dad-11d1-80b4-00c04fd430c8"),
            created_at=datetime.datetime(2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc),
            updated_at=datetime.datetime(2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc),
        )
        instance.status = PaymentStatus.pending
        response = RefundResponse.model_validate(instance, from_attributes=True)
        assert response.status == PaymentStatus.pending
