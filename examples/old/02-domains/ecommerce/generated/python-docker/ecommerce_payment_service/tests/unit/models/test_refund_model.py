"""Unit tests for Refund model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest

from ecommerce_payment_service.models.db.refund import Refund
from ecommerce_payment_service.enums.payment_method import PaymentMethod
from ecommerce_payment_service.enums.payment_status import PaymentStatus


@pytest.mark.unit
class TestRefundModel:
    """Test Refund SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a Refund instance with required fields."""
        _payment_id = uuid.uuid4()
        instance = Refund(
            amount=decimal.Decimal("149.00"),
            reason="foo_bar",
            payment_id=_payment_id,
        )
        assert instance.amount == decimal.Decimal("149.00")
        assert instance.reason == "foo_bar"
        assert instance.payment_id == _payment_id

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _payment_id = uuid.uuid4()
        instance = Refund(
            amount=decimal.Decimal("149.00"),
            reason="foo_bar",
            payment_id=_payment_id,
        )
        instance.amount = decimal.Decimal("4.50")
        assert instance.amount == decimal.Decimal("4.50")
        instance.reason = "test_string"
        assert instance.reason == "test_string"
        instance.status = PaymentStatus.processing
        assert instance.status == PaymentStatus.processing
        instance.refund_transaction_id = "hello"
        assert instance.refund_transaction_id == "hello"
        instance.error_message = "test_string"
        assert instance.error_message == "test_string"
        instance.processed_at = datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.processed_at == datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        _alt_payment_id = uuid.uuid4()
        instance.payment_id = _alt_payment_id
        assert instance.payment_id == _alt_payment_id

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(Refund, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = Refund(
            amount=decimal.Decimal("149.00"),
            reason="foo_bar",
            payment_id=uuid.uuid4(),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_reason_max_length_in_column(self) -> None:
        """reason column has max_length=500."""
        col = Refund.__table__.columns["reason"]
        assert col.type.length == 500

    def test_status_enum_round_trip(self) -> None:
        """status enum value survives assign and read."""
        instance = Refund(
            amount=decimal.Decimal("149.00"),
            reason="foo_bar",
            payment_id=uuid.uuid4(),
        )
        instance.status = PaymentStatus.pending
        assert instance.status == PaymentStatus.pending
        instance.status = PaymentStatus.processing
        assert instance.status == PaymentStatus.processing
        instance.status = PaymentStatus.completed
        assert instance.status == PaymentStatus.completed
        instance.status = PaymentStatus.failed
        assert instance.status == PaymentStatus.failed
        instance.status = PaymentStatus.refunded
        assert instance.status == PaymentStatus.refunded
