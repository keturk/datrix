"""Audit hooks for Refund.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_payment_service.models.db.refund_audit_log import RefundAuditLog

SENSITIVE_FIELDS: frozenset[str] = frozenset([])


def _sanitize(data: dict) -> dict:
    """Remove sensitive fields from audit data."""
    return {k: v for k, v in data.items() if k not in SENSITIVE_FIELDS}


async def record_audit(
    session: AsyncSession,
    entity_id: uuid.UUID,
    action: str,
    old_values: dict | None = None,
    new_values: dict | None = None,
    user_id: str | None = None,
) -> None:
    """Record an audit log entry."""
    entry = RefundAuditLog(
        entity_id=entity_id,
        action=action,
        old_values=_sanitize(old_values) if old_values else None,
        new_values=_sanitize(new_values) if new_values else None,
        user_id=user_id,
    )
    session.add(entry)
