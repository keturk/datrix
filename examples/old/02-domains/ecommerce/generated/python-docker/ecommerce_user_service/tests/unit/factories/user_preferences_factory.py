"""Test data factory for UserPreferences.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid
from dataclasses import dataclass, field
from typing import Any

from pydantic import JsonValue
from ecommerce_user_service.models.db.user_preferences import UserPreferences


@dataclass
class UserPreferencesFactory:
    """Factory for creating UserPreferences test instances."""

    id: uuid.UUID = field(default_factory=uuid.uuid4)
    created_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    updated_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    language: str = "test_value"
    timezone: str = "test_value"
    email_notifications: bool = True
    sms_notifications: bool = True
    preferences: JsonValue = field(default_factory=lambda: {"key": "value"})
    user_id: uuid.UUID = field(default_factory=uuid.uuid4)

    def build(self) -> UserPreferences:
        """Build a model instance from factory defaults."""
        return UserPreferences(
            id=self.id,
            created_at=self.created_at,
            updated_at=self.updated_at,
            language=self.language,
            timezone=self.timezone,
            email_notifications=self.email_notifications,
            sms_notifications=self.sms_notifications,
            preferences=self.preferences,
            user_id=self.user_id,
        )

    def build_dict(self) -> dict[str, object]:
        """Build a dictionary payload for API tests."""
        return {
            "language": self.language,
            "timezone": self.timezone,
            "email_notifications": self.email_notifications,
            "sms_notifications": self.sms_notifications,
            "preferences": self.preferences,
            "user_id": str(self.user_id),
        }

    @classmethod
    def create(cls, **overrides: Any) -> UserPreferences:
        """Create a model instance with optional overrides."""
        factory = cls(**overrides)
        return factory.build()

    @classmethod
    def create_dict(cls, **overrides: Any) -> dict[str, object]:
        """Create a payload dict with optional overrides."""
        factory = cls(**overrides)
        return factory.build_dict()
