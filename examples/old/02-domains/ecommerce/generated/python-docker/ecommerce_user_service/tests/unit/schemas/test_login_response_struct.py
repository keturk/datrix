"""Unit tests for LoginResponse struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_user_service.schemas.login_response import LoginResponse


@pytest.mark.unit
class TestLoginResponseStruct:
    """Test LoginResponse Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a LoginResponse instance with all fields."""
        _user = str(uuid.uuid4())
        instance = LoginResponse(
            user=_user,
            token="hello",
            expires_at=datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
        )
        assert instance is not None
        assert instance.user is not None
        assert instance.token == "hello"
        assert instance.expires_at == datetime.datetime(
            2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc
        )

    def test_user_required(self) -> None:
        """user is required — omitting it raises ValidationError."""
        kwargs = {
            "token": "hello",
            "expires_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
        }
        with pytest.raises(ValidationError):
            LoginResponse(**kwargs)

    def test_token_required(self) -> None:
        """token is required — omitting it raises ValidationError."""
        kwargs = {
            "user": str(uuid.uuid4()),
            "expires_at": datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
        }
        with pytest.raises(ValidationError):
            LoginResponse(**kwargs)

    def test_expires_at_required(self) -> None:
        """expires_at is required — omitting it raises ValidationError."""
        kwargs = {
            "user": str(uuid.uuid4()),
            "token": "hello",
        }
        with pytest.raises(ValidationError):
            LoginResponse(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        _user = str(uuid.uuid4())
        instance = LoginResponse(
            user=_user,
            token="hello",
            expires_at=datetime.datetime(2024, 6, 20, 8, 30, 0, tzinfo=datetime.timezone.utc),
        )
        data = instance.model_dump()
        restored = LoginResponse.model_validate(data)
        assert restored == instance
