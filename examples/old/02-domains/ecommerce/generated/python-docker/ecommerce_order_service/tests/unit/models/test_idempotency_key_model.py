"""Unit tests for IdempotencyKey model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest

from ecommerce_order_service.models.db.idempotency_key import IdempotencyKey


@pytest.mark.unit
class TestIdempotencyKeyModel:
    """Test IdempotencyKey SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a IdempotencyKey instance with required fields."""
        instance = IdempotencyKey(
            key="Valid Title",
            operation="world",
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
        )
        assert instance.key == "Valid Title"
        assert instance.operation == "world"
        assert instance.expires_at == datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        instance = IdempotencyKey(
            key="Valid Title",
            operation="world",
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
        )
        _alt_key = f"alt_{uuid.uuid4().hex[:8]}"
        instance.key = _alt_key
        assert instance.key == _alt_key
        instance.operation = "foo_bar"
        assert instance.operation == "foo_bar"
        instance.resource_id = uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7")
        assert instance.resource_id == uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7")
        instance.response = {"key": "value"}
        assert instance.response == {"key": "value"}
        instance.expires_at = datetime.datetime(2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc)
        assert instance.expires_at == datetime.datetime(
            2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc
        )

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(IdempotencyKey, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = IdempotencyKey(
            key="Valid Title",
            operation="world",
            expires_at=datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_key_max_length_in_column(self) -> None:
        """key column has max_length=100."""
        col = IdempotencyKey.__table__.columns["key"]
        assert col.type.length == 100

    def test_operation_max_length_in_column(self) -> None:
        """operation column has max_length=50."""
        col = IdempotencyKey.__table__.columns["operation"]
        assert col.type.length == 50

    def test_key_unique_constraint(self) -> None:
        """key column has unique=True."""
        col = IdempotencyKey.__table__.columns["key"]
        assert col.unique is True

    def test_operation_index_exists(self) -> None:
        """operation column has index=True."""
        col = IdempotencyKey.__table__.columns["operation"]
        assert col.index is True

    def test_expires_at_index_exists(self) -> None:
        """expires_at column has index=True."""
        col = IdempotencyKey.__table__.columns["expires_at"]
        assert col.index is True
