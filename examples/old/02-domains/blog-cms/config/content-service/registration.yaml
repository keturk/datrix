test:
  tags: [api, auth, users, v1]

  meta:
    team: identity

  healthCheck:
    type: http
    path: /health
    interval: 10s

development:
  tags: [api, auth, users, v1]

  meta:
    team: identity

  healthCheck:
    type: http
    path: /health
    interval: 10s

production:
  tags: [api, auth, users, v1]

  meta:
    team: identity

  healthCheck:
    type: http
    path: /health
    interval: 10s
