/**
 * Unit tests for store storage configuration.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */

describe('Store Storage Config', () => {
  describe('images', () => {
    it('should have a non-empty folder path', () => {
      const path = 'products/images/';
      expect(path.length).toBeGreaterThan(0);
    });

    it('should define allowed MIME types', () => {
      const allowed = ["image/jpeg", "image/png", "image/webp"];
      expect(Array.isArray(allowed)).toBe(true);
      expect(allowed.length).toBeGreaterThan(0);
    });

    it('should have a positive max size', () => {
      const maxBytes = 10485760;
      expect(maxBytes).toBeGreaterThan(0);
    });

  });

  describe('thumbnails', () => {
    it('should have a non-empty folder path', () => {
      const path = 'products/thumbnails/';
      expect(path.length).toBeGreaterThan(0);
    });

    it('should define allowed MIME types', () => {
      const allowed = ["image/jpeg", "image/png", "image/webp"];
      expect(Array.isArray(allowed)).toBe(true);
      expect(allowed.length).toBeGreaterThan(0);
    });

    it('should have a positive max size', () => {
      const maxBytes = 2097152;
      expect(maxBytes).toBeGreaterThan(0);
    });

  });

});
