/**
 * Secrets configuration types and constants.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */

export enum SecretsBackend {
  ENVIRONMENT = 'environment',
  VAULT = 'vault',
  AWS = 'aws',
}

export interface SecretsProvider {
  getSecret(key: string): Promise<string | undefined>;
  setSecret(key: string, value: string): Promise<void>;
  deleteSecret(key: string): Promise<boolean>;
}

export interface SecretsConfig {
  backend: SecretsBackend;
  prefix?: string;
  vault?: {
    addr?: string;
    token?: string;
    namespace?: string;
    mountPoint?: string;
    path?: string;
  };
  aws?: {
    region?: string;
    secretPrefix?: string;
  };
}

/** Well-known secret keys mapped to environment variable names. */
export const COMMON_SECRET_KEYS: Record<string, string> = {
  database_url: 'DATABASE_URL',
  database_url_replica: 'DATABASE_URL_REPLICA',
  jwt_secret_key: 'JWT_SECRET',
  internal_service_secret: 'INTERNAL_SERVICE_SECRET',
};
