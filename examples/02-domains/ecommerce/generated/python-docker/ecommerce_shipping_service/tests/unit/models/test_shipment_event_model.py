"""Unit tests for ShipmentEvent model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest

from ecommerce_shipping_service.models.db.shipment_event import ShipmentEvent
from ecommerce_shipping_service.enums.shipment_status import ShipmentStatus
from ecommerce_shipping_service.enums.shipping_carrier import ShippingCarrier


@pytest.mark.unit
class TestShipmentEventModel:
    """Test ShipmentEvent SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a ShipmentEvent instance with required fields."""
        _shipment_id = uuid.uuid4()
        instance = ShipmentEvent(
            timestamp=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            status=ShipmentStatus.pending,
            location="world",
            shipment_id=_shipment_id,
        )
        assert instance.timestamp == datetime.datetime(
            2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.status == ShipmentStatus.pending
        assert instance.location == "world"
        assert instance.shipment_id == _shipment_id

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _shipment_id = uuid.uuid4()
        instance = ShipmentEvent(
            timestamp=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            status=ShipmentStatus.pending,
            location="world",
            shipment_id=_shipment_id,
        )
        instance.timestamp = datetime.datetime(2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc)
        assert instance.timestamp == datetime.datetime(
            2025, 7, 22, 14, 0, 0, tzinfo=datetime.timezone.utc
        )
        instance.status = ShipmentStatus.picked_up
        assert instance.status == ShipmentStatus.picked_up
        instance.location = "foo_bar"
        assert instance.location == "foo_bar"
        instance.description = "Test content body with details"
        assert instance.description == "Test content body with details"
        _alt_shipment_id = uuid.uuid4()
        instance.shipment_id = _alt_shipment_id
        assert instance.shipment_id == _alt_shipment_id

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(ShipmentEvent, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = ShipmentEvent(
            timestamp=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            status=ShipmentStatus.pending,
            location="world",
            shipment_id=uuid.uuid4(),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_location_max_length_in_column(self) -> None:
        """location column has max_length=200."""
        col = ShipmentEvent.__table__.columns["location"]
        assert col.type.length == 200

    def test_status_enum_round_trip(self) -> None:
        """status enum value survives assign and read."""
        instance = ShipmentEvent(
            timestamp=datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc),
            status=ShipmentStatus.pending,
            location="world",
            shipment_id=uuid.uuid4(),
        )
        instance.status = ShipmentStatus.pending
        assert instance.status == ShipmentStatus.pending
        instance.status = ShipmentStatus.picked_up
        assert instance.status == ShipmentStatus.picked_up
        instance.status = ShipmentStatus.in_transit
        assert instance.status == ShipmentStatus.in_transit
        instance.status = ShipmentStatus.out_for_delivery
        assert instance.status == ShipmentStatus.out_for_delivery
        instance.status = ShipmentStatus.delivered
        assert instance.status == ShipmentStatus.delivered
        instance.status = ShipmentStatus.failed
        assert instance.status == ShipmentStatus.failed
        instance.status = ShipmentStatus.returned
        assert instance.status == ShipmentStatus.returned
