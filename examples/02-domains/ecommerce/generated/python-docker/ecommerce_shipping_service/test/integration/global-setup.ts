/**
 * Jest global setup for integration tests.
 *
 * Runs once before all integration test files. Drops and recreates
 * the database schema to ensure a clean starting state.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { DataSource } from 'typeorm';
import { ALL_ENTITIES, getTestDatabaseConfig } from './setup';

export default async function globalSetup(): Promise<void> {
  const config = getTestDatabaseConfig({ dropSchema: true });
  const dataSource = new DataSource({
    type: config.type as 'postgres',
    url: config.url as string,
    entities: ALL_ENTITIES,
    synchronize: true,
    dropSchema: true,
    logging: false,
  });

  await dataSource.initialize();
  await dataSource.destroy();
}
