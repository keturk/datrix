// Job configuration.
// Auto-generated by datrix-codegen-typescript. Do not edit manually.

export interface JobConfig {
  name: string;
  schedule: string;
  timeoutSeconds: number;
  maxAttempts: number;
  concurrency: number;
  deadLetterQueue?: string;
}

export const JOB_CONFIGS: Record<string, JobConfig> = {
  'CleanupExpiredIdempotencyKeys': {
    name: 'CleanupExpiredIdempotencyKeys',
    schedule: process.env.JOBS_CLEANUP_EXPIRED_IDEMPOTENCY_KEYS_SCHEDULE || '0 * * * *',
    timeoutSeconds: parseInt(process.env.JOBS_CLEANUP_EXPIRED_IDEMPOTENCY_KEYS_TIMEOUT || '30', 10),
    maxAttempts: parseInt(process.env.JOBS_CLEANUP_EXPIRED_IDEMPOTENCY_KEYS_MAX_ATTEMPTS || '1', 10),
    concurrency: parseInt(process.env.JOBS_CLEANUP_EXPIRED_IDEMPOTENCY_KEYS_CONCURRENCY || '1', 10),
  },
};
