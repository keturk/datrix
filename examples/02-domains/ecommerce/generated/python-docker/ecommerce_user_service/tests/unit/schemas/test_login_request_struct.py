"""Unit tests for LoginRequest struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations


import pytest
from pydantic import ValidationError

from ecommerce_user_service.schemas.login_request import LoginRequest


@pytest.mark.unit
class TestLoginRequestStruct:
    """Test LoginRequest Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a LoginRequest instance with all fields."""
        instance = LoginRequest(
            email="user@example.com",
            password="world",
        )
        assert instance is not None
        assert instance.email == "user@example.com"
        assert instance.password == "world"

    def test_email_required(self) -> None:
        """email is required — omitting it raises ValidationError."""
        kwargs = {
            "password": "world",
        }
        with pytest.raises(ValidationError):
            LoginRequest(**kwargs)

    def test_password_required(self) -> None:
        """password is required — omitting it raises ValidationError."""
        kwargs = {
            "email": "user@example.com",
        }
        with pytest.raises(ValidationError):
            LoginRequest(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        instance = LoginRequest(
            email="user@example.com",
            password="world",
        )
        data = instance.model_dump()
        restored = LoginRequest.model_validate(data)
        assert restored == instance
