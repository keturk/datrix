"""Unit tests for UserPreferences schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

import pytest
from pydantic import ValidationError

from ecommerce_user_service.schemas.db.user_preferences import (
    UserPreferencesCreate,
    UserPreferencesResponse,
    UserPreferencesUpdate,
)


@pytest.mark.unit
class TestUserPreferencesCreate:
    """Test UserPreferencesCreate schema validation."""

    def test_valid_data(self) -> None:
        """Create schema accepts valid data."""
        _user_id = uuid.uuid4()
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
            "user_id": _user_id,
        }
        schema = UserPreferencesCreate(**data)
        assert schema.language == "test_strin"
        assert schema.timezone == "sample_text"
        assert schema.email_notifications == True
        assert schema.sms_notifications == False
        assert schema.preferences == {"key": "value"}
        assert schema.user_id == _user_id

    def test_preferences_required(self) -> None:
        """preferences is required for Create schema."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "user_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            UserPreferencesCreate(**data)

    def test_user_id_required(self) -> None:
        """user_id is required for Create schema."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
        }
        with pytest.raises(ValidationError):
            UserPreferencesCreate(**data)

    def test_language_rejects_over_max_length(self) -> None:
        """UserPreferencesCreate rejects language exceeding 10 chars."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        data["language"] = "x" * (10 + 1)
        with pytest.raises(ValidationError):
            UserPreferencesCreate(**data)

    def test_language_accepts_at_max_length(self) -> None:
        """UserPreferencesCreate accepts language at exactly 10 chars."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        data["language"] = "x" * 10
        schema = UserPreferencesCreate(**data)
        assert len(schema.language) == 10

    def test_timezone_rejects_over_max_length(self) -> None:
        """UserPreferencesCreate rejects timezone exceeding 50 chars."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        data["timezone"] = "x" * (50 + 1)
        with pytest.raises(ValidationError):
            UserPreferencesCreate(**data)

    def test_timezone_accepts_at_max_length(self) -> None:
        """UserPreferencesCreate accepts timezone at exactly 50 chars."""
        data = {
            "language": "test_strin",
            "timezone": "sample_text",
            "email_notifications": True,
            "sms_notifications": False,
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        data["timezone"] = "x" * 50
        schema = UserPreferencesCreate(**data)
        assert len(schema.timezone) == 50

    def test_id_excluded_from_create(self) -> None:
        """Server-managed field id is not in UserPreferencesCreate."""
        assert "id" not in UserPreferencesCreate.model_fields

    def test_id_excluded_from_update(self) -> None:
        """Server-managed field id is not in UserPreferencesUpdate."""
        assert "id" not in UserPreferencesUpdate.model_fields

    def test_id_in_response(self) -> None:
        """Server-managed field id is in UserPreferencesResponse."""
        assert "id" in UserPreferencesResponse.model_fields

    def test_created_at_excluded_from_create(self) -> None:
        """Server-managed field created_at is not in UserPreferencesCreate."""
        assert "created_at" not in UserPreferencesCreate.model_fields

    def test_created_at_excluded_from_update(self) -> None:
        """Server-managed field created_at is not in UserPreferencesUpdate."""
        assert "created_at" not in UserPreferencesUpdate.model_fields

    def test_created_at_in_response(self) -> None:
        """Server-managed field created_at is in UserPreferencesResponse."""
        assert "created_at" in UserPreferencesResponse.model_fields

    def test_updated_at_excluded_from_create(self) -> None:
        """Server-managed field updated_at is not in UserPreferencesCreate."""
        assert "updated_at" not in UserPreferencesCreate.model_fields

    def test_updated_at_excluded_from_update(self) -> None:
        """Server-managed field updated_at is not in UserPreferencesUpdate."""
        assert "updated_at" not in UserPreferencesUpdate.model_fields

    def test_updated_at_in_response(self) -> None:
        """Server-managed field updated_at is in UserPreferencesResponse."""
        assert "updated_at" in UserPreferencesResponse.model_fields

    def test_language_default_applied(self) -> None:
        """language defaults to 'en' when omitted."""
        required_data = {
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        schema = UserPreferencesCreate(**required_data)
        assert schema.language == "en"

    def test_timezone_default_applied(self) -> None:
        """timezone defaults to 'UTC' when omitted."""
        required_data = {
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        schema = UserPreferencesCreate(**required_data)
        assert schema.timezone == "UTC"

    def test_email_notifications_default_applied(self) -> None:
        """email_notifications defaults to True when omitted."""
        required_data = {
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        schema = UserPreferencesCreate(**required_data)
        assert schema.email_notifications == True

    def test_sms_notifications_default_applied(self) -> None:
        """sms_notifications defaults to False when omitted."""
        required_data = {
            "preferences": {"key": "value"},
            "user_id": uuid.uuid4(),
        }
        schema = UserPreferencesCreate(**required_data)
        assert schema.sms_notifications == False


@pytest.mark.unit
class TestUserPreferencesUpdate:
    """Test UserPreferencesUpdate schema validation."""

    def test_all_fields_optional(self) -> None:
        """Update schema allows empty payload."""
        schema = UserPreferencesUpdate()
        assert schema.language is None
        assert schema.timezone is None
        assert schema.email_notifications is None
        assert schema.sms_notifications is None
        assert schema.preferences is None
        assert schema.user_id is None

    def test_partial_update(self) -> None:
        """Update schema accepts partial data."""
        schema = UserPreferencesUpdate(**{"language": "sample_tex"})
        assert schema.language == "sample_tex"


@pytest.mark.unit
class TestUserPreferencesResponse:
    """Test UserPreferencesResponse schema settings."""

    def test_from_attributes(self) -> None:
        """Response schema has ORM mode enabled."""
        config = UserPreferencesResponse.model_config
        assert config.get("from_attributes") is True
