"""Unit tests for UserSession model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest

from ecommerce_user_service.models.db.user_session import UserSession


@pytest.mark.unit
class TestUserSessionModel:
    """Test UserSession SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a UserSession instance with required fields."""
        _token = f"test_{uuid.uuid4().hex[:8]}"
        _user_id = uuid.uuid4()
        instance = UserSession(
            token=_token,
            expires_at=datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            user_id=_user_id,
        )
        assert instance.token == _token
        assert instance.expires_at == datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc)
        assert instance.user_id == _user_id

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _token = f"test_{uuid.uuid4().hex[:8]}"
        _user_id = uuid.uuid4()
        instance = UserSession(
            token=_token,
            expires_at=datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            user_id=_user_id,
        )
        _alt_token = f"alt_{uuid.uuid4().hex[:8]}"
        instance.token = _alt_token
        assert instance.token == _alt_token
        instance.device_name = "hello"
        assert instance.device_name == "hello"
        instance.ip_address = "192.168.1.1"
        assert instance.ip_address == "192.168.1.1"
        instance.user_agent = "foo_bar"
        assert instance.user_agent == "foo_bar"
        instance.expires_at = datetime.datetime(2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc)
        assert instance.expires_at == datetime.datetime(
            2024, 11, 5, 9, 15, 0, tzinfo=datetime.timezone.utc
        )
        instance.last_activity_at = datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        assert instance.last_activity_at == datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        _alt_user_id = uuid.uuid4()
        instance.user_id = _alt_user_id
        assert instance.user_id == _alt_user_id

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(UserSession, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = UserSession(
            token=f"test_{uuid.uuid4().hex[:8]}",
            expires_at=datetime.datetime(3000, 1, 1, tzinfo=datetime.timezone.utc),
            user_id=uuid.uuid4(),
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_token_max_length_in_column(self) -> None:
        """token column has max_length=255."""
        col = UserSession.__table__.columns["token"]
        assert col.type.length == 255

    def test_device_name_max_length_in_column(self) -> None:
        """device_name column has max_length=500."""
        col = UserSession.__table__.columns["device_name"]
        assert col.type.length == 500

    def test_user_agent_max_length_in_column(self) -> None:
        """user_agent column has max_length=255."""
        col = UserSession.__table__.columns["user_agent"]
        assert col.type.length == 255

    def test_token_unique_constraint(self) -> None:
        """token column has unique=True."""
        col = UserSession.__table__.columns["token"]
        assert col.unique is True
