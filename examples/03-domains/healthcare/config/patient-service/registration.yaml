test:
  tags: [api, patients, demographics, v1]

  meta: { team: "patient-care", compliance: "hipaa" }

  healthCheck: { type: http, path: /health, interval: 10s }

development:
  tags: [api, patients, demographics, v1]

  meta: { team: "patient-care", compliance: "hipaa" }

  healthCheck: { type: http, path: /health, interval: 10s }

production:
  tags: [api, patients, demographics, v1]

  meta: { team: "patient-care", compliance: "hipaa" }

  healthCheck: { type: http, path: /health, interval: 10s }
