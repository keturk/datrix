
// Shared module - defines reusable types across all learning management services
// BaseEntity, MAX_PAGE_SIZE, and DEFAULT_PAGE_SIZE are provided by the Datrix standard library.
module lms.common {

    const MAX_SCORE = 100;
    const MIN_SCORE = 0;
    const PASSING_SCORE = 60;
}
