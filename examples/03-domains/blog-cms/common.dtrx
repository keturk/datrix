
// Shared module - defines reusable types and functions across all blog services
// BaseEntity, hashPassword, generateToken, and verifyPassword (where used) are provided by the Datrix standard library.
module blog.common {
}
