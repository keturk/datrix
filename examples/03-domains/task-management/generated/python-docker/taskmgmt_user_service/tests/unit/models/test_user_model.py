"""Unit tests for User model.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

import pytest

from taskmgmt_user_service.models.db.user import User


@pytest.mark.unit
class TestUserModel:
    """Test User SQLAlchemy model."""

    def test_create_instance(self) -> None:
        """Can create a User instance with required fields."""
        _email = f"test_{uuid.uuid4().hex[:8]}@example.com"
        instance = User(
            name="sample_text",
            email=_email,
        )
        assert instance.name == "sample_text"
        assert instance.email == _email

    def test_field_assignment(self) -> None:
        """Fields can be assigned after creation."""
        _email = f"test_{uuid.uuid4().hex[:8]}@example.com"
        instance = User(
            name="sample_text",
            email=_email,
        )
        instance.name = "hello"
        assert instance.name == "hello"
        _alt_email = f"alt_{uuid.uuid4().hex[:8]}@example.com"
        instance.email = _alt_email
        assert instance.email == _alt_email

    def test_tablename(self) -> None:
        """Model has __tablename__."""
        assert hasattr(User, "__tablename__")

    def test_repr(self) -> None:
        """Model repr returns string."""
        instance = User(
            name="sample_text",
            email=f"test_{uuid.uuid4().hex[:8]}@example.com",
        )
        result = repr(instance)
        assert isinstance(result, str)

    def test_name_max_length_in_column(self) -> None:
        """name column has max_length=100."""
        col = User.__table__.columns["name"]
        assert col.type.length == 100

    def test_email_unique_constraint(self) -> None:
        """email column has unique=True."""
        col = User.__table__.columns["email"]
        assert col.unique is True
