"""add index and 7 more.

Revision ID: 0002_add_index_and_7_more
Revises: 0001_initial

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic
revision = "0002_add_index_and_7_more"
down_revision = "0001_initial"
branch_labels: str | None = None
depends_on: str | None = None


def upgrade() -> None:
    """Apply incremental schema changes."""
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_inventory_reservations_expires_at",
        "inventory_reservations",
        ["expires_at"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_inventory_reservations_product_id",
        "inventory_reservations",
        ["product_id"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_inventory_reservations_reservation_id",
        "inventory_reservations",
        ["reservation_id"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_inventory_reservations_reservation_id_status",
        "inventory_reservations",
        ["reservation_id", "status"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_products_category_id",
        "products",
        ["category_id"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_products_category_id_status",
        "products",
        ["category_id", "status"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_products_fulltext",
        "products",
        [
            sa.text(
                "to_tsvector('english', coalesce(name, '') || ' ' || coalesce(description, ''))"
            )
        ],
        unique=False,
        postgresql_using="gin",
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_products_status_inventory",
        "products",
        ["status", "inventory"],
        unique=False,
        if_not_exists=True,
    )


def downgrade() -> None:
    """Reverse incremental schema changes."""
    op.drop_index("ix_products_status_inventory", table_name="products", if_exists=True)
    op.drop_index("ix_products_fulltext", table_name="products", if_exists=True)
    op.drop_index(
        "ix_products_category_id_status", table_name="products", if_exists=True
    )
    op.drop_index("ix_products_category_id", table_name="products", if_exists=True)
    op.drop_index(
        "ix_inventory_reservations_reservation_id_status",
        table_name="inventory_reservations",
        if_exists=True,
    )
    op.drop_index(
        "ix_inventory_reservations_reservation_id",
        table_name="inventory_reservations",
        if_exists=True,
    )
    op.drop_index(
        "ix_inventory_reservations_product_id",
        table_name="inventory_reservations",
        if_exists=True,
    )
    op.drop_index(
        "ix_inventory_reservations_expires_at",
        table_name="inventory_reservations",
        if_exists=True,
    )
