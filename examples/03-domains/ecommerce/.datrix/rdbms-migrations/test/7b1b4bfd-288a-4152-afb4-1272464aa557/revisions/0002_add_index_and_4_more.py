"""add index and 4 more.

Revision ID: 0002_add_index_and_4_more
Revises: 0001_initial

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

from alembic import op

# revision identifiers, used by Alembic
revision = "0002_add_index_and_4_more"
down_revision = "0001_initial"
branch_labels: str | None = None
depends_on: str | None = None


def upgrade() -> None:
    """Apply incremental schema changes."""
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_users_email", "users", ["email"], unique=False, if_not_exists=True
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_users_status_role",
        "users",
        ["status", "role"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_user_sessions_token",
        "user_sessions",
        ["token"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_user_sessions_user_id",
        "user_sessions",
        ["user_id"],
        unique=False,
        if_not_exists=True,
    )
    # Repair: the recorded snapshot and this block's own revision chain disagree
    # about this index -- either no revision in the chain ever created one the
    # snapshot declares, or the chain already creates it and the diff still
    # reported it as new. Emitted IF NOT EXISTS because the ledger has been shown
    # not to describe the chain, so the index may already be there: failing an
    # already-correct database, or a replay from empty, is not a repair.
    op.create_index(
        "ix_user_sessions_user_id_expires_at",
        "user_sessions",
        ["user_id", "expires_at"],
        unique=False,
        if_not_exists=True,
    )


def downgrade() -> None:
    """Reverse incremental schema changes."""
    op.drop_index(
        "ix_user_sessions_user_id_expires_at",
        table_name="user_sessions",
        if_exists=True,
    )
    op.drop_index(
        "ix_user_sessions_user_id", table_name="user_sessions", if_exists=True
    )
    op.drop_index("ix_user_sessions_token", table_name="user_sessions", if_exists=True)
    op.drop_index("ix_users_status_role", table_name="users", if_exists=True)
    op.drop_index("ix_users_email", table_name="users", if_exists=True)
