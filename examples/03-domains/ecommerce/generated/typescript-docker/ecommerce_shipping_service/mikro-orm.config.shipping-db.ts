/**
 * MikroORM CLI config for migrations (shippingDb).
 *
 * Connection facts resolve through the same getDatabaseConfig() function the
 * running service and migration entrypoint use. No environment variable is
 * read at any point.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { defineConfig } from '@mikro-orm/core';
import { PostgreSqlDriver } from '@mikro-orm/postgresql';
import { Migrator } from '@mikro-orm/migrations';
import { Shipment } from './src/entities/shipping_db/shipment.entity';
import { ShipmentEvent } from './src/entities/shipping_db/shipment-event.entity';
import { ShipmentItem } from './src/entities/shipping_db/shipment-item.entity';
import { getDatabaseConfig } from './src/shipping-db/database.config';

export default getDatabaseConfig({ get: () => false } as never).then((base) =>
  defineConfig({
    ...base,
    driver: PostgreSqlDriver,
    entities: [Shipment, ShipmentEvent, ShipmentItem],
    extensions: [Migrator],
    migrations: {
      path: './dist/migrations/shipping-db',
      pathTs: './src/migrations/shipping-db',
      emit: 'ts',
    },
    contextName: 'shipping_db',
  }),
);
