/**
 * Unit tests for ShippingCarrier enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { ShippingCarrier } from '../src/enums/shipping-carrier.enum';

describe('ShippingCarrier', () => {
  it('is defined as a runtime value', () => {
    expect(ShippingCarrier).toBeDefined();
  });

  it('has 4 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(ShippingCarrier).filter(
      (key) => typeof (ShippingCarrier as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(4);
  });

  it('has member FedEx with value "fed_ex"', () => {
    expect(ShippingCarrier.FedEx).toBe('fed_ex');
  });

  it('has member Ups with value "ups"', () => {
    expect(ShippingCarrier.Ups).toBe('ups');
  });

  it('has member Usps with value "usps"', () => {
    expect(ShippingCarrier.Usps).toBe('usps');
  });

  it('has member Dhl with value "dhl"', () => {
    expect(ShippingCarrier.Dhl).toBe('dhl');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(ShippingCarrier).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(4);
    expect(stringValues).toContain('fed_ex');
    expect(stringValues).toContain('ups');
    expect(stringValues).toContain('usps');
    expect(stringValues).toContain('dhl');
  });
});
