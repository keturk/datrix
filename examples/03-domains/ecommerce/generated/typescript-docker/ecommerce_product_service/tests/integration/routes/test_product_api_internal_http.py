"""HTTP integration tests for internal endpoints on ProductAPI.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""
from __future__ import annotations

import pytest
from httpx import AsyncClient


@pytest.mark.integration
@pytest.mark.asyncio
class TestProductApiInternalHttp:
    """Internal routes require a valid service JWT (Authorization: Bearer)."""

    async def test_product_api_post_service_check_availability_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/check-availability without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/check-availability",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_check_availability_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/check-availability with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/check-availability",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_reserve_inventory_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/reserve-inventory without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/reserve-inventory",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_reserve_inventory_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/reserve-inventory with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/reserve-inventory",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_confirm_reservation_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/confirm-reservation without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/confirm-reservation",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_confirm_reservation_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/confirm-reservation with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/confirm-reservation",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_release_reservation_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/release-reservation without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/release-reservation",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_release_reservation_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/release-reservation with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/release-reservation",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_get_service_by_id_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """GET /api/v1/products/service/00000000-0000-0000-0000-000000000001 without Authorization returns 401."""
        response = await unauth_client.get(
            "/api/v1/products/service/00000000-0000-0000-0000-000000000001",
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_get_service_by_id_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """GET /api/v1/products/service/00000000-0000-0000-0000-000000000001 with invalid Bearer token returns 401."""
        response = await unauth_client.get(
            "/api/v1/products/service/00000000-0000-0000-0000-000000000001",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_bulk_internal_no_header(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/bulk without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/bulk",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_product_api_post_service_bulk_internal_wrong_token(self, unauth_client: AsyncClient) -> None:
        """POST /api/v1/products/service/bulk with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/products/service/bulk",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

