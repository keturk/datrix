/**
 * Unit tests for ReservationStatus enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { ReservationStatus } from '../src/enums/reservation-status.enum';

describe('ReservationStatus', () => {
  it('is defined as a runtime value', () => {
    expect(ReservationStatus).toBeDefined();
  });

  it('has 4 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(ReservationStatus).filter(
      (key) => typeof (ReservationStatus as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(4);
  });

  it('has member Reserved with value "reserved"', () => {
    expect(ReservationStatus.Reserved).toBe('reserved');
  });

  it('has member Confirmed with value "confirmed"', () => {
    expect(ReservationStatus.Confirmed).toBe('confirmed');
  });

  it('has member Released with value "released"', () => {
    expect(ReservationStatus.Released).toBe('released');
  });

  it('has member Expired with value "expired"', () => {
    expect(ReservationStatus.Expired).toBe('expired');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(ReservationStatus).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(4);
    expect(stringValues).toContain('reserved');
    expect(stringValues).toContain('confirmed');
    expect(stringValues).toContain('released');
    expect(stringValues).toContain('expired');
  });
});
