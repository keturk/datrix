/**
 * Configuration unit test for EcommerceProductService.
 * Verifies the application configuration module loads correctly.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import configuration from '../src/config/configuration';

describe('Application Configuration', () => {
  it('returns a configuration object', () => {
    const config = configuration();
    expect(config).toBeDefined();
    expect(typeof config).toBe('object');
  });

  it('provides a port number', () => {
    const config = configuration();
    expect(typeof config.port).toBe('number');
    expect(config.port).toBeGreaterThan(0);
  });

  it('exposes JWT metadata without any key material', () => {
    const config = configuration();
    expect(typeof config.jwtAlgorithm).toBe('string');
    expect(typeof config.jwtIssuer).toBe('string');
    expect(typeof config.jwtExpirySeconds).toBe('number');
  });

  it('loads with no signing secret in the environment', () => {
    // The service verifies tokens with the key file the deployment mounts, so
    // configuration must never require a symmetric secret to be resolvable.
    expect(Object.keys(configuration())).not.toContain('jwtSecret');
  });
});
