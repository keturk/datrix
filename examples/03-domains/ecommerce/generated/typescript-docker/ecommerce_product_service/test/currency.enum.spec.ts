/**
 * Unit tests for Currency enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Currency } from '../src/enums/currency.enum';

describe('Currency', () => {
  it('is defined as a runtime value', () => {
    expect(Currency).toBeDefined();
  });

  it('has 3 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(Currency).filter(
      (key) => typeof (Currency as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(3);
  });

  it('has member Usd with value "usd"', () => {
    expect(Currency.Usd).toBe('usd');
  });

  it('has member Eur with value "eur"', () => {
    expect(Currency.Eur).toBe('eur');
  });

  it('has member Gbp with value "gbp"', () => {
    expect(Currency.Gbp).toBe('gbp');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(Currency).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(3);
    expect(stringValues).toContain('usd');
    expect(stringValues).toContain('eur');
    expect(stringValues).toContain('gbp');
  });
});
