/**
 * Unit tests for ProductStatus enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { ProductStatus } from '../src/enums/product-status.enum';

describe('ProductStatus', () => {
  it('is defined as a runtime value', () => {
    expect(ProductStatus).toBeDefined();
  });

  it('has 3 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(ProductStatus).filter(
      (key) => typeof (ProductStatus as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(3);
  });

  it('has member Draft with value "draft"', () => {
    expect(ProductStatus.Draft).toBe('draft');
  });

  it('has member Active with value "active"', () => {
    expect(ProductStatus.Active).toBe('active');
  });

  it('has member Discontinued with value "discontinued"', () => {
    expect(ProductStatus.Discontinued).toBe('discontinued');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(ProductStatus).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(3);
    expect(stringValues).toContain('draft');
    expect(stringValues).toContain('active');
    expect(stringValues).toContain('discontinued');
  });
});
