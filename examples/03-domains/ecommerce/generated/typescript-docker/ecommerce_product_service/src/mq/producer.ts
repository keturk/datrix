/**
 * Kafka event producer.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { KafkaConnection } from './connection';
import { outboxActive, deferEvent } from '../eventOutbox';
import { recordPublish } from '../observability/pubsub-metrics';
import {
  ProductCreatedPayload,
  InventoryUpdatedPayload,
  InventoryReservedPayload,
  InventoryReleasedPayload,
} from './schemas';
import { validateInventoryReleasedContract, validateInventoryReservedContract, validateInventoryUpdatedContract, validateProductCreatedContract } from './contracts';

const BLOCK_NAME = 'mq';

export class KafkaEventProducer {
  constructor(
    private readonly connection: KafkaConnection,
  ) {}

  async publishProductCreated(payload: ProductCreatedPayload): Promise<void> {
    validateProductCreatedContract(
      payload.productId,
      payload.name,
      payload.price    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverProductCreated` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverProductCreated(payload));
      return;
    }
    await this._deliverProductCreated(payload);
  }

  private _deliverProductCreated(payload: ProductCreatedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_product_service.mq.product_events', 'ProductCreated', () =>
      this._sendProductCreated(payload),
    );
  }

  private async _sendProductCreated(payload: ProductCreatedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'ProductCreated',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_product_service.mq.product_events',
      messages: [{ value }],
    });
  }

  async publishInventoryUpdated(payload: InventoryUpdatedPayload): Promise<void> {
    validateInventoryUpdatedContract(
      payload.productId,
      payload.oldQuantity,
      payload.newQuantity    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverInventoryUpdated` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverInventoryUpdated(payload));
      return;
    }
    await this._deliverInventoryUpdated(payload);
  }

  private _deliverInventoryUpdated(payload: InventoryUpdatedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_product_service.mq.product_events', 'InventoryUpdated', () =>
      this._sendInventoryUpdated(payload),
    );
  }

  private async _sendInventoryUpdated(payload: InventoryUpdatedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'InventoryUpdated',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_product_service.mq.product_events',
      messages: [{ value }],
    });
  }

  async publishInventoryReserved(payload: InventoryReservedPayload): Promise<void> {
    validateInventoryReservedContract(
      payload.reservationId,
      payload.productIds    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverInventoryReserved` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverInventoryReserved(payload));
      return;
    }
    await this._deliverInventoryReserved(payload);
  }

  private _deliverInventoryReserved(payload: InventoryReservedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_product_service.mq.product_events', 'InventoryReserved', () =>
      this._sendInventoryReserved(payload),
    );
  }

  private async _sendInventoryReserved(payload: InventoryReservedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'InventoryReserved',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_product_service.mq.product_events',
      messages: [{ value }],
    });
  }

  async publishInventoryReleased(payload: InventoryReleasedPayload): Promise<void> {
    validateInventoryReleasedContract(
      payload.reservationId,
      payload.reason    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverInventoryReleased` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverInventoryReleased(payload));
      return;
    }
    await this._deliverInventoryReleased(payload);
  }

  private _deliverInventoryReleased(payload: InventoryReleasedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_product_service.mq.product_events', 'InventoryReleased', () =>
      this._sendInventoryReleased(payload),
    );
  }

  private async _sendInventoryReleased(payload: InventoryReleasedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'InventoryReleased',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_product_service.mq.product_events',
      messages: [{ value }],
    });
  }


  async publishRaw(topic: string, value: string): Promise<void> {
    const producer = await this.connection.getProducer();
    await producer.send({ topic, messages: [{ value }] });
  }
}

export let producerInstance: KafkaEventProducer | null = null;
