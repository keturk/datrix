/**
 * NoSQL database connection configuration for mongodb engine.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { MongooseModuleOptions } from '@nestjs/mongoose';
import { ConfigService } from '@nestjs/config';

export function getNosqlDatabaseConfig(
  _configService: ConfigService,
): MongooseModuleOptions {
  // Read from environment variables (set by Docker Compose or deployment config)
  const uri = process.env['NOSQL_URI'];
  if (!uri) {
    throw new Error('NOSQL_URI environment variable is required');
  }

  const database = process.env['NOSQL_DATABASE'];
  if (!database) {
    throw new Error('NOSQL_DATABASE environment variable is required');
  }

  return {
    uri,
    dbName: database,
    retryAttempts: 3,
    retryDelay: 1000,
  };
}
