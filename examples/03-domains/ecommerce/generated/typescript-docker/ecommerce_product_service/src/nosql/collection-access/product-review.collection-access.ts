/**
 * Typed collection access for ProductReview.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { Injectable } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model } from 'mongoose';
import {
  ProductReview,
  ProductReviewDocument,
} from '../schemas/product-review.schema';

@Injectable()
export class ProductReviewCollection {
  constructor(
    @InjectModel(ProductReview.name)
    private readonly model: Model<ProductReviewDocument>,
  ) {}

  async findByProductId(
    product_id: string,
  ): Promise<ProductReviewDocument[]> {
    return this.model.find({ product_id: product_id }).exec();
  }

  async findByUserId(
    user_id: string,
  ): Promise<ProductReviewDocument[]> {
    return this.model.find({ user_id: user_id }).exec();
  }

  async findAll(): Promise<ProductReviewDocument[]> {
    return this.model.find().exec();
  }

  async insert(document: Partial<ProductReview>): Promise<ProductReviewDocument> {
    const doc = new this.model(document);
    return doc.save();
  }
}
