/**
 * MikroORM CLI config for migrations (productDb).
 *
 * Connection facts resolve through the same getDatabaseConfig() function the
 * running service and migration entrypoint use. No environment variable is
 * read at any point.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { defineConfig } from '@mikro-orm/core';
import { PostgreSqlDriver } from '@mikro-orm/postgresql';
import { Migrator } from '@mikro-orm/migrations';
import { Category } from './src/entities/product_db/category.entity';
import { InventoryReservation } from './src/entities/product_db/inventory-reservation.entity';
import { Product } from './src/entities/product_db/product.entity';
import { getDatabaseConfig } from './src/product-db/database.config';

export default getDatabaseConfig({ get: () => false } as never).then((base) =>
  defineConfig({
    ...base,
    driver: PostgreSqlDriver,
    entities: [Category, InventoryReservation, Product],
    extensions: [Migrator],
    migrations: {
      path: './dist/migrations/product-db',
      pathTs: './src/migrations/product-db',
      emit: 'ts',
    },
    contextName: 'product_db',
  }),
);
