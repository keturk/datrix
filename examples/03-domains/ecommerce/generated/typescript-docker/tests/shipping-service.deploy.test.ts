/**
 * Deployment test for ecommerce.ShippingService.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { fetchWithRetry, httpRequest, waitForHealth, httpGetJson, makeTestJwt } from './helpers';

const SERVICE_PORT = 8005;
const BASE_URL = `http://localhost:${SERVICE_PORT}`;
const HEALTH_TIMEOUT_MS = 120_000;
const JWT_ISSUER = 'library';

describe('ShippingService Deployment', () => {
  it('health check returns 200 with healthy status', async () => {
    const healthy = await waitForHealth(BASE_URL, HEALTH_TIMEOUT_MS);
    expect(healthy).toBe(true);

    const data = await httpGetJson(`${BASE_URL}/health`);
    expect(data).toBeDefined();
    expect(['healthy', 'ok']).toContain(data.status);
  }, HEALTH_TIMEOUT_MS + 10_000);

  it('responds to GET /', async () => {
    const response = await fetchWithRetry(BASE_URL);
    expect(response.status).toBeLessThan(500);
  });

  it('OpenAPI docs endpoint /docs is accessible', async () => {
    const response = await fetchWithRetry(`${BASE_URL}/docs`);
    expect(response.status).toBe(200);
  });

  it('OpenAPI JSON schema /openapi.json is valid', async () => {
    const data = await httpGetJson(`${BASE_URL}/openapi.json`);
    expect(data).toBeDefined();
    expect(data.openapi).toBeDefined();
  }, 35_000);

  // --- Infrastructure health checks (checks object from /health) ---

  describe('ShippingService Health Checks Detail', () => {
    let healthData: Record<string, unknown>;

    beforeAll(async () => {
      healthData = await httpGetJson(`${BASE_URL}/health`);
    });

    it('health response includes checks record', () => {
      expect(healthData.checks).toBeDefined();
      expect(typeof healthData.checks).toBe('object');
    });

    it('health checks include Database (shippingDb) (database)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['database']).toBeDefined();
      expect(['up', 'down']).toContain(checks['database']);
    });

    it('health checks include PubSub (mq) (pubsub)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['pubsub']).toBeDefined();
      expect(['up', 'down']).toContain(checks['pubsub']);
    });

    it('health checks include Cache (redis) (cache)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['cache']).toBeDefined();
      expect(['up', 'down']).toContain(checks['cache']);
    });

  });

  // --- Endpoint Smoke Tests ---

  it('Custom GET /api/v1/shipments/order/00000000-0000-0000-0000-000000000001 responds with 200 or 403/404 when missing', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/shipments/order/00000000-0000-0000-0000-000000000001`, {
      headers: { 'Authorization': `Bearer ${makeTestJwt(["user"], JWT_ISSUER)}` },
    });
    expect([200, 403, 404]).toContain(response.status);
  });

  it('Custom GET /api/v1/shipments/track/test responds with 200 or 403/404 when missing', async () => {
    const response = await fetchWithRetry(`${BASE_URL}/api/v1/shipments/track/test`);
    expect([200, 403, 404]).toContain(response.status);
  });

  it('Nested GET /api/v1/shipments/00000000-0000-0000-0000-000000000001/shipment_events responds with 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001/shipment_events`, {
      headers: { 'Authorization': `Bearer ${makeTestJwt(["user"], JWT_ISSUER)}` },
    });
    // Nested GET targets a fabricated parent UUID that may not exist in the
    // database; the handler verifies the parent first and returns 404 when
    // the parent is not found (403 when ownership is rejected).
    expect([200, 403, 404]).toContain(response.status);
  });

  // --- Authentication / Authorization Tests ---

  describe('ShippingApi Auth', () => {
    it('Public GET /api/v1/shipments/track/test without auth returns 200 or 403/404 when resource missing', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/shipments/track/test`);
      expect([200, 403, 404]).toContain(response.status);
    });

    it('Public POST /api/v1/shipments/rates?request=integration-path-value without auth is accessible (not 401)', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/rates?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).not.toBe(401);
    });

    it('GET /api/v1/shipments/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/shipments/order/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/shipments/order/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/shipments/?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PATCH /api/v1/shipments/00000000-0000-0000-0000-000000000001/status?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001/status?request=integration-path-value`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/shipments/00000000-0000-0000-0000-000000000001/events?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001/events?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PATCH /api/v1/shipments/00000000-0000-0000-0000-000000000001/status?request=integration-path-value with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001/status?request=integration-path-value`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${makeTestJwt([], JWT_ISSUER)}` },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(403);
    });

    it('POST /api/v1/shipments/00000000-0000-0000-0000-000000000001/events?request=integration-path-value with wrong role returns 403', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/00000000-0000-0000-0000-000000000001/events?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${makeTestJwt([], JWT_ISSUER)}` },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(403);
    });

  });

  // --- OpenAPI Schema Completeness ---

  describe('ShippingService OpenAPI Schema', () => {
    let schema: Record<string, unknown>;

    beforeAll(async () => {
      schema = await httpGetJson(`${BASE_URL}/openapi.json`);
    }, 35_000);

    it('OpenAPI paths contains /api/v1/shipments/order/{orderId}', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/order/{orderId}']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/rates', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/rates']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/track/{trackingNumber}', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/track/{trackingNumber}']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/webhook/fedex', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/webhook/fedex']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/{id}', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/{id}']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/{id}/events', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/{id}/events']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/shipments/{id}/status', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/shipments/{id}/status']).toBeDefined();
    });

    it('OpenAPI schemas contains AddTrackingEventRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'AddTrackingEventRequest' || s.includes('AddTrackingEventRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains CreateShipmentItem', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'CreateShipmentItem' || s.includes('CreateShipmentItem'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains CreateShipmentRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'CreateShipmentRequest' || s.includes('CreateShipmentRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains FedExWebhookRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'FedExWebhookRequest' || s.includes('FedExWebhookRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains GetShippingRatesRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'GetShippingRatesRequest' || s.includes('GetShippingRatesRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains ShipmentTracking', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'ShipmentTracking' || s.includes('ShipmentTracking'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains ShippingRateResponse', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'ShippingRateResponse' || s.includes('ShippingRateResponse'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains UpdateShipmentStatusRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'UpdateShipmentStatusRequest' || s.includes('UpdateShipmentStatusRequest'),
      );
      expect(hasSchema).toBe(true);
    });

  });

  // --- Internal Endpoint Tests ---

  describe('ShippingService Internal Endpoints', () => {
    it('Internal POST /api/v1/shipments/?request=integration-path-value responds without error', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/shipments/?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBeLessThan(500);
    });

  });
});
