/**
 * Deployment test for ecommerce.OrderService.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { fetchWithRetry, httpRequest, waitForHealth, httpGetJson, makeTestJwt } from './helpers';

const SERVICE_PORT = 8002;
const BASE_URL = `http://localhost:${SERVICE_PORT}`;
const HEALTH_TIMEOUT_MS = 120_000;
const JWT_ISSUER = 'library';

describe('OrderService Deployment', () => {
  it('health check returns 200 with healthy status', async () => {
    const healthy = await waitForHealth(BASE_URL, HEALTH_TIMEOUT_MS);
    expect(healthy).toBe(true);

    const data = await httpGetJson(`${BASE_URL}/health`);
    expect(data).toBeDefined();
    expect(['healthy', 'ok']).toContain(data.status);
  }, HEALTH_TIMEOUT_MS + 10_000);

  it('responds to GET /', async () => {
    const response = await fetchWithRetry(BASE_URL);
    expect(response.status).toBeLessThan(500);
  });

  it('OpenAPI docs endpoint /docs is accessible', async () => {
    const response = await fetchWithRetry(`${BASE_URL}/docs`);
    expect(response.status).toBe(200);
  });

  it('OpenAPI JSON schema /openapi.json is valid', async () => {
    const data = await httpGetJson(`${BASE_URL}/openapi.json`);
    expect(data).toBeDefined();
    expect(data.openapi).toBeDefined();
  }, 35_000);

  // --- Infrastructure health checks (checks object from /health) ---

  describe('OrderService Health Checks Detail', () => {
    let healthData: Record<string, unknown>;

    beforeAll(async () => {
      healthData = await httpGetJson(`${BASE_URL}/health`);
    });

    it('health response includes checks record', () => {
      expect(healthData.checks).toBeDefined();
      expect(typeof healthData.checks).toBe('object');
    });

    it('health checks include Database (orderDb) (database)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['database']).toBeDefined();
      expect(['up', 'down']).toContain(checks['database']);
    });

    it('health checks include PubSub (mq) (pubsub)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['pubsub']).toBeDefined();
      expect(['up', 'down']).toContain(checks['pubsub']);
    });

    it('health checks include Cache (redis) (cache)', () => {
      const checks = healthData.checks as Record<string, string>;
      expect(checks['cache']).toBeDefined();
      expect(['up', 'down']).toContain(checks['cache']);
    });

  });

  // --- Endpoint Smoke Tests ---

  it('Custom GET /api/v1/orders/ responds with 200 or 403/404 when missing', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/`, {
      headers: { 'Authorization': `Bearer ${makeTestJwt(["user"], JWT_ISSUER)}` },
    });
    expect([200, 403, 404]).toContain(response.status);
  });

  it('Custom GET /api/v1/orders/00000000-0000-0000-0000-000000000001 responds with 200 or 403/404 when missing', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001`, {
      headers: { 'Authorization': `Bearer ${makeTestJwt(["user"], JWT_ISSUER)}` },
    });
    expect([200, 403, 404]).toContain(response.status);
  });

  it('Nested GET /api/v1/orders/00000000-0000-0000-0000-000000000001/order_items responds with 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/order_items`, {
      headers: { 'Authorization': `Bearer ${makeTestJwt(["user"], JWT_ISSUER)}` },
    });
    // Nested GET targets a fabricated parent UUID that may not exist in the
    // database; the handler verifies the parent first and returns 404 when
    // the parent is not found (403 when ownership is rejected).
    expect([200, 403, 404]).toContain(response.status);
  });

  // --- Authentication / Authorization Tests ---

  describe('OrderApi Auth', () => {
    it('GET /api/v1/orders/ without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/`);
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/orders/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/orders/?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/orders/00000000-0000-0000-0000-000000000001/cancel?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/cancel?request=integration-path-value`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/orders/service/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/service/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/orders/00000000-0000-0000-0000-000000000001/confirm-payment?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/confirm-payment?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/orders/00000000-0000-0000-0000-000000000001/update-shipment?request=integration-path-value without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/update-shipment?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

  });

  // --- OpenAPI Schema Completeness ---

  describe('OrderService OpenAPI Schema', () => {
    let schema: Record<string, unknown>;

    beforeAll(async () => {
      schema = await httpGetJson(`${BASE_URL}/openapi.json`);
    }, 35_000);

    it('OpenAPI paths contains /api/v1/orders', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/orders']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/orders/{id}', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/orders/{id}']).toBeDefined();
    });

    it('OpenAPI paths contains /api/v1/orders/{id}/cancel', () => {
      const paths = schema.paths as Record<string, unknown>;
      expect(paths).toBeDefined();
      expect(paths['/api/v1/orders/{id}/cancel']).toBeDefined();
    });

    it('OpenAPI schemas contains CancelOrderRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'CancelOrderRequest' || s.includes('CancelOrderRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains ConfirmPaymentRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'ConfirmPaymentRequest' || s.includes('ConfirmPaymentRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains CreateOrderRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'CreateOrderRequest' || s.includes('CreateOrderRequest'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains PaginatedOrders', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'PaginatedOrders' || s.includes('PaginatedOrders'),
      );
      expect(hasSchema).toBe(true);
    });

    it('OpenAPI schemas contains UpdateShipmentRequest', () => {
      const components = schema.components as Record<string, unknown> | undefined;
      expect(components).toBeDefined();
      const schemas = (components as Record<string, unknown>).schemas as Record<string, unknown>;
      expect(schemas).toBeDefined();
      const schemaNames = Object.keys(schemas);
      const hasSchema = schemaNames.some(
        (s) => s === 'UpdateShipmentRequest' || s.includes('UpdateShipmentRequest'),
      );
      expect(hasSchema).toBe(true);
    });

  });

  // --- Internal Endpoint Tests ---

  describe('OrderService Internal Endpoints', () => {
    it('Internal GET /api/v1/orders/service/00000000-0000-0000-0000-000000000001 responds without error', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/service/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBeLessThan(500);
    });

    it('Internal POST /api/v1/orders/00000000-0000-0000-0000-000000000001/confirm-payment?request=integration-path-value responds without error', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/confirm-payment?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBeLessThan(500);
    });

    it('Internal POST /api/v1/orders/00000000-0000-0000-0000-000000000001/update-shipment?request=integration-path-value responds without error', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/update-shipment?request=integration-path-value`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBeLessThan(500);
    });

  });
});
