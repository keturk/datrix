/**
 * Standalone queue enqueue-consumer worker entrypoint.
 * Runs as: node dist/workers/enqueue-worker.js
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { NestFactory } from '@nestjs/core';
import { Logger } from '@nestjs/common';
import { EnqueueWorkerModule } from './enqueue-worker.module';
import { EnqueueWorkerService } from './enqueue-worker.service';

const logger = new Logger('EnqueueWorker');

async function bootstrap(): Promise<void> {
  const app = await NestFactory.createApplicationContext(EnqueueWorkerModule);
  const worker = app.get(EnqueueWorkerService);
  logger.log('Enqueue worker started');
  await worker.start();
}

bootstrap().catch((err) => {
  logger.error('Enqueue worker failed to start', err);
  process.exit(1);
});
