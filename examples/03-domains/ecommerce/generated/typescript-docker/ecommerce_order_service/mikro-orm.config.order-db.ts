/**
 * MikroORM CLI config for migrations (orderDb).
 *
 * Connection facts resolve through the same getDatabaseConfig() function the
 * running service and migration entrypoint use. No environment variable is
 * read at any point.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { defineConfig } from '@mikro-orm/core';
import { PostgreSqlDriver } from '@mikro-orm/postgresql';
import { Migrator } from '@mikro-orm/migrations';
import { IdempotencyKey } from './src/entities/order_db/idempotency-key.entity';
import { Order } from './src/entities/order_db/order.entity';
import { OrderItem } from './src/entities/order_db/order-item.entity';
import { getDatabaseConfig } from './src/order-db/database.config';

export default getDatabaseConfig({ get: () => false } as never).then((base) =>
  defineConfig({
    ...base,
    driver: PostgreSqlDriver,
    entities: [IdempotencyKey, Order, OrderItem],
    extensions: [Migrator],
    migrations: {
      path: './dist/migrations/order-db',
      pathTs: './src/migrations/order-db',
      emit: 'ts',
    },
    contextName: 'order_db',
  }),
);
