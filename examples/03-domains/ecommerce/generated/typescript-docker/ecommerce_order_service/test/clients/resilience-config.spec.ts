// Unit tests for resilience client configuration.
//
// Auto-generated by datrix-codegen-typescript. Do not edit manually.

import { DEFAULT_TIMEOUT_MS } from '../../src/clients/resilient-client.service';
import {
  DEFAULT_TIMEOUT_MS as PRODUCT_SERVICE_TIMEOUT_MS,
  RETRY_MAX_ATTEMPTS as PRODUCT_SERVICE_RETRY_MAX_ATTEMPTS,
} from '../../src/clients/product-service-client.service';
import {
  DEFAULT_TIMEOUT_MS as USER_SERVICE_TIMEOUT_MS,
  RETRY_MAX_ATTEMPTS as USER_SERVICE_RETRY_MAX_ATTEMPTS,
} from '../../src/clients/user-service-client.service';

describe('Resilience Configuration', () => {
  it('bakes the declared default timeout into the base client', () => {
    expect(DEFAULT_TIMEOUT_MS).toBe(10000);
  });

  describe('Dependency: ProductService', () => {
    it('bakes the timeout resolved for this dependency', () => {
      expect(PRODUCT_SERVICE_TIMEOUT_MS).toBe(10000);
    });

    it('bakes the retry cap resolved for this dependency', () => {
      expect(PRODUCT_SERVICE_RETRY_MAX_ATTEMPTS).toBe(2);
    });
  });

  describe('Dependency: UserService', () => {
    it('bakes the timeout resolved for this dependency', () => {
      expect(USER_SERVICE_TIMEOUT_MS).toBe(10000);
    });

    it('bakes the retry cap resolved for this dependency', () => {
      expect(USER_SERVICE_RETRY_MAX_ATTEMPTS).toBe(2);
    });
  });

});
