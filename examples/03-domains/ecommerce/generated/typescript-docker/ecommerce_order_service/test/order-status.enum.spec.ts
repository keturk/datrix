/**
 * Unit tests for OrderStatus enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { OrderStatus } from '../src/enums/order-status.enum';

describe('OrderStatus', () => {
  it('is defined as a runtime value', () => {
    expect(OrderStatus).toBeDefined();
  });

  it('has 8 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(OrderStatus).filter(
      (key) => typeof (OrderStatus as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(8);
  });

  it('has member Pending with value "pending"', () => {
    expect(OrderStatus.Pending).toBe('pending');
  });

  it('has member PaymentPending with value "payment_pending"', () => {
    expect(OrderStatus.PaymentPending).toBe('payment_pending');
  });

  it('has member Confirmed with value "confirmed"', () => {
    expect(OrderStatus.Confirmed).toBe('confirmed');
  });

  it('has member Processing with value "processing"', () => {
    expect(OrderStatus.Processing).toBe('processing');
  });

  it('has member Shipped with value "shipped"', () => {
    expect(OrderStatus.Shipped).toBe('shipped');
  });

  it('has member Delivered with value "delivered"', () => {
    expect(OrderStatus.Delivered).toBe('delivered');
  });

  it('has member Cancelled with value "cancelled"', () => {
    expect(OrderStatus.Cancelled).toBe('cancelled');
  });

  it('has member Refunded with value "refunded"', () => {
    expect(OrderStatus.Refunded).toBe('refunded');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(OrderStatus).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(8);
    expect(stringValues).toContain('pending');
    expect(stringValues).toContain('payment_pending');
    expect(stringValues).toContain('confirmed');
    expect(stringValues).toContain('processing');
    expect(stringValues).toContain('shipped');
    expect(stringValues).toContain('delivered');
    expect(stringValues).toContain('cancelled');
    expect(stringValues).toContain('refunded');
  });
});
