/**
 * Unit tests for PaymentStatus enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { PaymentStatus } from '../src/enums/payment-status.enum';

describe('PaymentStatus', () => {
  it('is defined as a runtime value', () => {
    expect(PaymentStatus).toBeDefined();
  });

  it('has 5 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(PaymentStatus).filter(
      (key) => typeof (PaymentStatus as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(5);
  });

  it('has member Pending with value "pending"', () => {
    expect(PaymentStatus.Pending).toBe('pending');
  });

  it('has member Processing with value "processing"', () => {
    expect(PaymentStatus.Processing).toBe('processing');
  });

  it('has member Completed with value "completed"', () => {
    expect(PaymentStatus.Completed).toBe('completed');
  });

  it('has member Failed with value "failed"', () => {
    expect(PaymentStatus.Failed).toBe('failed');
  });

  it('has member Refunded with value "refunded"', () => {
    expect(PaymentStatus.Refunded).toBe('refunded');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(PaymentStatus).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(5);
    expect(stringValues).toContain('pending');
    expect(stringValues).toContain('processing');
    expect(stringValues).toContain('completed');
    expect(stringValues).toContain('failed');
    expect(stringValues).toContain('refunded');
  });
});
