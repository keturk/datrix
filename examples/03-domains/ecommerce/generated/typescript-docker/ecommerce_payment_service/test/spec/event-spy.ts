// Captures published events for assertions in spec tests.
//
// Auto-generated by datrix-codegen-typescript. Do not edit manually.

export class EventSpy {
  private readonly records: Array<[string, Record<string, unknown>]> = [];

  record(eventName: string, payload: Record<string, unknown>): void {
    this.records.push([eventName, payload]);
  }

  has(eventName: string, payload: Record<string, unknown>): boolean {
    for (const [name, existing] of this.records!) {
      if (name !== eventName) {
        continue;
      }
      if (this._payloadsEqual(existing, payload)) {
        return true;
      }
    }
    return false;
  }

  reset(): void {
    this.records.length = 0;
  }

  private _payloadsEqual(
    a: Record<string, unknown>,
    b: Record<string, unknown>,
  ): boolean {
    const keysA = Object.keys(a);
    const keysB = Object.keys(b);
    if (keysA.length !== keysB.length) {
      return false;
    }
    for (const k of keysA) {
      if (a[k] !== b[k]) {
        return false;
      }
    }
    return true;
  }
}
