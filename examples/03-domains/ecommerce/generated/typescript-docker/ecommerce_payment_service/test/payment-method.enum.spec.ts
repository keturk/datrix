/**
 * Unit tests for PaymentMethod enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { PaymentMethod } from '../src/enums/payment-method.enum';

describe('PaymentMethod', () => {
  it('is defined as a runtime value', () => {
    expect(PaymentMethod).toBeDefined();
  });

  it('has 4 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(PaymentMethod).filter(
      (key) => typeof (PaymentMethod as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(4);
  });

  it('has member CreditCard with value "credit_card"', () => {
    expect(PaymentMethod.CreditCard).toBe('credit_card');
  });

  it('has member DebitCard with value "debit_card"', () => {
    expect(PaymentMethod.DebitCard).toBe('debit_card');
  });

  it('has member PayPal with value "pay_pal"', () => {
    expect(PaymentMethod.PayPal).toBe('pay_pal');
  });

  it('has member BankTransfer with value "bank_transfer"', () => {
    expect(PaymentMethod.BankTransfer).toBe('bank_transfer');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(PaymentMethod).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(4);
    expect(stringValues).toContain('credit_card');
    expect(stringValues).toContain('debit_card');
    expect(stringValues).toContain('pay_pal');
    expect(stringValues).toContain('bank_transfer');
  });
});
