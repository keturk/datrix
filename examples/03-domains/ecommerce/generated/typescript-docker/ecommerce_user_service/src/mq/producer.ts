/**
 * Kafka event producer.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { KafkaConnection } from './connection';
import { outboxActive, deferEvent } from '../eventOutbox';
import { recordPublish } from '../observability/pubsub-metrics';
import {
  UserRegisteredPayload,
  UserVerifiedPayload,
  UserStatusChangedPayload,
  UserLoggedInPayload,
} from './schemas';
import { validateUserRegisteredContract, validateUserStatusChangedContract } from './contracts';

const BLOCK_NAME = 'mq';

export class KafkaEventProducer {
  constructor(
    private readonly connection: KafkaConnection,
  ) {}

  async publishUserRegistered(payload: UserRegisteredPayload): Promise<void> {
    validateUserRegisteredContract(
      payload.userId,
      payload.email,
      payload.fullName    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverUserRegistered` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverUserRegistered(payload));
      return;
    }
    await this._deliverUserRegistered(payload);
  }

  private _deliverUserRegistered(payload: UserRegisteredPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_user_service.mq.user_events', 'UserRegistered', () =>
      this._sendUserRegistered(payload),
    );
  }

  private async _sendUserRegistered(payload: UserRegisteredPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'UserRegistered',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_user_service.mq.user_events',
      messages: [{ value }],
    });
  }

  async publishUserVerified(payload: UserVerifiedPayload): Promise<void> {
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverUserVerified` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverUserVerified(payload));
      return;
    }
    await this._deliverUserVerified(payload);
  }

  private _deliverUserVerified(payload: UserVerifiedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_user_service.mq.user_events', 'UserVerified', () =>
      this._sendUserVerified(payload),
    );
  }

  private async _sendUserVerified(payload: UserVerifiedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'UserVerified',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_user_service.mq.user_events',
      messages: [{ value }],
    });
  }

  async publishUserStatusChanged(payload: UserStatusChangedPayload): Promise<void> {
    validateUserStatusChangedContract(
      payload.userId,
      payload.oldStatus,
      payload.newStatus    );
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverUserStatusChanged` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverUserStatusChanged(payload));
      return;
    }
    await this._deliverUserStatusChanged(payload);
  }

  private _deliverUserStatusChanged(payload: UserStatusChangedPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_user_service.mq.user_events', 'UserStatusChanged', () =>
      this._sendUserStatusChanged(payload),
    );
  }

  private async _sendUserStatusChanged(payload: UserStatusChangedPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'UserStatusChanged',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_user_service.mq.user_events',
      messages: [{ value }],
    });
  }

  async publishUserLoggedIn(payload: UserLoggedInPayload): Promise<void> {
    // Inside a `transaction(db)` scope the publish is deferred to the event outbox
    // and flushed once after commit; otherwise it publishes immediately. The transport
    // client is only ever called from `_deliverUserLoggedIn` below -- there is
    // no second, ungated call site.
    if (outboxActive()) {
      deferEvent(() => this._deliverUserLoggedIn(payload));
      return;
    }
    await this._deliverUserLoggedIn(payload);
  }

  private _deliverUserLoggedIn(payload: UserLoggedInPayload): Promise<void> {
    return recordPublish(BLOCK_NAME, 'ecommerce_user_service.mq.user_events', 'UserLoggedIn', () =>
      this._sendUserLoggedIn(payload),
    );
  }

  private async _sendUserLoggedIn(payload: UserLoggedInPayload): Promise<void> {
    const producer = await this.connection.getProducer();
    const value = JSON.stringify({
      eventType: 'UserLoggedIn',
      payload,
    });
    await producer.send({
      topic: 'ecommerce_user_service.mq.user_events',
      messages: [{ value }],
    });
  }


  async publishRaw(topic: string, value: string): Promise<void> {
    const producer = await this.connection.getProducer();
    await producer.send({ topic, messages: [{ value }] });
  }
}

export let producerInstance: KafkaEventProducer | null = null;
