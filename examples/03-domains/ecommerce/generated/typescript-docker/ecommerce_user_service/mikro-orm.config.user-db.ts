/**
 * MikroORM CLI config for migrations (userDb).
 *
 * Connection facts resolve through the same getDatabaseConfig() function the
 * running service and migration entrypoint use. No environment variable is
 * read at any point.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { defineConfig } from '@mikro-orm/core';
import { PostgreSqlDriver } from '@mikro-orm/postgresql';
import { Migrator } from '@mikro-orm/migrations';
import { User } from './src/entities/user_db/user.entity';
import { UserPreferences } from './src/entities/user_db/user-preferences.entity';
import { UserSession } from './src/entities/user_db/user-session.entity';
import { getDatabaseConfig } from './src/user-db/database.config';

export default getDatabaseConfig({ get: () => false } as never).then((base) =>
  defineConfig({
    ...base,
    driver: PostgreSqlDriver,
    entities: [User, UserPreferences, UserSession],
    extensions: [Migrator],
    migrations: {
      path: './dist/migrations/user-db',
      pathTs: './src/migrations/user-db',
      emit: 'ts',
    },
    contextName: 'user_db',
  }),
);
