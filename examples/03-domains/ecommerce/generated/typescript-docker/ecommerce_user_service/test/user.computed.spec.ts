/**
 * Unit tests for User computed fields.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { User } from '../src/ecommerce_user_service/entities/user_db/user.entity';
import { UserRole } from '../src/enums/user-role.enum';
import { UserStatus } from '../src/enums/user-status.enum';
import { Address } from '../src/dto/address.struct';
import { CreateUserDto } from '../src/dto/create-user.dto';
import { UpdateUserDto } from '../src/dto/update-user.dto';
import { UserResponseDto } from '../src/dto/user-response.dto';

function assignCreateFieldsForComputedTests(e: User) {
  e.email = `test-${Date.now()}@example.com`;
  e.passwordHash = 'SecureP@ss1';
  e.firstName = 'test-value';
  e.lastName = 'test-value';
  e.phoneNumber = '+15551234567';
  e.role = UserRole.Customer;
  e.status = UserStatus.Active;
  e.lastLoginAt = new Date('2025-01-15T12:00:00Z');
  e.emailVerifiedAt = new Date('2025-01-15T12:00:00Z');
  e.emailVerificationToken = 'test-value';
  e.passwordResetToken = 'test-value';
  e.passwordResetExpiry = new Date('2025-01-15T12:00:00Z');
  e.shippingAddress = { street: 'test-value', city: 'test-value', state: 'test-value', zipCode: 'test-value', country: 'US', phone: '+15551234567' } as Address;
  e.billingAddress = { street: 'test-value', city: 'test-value', state: 'test-value', zipCode: 'test-value', country: 'US', phone: '+15551234567' } as Address;
}

describe('User computed fields', () => {
  it('entity class is defined', () => {
    expect(User).toBeDefined();
  });

  it('has at least 4 computed field getter(s) on prototype', () => {
    const proto = User.prototype;
    const getterNames = Object.getOwnPropertyNames(proto).filter((name) => {
      const desc = Object.getOwnPropertyDescriptor(proto, name);
      return desc !== undefined && typeof desc.get === 'function';
    });
    expect(getterNames.length).toBeGreaterThanOrEqual(4);
  });

  it('has computed property names on prototype', () => {
    const names = ['fullName','isActive','isVerified','canLogin'];
    for (const n of names) {
      const d = Object.getOwnPropertyDescriptor(User.prototype, n);
      expect(d).toBeDefined();
      expect(typeof d?.get).toBe('function');
    }
  });

  describe('fullName', () => {
    it('formats the interpolated string', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.firstName = 'test-value';
      e.lastName = 'test-value';
      expect(e.fullName).toBe("test-value test-value");
    });
    it('with special characters in a interpolated part', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.firstName = 'Book "with quotes" & <tags>';
      expect(typeof e.fullName).toBe('string');
    });

    it('is read-only (assignment throws)', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      const key = "fullName";
      const target = e as unknown as Record<string, unknown>;
      expect(() => {
        target[key] = 1;
      }).toThrow();
    });
  });
  describe('isActive', () => {
    it('is true when status is Active', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.status = UserStatus.Active;
      expect(e.isActive).toBe(true);
    });
    it('is false when status is Inactive', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.status = UserStatus.Inactive;
      expect(e.isActive).toBe(false);
    });
    it('is false when status is Suspended', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.status = UserStatus.Suspended;
      expect(e.isActive).toBe(false);
    });
    it('is false when status is Pending', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      e.status = UserStatus.Pending;
      expect(e.isActive).toBe(false);
    });

    it('is read-only (assignment throws)', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      const key = "isActive";
      const target = e as unknown as Record<string, unknown>;
      expect(() => {
        target[key] = 1;
      }).toThrow();
    });
  });
  describe('isVerified', () => {
    it('getter runs', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      expect(e.isVerified).toBeDefined();
    });

    it('is read-only (assignment throws)', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      const key = "isVerified";
      const target = e as unknown as Record<string, unknown>;
      expect(() => {
        target[key] = 1;
      }).toThrow();
    });
  });
  describe('canLogin', () => {
    it('getter runs', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      expect(e.canLogin).toBeDefined();
    });

    it('is read-only (assignment throws)', () => {
      const e = new User();
      assignCreateFieldsForComputedTests(e);
      const key = "canLogin";
      const target = e as unknown as Record<string, unknown>;
      expect(() => {
        target[key] = 1;
      }).toThrow();
    });
  });

  describe('computed field DTO surface', () => {
    it('UserResponseDto includes field "fullName"', () => {
      const dto = new UserResponseDto();
      const _declaresComputedOnResponse: Pick<
        UserResponseDto,
        'fullName'
      > = dto;
      expect(_declaresComputedOnResponse).toBe(dto);
    });
    it('CreateUserDto omits field "fullName"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new CreateUserDto(),
          'fullName',
        ),
      ).toBe(false);
    });
    it('UpdateUserDto omits field "fullName"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new UpdateUserDto(),
          'fullName',
        ),
      ).toBe(false);
    });
    it('UserResponseDto includes field "isActive"', () => {
      const dto = new UserResponseDto();
      const _declaresComputedOnResponse: Pick<
        UserResponseDto,
        'isActive'
      > = dto;
      expect(_declaresComputedOnResponse).toBe(dto);
    });
    it('CreateUserDto omits field "isActive"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new CreateUserDto(),
          'isActive',
        ),
      ).toBe(false);
    });
    it('UpdateUserDto omits field "isActive"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new UpdateUserDto(),
          'isActive',
        ),
      ).toBe(false);
    });
    it('UserResponseDto includes field "isVerified"', () => {
      const dto = new UserResponseDto();
      const _declaresComputedOnResponse: Pick<
        UserResponseDto,
        'isVerified'
      > = dto;
      expect(_declaresComputedOnResponse).toBe(dto);
    });
    it('CreateUserDto omits field "isVerified"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new CreateUserDto(),
          'isVerified',
        ),
      ).toBe(false);
    });
    it('UpdateUserDto omits field "isVerified"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new UpdateUserDto(),
          'isVerified',
        ),
      ).toBe(false);
    });
    it('UserResponseDto includes field "canLogin"', () => {
      const dto = new UserResponseDto();
      const _declaresComputedOnResponse: Pick<
        UserResponseDto,
        'canLogin'
      > = dto;
      expect(_declaresComputedOnResponse).toBe(dto);
    });
    it('CreateUserDto omits field "canLogin"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new CreateUserDto(),
          'canLogin',
        ),
      ).toBe(false);
    });
    it('UpdateUserDto omits field "canLogin"', () => {
      expect(
        Object.prototype.hasOwnProperty.call(
          new UpdateUserDto(),
          'canLogin',
        ),
      ).toBe(false);
    });
  });
});
