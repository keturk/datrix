/**
 * Repository integration tests for UserPreferences.
 * Tests CRUD operations against a real PostgreSQL database.
 *
 * Requires TEST_DATABASE_URL environment variable.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { TestingModule } from '@nestjs/testing';
import { MikroORM } from '@mikro-orm/core';
import { createIntegrationTestModule, cleanDatabase } from './setup';
import { UserPreferences } from '../../src/entities/user-preferences.entity';
import { buildUserPreferences } from '../factories/user-preferences.factory';
import { User } from '../../src/entities/user.entity';
import { buildUser } from '../factories/user.factory';

describe('UserPreferences Repository', () => {
  let module: TestingModule;
  let orm: MikroORM;
  let userId: string;

  beforeAll(async () => {
    module = await createIntegrationTestModule();
    orm = module.get(MikroORM);
  });

  beforeEach(async () => {
    const em = orm.em.fork();
    const userEntity = em.create(
      User,
      buildUser(),
    );
    await em.persistAndFlush(userEntity);
    userId = userEntity.id;
  });

  afterEach(async () => {
    await cleanDatabase(orm);
  });

  afterAll(async () => {
    await module.close();
  });

  it('creates and persists an entity', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    const entity = em.create(UserPreferences, data);
    await em.persistAndFlush(entity);
    expect(entity.id).toBeDefined();
  });

  it('retrieves an entity by id', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    const saved = em.create(UserPreferences, data);
    await em.persistAndFlush(saved);
    const found = await em.findOne(UserPreferences, { id: saved.id });
    expect(found).not.toBeNull();
    expect(found!.id).toBe(saved.id);
  });

  it('returns null for non-existent id', async () => {
    const em = orm.em.fork();
    const found = await em.findOne(UserPreferences, {
      id: '00000000-0000-0000-0000-000000000099',
    });
    expect(found).toBeNull();
  });

  it('updates an entity field', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    const saved = em.create(UserPreferences, data);
    await em.persistAndFlush(saved);
    saved.language = 'updated-value';
    await em.flush();
    const reloaded = await em.findOne(UserPreferences, { id: saved.id });
    expect(reloaded).not.toBeNull();
    expect(reloaded!.language).toEqual(saved.language);
  });

  it('deletes an entity', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    const saved = em.create(UserPreferences, data);
    await em.persistAndFlush(saved);
    await em.removeAndFlush(saved);
    const found = await em.findOne(UserPreferences, { id: saved.id });
    expect(found).toBeNull();
  });

  it('lists all entities', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    await em.persistAndFlush(em.create(UserPreferences, data));
    const all = await em.find(UserPreferences, {});
    expect(all.length).toBeGreaterThanOrEqual(1);
  });

  it('persists with user foreign key', async () => {
    const em = orm.em.fork();
    const data = buildUserPreferences({ user: userId });
    const saved = em.create(UserPreferences, data);
    await em.persistAndFlush(saved);
    await em.populate(saved, ['user']);
    expect(saved.user.id).toBe(userId);
  });
});
