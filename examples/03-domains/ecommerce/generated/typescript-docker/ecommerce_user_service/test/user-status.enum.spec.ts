/**
 * Unit tests for UserStatus enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { UserStatus } from '../src/enums/user-status.enum';

describe('UserStatus', () => {
  it('is defined as a runtime value', () => {
    expect(UserStatus).toBeDefined();
  });

  it('has 4 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(UserStatus).filter(
      (key) => typeof (UserStatus as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(4);
  });

  it('has member Active with value "active"', () => {
    expect(UserStatus.Active).toBe('active');
  });

  it('has member Inactive with value "inactive"', () => {
    expect(UserStatus.Inactive).toBe('inactive');
  });

  it('has member Suspended with value "suspended"', () => {
    expect(UserStatus.Suspended).toBe('suspended');
  });

  it('has member Pending with value "pending"', () => {
    expect(UserStatus.Pending).toBe('pending');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(UserStatus).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(4);
    expect(stringValues).toContain('active');
    expect(stringValues).toContain('inactive');
    expect(stringValues).toContain('suspended');
    expect(stringValues).toContain('pending');
  });
});
