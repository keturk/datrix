/**
 * Unit tests for UserRole enum.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { UserRole } from '../src/enums/user-role.enum';

describe('UserRole', () => {
  it('is defined as a runtime value', () => {
    expect(UserRole).toBeDefined();
  });

  it('has 3 members', () => {
    // Filters to string-VALUED keys: every member this generator emits is
    // string-backed, and a merged namespace on the same runtime object can
    // attach non-member function properties -- those must never count.
    const memberKeys = Object.keys(UserRole).filter(
      (key) => typeof (UserRole as Record<string, unknown>)[key] === 'string',
    );
    expect(memberKeys).toHaveLength(3);
  });

  it('has member Customer with value "customer"', () => {
    expect(UserRole.Customer).toBe('customer');
  });

  it('has member Admin with value "admin"', () => {
    expect(UserRole.Admin).toBe('admin');
  });

  it('has member Support with value "support"', () => {
    expect(UserRole.Support).toBe('support');
  });

  it('lists each string value under Object.values', () => {
    const stringValues = Object.values(UserRole).filter(
      (v): v is string => typeof v === 'string',
    );
    expect(stringValues).toHaveLength(3);
    expect(stringValues).toContain('customer');
    expect(stringValues).toContain('admin');
    expect(stringValues).toContain('support');
  });
});
