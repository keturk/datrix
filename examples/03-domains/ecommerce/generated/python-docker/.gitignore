# Generated by Datrix — do not edit by hand.
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/
.mypy_cache/
*.egg-info/
.coverage
htmlcov/
build/
dist/
.venv/
node_modules/

# Written into this tree by the deployment tooling after generation; never commit.
# Per-service file-backed secrets the compose stack mounts at /run/secrets,
# regenerated on every build. The development JWT key pair directly under
# secrets/ stays tracked; the per-service directories never are.
secrets/*/
# The compose environment file scripts/deploy.py writes from .env.example; it
# carries the stack's credentials.
.env
