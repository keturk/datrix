"""Test data factory for ShipmentItem.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid
from dataclasses import dataclass, field

from ecommerce_shipping_service.models.shipping_db.shipment_item import ShipmentItem


@dataclass
class ShipmentItemFactory:
    """Factory for creating ShipmentItem test instances."""

    created_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    updated_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    id: uuid.UUID = field(default_factory=uuid.uuid4)
    product_id: uuid.UUID = field(default_factory=uuid.uuid4)
    quantity: int = 1
    shipment_id: uuid.UUID = field(default_factory=uuid.uuid4)

    def build(self) -> ShipmentItem:
        """Build a model instance from factory defaults."""
        return ShipmentItem(
            created_at=self.created_at,
            updated_at=self.updated_at,
            id=self.id,
            product_id=self.product_id,
            quantity=self.quantity,
            shipment_id=self.shipment_id,
        )

    def build_dict(self) -> dict[str, object]:
        """Build a dictionary payload for API tests."""
        return {
            "product_id": str(self.product_id),
            "quantity": self.quantity,
            "shipment_id": str(self.shipment_id),
        }

    @classmethod
    def create(cls, **overrides: object) -> ShipmentItem:
        """Create a model instance with optional overrides."""
        factory = cls(**overrides)
        return factory.build()

    @classmethod
    def create_dict(cls, **overrides: object) -> dict[str, object]:
        """Create a payload dict with optional overrides."""
        factory = cls(**overrides)
        return factory.build_dict()
