"""HTTP integration tests for internal endpoints on ShippingAPI.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest
from httpx import AsyncClient


@pytest.mark.integration
@pytest.mark.asyncio
class TestShippingApiInternalHttp:
    """Internal routes require a valid service JWT (Authorization: Bearer)."""

    async def test_shipping_api_post_endpoint_internal_no_header(
        self, unauth_client: AsyncClient
    ) -> None:
        """POST /api/v1/shipments without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/shipments",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_shipping_api_post_endpoint_internal_wrong_token(
        self, unauth_client: AsyncClient
    ) -> None:
        """POST /api/v1/shipments with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/shipments",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )
