"""PubSub broker configuration.

Auto-generated by datrix-codegen-python. Do not edit manually.
Broker connection settings are sourced from AppSettings (assembled at
lifespan startup) or from the secrets resolver. No environment variables
are read at any point.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class PubsubSettings:
    """Pubsub settings for this broker block.

    This module documents the expected field shapes only. Runtime values are
    assembled into AppSettings from the config store before broker connections
    are created.
    """

    bootstrap_servers: str = ""

    @property
    def broker_list(self) -> list[str]:
        """Parse comma-separated broker string into list."""
        return [b.strip() for b in self.bootstrap_servers.split(",") if b.strip()]
