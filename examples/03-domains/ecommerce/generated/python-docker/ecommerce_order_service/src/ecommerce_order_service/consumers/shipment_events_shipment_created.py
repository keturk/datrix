"""Serverless subscription handler: shipment_events_shipment_created.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def shipment_events_shipment_created(payload: object) -> None:
    """Handle ShipmentCreated event from subscription 'ShipmentEvents'.

    Invoked by the platform adapter when a ShipmentCreated event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "shipment_events_shipment_created",
        "ShipmentCreated",
    )
    shipment_id = _payload_value(payload, "shipment_id")
    order_id = _payload_value(payload, "order_id")
    try:
        logger.info(
            "shipment_created_for_order shipment_id=%s order_id=%s",
            shipment_id,
            order_id,
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "shipment_events_shipment_created",
            "ShipmentCreated",
        )
