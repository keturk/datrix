"""Cache configuration constants.

Auto-generated by datrix-codegen-python. Do not edit manually.
Cache connection parameters are sourced from AppSettings (assembled at lifespan
startup from the config-store ``connections`` namespace). No environment variables
are read at any point.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class CacheSettings:
    """Cache connection settings assembled from AppSettings at lifespan startup.

    Parameters come from AppSettings (config-store ``connections`` namespace).
    No environment variables are read.
    The Redis URL is sourced from ``AppSettings.redis_url``.
    """

    redis_url: str = ""
