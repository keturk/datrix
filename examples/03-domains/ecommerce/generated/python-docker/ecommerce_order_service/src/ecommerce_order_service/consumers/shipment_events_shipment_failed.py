"""Serverless subscription handler: shipment_events_shipment_failed.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def shipment_events_shipment_failed(payload: object) -> None:
    """Handle ShipmentFailed event from subscription 'ShipmentEvents'.

    Invoked by the platform adapter when a ShipmentFailed event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "shipment_events_shipment_failed",
        "ShipmentFailed",
    )
    shipment_id = _payload_value(payload, "shipment_id")
    order_id = _payload_value(payload, "order_id")
    reason = _payload_value(payload, "reason")
    try:
        logger.warning(
            "shipment_failed_for_order shipment_id=%s order_id=%s reason=%s",
            shipment_id,
            order_id,
            reason,
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "shipment_events_shipment_failed",
            "ShipmentFailed",
        )
