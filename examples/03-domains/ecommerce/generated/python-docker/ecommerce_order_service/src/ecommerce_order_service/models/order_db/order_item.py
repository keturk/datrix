"""OrderItem ORM model.
Database: orderDb (postgres)
Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import decimal
import uuid
from typing import TYPE_CHECKING

from sqlalchemy import CheckConstraint, ForeignKey, Integer, Numeric, String
from sqlalchemy.dialects.postgresql import UUID as PGUUID
from sqlalchemy.ext.hybrid import hybrid_property
from sqlalchemy.orm import Mapped, mapped_column, relationship

if TYPE_CHECKING:
    from ecommerce_order_service.models.order_db.order import Order

from ecommerce_order_service.models.order_db.base_entity import BaseEntity


class OrderItem(BaseEntity):
    """OrderItem ORM model."""

    __tablename__ = "order_items"

    product_id: Mapped[uuid.UUID] = mapped_column(
        PGUUID(as_uuid=True),
        index=True,
    )
    product_name: Mapped[str] = mapped_column(
        String(200),
    )
    quantity: Mapped[int] = mapped_column(
        Integer,
    )
    unit_price: Mapped[decimal.Decimal] = mapped_column(
        Numeric(19, 4),
    )

    order_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("orders.id", ondelete="RESTRICT"),
        nullable=False,
        index=True,
    )
    order: Mapped["Order"] = relationship(
        "Order",
        back_populates="items",
    )

    @hybrid_property
    def total(self) -> decimal.Decimal:
        """Computed: (self.unit_price * self.quantity)"""
        return self.unit_price * self.quantity

    __table_args__ = (
        CheckConstraint(
            "length(product_name) <= 200", name="ck_order_items_product_name_max_length"
        ),
    )
