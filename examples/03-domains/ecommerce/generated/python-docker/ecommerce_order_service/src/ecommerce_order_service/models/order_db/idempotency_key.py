"""IdempotencyKey ORM model.
Database: orderDb (postgres)
Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

from pydantic import JsonValue
from sqlalchemy import JSON, CheckConstraint, DateTime, String
from sqlalchemy.dialects.postgresql import UUID as PGUUID
from sqlalchemy.orm import Mapped, mapped_column

from ecommerce_order_service.models.order_db.base_entity import BaseEntity


class IdempotencyKey(BaseEntity):
    """IdempotencyKey ORM model."""

    __tablename__ = "idempotency_keys"

    key: Mapped[str] = mapped_column(
        String(100),
        unique=True,
    )
    operation: Mapped[str] = mapped_column(
        String(50),
        index=True,
    )
    resource_id: Mapped[uuid.UUID | None] = mapped_column(
        PGUUID(as_uuid=True),
        nullable=True,
    )
    response: Mapped[JsonValue | None] = mapped_column(
        JSON,
        nullable=True,
    )
    expires_at: Mapped[datetime.datetime] = mapped_column(
        DateTime(timezone=True),
        index=True,
    )

    __table_args__ = (
        CheckConstraint(
            "length(key) <= 100", name="ck_idempotency_keys_key_max_length"
        ),
        CheckConstraint(
            "length(operation) <= 50", name="ck_idempotency_keys_operation_max_length"
        ),
    )
