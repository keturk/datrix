"""Test data factory for Order.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid
from dataclasses import dataclass, field
from typing import Any

from ecommerce_order_service.enums.order_status import OrderStatus
from ecommerce_order_service.schemas.address import Address
from ecommerce_order_service.models.db.order import Order


@dataclass
class OrderFactory:
    """Factory for creating Order test instances."""

    id: uuid.UUID = field(default_factory=uuid.uuid4)
    created_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    updated_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    customer_id: uuid.UUID = field(default_factory=uuid.uuid4)
    order_number: str = field(default_factory=lambda: f"test_{uuid.uuid4().hex[:8]}")
    status: OrderStatus = OrderStatus.pending
    subtotal: decimal.Decimal = decimal.Decimal("19.99")
    tax: decimal.Decimal = decimal.Decimal("19.99")
    shipping_cost: decimal.Decimal = decimal.Decimal("19.99")
    discount: decimal.Decimal = decimal.Decimal("19.99")
    shipping_address: Address = field(
        default_factory=lambda: {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
    )
    billing_address: Address = field(
        default_factory=lambda: {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
    )
    inventory_reservation_id: uuid.UUID = field(default_factory=uuid.uuid4)
    payment_id: uuid.UUID | None = field(default_factory=uuid.uuid4)
    shipment_id: uuid.UUID | None = field(default_factory=uuid.uuid4)
    cancellation_reason: str | None = "test_value"

    def build(self) -> Order:
        """Build a model instance from factory defaults."""
        return Order(
            id=self.id,
            created_at=self.created_at,
            updated_at=self.updated_at,
            customer_id=self.customer_id,
            order_number=self.order_number,
            status=self.status,
            subtotal=self.subtotal,
            tax=self.tax,
            shipping_cost=self.shipping_cost,
            discount=self.discount,
            shipping_address=self.shipping_address,
            billing_address=self.billing_address,
            inventory_reservation_id=self.inventory_reservation_id,
            payment_id=self.payment_id,
            shipment_id=self.shipment_id,
            cancellation_reason=self.cancellation_reason,
        )

    def build_dict(self) -> dict[str, object]:
        """Build a dictionary payload for API tests."""
        return {
            "customer_id": str(self.customer_id),
            "order_number": self.order_number,
            "status": self.status,
            "subtotal": self.subtotal,
            "tax": self.tax,
            "shipping_cost": self.shipping_cost,
            "discount": self.discount,
            "shipping_address": self.shipping_address,
            "billing_address": self.billing_address,
            "inventory_reservation_id": str(self.inventory_reservation_id),
            "payment_id": str(self.payment_id),
            "shipment_id": str(self.shipment_id),
            "cancellation_reason": self.cancellation_reason,
        }

    @classmethod
    def create(cls, **overrides: Any) -> Order:
        """Create a model instance with optional overrides."""
        factory = cls(**overrides)
        return factory.build()

    @classmethod
    def create_dict(cls, **overrides: Any) -> dict[str, object]:
        """Create a payload dict with optional overrides."""
        factory = cls(**overrides)
        return factory.build_dict()
