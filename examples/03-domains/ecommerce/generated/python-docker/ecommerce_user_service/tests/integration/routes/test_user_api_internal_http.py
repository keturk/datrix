"""HTTP integration tests for internal endpoints on UserAPI.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest
from httpx import AsyncClient


@pytest.mark.integration
@pytest.mark.asyncio
class TestUserApiInternalHttp:
    """Internal routes require a valid service JWT (Authorization: Bearer)."""

    async def test_user_api_get_service_by_id_internal_no_header(
        self, unauth_client: AsyncClient
    ) -> None:
        """GET /api/v1/service/00000000-0000-0000-0000-000000000001 without Authorization returns 401."""
        response = await unauth_client.get(
            "/api/v1/service/00000000-0000-0000-0000-000000000001",
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_user_api_get_service_by_id_internal_wrong_token(
        self, unauth_client: AsyncClient
    ) -> None:
        """GET /api/v1/service/00000000-0000-0000-0000-000000000001 with invalid Bearer token returns 401."""
        response = await unauth_client.get(
            "/api/v1/service/00000000-0000-0000-0000-000000000001",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_user_api_post_service_validate_session_internal_no_header(
        self, unauth_client: AsyncClient
    ) -> None:
        """POST /api/v1/service/validate-session without Authorization returns 401."""
        response = await unauth_client.post(
            "/api/v1/service/validate-session",
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )

    async def test_user_api_post_service_validate_session_internal_wrong_token(
        self, unauth_client: AsyncClient
    ) -> None:
        """POST /api/v1/service/validate-session with invalid Bearer token returns 401."""
        response = await unauth_client.post(
            "/api/v1/service/validate-session",
            headers={"Authorization": "Bearer not-a-valid-jwt"},
            json={},
        )
        assert response.status_code == 401, (
            f"Expected 401, got {response.status_code}: {response.text!r}"
        )
