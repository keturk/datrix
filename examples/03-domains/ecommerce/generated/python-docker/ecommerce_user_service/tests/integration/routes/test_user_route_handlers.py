"""Integration tests for User route handlers.

Auto-generated by datrix-codegen-python. Do not edit manually.
Tests call endpoint functions directly as Python functions, bypassing HTTP.
Requires a database connection (classified as integration tests).
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_user_service.auth import UserClaims as _UserClaims
from ecommerce_user_service.enums.user_role import UserRole
from ecommerce_user_service.enums.user_status import UserStatus
from ecommerce_user_service.routes.user_api import (
    create_user,
    delete_user,
    get_user,
    update_user,
)
from ecommerce_user_service.schemas.user_db.user import UserCreate, UserUpdate
from ecommerce_user_service.services._base import EntityNotFoundError

# Stable test-user fixture: profile_id and id are the resolved local user id (UUID),
# subject/sub carry the raw provider sub.  Handlers read current_user.profile_id via
# Auth.userId(); current_user.subject / .sub expose the raw provider value separately.
_TEST_USER: _UserClaims = _UserClaims(
    sub="test-provider-sub",
    roles=[],
    token="test-token",
    plan="",
    provider="test",
    profile_id="00000000-0000-0000-0000-000000000001",
)


@pytest.mark.integration
@pytest.mark.asyncio
class TestUserRouteHandlers:
    """Test User route handler functions."""

    async def test_create_user_with_valid_data(self, db_session: AsyncSession) -> None:
        """create_user with valid schema returns entity."""
        _email = f"test_{uuid.uuid4().hex[:8]}@example.com"
        body = UserCreate(
            email=_email,
            password_hash="Str0ng#Pass42",
            first_name="TestName",
            last_name="hello",
            phone_number="+15551234567",
            role=UserRole.customer,
            status=UserStatus.active,
            last_login_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verified_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verification_token="user@example.com",
            password_reset_token="test_string",
            password_reset_expiry=datetime.datetime(
                2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc
            ),
            shipping_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            billing_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
        )
        result = await create_user(
            body=body, user_db=db_session, current_user=_TEST_USER
        )
        assert result is not None
        assert result.email == _email
        assert result.password_hash == "Str0ng#Pass42"
        assert result.first_name == "TestName"
        assert result.last_name == "hello"
        assert result.phone_number == "+15551234567"
        assert result.role == UserRole.customer
        assert result.status == UserStatus.active
        assert result.last_login_at == datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        assert result.email_verified_at == datetime.datetime(
            2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
        )
        assert result.email_verification_token == "user@example.com"
        assert result.password_reset_token == "test_string"
        assert result.password_reset_expiry == datetime.datetime(
            2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc
        )
        assert result.shipping_address == {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }
        assert result.billing_address == {
            "street": "test",
            "city": "test",
            "state": "test",
            "zip_code": "test",
            "country": "US",
            "phone": "test",
        }

    async def test_get_user_not_found(self, db_session: AsyncSession) -> None:
        """get_user with non-existent ID raises EntityNotFoundError."""
        _nonexistent_id = uuid.uuid4()
        with pytest.raises(EntityNotFoundError):
            await get_user(
                id=_nonexistent_id, user_db=db_session, current_user=_TEST_USER
            )

    async def test_update_user_delegates_to_service(
        self, db_session: AsyncSession
    ) -> None:
        """update_user passes schema to service correctly."""
        create_body = UserCreate(
            email=f"test_{uuid.uuid4().hex[:8]}@example.com",
            password_hash="Str0ng#Pass42",
            first_name="TestName",
            last_name="hello",
            phone_number="+15551234567",
            role=UserRole.customer,
            status=UserStatus.active,
            last_login_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verified_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verification_token="user@example.com",
            password_reset_token="test_string",
            password_reset_expiry=datetime.datetime(
                2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc
            ),
            shipping_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            billing_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
        )
        created = await create_user(
            body=create_body, user_db=db_session, current_user=_TEST_USER
        )
        update_body = UserUpdate(
            email=f"alt_{uuid.uuid4().hex[:8]}@example.com",
        )
        result = await update_user(
            id=created.id, body=update_body, user_db=db_session, current_user=_TEST_USER
        )
        assert result is not None

    async def test_delete_user_delegates_to_service(
        self, db_session: AsyncSession
    ) -> None:
        """delete_user delegates deletion to service."""
        create_body = UserCreate(
            email=f"test_{uuid.uuid4().hex[:8]}@example.com",
            password_hash="Str0ng#Pass42",
            first_name="TestName",
            last_name="hello",
            phone_number="+15551234567",
            role=UserRole.customer,
            status=UserStatus.active,
            last_login_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verified_at=datetime.datetime(
                2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc
            ),
            email_verification_token="user@example.com",
            password_reset_token="test_string",
            password_reset_expiry=datetime.datetime(
                2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc
            ),
            shipping_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
            billing_address={
                "street": "test",
                "city": "test",
                "state": "test",
                "zip_code": "test",
                "country": "US",
                "phone": "test",
            },
        )
        created = await create_user(
            body=create_body, user_db=db_session, current_user=_TEST_USER
        )
        await delete_user(id=created.id, user_db=db_session, current_user=_TEST_USER)
        with pytest.raises(EntityNotFoundError):
            await get_user(id=created.id, user_db=db_session, current_user=_TEST_USER)
