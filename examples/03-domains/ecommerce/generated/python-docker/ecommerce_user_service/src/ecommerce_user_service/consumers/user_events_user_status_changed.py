"""Serverless subscription handler: user_events_user_status_changed.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def user_events_user_status_changed(payload: object) -> None:
    """Handle UserStatusChanged event from subscription 'UserEvents'.

    Invoked by the platform adapter when a UserStatusChanged event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "user_events_user_status_changed",
        "UserStatusChanged",
    )
    user_id = _payload_value(payload, "user_id")
    old_status = _payload_value(payload, "old_status")
    new_status = _payload_value(payload, "new_status")
    try:
        logger.info(
            "user_status_changed user_id=%s old_status=%s new_status=%s",
            user_id,
            old_status,
            new_status,
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "user_events_user_status_changed",
            "UserStatusChanged",
        )
