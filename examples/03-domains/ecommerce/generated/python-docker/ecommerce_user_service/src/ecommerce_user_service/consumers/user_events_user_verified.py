"""Serverless subscription handler: user_events_user_verified.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def user_events_user_verified(payload: object) -> None:
    """Handle UserVerified event from subscription 'UserEvents'.

    Invoked by the platform adapter when a UserVerified event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "user_events_user_verified",
        "UserVerified",
    )
    user_id = _payload_value(payload, "user_id")
    email = _payload_value(payload, "email")
    try:
        logger.info("user_verified user_id=%s email=%s", user_id, email)
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "user_events_user_verified",
            "UserVerified",
        )
