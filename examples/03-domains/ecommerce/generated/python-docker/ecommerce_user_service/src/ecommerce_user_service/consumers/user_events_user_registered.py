"""Serverless subscription handler: user_events_user_registered.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def user_events_user_registered(payload: object) -> None:
    """Handle UserRegistered event from subscription 'UserEvents'.

    Invoked by the platform adapter when a UserRegistered event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "user_events_user_registered",
        "UserRegistered",
    )
    user_id = _payload_value(payload, "user_id")
    email = _payload_value(payload, "email")
    full_name = _payload_value(payload, "full_name")
    try:
        logger.info(
            "user_registered user_id=%s email=%s full_name=%s",
            user_id,
            email,
            full_name,
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "user_events_user_registered",
            "UserRegistered",
        )
