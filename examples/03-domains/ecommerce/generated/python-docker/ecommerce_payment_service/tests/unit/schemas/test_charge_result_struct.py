"""Unit tests for ChargeResult struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_payment_service.schemas.charge_result import ChargeResult


@pytest.mark.unit
class TestChargeResultStruct:
    """Test ChargeResult Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a ChargeResult instance with all fields."""
        instance = ChargeResult(
            success=True,
            transaction_id="world",
            response="sample_text",
            error="test_string",
        )
        assert instance is not None
        assert instance.success == True
        assert instance.transaction_id == "world"
        assert instance.response == "sample_text"
        assert instance.error == "test_string"

    def test_success_required(self) -> None:
        """success is required — omitting it raises ValidationError."""
        kwargs = {
            "transaction_id": "world",
            "response": "sample_text",
            "error": "test_string",
        }
        with pytest.raises(ValidationError):
            ChargeResult(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        instance = ChargeResult(
            success=True,
            transaction_id="world",
            response="sample_text",
            error="test_string",
        )
        data = instance.model_dump()
        restored = ChargeResult.model_validate(data)
        assert restored == instance
