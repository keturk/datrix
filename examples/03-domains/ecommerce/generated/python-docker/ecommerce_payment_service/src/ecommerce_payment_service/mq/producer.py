"""Kafka event producer.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import decimal
import functools
import json
import logging
import time
import uuid

from ecommerce_payment_service.event_outbox import defer_event, outbox_active
from ecommerce_payment_service.mq.connection import KafkaConnection
from ecommerce_payment_service.mq.contracts import (
    _validate_payment_failed_contract,
    _validate_payment_processed_contract,
    _validate_payment_refunded_contract,
)
from ecommerce_payment_service.mq.schemas import (
    PaymentFailedPayload,
    PaymentProcessedPayload,
    PaymentRefundedPayload,
)
from ecommerce_payment_service.observability.pubsub_metrics import (
    pubsub_publish_duration_seconds,
    pubsub_publish_total,
)

logger = logging.getLogger(__name__)


class KafkaEventProducer:
    """Typed Kafka event producer.

    Each event type has a dedicated publish method that serializes
    the payload via its Pydantic schema and sends to the correct topic.
    """

    def __init__(self, connection: KafkaConnection) -> None:
        self._connection = connection

    async def _deliver(self, topic: str, value: bytes, event_name: str) -> None:
        """Send one serialized event to Kafka and record metrics.

        Shared by immediate publishing and the transaction-scoped outbox flush,
        so both paths emit identical logs/metrics. The publish counter carries
        the outcome: a failed send is counted as an error and re-raised.
        """
        start_time = time.perf_counter()
        _metrics_status = "success"
        try:
            await self._send(topic, value, event_name)
        except Exception:
            _metrics_status = "error"
            raise
        finally:
            pubsub_publish_total.labels(
                service="ecommerce.PaymentService",
                block="mq",
                topic=topic,
                event=event_name,
                status=_metrics_status,
            ).inc()
            pubsub_publish_duration_seconds.labels(
                service="ecommerce.PaymentService",
                block="mq",
                topic=topic,
                event=event_name,
            ).observe(time.perf_counter() - start_time)

    async def _send(self, topic: str, value: bytes, event_name: str) -> None:
        producer = await self._connection.get_producer()
        await producer.send_and_wait(topic=topic, value=value)
        logger.debug("event_published event=%s topic=%s", event_name, topic)

    async def publish_payment_processed(
        self,
        payment_id: uuid.UUID,
        order_id: uuid.UUID,
        amount: decimal.Decimal,
    ) -> None:
        """Publish PaymentProcessed event to topic 'ecommerce_payment_service.mq.payment_events' (partitions=3, retention=7d).

        Inside a ``transaction(db)`` scope the send is deferred to the event
        outbox and flushed once after commit; otherwise it sends immediately.

        Args:
            payment_id: Event payload field.
            order_id: Event payload field.
            amount: Event payload field.
        """
        _validate_payment_processed_contract(payment_id, order_id, amount)
        payload = PaymentProcessedPayload(
            payment_id=payment_id,
            order_id=order_id,
            amount=amount,
        )
        value = json.dumps(
            {
                "event_type": "PaymentProcessed",
                "payload": payload.model_dump(mode="json"),
            }
        ).encode("utf-8")
        if outbox_active():
            defer_event(
                functools.partial(
                    self._deliver,
                    "ecommerce_payment_service.mq.payment_events",
                    value,
                    "PaymentProcessed",
                ),
                event_type="PaymentProcessed",
                topic="ecommerce_payment_service.mq.payment_events",
            )
            return
        await self._deliver(
            "ecommerce_payment_service.mq.payment_events", value, "PaymentProcessed"
        )

    async def publish_payment_failed(
        self,
        payment_id: uuid.UUID,
        order_id: uuid.UUID,
        reason: str,
    ) -> None:
        """Publish PaymentFailed event to topic 'ecommerce_payment_service.mq.payment_events' (partitions=3, retention=7d).

        Inside a ``transaction(db)`` scope the send is deferred to the event
        outbox and flushed once after commit; otherwise it sends immediately.

        Args:
            payment_id: Event payload field.
            order_id: Event payload field.
            reason: Event payload field.
        """
        _validate_payment_failed_contract(payment_id, order_id, reason)
        payload = PaymentFailedPayload(
            payment_id=payment_id,
            order_id=order_id,
            reason=reason,
        )
        value = json.dumps(
            {
                "event_type": "PaymentFailed",
                "payload": payload.model_dump(mode="json"),
            }
        ).encode("utf-8")
        if outbox_active():
            defer_event(
                functools.partial(
                    self._deliver,
                    "ecommerce_payment_service.mq.payment_events",
                    value,
                    "PaymentFailed",
                ),
                event_type="PaymentFailed",
                topic="ecommerce_payment_service.mq.payment_events",
            )
            return
        await self._deliver(
            "ecommerce_payment_service.mq.payment_events", value, "PaymentFailed"
        )

    async def publish_payment_refunded(
        self,
        payment_id: uuid.UUID,
        order_id: uuid.UUID,
        amount: decimal.Decimal,
    ) -> None:
        """Publish PaymentRefunded event to topic 'ecommerce_payment_service.mq.payment_events' (partitions=3, retention=7d).

        Inside a ``transaction(db)`` scope the send is deferred to the event
        outbox and flushed once after commit; otherwise it sends immediately.

        Args:
            payment_id: Event payload field.
            order_id: Event payload field.
            amount: Event payload field.
        """
        _validate_payment_refunded_contract(payment_id, order_id, amount)
        payload = PaymentRefundedPayload(
            payment_id=payment_id,
            order_id=order_id,
            amount=amount,
        )
        value = json.dumps(
            {
                "event_type": "PaymentRefunded",
                "payload": payload.model_dump(mode="json"),
            }
        ).encode("utf-8")
        if outbox_active():
            defer_event(
                functools.partial(
                    self._deliver,
                    "ecommerce_payment_service.mq.payment_events",
                    value,
                    "PaymentRefunded",
                ),
                event_type="PaymentRefunded",
                topic="ecommerce_payment_service.mq.payment_events",
            )
            return
        await self._deliver(
            "ecommerce_payment_service.mq.payment_events", value, "PaymentRefunded"
        )

    async def publish_raw(self, topic: str, value: bytes) -> None:
        """Publish raw bytes to a topic (e.g. for DLQ)."""
        producer = await self._connection.get_producer()
        await producer.send_and_wait(topic=topic, value=value)
        logger.debug("raw_published topic=%s", topic)


# Set by app lifespan when pubsub connection is ready. Used by jobs runner for DLQ.
producer_instance: KafkaEventProducer | None = None


async def get_producer() -> KafkaEventProducer:
    """Return a warm, lazily-initialized producer.

    Serverless handlers have no app lifespan to populate ``producer_instance``,
    so they call this accessor instead. The producer is cached at module level
    and reused across warm invocations; the underlying Kafka connection
    connects lazily on first publish.
    """
    global producer_instance
    if producer_instance is None:
        producer_instance = KafkaEventProducer(KafkaConnection())
    return producer_instance
