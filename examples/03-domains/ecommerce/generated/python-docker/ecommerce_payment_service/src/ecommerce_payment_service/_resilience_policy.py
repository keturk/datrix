"""Resolved per-dependency resilience policy metadata for this service.

Auto-generated by datrix-codegen-python from the resolved dependency-policy plan.
Do not edit manually. Every entry is the merged, resolved decision (application
baseline merged with per-dependency overrides); the generator never synthesizes a
policy, an availability/health value, or a per-operation failure action.

Each entry's ``operations`` maps an operation name to its resolved failure
behavior:

* ``on_failure`` -- one of ``raise`` / ``deny`` / ``degrade`` / ``warn``
  (the resolved ``OperationFailureAction``).
"""

from __future__ import annotations

from typing import TypedDict


class OperationPolicy(TypedDict, total=False):
    """One operation's resolved failure behavior."""

    on_failure: str


class DependencyPolicy(TypedDict):
    """One dependency's resolved policy."""

    kind: str
    availability: str
    health: str
    operations: dict[str, OperationPolicy]


DEPENDENCY_POLICIES: dict[str, DependencyPolicy] = {
    "ecommerce.OrderService": {
        "kind": "service",
        "availability": "required",
        "health": "ready",
        "operations": {},
    },
}
