# ecommerce

Generated by [Datrix](https://datrix.dev).

## Quick Start

1. Copy `.env.example` to `.env` and configure values
2. Start services: `./scripts/deploy.sh` (Linux/macOS) or `scripts/deploy.ps1` (Windows).
   These build the shared base image (`python-base/`) that every service is `FROM`
   before running `docker compose up -d --build`. A bare `docker compose up` fails
   here: Compose does not build a `FROM` image, so the base image must be built first.
3. Run unit tests: `python tests/unit_tests.py`

## Services

| Service | Port | Description |
|---------|------|-------------|
| UserService | 8006 | FastAPI service (3 entities) |
| ProductService | 8004 | FastAPI service (3 entities) |
| OrderService | 8002 | FastAPI service (3 entities) |
| PaymentService | 8003 | FastAPI service (2 entities) |
| ShippingService | 8005 | FastAPI service (3 entities) |
| NotificationService | 8001 | FastAPI service (1 entities) |

## Development

```bash
# Start all services locally (builds the shared base image first, then the stack)
./scripts/deploy.sh          # Linux/macOS   (Windows: scripts/deploy.ps1)

# Stop all services
docker compose down

# Run unit tests
python tests/unit_tests.py

# Run with coverage
python tests/unit_tests.py --coverage
```

## Architecture

```mermaid
graph TD
    clientNode[Client]
    clientNode --> gatewayNode[API_Gateway]
    gatewayNode --> user[UserService]
    user --> user_db[(Database)]
    gatewayNode --> product[ProductService]
    product --> product_db[(Database)]
    gatewayNode --> order[OrderService]
    order --> order_db[(Database)]
    gatewayNode --> payment[PaymentService]
    payment --> payment_db[(Database)]
    gatewayNode --> shipping[ShippingService]
    shipping --> shipping_db[(Database)]
    gatewayNode --> notification[NotificationService]
    notification --> notification_db[(Database)]
```

## API Documentation

Each service exposes interactive API docs at:
- **UserService**: http://localhost:8006/docs
- **ProductService**: http://localhost:8004/docs
- **OrderService**: http://localhost:8002/docs
- **PaymentService**: http://localhost:8003/docs
- **ShippingService**: http://localhost:8005/docs
- **NotificationService**: http://localhost:8001/docs

## Environment Variables

See [ENV.md](ENV.md) for the complete list of environment variables.

---

*Generated by datrix-codegen-python*
