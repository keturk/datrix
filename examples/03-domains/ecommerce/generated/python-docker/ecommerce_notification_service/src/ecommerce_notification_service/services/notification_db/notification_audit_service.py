"""NotificationAudit service.
Database: notificationDb (postgres)
Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import logging
import uuid

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_notification_service.models.notification_db.notification_audit import (
    NotificationAudit,
)
from ecommerce_notification_service.schemas.notification_db.notification_audit import (
    NotificationAuditCreate,
    NotificationAuditUpdate,
)
from ecommerce_notification_service.services._base import EntityNotFoundError

logger = logging.getLogger(__name__)


class NotificationAuditService:
    """Business logic for NotificationAudit entity.

    Provides CRUD operations with lifecycle hook integration.
    """

    def __init__(self, notification_db: AsyncSession) -> None:
        self.notification_db = notification_db

    async def create(
        self, data: NotificationAuditCreate, *, _commit: bool = True
    ) -> NotificationAudit:
        """Create a new NotificationAudit.

        ``_commit`` defaults to True so each call is its own unit of work. A
        bulk caller inside a ``transaction(db) { ... }`` scope passes
        ``_commit=False`` so the row flushes into the shared transaction and the
        enclosing scope commits all rows once instead of once per row.
        """
        entity = NotificationAudit(**data.model_dump())
        self.notification_db.add(entity)
        if _commit:
            await self.notification_db.commit()
        else:
            await self.notification_db.flush()
        await self.notification_db.refresh(entity)
        logger.debug(
            "entity_created entity=%s id=%s", "NotificationAudit", str(entity.id)
        )
        return entity

    async def get(self, entity_id: uuid.UUID) -> NotificationAudit:
        """Get NotificationAudit by ID."""
        entity = await self.notification_db.get(NotificationAudit, entity_id)
        if entity is None:
            raise EntityNotFoundError(
                f"NotificationAudit with id '{entity_id}' not found."
            )
        return entity

    async def get_all(
        self,
        skip: int = 0,
        limit: int = 100,
    ) -> list[NotificationAudit]:
        """Get all NotificationAudit entities with pagination."""
        stmt = select(NotificationAudit)
        stmt = stmt.offset(skip).limit(limit)
        result = await self.notification_db.execute(stmt)
        return list(result.scalars().all())

    async def update(
        self,
        entity_id: uuid.UUID,
        data: NotificationAuditUpdate,
        *,
        _commit: bool = True,
    ) -> NotificationAudit:
        """Update an existing NotificationAudit.

        ``_commit=False`` (used by bulk callers inside a ``transaction(db)``
        scope) flushes the change into the shared transaction instead of
        committing per row; the enclosing scope owns the single commit.
        """
        entity = await self.get(entity_id)
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            setattr(entity, field, value)
        if _commit:
            await self.notification_db.commit()
        else:
            await self.notification_db.flush()
        await self.notification_db.refresh(entity)
        logger.debug(
            "entity_updated entity=%s id=%s", "NotificationAudit", str(entity.id)
        )
        return entity

    async def delete(self, entity_id: uuid.UUID, *, _commit: bool = True) -> None:
        """Delete a NotificationAudit.

        ``_commit=False`` (bulk callers inside a ``transaction(db)`` scope)
        flushes the delete into the shared transaction; the enclosing scope
        owns the single commit.
        """
        entity = await self.get(entity_id)
        await self.notification_db.delete(entity)
        if _commit:
            await self.notification_db.commit()
        else:
            await self.notification_db.flush()
        logger.debug(
            "entity_deleted entity=%s id=%s", "NotificationAudit", str(entity_id)
        )

    async def find_by_order_id(
        self, order_id: uuid.UUID, skip: int = 0, limit: int = 100
    ) -> list[NotificationAudit]:
        """Find NotificationAudit entities by order_id."""
        stmt = select(NotificationAudit).where(NotificationAudit.order_id == order_id)
        stmt = stmt.offset(skip).limit(limit)
        result = await self.notification_db.execute(stmt)
        return list(result.scalars().all())
