"""Test data factory for NotificationAudit.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import uuid
from dataclasses import dataclass, field

from ecommerce_notification_service.models.notification_db.notification_audit import (
    NotificationAudit,
)


@dataclass
class NotificationAuditFactory:
    """Factory for creating NotificationAudit test instances."""

    created_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    updated_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    id: uuid.UUID = field(default_factory=uuid.uuid4)
    order_id: uuid.UUID = field(default_factory=uuid.uuid4)
    recipient_email: str = "user@example.com"
    order_number: str = "test_value"

    def build(self) -> NotificationAudit:
        """Build a model instance from factory defaults."""
        return NotificationAudit(
            created_at=self.created_at,
            updated_at=self.updated_at,
            id=self.id,
            order_id=self.order_id,
            recipient_email=self.recipient_email,
            order_number=self.order_number,
        )

    def build_dict(self) -> dict[str, object]:
        """Build a dictionary payload for API tests."""
        return {
            "order_id": str(self.order_id),
            "recipient_email": self.recipient_email,
            "order_number": self.order_number,
        }

    @classmethod
    def create(cls, **overrides: object) -> NotificationAudit:
        """Create a model instance with optional overrides."""
        factory = cls(**overrides)
        return factory.build()

    @classmethod
    def create_dict(cls, **overrides: object) -> dict[str, object]:
        """Create a payload dict with optional overrides."""
        factory = cls(**overrides)
        return factory.build_dict()
