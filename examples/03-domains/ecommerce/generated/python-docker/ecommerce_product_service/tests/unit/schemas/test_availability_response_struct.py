"""Unit tests for AvailabilityResponse struct.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import uuid

import pytest
from pydantic import ValidationError

from ecommerce_product_service.schemas.availability_response import AvailabilityResponse


@pytest.mark.unit
class TestAvailabilityResponseStruct:
    """Test AvailabilityResponse Pydantic model."""

    def test_create_instance(self) -> None:
        """Can create a AvailabilityResponse instance with all fields."""
        _items = [{"product_id": uuid.uuid4(), "available": True, "available_quantity": 1}]
        instance = AvailabilityResponse(
            all_available=True,
            items=_items,
        )
        assert instance is not None
        assert instance.all_available == True
        assert instance.items is not None

    def test_all_available_required(self) -> None:
        """all_available is required — omitting it raises ValidationError."""
        kwargs = {
            "items": [{"product_id": uuid.uuid4(), "available": True, "available_quantity": 1}],
        }
        with pytest.raises(ValidationError):
            AvailabilityResponse(**kwargs)

    def test_items_required(self) -> None:
        """items is required — omitting it raises ValidationError."""
        kwargs = {
            "all_available": True,
        }
        with pytest.raises(ValidationError):
            AvailabilityResponse(**kwargs)

    def test_serialization_round_trip(self) -> None:
        """model_dump -> model_validate produces equivalent instance."""
        _items = [{"product_id": uuid.uuid4(), "available": True, "available_quantity": 1}]
        instance = AvailabilityResponse(
            all_available=True,
            items=_items,
        )
        data = instance.model_dump()
        restored = AvailabilityResponse.model_validate(data)
        assert restored == instance
