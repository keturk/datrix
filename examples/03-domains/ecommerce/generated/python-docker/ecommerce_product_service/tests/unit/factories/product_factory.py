"""Test data factory for Product.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid
from dataclasses import dataclass, field
from typing import Any

from ecommerce_product_service.enums.product_status import ProductStatus
from pydantic import JsonValue
from ecommerce_product_service.models.db.product import Product


@dataclass
class ProductFactory:
    """Factory for creating Product test instances."""

    id: uuid.UUID = field(default_factory=uuid.uuid4)
    created_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    updated_at: datetime.datetime = field(
        default_factory=lambda: datetime.datetime.now(datetime.timezone.utc)
    )
    slug: str = field(default_factory=lambda: f"test-{uuid.uuid4().hex[:8]}")
    price: decimal.Decimal = decimal.Decimal("19.99")
    compare_at_price: decimal.Decimal | None = decimal.Decimal("19.99")
    inventory: int = 1
    name: str = "test_value"
    description: str = "test_value"
    status: ProductStatus = ProductStatus.draft
    product_metadata: JsonValue | None = field(default_factory=lambda: {"key": "value"})
    images: JsonValue = field(default_factory=lambda: {"key": "value"})
    tags: JsonValue = field(default_factory=lambda: {"key": "value"})
    category_id: uuid.UUID = field(default_factory=uuid.uuid4)

    def build(self) -> Product:
        """Build a model instance from factory defaults."""
        return Product(
            id=self.id,
            created_at=self.created_at,
            updated_at=self.updated_at,
            slug=self.slug,
            price=self.price,
            compare_at_price=self.compare_at_price,
            inventory=self.inventory,
            name=self.name,
            description=self.description,
            status=self.status,
            product_metadata=self.product_metadata,
            images=self.images,
            tags=self.tags,
            category_id=self.category_id,
        )

    def build_dict(self) -> dict[str, object]:
        """Build a dictionary payload for API tests."""
        return {
            "slug": self.slug,
            "price": self.price,
            "compare_at_price": self.compare_at_price,
            "inventory": self.inventory,
            "name": self.name,
            "description": self.description,
            "status": self.status,
            "product_metadata": self.product_metadata,
            "images": self.images,
            "tags": self.tags,
            "category_id": str(self.category_id),
        }

    @classmethod
    def create(cls, **overrides: Any) -> Product:
        """Create a model instance with optional overrides."""
        factory = cls(**overrides)
        return factory.build()

    @classmethod
    def create_dict(cls, **overrides: Any) -> dict[str, object]:
        """Create a payload dict with optional overrides."""
        factory = cls(**overrides)
        return factory.build_dict()
