"""Unit tests for ProductReview NoSQL collection.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest

from ecommerce_product_service.docdb.models.product_review import (
    ProductReviewDocument,
)


@pytest.mark.unit
class TestProductReviewCollection:
    """Test ProductReview collection schema."""

    def test_model_has_expected_fields(self) -> None:
        """Collection model defines expected field names."""
        expected = [
            "body",
            "created_at",
            "media",
            "product_id",
            "rating",
            "title",
            "user_id",
            "verified",
        ]
        for name in expected:
            assert name in ProductReviewDocument.model_fields, (
                f"field '{name}' not in model_fields: {list(ProductReviewDocument.model_fields)}"
            )

    def test_model_has_indexes(self) -> None:
        """Collection has 2 index definition(s)."""
        indexes = ["idx_product_id_created_at", "idx_user_id_created_at"]
        assert len(indexes) == 2
