"""Unit tests for Product schemas.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import decimal
import uuid

import pytest
from pydantic import ValidationError

from ecommerce_product_service.schemas.db.product import (
    ProductCreate,
    ProductResponse,
    ProductUpdate,
)
from ecommerce_product_service.enums.product_status import ProductStatus


@pytest.mark.unit
class TestProductCreate:
    """Test ProductCreate schema validation."""

    def test_valid_data(self) -> None:
        """Create schema accepts valid data."""
        _slug = f"test-{uuid.uuid4().hex[:8]}"
        _category_id = uuid.uuid4()
        data = {
            "slug": _slug,
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": _category_id,
        }
        schema = ProductCreate(**data)
        assert schema.slug == _slug
        assert schema.price == decimal.Decimal("19.99")
        assert schema.compare_at_price == decimal.Decimal("9.99")
        assert schema.inventory == 256
        assert schema.name == "sample_text"
        assert schema.description == "The quick brown fox jumps over the lazy dog"
        assert schema.status == ProductStatus.draft
        assert schema.product_metadata == {"enabled": True}
        assert schema.images == {"items": [1, 2, 3]}
        assert schema.tags == {"key": "value"}
        assert schema.category_id == _category_id

    def test_slug_required(self) -> None:
        """slug is required for Create schema."""
        data = {
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_price_required(self) -> None:
        """price is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_name_required(self) -> None:
        """name is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_description_required(self) -> None:
        """description is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_images_required(self) -> None:
        """images is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_tags_required(self) -> None:
        """tags is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "category_id": uuid.uuid4(),
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_category_id_required(self) -> None:
        """category_id is required for Create schema."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
        }
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_name_rejects_over_max_length(self) -> None:
        """ProductCreate rejects name exceeding 200 chars."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["name"] = "x" * (200 + 1)
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_name_accepts_at_max_length(self) -> None:
        """ProductCreate accepts name at exactly 200 chars."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["name"] = "x" * 200
        schema = ProductCreate(**data)
        assert len(schema.name) == 200

    def test_name_trim_strips_whitespace(self) -> None:
        """name with leading/trailing whitespace is stripped."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["name"] = "  test value  "
        schema = ProductCreate(**data)
        assert schema.name == "test value"

    def test_id_excluded_from_create(self) -> None:
        """Server-managed field id is not in ProductCreate."""
        assert "id" not in ProductCreate.model_fields

    def test_id_excluded_from_update(self) -> None:
        """Server-managed field id is not in ProductUpdate."""
        assert "id" not in ProductUpdate.model_fields

    def test_id_in_response(self) -> None:
        """Server-managed field id is in ProductResponse."""
        assert "id" in ProductResponse.model_fields

    def test_created_at_excluded_from_create(self) -> None:
        """Server-managed field created_at is not in ProductCreate."""
        assert "created_at" not in ProductCreate.model_fields

    def test_created_at_excluded_from_update(self) -> None:
        """Server-managed field created_at is not in ProductUpdate."""
        assert "created_at" not in ProductUpdate.model_fields

    def test_created_at_in_response(self) -> None:
        """Server-managed field created_at is in ProductResponse."""
        assert "created_at" in ProductResponse.model_fields

    def test_updated_at_excluded_from_create(self) -> None:
        """Server-managed field updated_at is not in ProductCreate."""
        assert "updated_at" not in ProductCreate.model_fields

    def test_updated_at_excluded_from_update(self) -> None:
        """Server-managed field updated_at is not in ProductUpdate."""
        assert "updated_at" not in ProductUpdate.model_fields

    def test_updated_at_in_response(self) -> None:
        """Server-managed field updated_at is in ProductResponse."""
        assert "updated_at" in ProductResponse.model_fields

    def test_inventory_default_applied(self) -> None:
        """inventory defaults to 0 when omitted."""
        required_data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        schema = ProductCreate(**required_data)
        assert schema.inventory == 0

    def test_status_default_applied(self) -> None:
        """status defaults to ProductStatus.draft when omitted."""
        required_data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        schema = ProductCreate(**required_data)
        assert schema.status == ProductStatus.draft

    def test_inventory_rejects_below_min(self) -> None:
        """inventory below 0 is rejected."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["inventory"] = 0 - 1
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_price_rejects_zero(self) -> None:
        """price must be positive (> 0)."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["price"] = 0
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_price_rejects_negative(self) -> None:
        """price rejects negative values."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["price"] = -1
        with pytest.raises(ValidationError):
            ProductCreate(**data)


@pytest.mark.unit
class TestProductStatusEnumValidation:
    """Test enum validation for status field."""

    def test_status_accepts_draft(self) -> None:
        """status accepts ProductStatus.draft."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = ProductStatus.draft
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.draft

    def test_status_accepts_active(self) -> None:
        """status accepts ProductStatus.active."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = ProductStatus.active
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.active

    def test_status_accepts_discontinued(self) -> None:
        """status accepts ProductStatus.discontinued."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = ProductStatus.discontinued
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.discontinued

    def test_status_coerces_string_draft(self) -> None:
        """status coerces string "draft" to ProductStatus.draft."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = "draft"
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.draft

    def test_status_coerces_string_active(self) -> None:
        """status coerces string "active" to ProductStatus.active."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = "active"
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.active

    def test_status_coerces_string_discontinued(self) -> None:
        """status coerces string "discontinued" to ProductStatus.discontinued."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = "discontinued"
        schema = ProductCreate(**data)
        assert schema.status == ProductStatus.discontinued

    def test_status_rejects_invalid_string(self) -> None:
        """status rejects non-member string value."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = "NotAValidMember"
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_status_rejects_invalid_type(self) -> None:
        """status rejects non-string non-enum value."""
        data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "compare_at_price": decimal.Decimal("9.99"),
            "inventory": 256,
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "status": ProductStatus.draft,
            "product_metadata": {"enabled": True},
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        data["status"] = 42
        with pytest.raises(ValidationError):
            ProductCreate(**data)

    def test_status_default_applied_when_omitted(self) -> None:
        """status defaults to ProductStatus.draft when omitted."""
        required_data = {
            "slug": f"test-{uuid.uuid4().hex[:8]}",
            "price": decimal.Decimal("19.99"),
            "name": "sample_text",
            "description": "The quick brown fox jumps over the lazy dog",
            "images": {"items": [1, 2, 3]},
            "tags": {"key": "value"},
            "category_id": uuid.uuid4(),
        }
        schema = ProductCreate(**required_data)
        assert schema.status == ProductStatus.draft

    def test_status_in_update_is_optional(self) -> None:
        """status in Update schema is optional (None by default)."""
        schema = ProductUpdate()
        assert schema.status is None


@pytest.mark.unit
class TestProductUpdate:
    """Test ProductUpdate schema validation."""

    def test_all_fields_optional(self) -> None:
        """Update schema allows empty payload."""
        schema = ProductUpdate()
        assert schema.slug is None
        assert schema.price is None
        assert schema.compare_at_price is None
        assert schema.inventory is None
        assert schema.name is None
        assert schema.description is None
        assert schema.status is None
        assert schema.product_metadata is None
        assert schema.images is None
        assert schema.tags is None
        assert schema.category_id is None

    def test_partial_update(self) -> None:
        """Update schema accepts partial data."""
        _slug = f"alt-{uuid.uuid4().hex[:8]}"
        schema = ProductUpdate(**{"slug": _slug})
        assert schema.slug == _slug


@pytest.mark.unit
class TestProductResponse:
    """Test ProductResponse schema settings."""

    def test_from_attributes(self) -> None:
        """Response schema has ORM mode enabled."""
        config = ProductResponse.model_config
        assert config.get("from_attributes") is True

    def test_response_serializes_enum_fields(self) -> None:
        """Response schema correctly serializes enum fields from ORM model."""
        from ecommerce_product_service.models.db.product import Product

        _slug = f"test-{uuid.uuid4().hex[:8]}"
        _category_id = uuid.uuid4()

        instance = Product(
            slug=_slug,
            price=decimal.Decimal("19.99"),
            name="sample_text",
            description="The quick brown fox jumps over the lazy dog",
            images={"items": [1, 2, 3]},
            tags={"key": "value"},
            category_id=_category_id,
            compare_at_price=decimal.Decimal("9.99"),
            inventory=256,
            status=ProductStatus.draft,
            product_metadata={"enabled": True},
            id=uuid.UUID("7c9e6679-7425-40de-944b-e07fc1f90ae7"),
            created_at=datetime.datetime(2025, 3, 10, 16, 45, 0, tzinfo=datetime.timezone.utc),
            updated_at=datetime.datetime(2025, 1, 15, 12, 0, 0, tzinfo=datetime.timezone.utc),
        )
        instance.status = ProductStatus.draft
        response = ProductResponse.model_validate(instance, from_attributes=True)
        assert response.status == ProductStatus.draft
