"""Unit tests for ProductStatus enum.

Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import pytest

from ecommerce_product_service.enums.product_status import ProductStatus


@pytest.mark.unit
class TestProductStatusEnum:
    """Test ProductStatus enum definition and members."""

    def test_is_defined(self) -> None:
        """ProductStatus enum class exists and is importable."""
        assert ProductStatus is not None

    def test_is_str_backed(self) -> None:
        """ProductStatus members are str instances."""
        for member in ProductStatus:
            assert isinstance(member.value, str), (
                f"Expected str value for {member.name}, got {type(member.value).__name__}"
            )

    def test_has_3_members(self) -> None:
        """ProductStatus has exactly 3 members."""
        assert len(ProductStatus) == 3

    def test_member_draft(self) -> None:
        """ProductStatus.draft has value "Draft"."""
        assert ProductStatus.draft.value == "Draft"

    def test_member_active(self) -> None:
        """ProductStatus.active has value "Active"."""
        assert ProductStatus.active.value == "Active"

    def test_member_discontinued(self) -> None:
        """ProductStatus.discontinued has value "Discontinued"."""
        assert ProductStatus.discontinued.value == "Discontinued"

    def test_list_values(self) -> None:
        """All 3 members are iterable via list()."""
        members = list(ProductStatus)
        assert len(members) == 3
        assert ProductStatus.draft in members
        assert ProductStatus.active in members
        assert ProductStatus.discontinued in members
