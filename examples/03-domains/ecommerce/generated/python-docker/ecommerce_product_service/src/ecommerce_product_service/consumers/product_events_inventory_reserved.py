"""Serverless subscription handler: product_events_inventory_reserved.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def product_events_inventory_reserved(payload: object) -> None:
    """Handle InventoryReserved event from subscription 'ProductEvents'.

    Invoked by the platform adapter when a InventoryReserved event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "product_events_inventory_reserved",
        "InventoryReserved",
    )
    reservation_id = _payload_value(payload, "reservation_id")
    product_ids = _payload_value(payload, "product_ids")
    try:
        logger.info(
            "inventory_reserved reservation_id=%s product_ids=%s",
            reservation_id,
            product_ids,
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "product_events_inventory_reserved",
            "InventoryReserved",
        )
