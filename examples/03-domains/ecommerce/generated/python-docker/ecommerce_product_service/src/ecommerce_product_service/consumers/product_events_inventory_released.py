"""Serverless subscription handler: product_events_inventory_released.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def product_events_inventory_released(payload: object) -> None:
    """Handle InventoryReleased event from subscription 'ProductEvents'.

    Invoked by the platform adapter when a InventoryReleased event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "product_events_inventory_released",
        "InventoryReleased",
    )
    reservation_id = _payload_value(payload, "reservation_id")
    reason = _payload_value(payload, "reason")
    try:
        logger.info(
            "inventory_released reservation_id=%s reason=%s", reservation_id, reason
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "product_events_inventory_released",
            "InventoryReleased",
        )
