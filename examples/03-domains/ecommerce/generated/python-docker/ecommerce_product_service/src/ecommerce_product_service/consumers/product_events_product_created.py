"""Serverless subscription handler: product_events_product_created.

Auto-generated by datrix-codegen-python. Do not edit manually.
Platform-agnostic handler -- adapters wire this into Lambda, Azure Functions, etc.
"""

from __future__ import annotations

import logging

logger = logging.getLogger(__name__)


def _payload_value(payload: object, name: str) -> object:
    if isinstance(payload, dict):
        return payload.get(name)
    return getattr(payload, name)


async def product_events_product_created(payload: object) -> None:
    """Handle ProductCreated event from subscription 'ProductEvents'.

    Invoked by the platform adapter when a ProductCreated event
    is received. Business logic is transpiled from the DSL handler body.
    """
    logger.debug(
        "subscription_handler_start handler=%s event=%s",
        "product_events_product_created",
        "ProductCreated",
    )
    product_id = _payload_value(payload, "product_id")
    name = _payload_value(payload, "name")
    price = _payload_value(payload, "price")
    try:
        logger.info(
            "product_created product_id=%s name=%s price=%s", product_id, name, price
        )
    finally:
        logger.debug(
            "subscription_handler_complete handler=%s event=%s",
            "product_events_product_created",
            "ProductCreated",
        )
