"""InventoryReservation service.
Database: productDb (postgres)
Auto-generated by datrix-codegen-python. Do not edit manually.
"""

from __future__ import annotations

import datetime
import logging
import uuid

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from ecommerce_product_service.enums.reservation_status import ReservationStatus
from ecommerce_product_service.models.product_db.inventory_reservation import (
    InventoryReservation,
)
from ecommerce_product_service.schemas.product_db.inventory_reservation import (
    InventoryReservationCreate,
    InventoryReservationUpdate,
)
from ecommerce_product_service.services._base import EntityNotFoundError
from ecommerce_product_service.services.product_db._validators import (
    validate_inventory_reservation,
)

logger = logging.getLogger(__name__)


class InventoryReservationService:
    """Business logic for InventoryReservation entity.

    Provides CRUD operations with lifecycle hook integration.
    """

    def __init__(self, product_db: AsyncSession) -> None:
        self.product_db = product_db

    async def create(
        self, data: InventoryReservationCreate, *, _commit: bool = True
    ) -> InventoryReservation:
        """Create a new InventoryReservation.

        ``_commit`` defaults to True so each call is its own unit of work. A
        bulk caller inside a ``transaction(db) { ... }`` scope passes
        ``_commit=False`` so the row flushes into the shared transaction and the
        enclosing scope commits all rows once instead of once per row.
        """
        entity = InventoryReservation(**data.model_dump())
        if entity.created_at is None:
            entity.created_at = datetime.datetime.now(datetime.timezone.utc)
        if entity.updated_at is None:
            entity.updated_at = datetime.datetime.now(datetime.timezone.utc)
        self.product_db.add(entity)
        validate_inventory_reservation(entity)
        if _commit:
            await self.product_db.commit()
        else:
            await self.product_db.flush()
        await self.product_db.refresh(entity)
        logger.debug(
            "entity_created entity=%s id=%s", "InventoryReservation", str(entity.id)
        )
        return entity

    async def get(self, entity_id: uuid.UUID) -> InventoryReservation:
        """Get InventoryReservation by ID."""
        entity = await self.product_db.get(InventoryReservation, entity_id)
        if entity is None:
            raise EntityNotFoundError(
                f"InventoryReservation with id '{entity_id}' not found."
            )
        return entity

    async def get_all(
        self,
        skip: int = 0,
        limit: int = 100,
    ) -> list[InventoryReservation]:
        """Get all InventoryReservation entities with pagination."""
        stmt = select(InventoryReservation)
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())

    async def update(
        self,
        entity_id: uuid.UUID,
        data: InventoryReservationUpdate,
        *,
        _commit: bool = True,
    ) -> InventoryReservation:
        """Update an existing InventoryReservation.

        ``_commit=False`` (used by bulk callers inside a ``transaction(db)``
        scope) flushes the change into the shared transaction instead of
        committing per row; the enclosing scope owns the single commit.
        """
        entity = await self.get(entity_id)
        update_data = data.model_dump(exclude_unset=True)
        for field, value in update_data.items():
            setattr(entity, field, value)
        validate_inventory_reservation(entity)
        if _commit:
            await self.product_db.commit()
        else:
            await self.product_db.flush()
        await self.product_db.refresh(entity)
        logger.debug(
            "entity_updated entity=%s id=%s", "InventoryReservation", str(entity.id)
        )
        return entity

    async def delete(self, entity_id: uuid.UUID, *, _commit: bool = True) -> None:
        """Delete a InventoryReservation.

        ``_commit=False`` (bulk callers inside a ``transaction(db)`` scope)
        flushes the delete into the shared transaction; the enclosing scope
        owns the single commit.
        """
        entity = await self.get(entity_id)
        await self.product_db.delete(entity)
        if _commit:
            await self.product_db.commit()
        else:
            await self.product_db.flush()
        logger.debug(
            "entity_deleted entity=%s id=%s", "InventoryReservation", str(entity_id)
        )

    async def find_by_reservation_id(
        self, reservation_id: uuid.UUID, skip: int = 0, limit: int = 100
    ) -> list[InventoryReservation]:
        """Find InventoryReservation entities by reservation_id."""
        stmt = select(InventoryReservation).where(
            InventoryReservation.reservation_id == reservation_id
        )
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())

    async def find_by_expires_at(
        self, expires_at: datetime.datetime, skip: int = 0, limit: int = 100
    ) -> list[InventoryReservation]:
        """Find InventoryReservation entities by expires_at."""
        stmt = select(InventoryReservation).where(
            InventoryReservation.expires_at == expires_at
        )
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())

    async def find_by_product_id(
        self, product_id: uuid.UUID, skip: int = 0, limit: int = 100
    ) -> list[InventoryReservation]:
        """Find InventoryReservation entities by product_id."""
        stmt = select(InventoryReservation).where(
            InventoryReservation.product_id == product_id
        )
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())

    async def find_by_reservation_id_and_status(
        self,
        reservation_id: uuid.UUID,
        status: ReservationStatus,
        skip: int = 0,
        limit: int = 100,
    ) -> list[InventoryReservation]:
        """Find InventoryReservation entities by (reservation_id, status)."""
        stmt = select(InventoryReservation).where(
            InventoryReservation.reservation_id == reservation_id,
            InventoryReservation.status == status,
        )
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())

    async def get_by_product(
        self,
        product_id: uuid.UUID,
        skip: int = 0,
        limit: int = 100,
    ) -> list[InventoryReservation]:
        """Get all InventoryReservation entities belonging to Product with given ID."""
        stmt = select(InventoryReservation).where(
            InventoryReservation.product_id == product_id
        )
        stmt = stmt.offset(skip).limit(limit)
        result = await self.product_db.execute(stmt)
        return list(result.scalars().all())
