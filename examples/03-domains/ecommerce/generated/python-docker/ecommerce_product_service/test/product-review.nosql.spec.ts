/**
 * Unit tests for ProductReview NoSQL collection.
 *
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */

describe('ProductReview Collection', () => {
  it('should define expected field names', () => {
    const expectedFields = ["body", "createdAt", "media", "productId", "rating", "title", "userId", "verified"];
    expect(expectedFields.length).toBeGreaterThan(0);
    for (const name of expectedFields) {
      expect(typeof name).toBe('string');
      expect(name.length).toBeGreaterThan(0);
    }
  });

  it('should have 2 index definition(s)', () => {
    const indexes = ["idx_productId_createdAt", "idx_userId_createdAt"];
    expect(indexes).toHaveLength(2);
  });
});
