/**
 * Deployment test for ecommerce.OrderService.
 * Auto-generated by datrix-codegen-typescript. Do not edit manually.
 */
import { fetchWithRetry, httpRequest, waitForHealth, httpGetJson } from './helpers';

const SERVICE_PORT = 8002;
const BASE_URL = `http://localhost:${SERVICE_PORT}`;
const HEALTH_TIMEOUT_MS = 120_000;

describe('OrderService Deployment', () => {
  it('health check returns 200 with healthy status', async () => {
    const healthy = await waitForHealth(BASE_URL, HEALTH_TIMEOUT_MS);
    expect(healthy).toBe(true);

    const data = await httpGetJson(`${BASE_URL}/health`);
    expect(data).toBeDefined();
    expect(['healthy', 'ok']).toContain(data.status);
  }, HEALTH_TIMEOUT_MS + 10_000);

  it('responds to GET /', async () => {
    const response = await fetchWithRetry(BASE_URL);
    expect(response.status).toBeLessThan(500);
  });

  // --- Endpoint Smoke Tests ---

  it('GET /api/v1/orders/ returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/`, {
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(200);
  });

  it('GET /api/v1/orders/00000000-0000-0000-0000-000000000001 returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001`, {
      headers: { 'Authorization': 'Bearer test-token' },
    });
    expect(response.status).toBe(200);
  });

  it('POST /api/v1/orders/ returns 201', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(201);
  });

  it('PUT /api/v1/orders/00000000-0000-0000-0000-000000000001/cancel returns 200', async () => {
    const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/cancel`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer test-token' },
      body: JSON.stringify({}),
    });
    expect(response.status).toBe(200);
  });

  // --- Authentication / Authorization Tests ---

  describe('OrderApi Auth', () => {
    it('GET /api/v1/orders/ without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/`);
      expect(response.status).toBe(401);
    });

    it('GET /api/v1/orders/00000000-0000-0000-0000-000000000001 without auth returns 401', async () => {
      const response = await fetchWithRetry(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001`);
      expect(response.status).toBe(401);
    });

    it('POST /api/v1/orders/ without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

    it('PUT /api/v1/orders/00000000-0000-0000-0000-000000000001/cancel without auth returns 401', async () => {
      const response = await httpRequest(`${BASE_URL}/api/v1/orders/00000000-0000-0000-0000-000000000001/cancel`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({}),
      });
      expect(response.status).toBe(401);
    });

  });
});
