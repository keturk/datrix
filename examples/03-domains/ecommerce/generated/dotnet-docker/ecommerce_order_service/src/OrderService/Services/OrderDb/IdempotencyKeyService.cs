// IdempotencyKeyService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against IdempotencyKey's own
// persistence set (IdempotencyKeys) on the project's single
// OrderServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using OrderService.Data;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace OrderService.Services.OrderDb;

public sealed class IdempotencyKeyService
{
    private readonly OrderServiceDbContext _context;

    public IdempotencyKeyService(OrderServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::OrderService.Models.OrderDb.IdempotencyKey?> GetByIdAsync(Guid id)
    {
        return await _context.IdempotencyKeys.FindAsync(id);
    }

    public async Task<List<global::OrderService.Models.OrderDb.IdempotencyKey>> GetAllAsync()
    {
        return await _context.IdempotencyKeys.ToListAsync();
    }

    public async Task<global::OrderService.Models.OrderDb.IdempotencyKey> CreateAsync(global::OrderService.Models.OrderDb.IdempotencyKey entity)
    {
        _context.IdempotencyKeys.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::OrderService.Models.OrderDb.IdempotencyKey?> UpdateAsync(Guid id, global::OrderService.Models.OrderDb.IdempotencyKey updated)
    {
        var existing = await _context.IdempotencyKeys.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.IdempotencyKeys.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.IdempotencyKeys.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
