using System;
using System.Net.Http;
using System.Net.Http.Json;
using System.Threading;
using System.Threading.Tasks;
using OrderService.Http;

namespace OrderService.Clients;

/// <summary>
/// Resilient HTTP client for the "ProductService" dependency.
/// Timeout: 10.0s
/// Retry: max 2 attempts, exponential backoff (initial=100.0ms).
/// Retry budget: window=100, ratio=0.1.
/// Registered through <c>IHttpClientFactory</c> + <c>AddResilienceHandler</c>
/// (via <c>AddResilientHttpClient</c>) -- retry, circuit breaker, and
/// timeout are owned entirely by that pipeline and are never re-implemented here.
/// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
/// </summary>
public sealed class ProductServiceClient
{
    /// <summary>
    /// The resilience-pipeline parameters resolved for this dependency from the
    /// service's resilience profile. <c>Program.cs</c> passes this record to
    /// <c>AddResilientHttpClient</c>, so these values are what the Polly retry
    /// and timeout strategies actually run with -- not documentation. Public so
    /// the generated <c>ResilienceConfigTests</c> can assert the numbers this
    /// generator wrote are the numbers the profile declared. Circuit-breaker
    /// parameters stay on <c>ResilientClientOptions.Default</c>: the DSL's
    /// <c>failureThreshold</c> is a failure COUNT and Polly's
    /// <c>FailureRatio</c> is a fraction, so mapping one onto the other would
    /// change what the declaration means.
    /// </summary>
    public static readonly ResilientClientOptions Resilience =
        ResilientClientOptions.Default with
        {
            Timeout = TimeSpan.FromSeconds(10.0),
            MaxRetryAttempts = 2,
        };

    private readonly HttpClient _httpClient;

    public ProductServiceClient(HttpClient httpClient)
    {
        _httpClient = httpClient;
    }

    public async Task<T?> GetAsync<T>(string path, CancellationToken cancellationToken = default)
    {
        using var response = await _httpClient.GetAsync(path, cancellationToken).ConfigureAwait(false);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<T>(cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    public async Task<T?> PostAsync<T>(string path, object body, CancellationToken cancellationToken = default)
    {
        using var response = await _httpClient.PostAsJsonAsync(path, body, cancellationToken).ConfigureAwait(false);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<T>(cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    public async Task<T?> PutAsync<T>(string path, object body, CancellationToken cancellationToken = default)
    {
        using var response = await _httpClient.PutAsJsonAsync(path, body, cancellationToken).ConfigureAwait(false);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<T>(cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    public async Task<T?> PatchAsync<T>(string path, object body, CancellationToken cancellationToken = default)
    {
        using var response = await _httpClient.PatchAsJsonAsync(path, body, cancellationToken).ConfigureAwait(false);
        response.EnsureSuccessStatusCode();
        return await response.Content.ReadFromJsonAsync<T>(cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    public async Task DeleteAsync(string path, CancellationToken cancellationToken = default)
    {
        using var response = await _httpClient.DeleteAsync(path, cancellationToken).ConfigureAwait(false);
        response.EnsureSuccessStatusCode();
    }
}
