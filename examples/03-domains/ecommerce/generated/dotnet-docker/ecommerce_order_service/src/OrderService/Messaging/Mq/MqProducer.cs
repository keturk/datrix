// MqProducer.cs -- typed Kafka publish methods.
// Each message carries an "eventType" header naming the DSL event -- a Kafka
// topic may deliver more than one distinct event type, so the consumer
// dispatches on this header, not the topic name alone.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.

using Confluent.Kafka;
using Microsoft.Extensions.Options;
using Prometheus;
using System.Text;
using System.Text.Json;
using OrderService.Messaging;

namespace OrderService.Messaging.Mq;

public sealed class MqProducer : IDisposable, IMqProducer
{
    private readonly IProducer<Null, string> _producer;
    private readonly EventOutbox _eventOutbox;

    public MqProducer(IOptions<MqConnectionSettings> options, EventOutbox eventOutbox)
    {
        var config = new ProducerConfig { BootstrapServers = options.Value.BootstrapServers };
        _producer = new ProducerBuilder<Null, string>(config).Build();
        _eventOutbox = eventOutbox;
    }

    public async Task PublishOrderCreatedAsync(OrderCreatedPayload payload)
    {
        MqContracts.EnsureOrderCreatedValid(payload);
        Task SendAsync() => MeasuredPublishAsync("ecommerce_order_service.mq.order_events", "OrderCreated", async () =>
        {
            var json = JsonSerializer.Serialize(payload);
            var headers = new Headers { { "eventType", Encoding.UTF8.GetBytes("OrderCreated") } };
            await _producer.ProduceAsync(
                "ecommerce_order_service.mq.order_events", new Message<Null, string> { Value = json, Headers = headers });
        });
        if (_eventOutbox.OutboxActive)
        {
            _eventOutbox.DeferEvent(SendAsync);
            return;
        }
        await SendAsync();
    }

    public async Task PublishOrderConfirmedAsync(OrderConfirmedPayload payload)
    {
        MqContracts.EnsureOrderConfirmedValid(payload);
        Task SendAsync() => MeasuredPublishAsync("ecommerce_order_service.mq.order_events", "OrderConfirmed", async () =>
        {
            var json = JsonSerializer.Serialize(payload);
            var headers = new Headers { { "eventType", Encoding.UTF8.GetBytes("OrderConfirmed") } };
            await _producer.ProduceAsync(
                "ecommerce_order_service.mq.order_events", new Message<Null, string> { Value = json, Headers = headers });
        });
        if (_eventOutbox.OutboxActive)
        {
            _eventOutbox.DeferEvent(SendAsync);
            return;
        }
        await SendAsync();
    }

    public async Task PublishOrderCancelledAsync(OrderCancelledPayload payload)
    {
        MqContracts.EnsureOrderCancelledValid(payload);
        Task SendAsync() => MeasuredPublishAsync("ecommerce_order_service.mq.order_events", "OrderCancelled", async () =>
        {
            var json = JsonSerializer.Serialize(payload);
            var headers = new Headers { { "eventType", Encoding.UTF8.GetBytes("OrderCancelled") } };
            await _producer.ProduceAsync(
                "ecommerce_order_service.mq.order_events", new Message<Null, string> { Value = json, Headers = headers });
        });
        if (_eventOutbox.OutboxActive)
        {
            _eventOutbox.DeferEvent(SendAsync);
            return;
        }
        await SendAsync();
    }

    public async Task PublishOrderStatusChangedAsync(OrderStatusChangedPayload payload)
    {
        MqContracts.EnsureOrderStatusChangedValid(payload);
        Task SendAsync() => MeasuredPublishAsync("ecommerce_order_service.mq.order_events", "OrderStatusChanged", async () =>
        {
            var json = JsonSerializer.Serialize(payload);
            var headers = new Headers { { "eventType", Encoding.UTF8.GetBytes("OrderStatusChanged") } };
            await _producer.ProduceAsync(
                "ecommerce_order_service.mq.order_events", new Message<Null, string> { Value = json, Headers = headers });
        });
        if (_eventOutbox.OutboxActive)
        {
            _eventOutbox.DeferEvent(SendAsync);
            return;
        }
        await SendAsync();
    }

// Application-metric instrumentation shared by every publish method: one
    // timer + one status-labelled counter per send. A failed send is counted
    // as an error and rethrown, never counted as a success.
    private static async Task MeasuredPublishAsync(string topic, string eventName, Func<Task> send)
    {
        var status = "success";
        var timer = MqMetrics.PublishDurationSeconds
            .WithLabels("ecommerce.OrderService", "mq", topic, eventName)
            .NewTimer();
        try
        {
            await send().ConfigureAwait(false);
        }
        catch
        {
            status = "error";
            throw;
        }
        finally
        {
            timer.Dispose();
            MqMetrics.PublishTotal
                .WithLabels("ecommerce.OrderService", "mq", topic, eventName, status)
                .Inc();
        }
    }

    public void Dispose()
    {
        _producer.Flush(TimeSpan.FromSeconds(5));
        _producer.Dispose();
    }
}
