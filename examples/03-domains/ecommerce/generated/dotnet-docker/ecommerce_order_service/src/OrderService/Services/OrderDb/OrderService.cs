// OrderService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against Order's own
// persistence set (Orders) on the project's single
// OrderServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using OrderService.Data;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace OrderService.Services.OrderDb;

public sealed class OrderService
{
    private readonly OrderServiceDbContext _context;

    public OrderService(OrderServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::OrderService.Models.OrderDb.Order?> GetByIdAsync(Guid id)
    {
        return await _context.Orders.FindAsync(id);
    }

    public async Task<List<global::OrderService.Models.OrderDb.Order>> GetAllAsync()
    {
        return await _context.Orders.ToListAsync();
    }

    public async Task<global::OrderService.Models.OrderDb.Order> CreateAsync(global::OrderService.Models.OrderDb.Order entity)
    {
        _context.Orders.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::OrderService.Models.OrderDb.Order?> UpdateAsync(Guid id, global::OrderService.Models.OrderDb.Order updated)
    {
        var existing = await _context.Orders.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.Orders.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.Orders.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
