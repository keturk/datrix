// ProductService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against Product's own
// persistence set (Products) on the project's single
// ProductServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using ProductService.Data;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace ProductService.Services.ProductDb;

public sealed class ProductService
{
    private readonly ProductServiceDbContext _context;

    public ProductService(ProductServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::ProductService.Models.ProductDb.Product?> GetByIdAsync(Guid id)
    {
        return await _context.Products.FindAsync(id);
    }

    public async Task<List<global::ProductService.Models.ProductDb.Product>> GetAllAsync()
    {
        return await _context.Products.ToListAsync();
    }

    public async Task<global::ProductService.Models.ProductDb.Product> CreateAsync(global::ProductService.Models.ProductDb.Product entity)
    {
        _context.Products.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::ProductService.Models.ProductDb.Product?> UpdateAsync(Guid id, global::ProductService.Models.ProductDb.Product updated)
    {
        var existing = await _context.Products.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.Products.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.Products.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
