// DocdbNosqlIndexBootstrap.cs -- MongoDB index bootstrap for the "docdb"
// nosql block. Auto-generated by datrix-codegen-dotnet.
// Do not edit manually.

using System.Threading.Tasks;
using MongoDB.Bson;
using MongoDB.Driver;

namespace ProductService.Nosql.Docdb;

/// <summary>Creates every declared index for each collection in the
/// "docdb" nosql block. Run once at application startup --
/// idempotent (MongoDB no-ops re-creating an identical index), so calling
/// this more than once is safe but wasteful. Never call this from a
/// repository constructor (startup-time, block-scoped concern, not a
/// per-request/per-instantiation one).</summary>
public sealed class DocdbNosqlIndexBootstrap
{
    private readonly DocdbNosqlClient _client;

    public DocdbNosqlIndexBootstrap(DocdbNosqlClient client)
    {
        _client = client;
    }

    public async Task RunAsync()
    {
        await Task.CompletedTask.ConfigureAwait(false);
        var ProductReviewCollection = _client.Database.GetCollection<BsonDocument>("product-review");
        await ProductReviewCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("product_id"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
        await ProductReviewCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("user_id"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
        await ProductReviewCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("product_id").Ascending("created_at"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
        await ProductReviewCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("user_id").Ascending("created_at"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
        var ProductAnalyticsCollection = _client.Database.GetCollection<BsonDocument>("product-analytics");
        await ProductAnalyticsCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("product_id"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
        await ProductAnalyticsCollection.Indexes.CreateOneAsync(
            new CreateIndexModel<BsonDocument>(
                Builders<BsonDocument>.IndexKeys.Ascending("product_id").Ascending("event_type").Ascending("timestamp"),
                new CreateIndexOptions { Unique = false })).ConfigureAwait(false);
    }
}
