// InventoryReservationService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against InventoryReservation's own
// persistence set (InventoryReservations) on the project's single
// ProductServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using ProductService.Data;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace ProductService.Services.ProductDb;

public sealed class InventoryReservationService
{
    private readonly ProductServiceDbContext _context;

    public InventoryReservationService(ProductServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::ProductService.Models.ProductDb.InventoryReservation?> GetByIdAsync(Guid id)
    {
        return await _context.InventoryReservations.FindAsync(id);
    }

    public async Task<List<global::ProductService.Models.ProductDb.InventoryReservation>> GetAllAsync()
    {
        return await _context.InventoryReservations.ToListAsync();
    }

    public async Task<global::ProductService.Models.ProductDb.InventoryReservation> CreateAsync(global::ProductService.Models.ProductDb.InventoryReservation entity)
    {
        _context.InventoryReservations.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::ProductService.Models.ProductDb.InventoryReservation?> UpdateAsync(Guid id, global::ProductService.Models.ProductDb.InventoryReservation updated)
    {
        var existing = await _context.InventoryReservations.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.InventoryReservations.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.InventoryReservations.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
