// MqConsumerService.cs -- in-process Kafka consumer loop.
// Dispatches on the "eventType" message header (a topic may
// carry more than one distinct event type), not the topic name alone.
// Hosting/scaling (worker count, DI scope per message, graceful shutdown
// tuning) is left to the caller; this class is the `BackgroundService` + its
// `AddHostedService<T>()` registration only.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.

using Confluent.Kafka;
using Microsoft.Extensions.Hosting;
using Microsoft.Extensions.Options;
using Prometheus;
using System.Text;
using System.Text.Json;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using ProductService.Data;
using ProductService.Messaging.Mq;
using ProductService.Messaging;
using ProductService.Models.ProductDb;
using System.Text.Json.Nodes;

namespace ProductService.Messaging.Mq;

public sealed class MqConsumerService : BackgroundService
{
    private readonly IConsumer<Null, string> _consumer;
    private readonly IServiceScopeFactory _scopeFactory;

    public MqConsumerService(IOptions<MqConnectionSettings> options, IServiceScopeFactory scopeFactory)
    {
        var config = new ConsumerConfig
        {
            BootstrapServers = options.Value.BootstrapServers,
            GroupId = "mq-consumer",
            AutoOffsetReset = AutoOffsetReset.Earliest,
        };
        _consumer = new ConsumerBuilder<Null, string>(config).Build();
        _scopeFactory = scopeFactory;
    }

    protected override async System.Threading.Tasks.Task ExecuteAsync(CancellationToken stoppingToken)
    {
        _consumer.Subscribe(new[]
        {
            "ecommerce_product_service.mq.product_events",
            "ecommerce_order_service.mq.order_events",
        });

        await System.Threading.Tasks.Task.Yield();
        while (!stoppingToken.IsCancellationRequested)
        {
            ConsumeResult<Null, string>? result;
            try
            {
                result = _consumer.Consume(stoppingToken);
            }
            catch (OperationCanceledException)
            {
                break;
            }
            if (result?.Message is null)
            {
                continue;
            }
            string? eventType = null;
            if (result.Message.Headers is not null
                && result.Message.Headers.TryGetLastBytes("eventType", out var eventTypeBytes))
            {
                eventType = Encoding.UTF8.GetString(eventTypeBytes);
            }
            switch (eventType)
            {
                case "ProductCreated":
                {
                    var payload = JsonSerializer.Deserialize<ProductCreatedPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize ProductCreatedPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "ProductCreated")).NewTimer())
                        {
                            await HandleProductCreatedAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "ProductCreated", consumeStatus).Inc();
                    }
                    break;
                }
                case "InventoryUpdated":
                {
                    var payload = JsonSerializer.Deserialize<InventoryUpdatedPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize InventoryUpdatedPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryUpdated")).NewTimer())
                        {
                            await HandleInventoryUpdatedAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryUpdated", consumeStatus).Inc();
                    }
                    break;
                }
                case "InventoryReserved":
                {
                    var payload = JsonSerializer.Deserialize<InventoryReservedPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize InventoryReservedPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryReserved")).NewTimer())
                        {
                            await HandleInventoryReservedAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryReserved", consumeStatus).Inc();
                    }
                    break;
                }
                case "InventoryReleased":
                {
                    var payload = JsonSerializer.Deserialize<InventoryReleasedPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize InventoryReleasedPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryReleased")).NewTimer())
                        {
                            await HandleInventoryReleasedAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "InventoryReleased", consumeStatus).Inc();
                    }
                    break;
                }
                case "OrderConfirmed":
                {
                    var payload = JsonSerializer.Deserialize<OrderConfirmedPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize OrderConfirmedPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "OrderConfirmed")).NewTimer())
                        {
                            await HandleOrderConfirmedAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "OrderConfirmed", consumeStatus).Inc();
                    }
                    break;
                }
                case "OrderCancelled":
                {
                    var payload = JsonSerializer.Deserialize<OrderCancelledPayload>(result.Message.Value)
                        ?? throw new InvalidOperationException("Failed to deserialize OrderCancelledPayload.");
                    var consumeStatus = "success";
                    try
                    {
                        using ((MqMetrics.ConsumeDurationSeconds.WithLabels("ecommerce.ProductService", "mq", result.Topic, "OrderCancelled")).NewTimer())
                        {
                            await HandleOrderCancelledAsync(payload);
                        }
                    }
                    catch
                    {
                        consumeStatus = "error";
                        throw;
                    }
                    finally
                    {
                        MqMetrics.ConsumeTotal.WithLabels("ecommerce.ProductService", "mq", result.Topic, "OrderCancelled", consumeStatus).Inc();
                    }
                    break;
                }
                default:
                    break;
            }
        }
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleProductCreatedAsync(ProductCreatedPayload payload)
    {
        var productId = payload.ProductId;
        var name = payload.Name;
        var price = payload.Price;
            Console.WriteLine("Product created");
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleInventoryUpdatedAsync(InventoryUpdatedPayload payload)
    {
        var productId = payload.ProductId;
        var oldQuantity = payload.OldQuantity;
        var newQuantity = payload.NewQuantity;
            Console.WriteLine("Inventory updated");
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleInventoryReservedAsync(InventoryReservedPayload payload)
    {
        var reservationId = payload.ReservationId;
        var productIds = payload.ProductIds;
            Console.WriteLine("Inventory reserved");
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleInventoryReleasedAsync(InventoryReleasedPayload payload)
    {
        var reservationId = payload.ReservationId;
        var reason = payload.Reason;
            Console.WriteLine("Inventory released");
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleOrderConfirmedAsync(OrderConfirmedPayload payload)
    {
        var orderId = payload.OrderId;
        var paymentId = payload.PaymentId;
        var reservationId = payload.ReservationId;
        var shippingAddress = payload.ShippingAddress;
        var items = payload.Items;
        var estimatedWeight = payload.EstimatedWeight;
        using var scope = _scopeFactory.CreateScope();
        var db = scope.ServiceProvider.GetRequiredService<ProductServiceDbContext>();
            List<global::ProductService.Models.ProductDb.InventoryReservation> reservations = await db.InventoryReservations.Where(x => x.ReservationId == reservationId && x.Status == global::ProductService.Enums.ReservationStatus.Reserved).ToListAsync();
            foreach (var reservation in reservations) {
                reservation.Status = global::ProductService.Enums.ReservationStatus.Confirmed;
                await db.SaveChangesAsync();
            }
            Console.WriteLine("Inventory reservation confirmed");
    }

    // Extracted (rather than inlined in the switch above) so the generated
    // pubsub behavior test can invoke this handler's real transpiled logic
    // directly via reflection (`internal` is enough -- reflection's Invoke
    // does not enforce compile-time accessibility, no InternalsVisibleTo
    // needed, mirroring generators/cross_cutting/lifecycle_hook_test_generator.py's
    // own established precedent).
    internal async System.Threading.Tasks.Task HandleOrderCancelledAsync(OrderCancelledPayload payload)
    {
        var orderId = payload.OrderId;
        var reason = payload.Reason;
        var reservationId = payload.ReservationId;
        using var scope = _scopeFactory.CreateScope();
        var db = scope.ServiceProvider.GetRequiredService<ProductServiceDbContext>();
            List<global::ProductService.Models.ProductDb.InventoryReservation> reservations = await db.InventoryReservations.Where(x => x.ReservationId == reservationId && x.Status == global::ProductService.Enums.ReservationStatus.Reserved).ToListAsync();
            // 'transaction(productDb)' wraps operations in a database transaction
            foreach (var reservation in reservations) {
                var product = await db.Products.FindAsync(reservation.ProductId);
                if ((product != null)) {
                    product.Inventory = (product.Inventory + reservation.Quantity);
                    await db.SaveChangesAsync();
                }
                reservation.Status = global::ProductService.Enums.ReservationStatus.Released;
                await db.SaveChangesAsync();
            }
await db.SaveChangesAsync();
            await AppServiceLocator.GetRequiredService<IMqProducer>().PublishInventoryReleasedAsync(new InventoryReleasedPayload(ReservationId: reservationId, Reason: $"Order cancelled: {reason}")).ConfigureAwait(false);
            Console.WriteLine("Inventory released for cancelled order");
    }

    // How long shutdown waits for the consumer-group leave handshake.
    private static readonly TimeSpan ConsumerCloseTimeout = TimeSpan.FromSeconds(5);

    public override void Dispose()
    {
        // `IConsumer.Close()` is a blocking librdkafka call with no timeout
        // overload: with no reachable broker it retries the group coordinator
        // forever, wedging host shutdown -- and with it any in-process test
        // host that booted this service. The close and the handle disposal
        // share one background task, so abandoning the wait can never race a
        // close that is still running.
        var close = System.Threading.Tasks.Task.Run(() =>
        {
            _consumer.Close();
            _consumer.Dispose();
        });
        close.Wait(ConsumerCloseTimeout);
        base.Dispose();
    }
}
