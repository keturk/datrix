// UserSessionService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against UserSession's own
// persistence set (UserSessions) on the project's single
// UserServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading.Tasks;
using UserService.Data;

namespace UserService.Services.UserDb;

public sealed class UserSessionService
{
    private readonly UserServiceDbContext _context;

    public UserSessionService(UserServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::UserService.Models.UserDb.UserSession?> GetByIdAsync(Guid id)
    {
        return await _context.UserSessions.FindAsync(id);
    }

    public async Task<List<global::UserService.Models.UserDb.UserSession>> GetAllAsync()
    {
        return await _context.UserSessions.ToListAsync();
    }

    public async Task<global::UserService.Models.UserDb.UserSession> CreateAsync(global::UserService.Models.UserDb.UserSession entity)
    {
        _context.UserSessions.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::UserService.Models.UserDb.UserSession?> UpdateAsync(Guid id, global::UserService.Models.UserDb.UserSession updated)
    {
        var existing = await _context.UserSessions.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.UserSessions.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.UserSessions.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
