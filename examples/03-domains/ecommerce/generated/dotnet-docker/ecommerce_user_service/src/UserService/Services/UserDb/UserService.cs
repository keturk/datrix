// UserService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against User's own
// persistence set (Users) on the project's single
// UserServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using System.Collections.Generic;
using System.Threading.Tasks;
using UserService.Data;

namespace UserService.Services.UserDb;

public sealed class UserService
{
    private readonly UserServiceDbContext _context;

    public UserService(UserServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::UserService.Models.UserDb.User?> GetByIdAsync(Guid id)
    {
        return await _context.Users.FindAsync(id);
    }

    public async Task<List<global::UserService.Models.UserDb.User>> GetAllAsync()
    {
        return await _context.Users.ToListAsync();
    }

    public async Task<global::UserService.Models.UserDb.User> CreateAsync(global::UserService.Models.UserDb.User entity)
    {
        _context.Users.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::UserService.Models.UserDb.User?> UpdateAsync(Guid id, global::UserService.Models.UserDb.User updated)
    {
        var existing = await _context.Users.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.Users.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.Users.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
