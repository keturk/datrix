// UserModelTests.cs -- generated xUnit.v3 POCO invariant tests.
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
using UserService.Enums;
using UserService.Models.UserDb;
using Xunit;

namespace UserService.Tests.ModelTests.UserDb;

public sealed class UserModelTests
{
    [Fact]
    public void Create_WithProvidedValues_SetsExpectedProperties()
    {
        var entity = new global::UserService.Models.UserDb.User
        {
            Email = "user@example.com",
            PasswordHash = "********",
            FirstName = "test_value",
            LastName = "test_value",
            PhoneNumber = "15551234567",
            Role = global::UserService.Enums.UserRole.Customer,
            Status = global::UserService.Enums.UserStatus.Active,
            LastLoginAt = new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero),
            EmailVerifiedAt = new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero),
            EmailVerificationToken = "test_value",
            PasswordResetToken = "test_value",
            PasswordResetExpiry = new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero),
        };

        Assert.Equal("user@example.com", entity.Email);
        Assert.Equal("********", entity.PasswordHash);
        Assert.Equal("test_value", entity.FirstName);
        Assert.Equal("test_value", entity.LastName);
        Assert.Equal("15551234567", entity.PhoneNumber);
        Assert.Equal(global::UserService.Enums.UserRole.Customer, entity.Role);
        Assert.Equal(global::UserService.Enums.UserStatus.Active, entity.Status);
        Assert.Equal(new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero), entity.LastLoginAt);
        Assert.Equal(new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero), entity.EmailVerifiedAt);
        Assert.Equal("test_value", entity.EmailVerificationToken);
        Assert.Equal("test_value", entity.PasswordResetToken);
        Assert.Equal(new DateTimeOffset(2025, 1, 15, 12, 0, 0, TimeSpan.Zero), entity.PasswordResetExpiry);
    }
}
