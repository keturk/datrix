// PaymentService.cs -- generated entity service class (EF Core CRUD).
// Auto-generated by datrix-codegen-dotnet. Do not edit manually.
//
// Additive service layer: real EF Core CRUD against Payment's own
// persistence set (Payments) on the project's single
// PaymentServiceDbContext. NOT yet wired into any controller -- existing
// controller-embedded CRUD (generators/api/_endpoint_crud_body.py) is untouched
// by this domain. Controller delegation (Option B) is a separately scoped,
// deferred future change.
using Microsoft.EntityFrameworkCore;
using PaymentService.Data;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace PaymentService.Services.PaymentDb;

public sealed class PaymentService
{
    private readonly PaymentServiceDbContext _context;

    public PaymentService(PaymentServiceDbContext context)
    {
        _context = context;
    }

    public async Task<global::PaymentService.Models.PaymentDb.Payment?> GetByIdAsync(Guid id)
    {
        return await _context.Payments.FindAsync(id);
    }

    public async Task<List<global::PaymentService.Models.PaymentDb.Payment>> GetAllAsync()
    {
        return await _context.Payments.ToListAsync();
    }

    public async Task<global::PaymentService.Models.PaymentDb.Payment> CreateAsync(global::PaymentService.Models.PaymentDb.Payment entity)
    {
        _context.Payments.Add(entity);
        await _context.SaveChangesAsync();
        return entity;
    }

    public async Task<global::PaymentService.Models.PaymentDb.Payment?> UpdateAsync(Guid id, global::PaymentService.Models.PaymentDb.Payment updated)
    {
        var existing = await _context.Payments.FindAsync(id);
        if (existing is null)
        {
            return null;
        }

        _context.Entry(existing).CurrentValues.SetValues(updated);
        await _context.SaveChangesAsync();
        return existing;
    }

    public async Task<bool> DeleteAsync(Guid id)
    {
        var existing = await _context.Payments.FindAsync(id);
        if (existing is null)
        {
            return false;
        }

        _context.Payments.Remove(existing);
        await _context.SaveChangesAsync();
        return true;
    }
}
