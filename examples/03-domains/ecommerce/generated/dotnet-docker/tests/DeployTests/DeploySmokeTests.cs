// <auto-generated>
// Generated by datrix-codegen-dotnet. Do not edit by hand.
// </auto-generated>
#nullable enable

using System.Net;
using Xunit;

namespace DeployTests;

/// <summary>
/// Deployment smoke suite: HTTP reachability checks against a
/// live compose stack. Each service's own `{ENV_PREFIX}_BASE_URL` env var (set
/// by `run-tests.ps1`'s own `docker compose port` resolution -- the generated
/// `docker-compose.yml` publishes ports ephemerally, never a fixed host port
/// equal to the container port) takes precedence; BASE_URL + the service's
/// configured port is the fallback for callers that already know a fixed,
/// reachable port (e.g. a `docker-compose.override.yml` pinning host ports).
/// </summary>
public class DeploySmokeTests
{
    private static readonly HttpClient Client = new();

    private static string BaseUrlFor(int port, string envPrefix)
    {
        var overrideUrl = Environment.GetEnvironmentVariable($"{envPrefix}_BASE_URL");
        if (!string.IsNullOrEmpty(overrideUrl))
        {
            return overrideUrl;
        }

        var baseUrl = Environment.GetEnvironmentVariable("BASE_URL") ?? "http://localhost";
        return $"{baseUrl.TrimEnd('/')}:{port}";
    }

    [Fact]
    public async Task UserService_HealthEndpoint_ReturnsOk()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8006, "USER_SERVICE");
        var response = await Client.GetAsync($"{baseUrl}/health", cancellationToken);
        Assert.Equal(HttpStatusCode.OK, response.StatusCode);
    }

    [Fact]
    public async Task UserService_UserApi_RepresentativeEndpoint_IsReachable()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8006, "USER_SERVICE");
        var request = new HttpRequestMessage(new HttpMethod("GET"), $"{baseUrl}/api/v1/users");
        var response = await Client.SendAsync(request, cancellationToken);

        Assert.NotEqual(HttpStatusCode.NotFound, response.StatusCode);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx response from GET /api/v1/users, got {(int)response.StatusCode}.");
    }

    [Fact]
    public async Task ProductService_HealthEndpoint_ReturnsOk()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8004, "PRODUCT_SERVICE");
        var response = await Client.GetAsync($"{baseUrl}/health", cancellationToken);
        Assert.Equal(HttpStatusCode.OK, response.StatusCode);
    }

    [Fact]
    public async Task ProductService_ProductApi_RepresentativeEndpoint_IsReachable()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8004, "PRODUCT_SERVICE");
        var request = new HttpRequestMessage(new HttpMethod("GET"), $"{baseUrl}/api/v1/products/products");
        var response = await Client.SendAsync(request, cancellationToken);

        Assert.NotEqual(HttpStatusCode.NotFound, response.StatusCode);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx response from GET /api/v1/products/products, got {(int)response.StatusCode}.");
    }

    [Fact]
    public async Task OrderService_HealthEndpoint_ReturnsOk()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8002, "ORDER_SERVICE");
        var response = await Client.GetAsync($"{baseUrl}/health", cancellationToken);
        Assert.Equal(HttpStatusCode.OK, response.StatusCode);
    }

    [Fact]
    public async Task OrderService_OrderApi_RepresentativeEndpoint_IsReachable()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8002, "ORDER_SERVICE");
        var request = new HttpRequestMessage(new HttpMethod("GET"), $"{baseUrl}/api/v1/orders");
        var response = await Client.SendAsync(request, cancellationToken);

        Assert.NotEqual(HttpStatusCode.NotFound, response.StatusCode);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx response from GET /api/v1/orders, got {(int)response.StatusCode}.");
    }

    [Fact]
    public async Task PaymentService_HealthEndpoint_ReturnsOk()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8003, "PAYMENT_SERVICE");
        var response = await Client.GetAsync($"{baseUrl}/health", cancellationToken);
        Assert.Equal(HttpStatusCode.OK, response.StatusCode);
    }

    [Fact]
    public async Task PaymentService_PaymentApi_RepresentativeEndpoint_IsReachable()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8003, "PAYMENT_SERVICE");
        var request = new HttpRequestMessage(new HttpMethod("GET"), $"{baseUrl}/api/v1/payments/my-payments");
        var response = await Client.SendAsync(request, cancellationToken);

        Assert.NotEqual(HttpStatusCode.NotFound, response.StatusCode);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx response from GET /api/v1/payments/my-payments, got {(int)response.StatusCode}.");
    }

    [Fact]
    public async Task ShippingService_HealthEndpoint_ReturnsOk()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8005, "SHIPPING_SERVICE");
        var response = await Client.GetAsync($"{baseUrl}/health", cancellationToken);
        Assert.Equal(HttpStatusCode.OK, response.StatusCode);
    }

    [Fact]
    public async Task ShippingService_ShippingApi_RepresentativeEndpoint_IsReachable()
    {
        var cancellationToken = TestContext.Current.CancellationToken;
        var baseUrl = BaseUrlFor(8005, "SHIPPING_SERVICE");
        var request = new HttpRequestMessage(new HttpMethod("POST"), $"{baseUrl}/api/v1/shipments");
        var response = await Client.SendAsync(request, cancellationToken);

        Assert.NotEqual(HttpStatusCode.NotFound, response.StatusCode);
        Assert.True(
            (int)response.StatusCode < 500,
            $"Expected a non-5xx response from POST /api/v1/shipments, got {(int)response.StatusCode}.");
    }
}
