package ecommerce_shipping_service.dto.shipping_db;

import ecommerce_shipping_service.enums.ShipmentStatus;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * ShipmentEvent batch-update-request DTO -- carries the row's own {@code id} alongside every field
 * {@link ShipmentEventUpdateRequest} allows (server-managed and immutable fields excluded
 * identically), so a batch-update payload item can identify which row to update without widening
 * what a client may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record ShipmentEventBatchUpdateRequestItem(
    @NotNull UUID id,
    OffsetDateTime timestamp,
    ShipmentStatus status,
    @Size(max = 200) String location,
    String description,
    UUID shipmentId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.ShipmentEvent.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * ShipmentEvent#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("timestamp", timestamp());
    __m.put("status", status());
    __m.put("location", location());
    __m.put("description", description());
    __m.put("shipmentId", shipmentId());
    return __m;
  }
}
