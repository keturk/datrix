package ecommerce_shipping_service.pubsub.mq;

import ecommerce_shipping_service.errors.ContractViolationError;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Event contract validation functions, auto-generated from {@code ensure} clauses in event
 * declarations. Each function validates preconditions before publication.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public final class MqPubsubContracts {

  private static final Logger logger = LoggerFactory.getLogger(MqPubsubContracts.class);

  private MqPubsubContracts() {}

  /** Validate contracts for the ShipmentDispatched event. */
  public static void validateShipmentDispatched(
      UUID shipmentId, UUID orderId, String trackingNumber) {
    if (!((trackingNumber.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("trackingNumber", trackingNumber);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "ShipmentDispatched",
          "ensure_0",
          actual);
      throw new ContractViolationError("ShipmentDispatched", "ensure_0", actual);
    }
  }

  /** Validate contracts for the ShipmentFailed event. */
  public static void validateShipmentFailed(UUID shipmentId, UUID orderId, String reason) {
    if (!((reason.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("reason", reason);
      logger.error(
          "contract_violation event={} clause={} actual={}", "ShipmentFailed", "ensure_0", actual);
      throw new ContractViolationError("ShipmentFailed", "ensure_0", actual);
    }
  }
}
