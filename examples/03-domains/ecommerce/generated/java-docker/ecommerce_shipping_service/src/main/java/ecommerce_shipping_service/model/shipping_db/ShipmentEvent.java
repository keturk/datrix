package ecommerce_shipping_service.model.shipping_db;

import ecommerce_shipping_service.enums.ShipmentStatus;
import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.Index;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import java.time.OffsetDateTime;

// Event log entity for shipment tracking history
/**
 * ShipmentEvent JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "shipment_events",
    indexes = {
      @Index(
          name = "ix_shipment_events_shipment_id_timestamp",
          columnList = "shipment_id, timestamp")
    },
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_shipment_events_location_max_length",
          constraint = "length(location) <= 200")
    })
public class ShipmentEvent extends BaseEntity {

  @Column(name = "timestamp", columnDefinition = "timestamptz", nullable = false)
  private OffsetDateTime timestamp;

  @Column(name = "status", columnDefinition = "varchar(255)", nullable = false)
  private ShipmentStatus status;

  @Column(name = "location", columnDefinition = "varchar(200)", nullable = false)
  private String location;

  @Column(name = "description", columnDefinition = "text", nullable = true)
  private String description;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "shipment_id", nullable = false)
  private Shipment shipment;

  public OffsetDateTime getTimestamp() {
    return this.timestamp;
  }

  public void setTimestamp(OffsetDateTime timestamp) {
    this.timestamp = timestamp;
  }

  public ShipmentStatus getStatus() {
    return this.status;
  }

  public void setStatus(ShipmentStatus status) {
    this.status = status;
  }

  public String getLocation() {
    return this.location;
  }

  public void setLocation(String location) {
    this.location = location;
  }

  public String getDescription() {
    return this.description;
  }

  public void setDescription(String description) {
    this.description = description;
  }

  public Shipment getShipment() {
    return this.shipment;
  }

  public void setShipment(Shipment shipment) {
    this.shipment = shipment;
  }

  /**
   * Build a new {@link ShipmentEvent} from a DSL object-literal map, setting only the keys present
   * in {@code data} (a key absent from {@code data} leaves the corresponding field at its normal
   * Java default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.ShipmentEvent.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new ShipmentEvent(...)`.
   */
  public static ShipmentEvent fromMap(java.util.Map<String, Object> data) {
    ShipmentEvent result = new ShipmentEvent();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link ShipmentEvent}
   * instance (a key absent from {@code data} leaves the corresponding field at its CURRENT value,
   * unlike {@link #fromMap} which starts from a freshly constructed instance). Same key convention
   * and per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.ShipmentEvent.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(ShipmentEvent target, java.util.Map<String, Object> data) {
    if (data.containsKey("timestamp")) {
      Object __v = data.get("timestamp");
      target.setTimestamp(
          __v instanceof OffsetDateTime __n
              ? __n
              : ((java.time.LocalDateTime) __v).atOffset(java.time.ZoneOffset.UTC));
    }
    if (data.containsKey("status")) {
      Object __v = data.get("status");
      target.setStatus((ShipmentStatus) __v);
    }
    if (data.containsKey("location")) {
      Object __v = data.get("location");
      target.setLocation((String) __v);
    }
    if (data.containsKey("description")) {
      Object __v = data.get("description");
      target.setDescription((String) __v);
    }
  }
}
