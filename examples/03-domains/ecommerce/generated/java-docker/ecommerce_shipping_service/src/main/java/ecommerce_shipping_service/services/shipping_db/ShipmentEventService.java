package ecommerce_shipping_service.services.shipping_db;

import ecommerce_shipping_service.model.shipping_db.Shipment;
import ecommerce_shipping_service.model.shipping_db.ShipmentEvent;
import ecommerce_shipping_service.repositories.shipping_db.ShipmentEventRepository;
import ecommerce_shipping_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * ShipmentEvent service -- request-scoped unit-of-work wrapper over {@link ShipmentEventRepository
 * }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class ShipmentEventService extends ServiceBase {

  private final ShipmentEventRepository shipmentEventRepository;

  @PersistenceContext(unitName = "shipping_db")
  private EntityManager entityManager;

  public ShipmentEventService(ShipmentEventRepository shipmentEventRepository) {
    this.shipmentEventRepository = shipmentEventRepository;
  }

  public Optional<ShipmentEvent> get(UUID id) {
    return this.shipmentEventRepository.findById(id);
  }

  public List<ShipmentEvent> list() {
    return this.shipmentEventRepository.findAll();
  }

  public List<ShipmentEvent> getByShipmentId(UUID shipmentId) {
    return this.shipmentEventRepository.findByShipmentId(shipmentId);
  }

  public List<ShipmentEvent> getByShipmentIdAndTimestamp(
      UUID shipmentId, OffsetDateTime timestamp) {
    return this.shipmentEventRepository.findByShipmentIdAndTimestamp(shipmentId, timestamp);
  }

  public Page<ShipmentEvent> listByShipment(UUID shipmentId, Pageable pageable) {
    return this.shipmentEventRepository.findByShipmentId(shipmentId, pageable);
  }

  public Shipment getShipmentReference(UUID id) {
    return this.entityManager.getReference(Shipment.class, id);
  }

  @Transactional
  public ShipmentEvent create(ShipmentEvent entity) {
    return this.shipmentEventRepository.save(entity);
  }

  @Transactional
  public ShipmentEvent update(ShipmentEvent entity) {
    return this.shipmentEventRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.shipmentEventRepository.deleteById(id);
  }
}
