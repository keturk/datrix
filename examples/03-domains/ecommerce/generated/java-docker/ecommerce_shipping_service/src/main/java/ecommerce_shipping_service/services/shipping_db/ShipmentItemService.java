package ecommerce_shipping_service.services.shipping_db;

import ecommerce_shipping_service.model.shipping_db.Shipment;
import ecommerce_shipping_service.model.shipping_db.ShipmentItem;
import ecommerce_shipping_service.repositories.shipping_db.ShipmentItemRepository;
import ecommerce_shipping_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * ShipmentItem service -- request-scoped unit-of-work wrapper over {@link ShipmentItemRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class ShipmentItemService extends ServiceBase {

  private final ShipmentItemRepository shipmentItemRepository;

  @PersistenceContext(unitName = "shipping_db")
  private EntityManager entityManager;

  public ShipmentItemService(ShipmentItemRepository shipmentItemRepository) {
    this.shipmentItemRepository = shipmentItemRepository;
  }

  public Optional<ShipmentItem> get(UUID id) {
    return this.shipmentItemRepository.findById(id);
  }

  public List<ShipmentItem> list() {
    return this.shipmentItemRepository.findAll();
  }

  public List<ShipmentItem> getByShipmentId(UUID shipmentId) {
    return this.shipmentItemRepository.findByShipmentId(shipmentId);
  }

  public Page<ShipmentItem> listByShipment(UUID shipmentId, Pageable pageable) {
    return this.shipmentItemRepository.findByShipmentId(shipmentId, pageable);
  }

  public Shipment getShipmentReference(UUID id) {
    return this.entityManager.getReference(Shipment.class, id);
  }

  @Transactional
  public ShipmentItem create(ShipmentItem entity) {
    return this.shipmentItemRepository.save(entity);
  }

  @Transactional
  public ShipmentItem update(ShipmentItem entity) {
    return this.shipmentItemRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.shipmentItemRepository.deleteById(id);
  }
}
