package ecommerce_payment_service.model.payment_db;

import ecommerce_payment_service.enums.PaymentStatus;
import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import jakarta.persistence.Transient;
import java.math.BigDecimal;
import java.time.OffsetDateTime;
import java.util.Objects;

/**
 * Refund JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "refunds",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(name = "ck_refunds_reason_max_length", constraint = "length(reason) <= 500")
    })
public class Refund extends BaseEntity {

  @Column(name = "amount", columnDefinition = "numeric(19, 4)", nullable = false)
  private BigDecimal amount;

  @Column(name = "reason", columnDefinition = "varchar(500)", nullable = false)
  private String reason;

  @Column(name = "status", columnDefinition = "varchar(255)", nullable = false)
  private PaymentStatus status = PaymentStatus.PENDING;

  @Column(name = "refundTransactionId", columnDefinition = "text", nullable = true)
  private String refundTransactionId;

  @Column(name = "errorMessage", columnDefinition = "text", nullable = true)
  private String errorMessage;

  @Column(name = "processedAt", columnDefinition = "timestamptz", nullable = true)
  private OffsetDateTime processedAt;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "payment_id", nullable = false)
  private Payment payment;

  public BigDecimal getAmount() {
    return this.amount;
  }

  public void setAmount(BigDecimal amount) {
    this.amount = amount;
  }

  public String getReason() {
    return this.reason;
  }

  public void setReason(String reason) {
    this.reason = reason;
  }

  public PaymentStatus getStatus() {
    return this.status;
  }

  public void setStatus(PaymentStatus status) {
    this.status = status;
  }

  public String getRefundTransactionId() {
    return this.refundTransactionId;
  }

  public void setRefundTransactionId(String refundTransactionId) {
    this.refundTransactionId = refundTransactionId;
  }

  public String getErrorMessage() {
    return this.errorMessage;
  }

  public void setErrorMessage(String errorMessage) {
    this.errorMessage = errorMessage;
  }

  public OffsetDateTime getProcessedAt() {
    return this.processedAt;
  }

  public void setProcessedAt(OffsetDateTime processedAt) {
    this.processedAt = processedAt;
  }

  public Payment getPayment() {
    return this.payment;
  }

  public void setPayment(Payment payment) {
    this.payment = payment;
  }

  /** Computed: (Objects.equals(this.getStatus(), PaymentStatus.COMPLETED)) */
  @Transient
  public Boolean getIsSuccessful() {
    return (Objects.equals(this.getStatus(), PaymentStatus.COMPLETED));
  }

  /**
   * Build a new {@link Refund} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.Refund.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new Refund(...)`.
   */
  public static Refund fromMap(java.util.Map<String, Object> data) {
    Refund result = new Refund();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link Refund} instance (a
   * key absent from {@code data} leaves the corresponding field at its CURRENT value, unlike {@link
   * #fromMap} which starts from a freshly constructed instance). Same key convention and per-type
   * coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.Refund.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(Refund target, java.util.Map<String, Object> data) {
    if (data.containsKey("amount")) {
      Object __v = data.get("amount");
      target.setAmount(
          __v instanceof java.math.BigDecimal __n ? __n : new java.math.BigDecimal(__v.toString()));
    }
    if (data.containsKey("reason")) {
      Object __v = data.get("reason");
      target.setReason((String) __v);
    }
    if (data.containsKey("status")) {
      Object __v = data.get("status");
      target.setStatus((PaymentStatus) __v);
    }
    if (data.containsKey("refundTransactionId")) {
      Object __v = data.get("refundTransactionId");
      target.setRefundTransactionId((String) __v);
    }
    if (data.containsKey("errorMessage")) {
      Object __v = data.get("errorMessage");
      target.setErrorMessage((String) __v);
    }
    if (data.containsKey("processedAt")) {
      Object __v = data.get("processedAt");
      target.setProcessedAt(
          __v instanceof OffsetDateTime __n
              ? __n
              : ((java.time.LocalDateTime) __v).atOffset(java.time.ZoneOffset.UTC));
    }
  }
}
