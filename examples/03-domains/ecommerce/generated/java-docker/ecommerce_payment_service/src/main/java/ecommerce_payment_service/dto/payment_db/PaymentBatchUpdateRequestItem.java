package ecommerce_payment_service.dto.payment_db;

import ecommerce_payment_service.enums.PaymentMethod;
import ecommerce_payment_service.enums.PaymentStatus;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.math.BigDecimal;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * Payment batch-update-request DTO -- carries the row's own {@code id} alongside every field {@link
 * PaymentUpdateRequest} allows (server-managed and immutable fields excluded identically), so a
 * batch-update payload item can identify which row to update without widening what a client may
 * change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record PaymentBatchUpdateRequestItem(
    @NotNull UUID id,
    UUID orderId,
    UUID customerId,
    BigDecimal amount,
    PaymentMethod method,
    PaymentStatus status,
    @Size(max = 100) String transactionId,
    String gatewayResponse,
    String errorMessage,
    OffsetDateTime processedAt) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.Payment.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * Payment#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("orderId", orderId());
    __m.put("customerId", customerId());
    __m.put("amount", amount());
    __m.put("method", method());
    __m.put("status", status());
    __m.put("transactionId", transactionId());
    __m.put("gatewayResponse", gatewayResponse());
    __m.put("errorMessage", errorMessage());
    __m.put("processedAt", processedAt());
    return __m;
  }
}
