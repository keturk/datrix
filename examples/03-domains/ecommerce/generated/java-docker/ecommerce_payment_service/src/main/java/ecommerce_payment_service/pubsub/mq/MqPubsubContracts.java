package ecommerce_payment_service.pubsub.mq;

import ecommerce_payment_service.errors.ContractViolationError;
import java.math.BigDecimal;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Event contract validation functions, auto-generated from {@code ensure} clauses in event
 * declarations. Each function validates preconditions before publication.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public final class MqPubsubContracts {

  private static final Logger logger = LoggerFactory.getLogger(MqPubsubContracts.class);

  private MqPubsubContracts() {}

  /** Validate contracts for the PaymentProcessed event. */
  public static void validatePaymentProcessed(UUID paymentId, UUID orderId, BigDecimal amount) {
    if (!((amount.compareTo(BigDecimal.valueOf(0)) > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("amount", amount);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "PaymentProcessed",
          "ensure_0",
          actual);
      throw new ContractViolationError("PaymentProcessed", "ensure_0", actual);
    }
  }

  /** Validate contracts for the PaymentFailed event. */
  public static void validatePaymentFailed(UUID paymentId, UUID orderId, String reason) {
    if (!((reason.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("reason", reason);
      logger.error(
          "contract_violation event={} clause={} actual={}", "PaymentFailed", "ensure_0", actual);
      throw new ContractViolationError("PaymentFailed", "ensure_0", actual);
    }
  }

  /** Validate contracts for the PaymentRefunded event. */
  public static void validatePaymentRefunded(UUID paymentId, UUID orderId, BigDecimal amount) {
    if (!((amount.compareTo(BigDecimal.valueOf(0)) > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("amount", amount);
      logger.error(
          "contract_violation event={} clause={} actual={}", "PaymentRefunded", "ensure_0", actual);
      throw new ContractViolationError("PaymentRefunded", "ensure_0", actual);
    }
  }
}
