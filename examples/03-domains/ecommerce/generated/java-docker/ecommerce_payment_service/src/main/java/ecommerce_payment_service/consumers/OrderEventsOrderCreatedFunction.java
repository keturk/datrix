package ecommerce_payment_service.consumers;

import java.math.BigDecimal;
import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: order_events_order_created (subscription 'OrderEvents', event
 * 'OrderCreated').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersOrderEventsOrderCreated")
public class OrderEventsOrderCreatedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(OrderEventsOrderCreatedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "order_events_order_created",
        "OrderCreated");
    UUID orderId = (UUID) input.get("orderId");
    String orderNumber = (String) input.get("orderNumber");
    UUID customerId = (UUID) input.get("customerId");
    BigDecimal total = (BigDecimal) input.get("total");
    UUID reservationId = (UUID) input.get("reservationId");
    try {
      logger.info("New order created, awaiting payment");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "order_events_order_created",
          "OrderCreated");
    }
  }
}
