package ecommerce_notification_service.model.notification_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.Table;
import java.util.UUID;

/**
 * NotificationAudit JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "notification_audits",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_notification_audits_order_number_max_length",
          constraint = "length(order_number) <= 64")
    })
public class NotificationAudit extends BaseEntity {

  @Column(name = "orderId", columnDefinition = "uuid", nullable = false)
  private UUID orderId;

  @Column(name = "recipientEmail", columnDefinition = "varchar(320)", nullable = false)
  private String recipientEmail;

  @Column(name = "orderNumber", columnDefinition = "varchar(64)", nullable = false)
  private String orderNumber;

  public UUID getOrderId() {
    return this.orderId;
  }

  public void setOrderId(UUID orderId) {
    this.orderId = orderId;
  }

  public String getRecipientEmail() {
    return this.recipientEmail;
  }

  public void setRecipientEmail(String recipientEmail) {
    this.recipientEmail = recipientEmail;
  }

  public String getOrderNumber() {
    return this.orderNumber;
  }

  public void setOrderNumber(String orderNumber) {
    this.orderNumber = orderNumber;
  }

  /**
   * Build a new {@link NotificationAudit} from a DSL object-literal map, setting only the keys
   * present in {@code data} (a key absent from {@code data} leaves the corresponding field at its
   * normal Java default -- {@code null} unless the field declares its own initializer above). Keys
   * are the AS-AUTHORED DSL field name (Java's own object-literal emission, {@code
   * _datrixOrderedMap(...)}, preserves author-declared spelling -- no case conversion), which for a
   * camelCase-authored DSL field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.NotificationAudit.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new NotificationAudit(...)`.
   */
  public static NotificationAudit fromMap(java.util.Map<String, Object> data) {
    NotificationAudit result = new NotificationAudit();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link NotificationAudit}
   * instance (a key absent from {@code data} leaves the corresponding field at its CURRENT value,
   * unlike {@link #fromMap} which starts from a freshly constructed instance). Same key convention
   * and per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.NotificationAudit.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(NotificationAudit target, java.util.Map<String, Object> data) {
    if (data.containsKey("orderId")) {
      Object __v = data.get("orderId");
      target.setOrderId((UUID) __v);
    }
    if (data.containsKey("recipientEmail")) {
      Object __v = data.get("recipientEmail");
      target.setRecipientEmail((String) __v);
    }
    if (data.containsKey("orderNumber")) {
      Object __v = data.get("orderNumber");
      target.setOrderNumber((String) __v);
    }
  }
}
