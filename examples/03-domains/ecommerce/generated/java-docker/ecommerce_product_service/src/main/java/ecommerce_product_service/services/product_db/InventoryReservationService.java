package ecommerce_product_service.services.product_db;

import ecommerce_product_service.enums.ReservationStatus;
import ecommerce_product_service.model.product_db.InventoryReservation;
import ecommerce_product_service.model.product_db.Product;
import ecommerce_product_service.repositories.product_db.InventoryReservationRepository;
import ecommerce_product_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * InventoryReservation service -- request-scoped unit-of-work wrapper over {@link
 * InventoryReservationRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class InventoryReservationService extends ServiceBase {

  private final InventoryReservationRepository inventoryReservationRepository;

  @PersistenceContext(unitName = "product_db")
  private EntityManager entityManager;

  public InventoryReservationService(
      InventoryReservationRepository inventoryReservationRepository) {
    this.inventoryReservationRepository = inventoryReservationRepository;
  }

  public Optional<InventoryReservation> get(UUID id) {
    return this.inventoryReservationRepository.findById(id);
  }

  public List<InventoryReservation> list() {
    return this.inventoryReservationRepository.findAll();
  }

  public List<InventoryReservation> getByReservationId(UUID reservationId) {
    return this.inventoryReservationRepository.findByReservationId(reservationId);
  }

  public List<InventoryReservation> getByExpiresAt(OffsetDateTime expiresAt) {
    return this.inventoryReservationRepository.findByExpiresAt(expiresAt);
  }

  public List<InventoryReservation> getByProductId(UUID productId) {
    return this.inventoryReservationRepository.findByProductId(productId);
  }

  public List<InventoryReservation> getByReservationIdAndStatus(
      UUID reservationId, ReservationStatus status) {
    return this.inventoryReservationRepository.findByReservationIdAndStatus(reservationId, status);
  }

  public Page<InventoryReservation> listByProduct(UUID productId, Pageable pageable) {
    return this.inventoryReservationRepository.findByProductId(productId, pageable);
  }

  public Product getProductReference(UUID id) {
    return this.entityManager.getReference(Product.class, id);
  }

  @Transactional
  public InventoryReservation create(InventoryReservation entity) {
    return this.inventoryReservationRepository.save(entity);
  }

  @Transactional
  public InventoryReservation update(InventoryReservation entity) {
    return this.inventoryReservationRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.inventoryReservationRepository.deleteById(id);
  }
}
