package ecommerce_product_service.consumers;

import java.math.BigDecimal;
import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: product_events_product_created (subscription 'ProductEvents',
 * event 'ProductCreated').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersProductEventsProductCreated")
public class ProductEventsProductCreatedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(ProductEventsProductCreatedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "product_events_product_created",
        "ProductCreated");
    UUID productId = (UUID) input.get("productId");
    String name = (String) input.get("name");
    BigDecimal price = (BigDecimal) input.get("price");
    try {
      logger.info("Product created");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "product_events_product_created",
          "ProductCreated");
    }
  }
}
