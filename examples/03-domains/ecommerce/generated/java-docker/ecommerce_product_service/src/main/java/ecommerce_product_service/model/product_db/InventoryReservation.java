package ecommerce_product_service.model.product_db;

import ecommerce_product_service.enums.ReservationStatus;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.Index;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * InventoryReservation JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "inventory_reservations",
    indexes = {
      @Index(
          name = "ix_inventory_reservations_reservation_id_status",
          columnList = "reservation_id, status")
    },
    uniqueConstraints = {},
    check = {})
public class InventoryReservation extends BaseEntity {

  @Column(name = "reservationId", columnDefinition = "uuid", nullable = false)
  private UUID reservationId;

  @Column(name = "quantity", columnDefinition = "integer", nullable = false)
  private Integer quantity;

  @Column(name = "status", columnDefinition = "varchar(255)", nullable = false)
  private ReservationStatus status = ReservationStatus.RESERVED;

  @Column(name = "expiresAt", columnDefinition = "timestamptz", nullable = false)
  private OffsetDateTime expiresAt;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "product_id", nullable = false)
  private Product product;

  public UUID getReservationId() {
    return this.reservationId;
  }

  public void setReservationId(UUID reservationId) {
    this.reservationId = reservationId;
  }

  public Integer getQuantity() {
    return this.quantity;
  }

  public void setQuantity(Integer quantity) {
    this.quantity = quantity;
  }

  public ReservationStatus getStatus() {
    return this.status;
  }

  public void setStatus(ReservationStatus status) {
    this.status = status;
  }

  public OffsetDateTime getExpiresAt() {
    return this.expiresAt;
  }

  public void setExpiresAt(OffsetDateTime expiresAt) {
    this.expiresAt = expiresAt;
  }

  public Product getProduct() {
    return this.product;
  }

  public void setProduct(Product product) {
    this.product = product;
  }

  /**
   * Build a new {@link InventoryReservation} from a DSL object-literal map, setting only the keys
   * present in {@code data} (a key absent from {@code data} leaves the corresponding field at its
   * normal Java default -- {@code null} unless the field declares its own initializer above). Keys
   * are the AS-AUTHORED DSL field name (Java's own object-literal emission, {@code
   * _datrixOrderedMap(...)}, preserves author-declared spelling -- no case conversion), which for a
   * camelCase-authored DSL field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.InventoryReservation.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new InventoryReservation(...)`.
   */
  public static InventoryReservation fromMap(java.util.Map<String, Object> data) {
    InventoryReservation result = new InventoryReservation();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link
   * InventoryReservation} instance (a key absent from {@code data} leaves the corresponding field
   * at its CURRENT value, unlike {@link #fromMap} which starts from a freshly constructed
   * instance). Same key convention and per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.InventoryReservation.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(InventoryReservation target, java.util.Map<String, Object> data) {
    if (data.containsKey("reservationId")) {
      Object __v = data.get("reservationId");
      target.setReservationId((UUID) __v);
    }
    if (data.containsKey("quantity")) {
      Object __v = data.get("quantity");
      target.setQuantity(__v instanceof Integer __n ? __n : ((Number) __v).intValue());
    }
    if (data.containsKey("status")) {
      Object __v = data.get("status");
      target.setStatus((ReservationStatus) __v);
    }
    if (data.containsKey("expiresAt")) {
      Object __v = data.get("expiresAt");
      target.setExpiresAt(
          __v instanceof OffsetDateTime __n
              ? __n
              : ((java.time.LocalDateTime) __v).atOffset(java.time.ZoneOffset.UTC));
    }
  }
}
