package ecommerce_product_service.services.product_db;

import ecommerce_product_service.enums.ProductStatus;
import ecommerce_product_service.model.product_db.Category;
import ecommerce_product_service.model.product_db.Product;
import ecommerce_product_service.repositories.product_db.ProductRepository;
import ecommerce_product_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * Product service -- request-scoped unit-of-work wrapper over {@link ProductRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class ProductService extends ServiceBase {

  private final ProductRepository productRepository;

  @PersistenceContext(unitName = "product_db")
  private EntityManager entityManager;

  public ProductService(ProductRepository productRepository) {
    this.productRepository = productRepository;
  }

  public Optional<Product> get(UUID id) {
    return this.productRepository.findById(id);
  }

  public List<Product> list() {
    return this.productRepository.findAll();
  }

  public Optional<Product> getBySlug(String slug) {
    return this.productRepository.findBySlug(slug);
  }

  public List<Product> getByCategoryId(UUID categoryId) {
    return this.productRepository.findByCategoryId(categoryId);
  }

  public List<Product> getByCategoryIdAndStatus(UUID categoryId, ProductStatus status) {
    return this.productRepository.findByCategoryIdAndStatus(categoryId, status);
  }

  public List<Product> getByStatusAndInventory(ProductStatus status, Integer inventory) {
    return this.productRepository.findByStatusAndInventory(status, inventory);
  }

  public Page<Product> listByCategory(UUID categoryId, Pageable pageable) {
    return this.productRepository.findByCategoryId(categoryId, pageable);
  }

  public Category getCategoryReference(UUID id) {
    return this.entityManager.getReference(Category.class, id);
  }

  @Transactional
  public Product create(Product entity) {
    return this.productRepository.save(entity);
  }

  @Transactional
  public Product update(Product entity) {
    return this.productRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.productRepository.deleteById(id);
  }
}
