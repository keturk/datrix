package ecommerce_product_service.model.product_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.OneToMany;
import jakarta.persistence.Table;
import java.util.ArrayList;
import java.util.List;

/**
 * Category JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "categories",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(name = "ck_categories_name_max_length", constraint = "length(name) <= 100")
    })
public class Category extends BaseEntity {

  @Column(name = "name", columnDefinition = "varchar(100)", nullable = false, unique = true)
  private String name;

  @Column(name = "description", columnDefinition = "text", nullable = true)
  private String description;

  @Column(name = "slug", columnDefinition = "varchar(255)", nullable = false, unique = true)
  private String slug;

  @OneToMany(mappedBy = "category", fetch = FetchType.LAZY)
  private List<Product> products = new ArrayList<>();

  public String getName() {
    return this.name;
  }

  public void setName(String name) {
    this.name = name;
  }

  public String getDescription() {
    return this.description;
  }

  public void setDescription(String description) {
    this.description = description;
  }

  public String getSlug() {
    return this.slug;
  }

  public void setSlug(String slug) {
    this.slug = slug;
  }

  public List<Product> getProducts() {
    return this.products;
  }

  public void setProducts(List<Product> products) {
    this.products = products;
  }

  /**
   * Build a new {@link Category} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.Category.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new Category(...)`.
   */
  public static Category fromMap(java.util.Map<String, Object> data) {
    Category result = new Category();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link Category} instance
   * (a key absent from {@code data} leaves the corresponding field at its CURRENT value, unlike
   * {@link #fromMap} which starts from a freshly constructed instance). Same key convention and
   * per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.Category.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(Category target, java.util.Map<String, Object> data) {
    if (data.containsKey("name")) {
      Object __v = data.get("name");
      target.setName((String) __v);
    }
    if (data.containsKey("description")) {
      Object __v = data.get("description");
      target.setDescription((String) __v);
    }
    if (data.containsKey("slug")) {
      Object __v = data.get("slug");
      target.setSlug((String) __v);
    }
  }
}
