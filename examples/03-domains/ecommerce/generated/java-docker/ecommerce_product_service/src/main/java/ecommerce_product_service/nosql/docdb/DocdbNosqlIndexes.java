package ecommerce_product_service.nosql.docdb;

import com.mongodb.client.MongoCollection;
import com.mongodb.client.model.IndexOptions;
import com.mongodb.client.model.Indexes;
import java.util.List;
import org.bson.Document;
import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

/**
 * Startup index/table bootstrap for this NoSQL block (mongodb).
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component
@Profile("!test")
public class DocdbNosqlIndexes implements CommandLineRunner {

  private final DocdbNosqlSession session;

  public DocdbNosqlIndexes(DocdbNosqlSession session) {
    this.session = session;
  }

  @Override
  public void run(String... args) {
    {
      MongoCollection<Document> collection = session.getDatabase().getCollection("product-review");
      collection.createIndex(
          Indexes.ascending(List.of("product_id")), new IndexOptions().unique(false));
      collection.createIndex(
          Indexes.ascending(List.of("user_id")), new IndexOptions().unique(false));
      collection.createIndex(
          Indexes.ascending(List.of("product_id", "created_at")), new IndexOptions().unique(false));
      collection.createIndex(
          Indexes.ascending(List.of("user_id", "created_at")), new IndexOptions().unique(false));
    }
    {
      MongoCollection<Document> collection =
          session.getDatabase().getCollection("product-analytics");
      collection.createIndex(
          Indexes.ascending(List.of("product_id")), new IndexOptions().unique(false));
      collection.createIndex(
          Indexes.ascending(List.of("product_id", "event_type", "timestamp")),
          new IndexOptions().unique(false));
    }
  }
}
