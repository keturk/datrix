package ecommerce_product_service.dto.product_db;

import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import java.util.UUID;

/**
 * Category batch-update-request DTO -- carries the row's own {@code id} alongside every field
 * {@link CategoryUpdateRequest} allows (server-managed and immutable fields excluded identically),
 * so a batch-update payload item can identify which row to update without widening what a client
 * may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record CategoryBatchUpdateRequestItem(
    @NotNull UUID id,
    @Size(max = 100) String name,
    String description,
    @Pattern(regexp = "^[a-z0-9]+(?:-[a-z0-9]+)*$") String slug) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.Category.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * Category#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("name", name());
    __m.put("description", description());
    __m.put("slug", slug());
    return __m;
  }
}
