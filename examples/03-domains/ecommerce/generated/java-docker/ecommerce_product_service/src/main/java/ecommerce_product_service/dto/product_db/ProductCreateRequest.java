package ecommerce_product_service.dto.product_db;

import com.fasterxml.jackson.databind.JsonNode;
import ecommerce_product_service.enums.ProductStatus;
import jakarta.validation.constraints.DecimalMin;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.math.BigDecimal;
import java.util.UUID;

// 'with' mixes in multiple traits, adding their fields and methods
/**
 * Product create-request DTO.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record ProductCreateRequest(
    @Size(max = 200) String slug,
    // Money is a semantic amount type; currency is modeled explicitly where needed
    @NotNull BigDecimal price,
    BigDecimal compareAtPrice,
    // 'min(0)' sets a minimum value constraint
    @DecimalMin("0") Integer inventory,
    @NotNull @Size(max = 200) String name,
    @NotNull String description,
    ProductStatus status,
    JsonNode productMetadata,
    @NotNull JsonNode images,
    @NotNull JsonNode tags,
    @NotNull UUID categoryId) {
  /**
   * Convert this record to a DSL-object-literal-shaped map, keyed by field name -- backs {@code
   * db.Product.batchCreate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_create`), fed through {@link
   * Product#fromMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("slug", slug());
    __m.put("price", price());
    __m.put("compareAtPrice", compareAtPrice());
    __m.put("inventory", inventory());
    __m.put("name", name());
    __m.put("description", description());
    __m.put("status", status());
    __m.put("productMetadata", productMetadata());
    __m.put("images", images());
    __m.put("tags", tags());
    __m.put("categoryId", categoryId());
    return __m;
  }
}
