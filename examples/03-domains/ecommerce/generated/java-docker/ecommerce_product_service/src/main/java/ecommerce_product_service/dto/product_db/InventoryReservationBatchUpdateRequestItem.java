package ecommerce_product_service.dto.product_db;

import ecommerce_product_service.enums.ReservationStatus;
import jakarta.validation.constraints.NotNull;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * InventoryReservation batch-update-request DTO -- carries the row's own {@code id} alongside every
 * field {@link InventoryReservationUpdateRequest} allows (server-managed and immutable fields
 * excluded identically), so a batch-update payload item can identify which row to update without
 * widening what a client may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record InventoryReservationBatchUpdateRequestItem(
    @NotNull UUID id,
    UUID reservationId,
    Integer quantity,
    ReservationStatus status,
    OffsetDateTime expiresAt,
    UUID productId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.InventoryReservation.batchUpdate(items)} lowering
   * (`transpiler/ _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * InventoryReservation#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("reservationId", reservationId());
    __m.put("quantity", quantity());
    __m.put("status", status());
    __m.put("expiresAt", expiresAt());
    __m.put("productId", productId());
    return __m;
  }
}
