package ecommerce_product_service.dto.product_db;

import com.fasterxml.jackson.databind.JsonNode;
import ecommerce_product_service.enums.ProductStatus;
import jakarta.validation.constraints.DecimalMin;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.math.BigDecimal;
import java.util.UUID;

/**
 * Product batch-update-request DTO -- carries the row's own {@code id} alongside every field {@link
 * ProductUpdateRequest} allows (server-managed and immutable fields excluded identically), so a
 * batch-update payload item can identify which row to update without widening what a client may
 * change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record ProductBatchUpdateRequestItem(
    @NotNull UUID id,
    @Size(max = 200) String slug,
    BigDecimal price,
    BigDecimal compareAtPrice,
    @DecimalMin("0") Integer inventory,
    @Size(max = 200) String name,
    String description,
    ProductStatus status,
    JsonNode productMetadata,
    JsonNode images,
    JsonNode tags,
    UUID categoryId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.Product.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * Product#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("slug", slug());
    __m.put("price", price());
    __m.put("compareAtPrice", compareAtPrice());
    __m.put("inventory", inventory());
    __m.put("name", name());
    __m.put("description", description());
    __m.put("status", status());
    __m.put("productMetadata", productMetadata());
    __m.put("images", images());
    __m.put("tags", tags());
    __m.put("categoryId", categoryId());
    return __m;
  }
}
