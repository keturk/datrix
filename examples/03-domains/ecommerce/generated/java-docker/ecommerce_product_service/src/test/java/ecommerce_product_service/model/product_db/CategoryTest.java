package ecommerce_product_service.model.product_db;

import static org.junit.jupiter.api.Assertions.*;

import ecommerce_product_service.dto.product_db.CategoryCreateRequest;
import jakarta.validation.ConstraintViolation;
import jakarta.validation.Validation;
import jakarta.validation.Validator;
import java.util.Set;
import org.hibernate.validator.HibernateValidator;
import org.hibernate.validator.messageinterpolation.ParameterMessageInterpolator;
import org.junit.jupiter.api.Test;

/**
 * Generated tests for Category: field round-trip, update-assertion, relationship navigation, and
 * (when the entity's CreateRequest DTO carries at least one @NotNull field) Bean Validation
 * constraint-violation tests.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
class CategoryTest {

  @Test
  void fieldRoundTripAssignsAndReturnsSameValue() {
    Category instance = CategoryTestFactory.create();
    instance.setName("test_value");
    assertEquals("test_value", instance.getName());
    instance.setDescription("test_value");
    assertEquals("test_value", instance.getDescription());
    instance.setSlug("my-slug");
    assertEquals("my-slug", instance.getSlug());
  }

  @Test
  void fieldUpdateAssignsDistinctValue() {
    Category instance = CategoryTestFactory.create();
    instance.setName("test_value");
    instance.setDescription("test_value");
    instance.setSlug("my-slug");
    instance.setName("alt_value");
    assertEquals("alt_value", instance.getName());
    instance.setDescription("alt_value");
    assertEquals("alt_value", instance.getDescription());
    instance.setSlug("my-alt-slug");
    assertEquals("my-alt-slug", instance.getSlug());
  }

  private static Validator buildValidator() {
    return Validation.byProvider(HibernateValidator.class)
        .configure()
        .messageInterpolator(new ParameterMessageInterpolator())
        .buildValidatorFactory()
        .getValidator();
  }

  @Test
  void requiredFieldLeftNullFailsBeanValidation() {
    Validator validator = buildValidator();
    CategoryCreateRequest invalid = new CategoryCreateRequest(null, "test_value", "my-slug");
    Set<ConstraintViolation<CategoryCreateRequest>> violations = validator.validate(invalid);
    assertFalse(violations.isEmpty(), "expected a violation for a null required field");
  }

  @Test
  void allRequiredFieldsPresentPassesBeanValidation() {
    Validator validator = buildValidator();
    CategoryCreateRequest valid = new CategoryCreateRequest("test_value", "test_value", "my-slug");
    Set<ConstraintViolation<CategoryCreateRequest>> violations = validator.validate(valid);
    assertTrue(violations.isEmpty(), "expected no violations, got: " + violations);
  }
}
