// GENERATED by datrix-codegen-angular. DO NOT EDIT.
//
// Branded string aliases. Each type is `string` at runtime -- the branding
// exists only at compile time, to prevent an arbitrary string from being
// silently assigned where a specific wire shape is expected (a path
// parameter, a WKT/EWKT geometry value, an ISO-8601 timestamp).

declare const brand: unique symbol;
type Brand<T, B extends string> = T & { readonly [brand]: B };

/** RFC 4122 UUID string. */
export type Uuid = Brand<string, 'Uuid'>;

/** ISO-8601 date-time string. Never `Date` -- nothing on the wire revives a
 * JSON string into a real Date instance. */
export type IsoDateTime = Brand<string, 'IsoDateTime'>;

/** Well-Known Text geometry string (e.g. `POINT(0 0)`). */
export type WktGeometry = Brand<string, 'WktGeometry'>;

/** Extended Well-Known Text geography string (e.g. `SRID=4326;POINT(0 0)`). */
export type EwktGeography = Brand<string, 'EwktGeography'>;

/** An exact decimal carried as a string (e.g. `"19.99"`).
 *
 * Used where the backend this client was generated against serializes
 * `Decimal`/`Money` as a JSON string -- which some backends do precisely so the
 * value does not pass through an IEEE-754 double on the way to you. Branded
 * rather than bare `string` for the same reason: `a.amount + b.amount` on two
 * of these is string concatenation, and it should not compile by accident.
 * Parse with a decimal library at the point you actually need arithmetic.
 *
 * A backend that serializes these as JSON numbers gets `number` in the emitted
 * models instead, and this alias simply goes unused. */
export type DecimalString = Brand<string, 'DecimalString'>;
