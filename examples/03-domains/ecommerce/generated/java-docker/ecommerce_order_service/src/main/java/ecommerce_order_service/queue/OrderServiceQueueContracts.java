package ecommerce_order_service.queue;

import ecommerce_order_service.errors.ContractViolationError;
import java.math.BigDecimal;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Event contract validation functions, auto-generated from {@code ensure} clauses in event
 * declarations. Each function validates preconditions before publication.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public final class OrderServiceQueueContracts {

  private static final Logger logger = LoggerFactory.getLogger(OrderServiceQueueContracts.class);

  private OrderServiceQueueContracts() {}

  /** Validate contracts for the ProcessPayment event. */
  public static void validateProcessPayment(UUID orderId, BigDecimal amount, String currency) {
    if (!((amount.compareTo(BigDecimal.valueOf(0)) > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("amount", amount);
      logger.error(
          "contract_violation event={} clause={} actual={}", "ProcessPayment", "ensure_0", actual);
      throw new ContractViolationError("ProcessPayment", "ensure_0", actual);
    }
    if (!((currency.length() == 3))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("currency", currency);
      logger.error(
          "contract_violation event={} clause={} actual={}", "ProcessPayment", "ensure_1", actual);
      throw new ContractViolationError("ProcessPayment", "ensure_1", actual);
    }
  }

  /** Validate contracts for the SendOrderConfirmation event. */
  public static void validateSendOrderConfirmation(
      UUID orderId, String customerEmail, String orderNumber) {
    if (!((customerEmail.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("customerEmail", customerEmail);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "SendOrderConfirmation",
          "ensure_0",
          actual);
      throw new ContractViolationError("SendOrderConfirmation", "ensure_0", actual);
    }
  }

  /** Validate contracts for the SettlePayment event. */
  public static void validateSettlePayment(UUID paymentId, String merchantId, BigDecimal amount) {
    if (!((amount.compareTo(BigDecimal.valueOf(0)) > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("amount", amount);
      logger.error(
          "contract_violation event={} clause={} actual={}", "SettlePayment", "ensure_0", actual);
      throw new ContractViolationError("SettlePayment", "ensure_0", actual);
    }
    if (!((merchantId.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("merchantId", merchantId);
      logger.error(
          "contract_violation event={} clause={} actual={}", "SettlePayment", "ensure_1", actual);
      throw new ContractViolationError("SettlePayment", "ensure_1", actual);
    }
  }
}
