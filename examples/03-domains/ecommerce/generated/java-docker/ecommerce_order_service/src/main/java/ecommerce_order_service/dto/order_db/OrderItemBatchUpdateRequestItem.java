package ecommerce_order_service.dto.order_db;

import jakarta.validation.constraints.DecimalMin;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.math.BigDecimal;
import java.util.UUID;

/**
 * OrderItem batch-update-request DTO -- carries the row's own {@code id} alongside every field
 * {@link OrderItemUpdateRequest} allows (server-managed and immutable fields excluded identically),
 * so a batch-update payload item can identify which row to update without widening what a client
 * may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record OrderItemBatchUpdateRequestItem(
    @NotNull UUID id,
    UUID productId,
    @Size(max = 200) String productName,
    @DecimalMin("1") Integer quantity,
    BigDecimal unitPrice,
    UUID orderId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.OrderItem.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * OrderItem#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("productId", productId());
    __m.put("productName", productName());
    __m.put("quantity", quantity());
    __m.put("unitPrice", unitPrice());
    __m.put("orderId", orderId());
    return __m;
  }
}
