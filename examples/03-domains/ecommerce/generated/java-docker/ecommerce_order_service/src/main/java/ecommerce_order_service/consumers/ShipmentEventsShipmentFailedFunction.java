package ecommerce_order_service.consumers;

import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: shipment_events_shipment_failed (subscription 'ShipmentEvents',
 * event 'ShipmentFailed').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersShipmentEventsShipmentFailed")
public class ShipmentEventsShipmentFailedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(ShipmentEventsShipmentFailedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "shipment_events_shipment_failed",
        "ShipmentFailed");
    UUID shipmentId = (UUID) input.get("shipmentId");
    UUID orderId = (UUID) input.get("orderId");
    String reason = (String) input.get("reason");
    try {
      logger.warn("Shipment failed for order");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "shipment_events_shipment_failed",
          "ShipmentFailed");
    }
  }
}
