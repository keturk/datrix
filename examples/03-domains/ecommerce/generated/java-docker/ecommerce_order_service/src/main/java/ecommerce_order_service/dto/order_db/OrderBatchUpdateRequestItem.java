package ecommerce_order_service.dto.order_db;

import ecommerce_order_service.enums.OrderStatus;
import ecommerce_order_service.schemas.Address;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.math.BigDecimal;
import java.util.UUID;

/**
 * Order batch-update-request DTO -- carries the row's own {@code id} alongside every field {@link
 * OrderUpdateRequest} allows (server-managed and immutable fields excluded identically), so a
 * batch-update payload item can identify which row to update without widening what a client may
 * change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record OrderBatchUpdateRequestItem(
    @NotNull UUID id,
    UUID customerId,
    @Size(max = 20) String orderNumber,
    OrderStatus status,
    BigDecimal subtotal,
    BigDecimal tax,
    BigDecimal shippingCost,
    BigDecimal discount,
    Address shippingAddress,
    Address billingAddress,
    UUID inventoryReservationId,
    UUID paymentId,
    UUID shipmentId,
    String cancellationReason) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.Order.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link Order#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("customerId", customerId());
    __m.put("orderNumber", orderNumber());
    __m.put("status", status());
    __m.put("subtotal", subtotal());
    __m.put("tax", tax());
    __m.put("shippingCost", shippingCost());
    __m.put("discount", discount());
    __m.put("shippingAddress", shippingAddress());
    __m.put("billingAddress", billingAddress());
    __m.put("inventoryReservationId", inventoryReservationId());
    __m.put("paymentId", paymentId());
    __m.put("shipmentId", shipmentId());
    __m.put("cancellationReason", cancellationReason());
    return __m;
  }
}
