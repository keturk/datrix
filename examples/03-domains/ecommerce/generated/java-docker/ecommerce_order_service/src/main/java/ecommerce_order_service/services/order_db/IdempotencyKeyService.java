package ecommerce_order_service.services.order_db;

import ecommerce_order_service.model.order_db.IdempotencyKey;
import ecommerce_order_service.repositories.order_db.IdempotencyKeyRepository;
import ecommerce_order_service.services.ServiceBase;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * IdempotencyKey service -- request-scoped unit-of-work wrapper over {@link
 * IdempotencyKeyRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class IdempotencyKeyService extends ServiceBase {

  private final IdempotencyKeyRepository idempotencyKeyRepository;

  public IdempotencyKeyService(IdempotencyKeyRepository idempotencyKeyRepository) {
    this.idempotencyKeyRepository = idempotencyKeyRepository;
  }

  public Optional<IdempotencyKey> get(UUID id) {
    return this.idempotencyKeyRepository.findById(id);
  }

  public List<IdempotencyKey> list() {
    return this.idempotencyKeyRepository.findAll();
  }

  public Optional<IdempotencyKey> getByKey(String key) {
    return this.idempotencyKeyRepository.findByKey(key);
  }

  public List<IdempotencyKey> getByOperation(String operation) {
    return this.idempotencyKeyRepository.findByOperation(operation);
  }

  public List<IdempotencyKey> getByExpiresAt(OffsetDateTime expiresAt) {
    return this.idempotencyKeyRepository.findByExpiresAt(expiresAt);
  }

  @Transactional
  public IdempotencyKey create(IdempotencyKey entity) {
    return this.idempotencyKeyRepository.save(entity);
  }

  @Transactional
  public IdempotencyKey update(IdempotencyKey entity) {
    return this.idempotencyKeyRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.idempotencyKeyRepository.deleteById(id);
  }
}
