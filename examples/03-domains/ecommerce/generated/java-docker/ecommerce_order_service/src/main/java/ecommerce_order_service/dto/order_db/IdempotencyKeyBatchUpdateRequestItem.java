package ecommerce_order_service.dto.order_db;

import com.fasterxml.jackson.databind.JsonNode;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * IdempotencyKey batch-update-request DTO -- carries the row's own {@code id} alongside every field
 * {@link IdempotencyKeyUpdateRequest} allows (server-managed and immutable fields excluded
 * identically), so a batch-update payload item can identify which row to update without widening
 * what a client may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record IdempotencyKeyBatchUpdateRequestItem(
    @NotNull UUID id,
    @Size(max = 100) String key,
    @Size(max = 50) String operation,
    UUID resourceId,
    JsonNode response,
    OffsetDateTime expiresAt) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.IdempotencyKey.batchUpdate(items)} lowering
   * (`transpiler/ _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * IdempotencyKey#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("key", key());
    __m.put("operation", operation());
    __m.put("resourceId", resourceId());
    __m.put("response", response());
    __m.put("expiresAt", expiresAt());
    return __m;
  }
}
