package ecommerce_order_service.model.order_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import jakarta.persistence.Transient;
import java.math.BigDecimal;
import java.util.UUID;

/**
 * OrderItem JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "order_items",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_order_items_product_name_max_length",
          constraint = "length(product_name) <= 200")
    })
public class OrderItem extends BaseEntity {

  @Column(name = "productId", columnDefinition = "uuid", nullable = false)
  private UUID productId;

  @Column(name = "productName", columnDefinition = "varchar(200)", nullable = false)
  private String productName;

  // 'min(1)' sets a minimum value constraint
  @Column(name = "quantity", columnDefinition = "integer", nullable = false)
  private Integer quantity;

  // 'positive' ensures the value is greater than zero
  @Column(name = "unitPrice", columnDefinition = "numeric(19, 4)", nullable = false)
  private BigDecimal unitPrice;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "order_id", nullable = false)
  private Order order;

  public UUID getProductId() {
    return this.productId;
  }

  public void setProductId(UUID productId) {
    this.productId = productId;
  }

  public String getProductName() {
    return this.productName;
  }

  public void setProductName(String productName) {
    this.productName = productName;
  }

  public Integer getQuantity() {
    return this.quantity;
  }

  public void setQuantity(Integer quantity) {
    this.quantity = quantity;
  }

  public BigDecimal getUnitPrice() {
    return this.unitPrice;
  }

  public void setUnitPrice(BigDecimal unitPrice) {
    this.unitPrice = unitPrice;
  }

  public Order getOrder() {
    return this.order;
  }

  public void setOrder(Order order) {
    this.order = order;
  }

  /** Computed: this.getUnitPrice().multiply(BigDecimal.valueOf(this.getQuantity())) */
  @Transient
  public BigDecimal getTotal() {
    return this.getUnitPrice().multiply(BigDecimal.valueOf(this.getQuantity()));
  }

  /**
   * Build a new {@link OrderItem} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.OrderItem.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new OrderItem(...)`.
   */
  public static OrderItem fromMap(java.util.Map<String, Object> data) {
    OrderItem result = new OrderItem();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link OrderItem} instance
   * (a key absent from {@code data} leaves the corresponding field at its CURRENT value, unlike
   * {@link #fromMap} which starts from a freshly constructed instance). Same key convention and
   * per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.OrderItem.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(OrderItem target, java.util.Map<String, Object> data) {
    if (data.containsKey("productId")) {
      Object __v = data.get("productId");
      target.setProductId((UUID) __v);
    }
    if (data.containsKey("productName")) {
      Object __v = data.get("productName");
      target.setProductName((String) __v);
    }
    if (data.containsKey("quantity")) {
      Object __v = data.get("quantity");
      target.setQuantity(__v instanceof Integer __n ? __n : ((Number) __v).intValue());
    }
    if (data.containsKey("unitPrice")) {
      Object __v = data.get("unitPrice");
      target.setUnitPrice(
          __v instanceof java.math.BigDecimal __n ? __n : new java.math.BigDecimal(__v.toString()));
    }
  }
}
