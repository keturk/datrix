package ecommerce_order_service.services.order_db;

import ecommerce_order_service.model.order_db.Order;
import ecommerce_order_service.model.order_db.OrderItem;
import ecommerce_order_service.repositories.order_db.OrderItemRepository;
import ecommerce_order_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * OrderItem service -- request-scoped unit-of-work wrapper over {@link OrderItemRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class OrderItemService extends ServiceBase {

  private final OrderItemRepository orderItemRepository;

  @PersistenceContext(unitName = "order_db")
  private EntityManager entityManager;

  public OrderItemService(OrderItemRepository orderItemRepository) {
    this.orderItemRepository = orderItemRepository;
  }

  public Optional<OrderItem> get(UUID id) {
    return this.orderItemRepository.findById(id);
  }

  public List<OrderItem> list() {
    return this.orderItemRepository.findAll();
  }

  public List<OrderItem> getByProductId(UUID productId) {
    return this.orderItemRepository.findByProductId(productId);
  }

  public List<OrderItem> getByOrderId(UUID orderId) {
    return this.orderItemRepository.findByOrderId(orderId);
  }

  public Page<OrderItem> listByOrder(UUID orderId, Pageable pageable) {
    return this.orderItemRepository.findByOrderId(orderId, pageable);
  }

  public Order getOrderReference(UUID id) {
    return this.entityManager.getReference(Order.class, id);
  }

  @Transactional
  public OrderItem create(OrderItem entity) {
    return this.orderItemRepository.save(entity);
  }

  @Transactional
  public OrderItem update(OrderItem entity) {
    return this.orderItemRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.orderItemRepository.deleteById(id);
  }
}
