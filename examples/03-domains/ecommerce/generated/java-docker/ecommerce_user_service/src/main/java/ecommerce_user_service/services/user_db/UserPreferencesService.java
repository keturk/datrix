package ecommerce_user_service.services.user_db;

import ecommerce_user_service.model.user_db.User;
import ecommerce_user_service.model.user_db.UserPreferences;
import ecommerce_user_service.repositories.user_db.UserPreferencesRepository;
import ecommerce_user_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * UserPreferences service -- request-scoped unit-of-work wrapper over {@link
 * UserPreferencesRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class UserPreferencesService extends ServiceBase {

  private final UserPreferencesRepository userPreferencesRepository;

  @PersistenceContext(unitName = "user_db")
  private EntityManager entityManager;

  public UserPreferencesService(UserPreferencesRepository userPreferencesRepository) {
    this.userPreferencesRepository = userPreferencesRepository;
  }

  public Optional<UserPreferences> get(UUID id) {
    return this.userPreferencesRepository.findById(id);
  }

  public List<UserPreferences> list() {
    return this.userPreferencesRepository.findAll();
  }

  public Page<UserPreferences> listByUser(UUID userId, Pageable pageable) {
    return this.userPreferencesRepository.findByUserId(userId, pageable);
  }

  public User getUserReference(UUID id) {
    return this.entityManager.getReference(User.class, id);
  }

  @Transactional
  public UserPreferences create(UserPreferences entity) {
    return this.userPreferencesRepository.save(entity);
  }

  @Transactional
  public UserPreferences update(UserPreferences entity) {
    return this.userPreferencesRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.userPreferencesRepository.deleteById(id);
  }
}
