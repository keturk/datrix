package ecommerce_user_service.dto.user_db;

import com.fasterxml.jackson.databind.JsonNode;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Size;
import java.util.UUID;

/**
 * UserPreferences batch-update-request DTO -- carries the row's own {@code id} alongside every
 * field {@link UserPreferencesUpdateRequest} allows (server-managed and immutable fields excluded
 * identically), so a batch-update payload item can identify which row to update without widening
 * what a client may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record UserPreferencesBatchUpdateRequestItem(
    @NotNull UUID id,
    @Size(max = 10) String language,
    @Size(max = 50) String timezone,
    Boolean emailNotifications,
    Boolean smsNotifications,
    JsonNode preferences,
    UUID userId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.UserPreferences.batchUpdate(items)} lowering
   * (`transpiler/ _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * UserPreferences#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("language", language());
    __m.put("timezone", timezone());
    __m.put("emailNotifications", emailNotifications());
    __m.put("smsNotifications", smsNotifications());
    __m.put("preferences", preferences());
    __m.put("userId", userId());
    return __m;
  }
}
