package ecommerce_user_service.model.user_db;

import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.Index;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.ManyToOne;
import jakarta.persistence.Table;
import jakarta.persistence.Transient;
import java.time.OffsetDateTime;
import java.util.Objects;

/**
 * UserSession JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "user_sessions",
    indexes = {
      @Index(name = "ix_user_sessions_user_id_expires_at", columnList = "user_id, expires_at")
    },
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_user_sessions_token_max_length",
          constraint = "length(token) <= 255"),
      @CheckConstraint(
          name = "ck_user_sessions_device_name_max_length",
          constraint = "length(device_name) <= 500"),
      @CheckConstraint(
          name = "ck_user_sessions_user_agent_max_length",
          constraint = "length(user_agent) <= 255")
    })
public class UserSession extends BaseEntity {

  @Column(name = "token", columnDefinition = "varchar(255)", nullable = false, unique = true)
  private String token;

  @Column(name = "deviceName", columnDefinition = "varchar(500)", nullable = true)
  private String deviceName;

  @Column(name = "ipAddress", columnDefinition = "varchar(45)", nullable = true)
  private String ipAddress;

  @Column(name = "userAgent", columnDefinition = "varchar(255)", nullable = true)
  private String userAgent;

  @Column(name = "expiresAt", columnDefinition = "timestamptz", nullable = false)
  private OffsetDateTime expiresAt;

  @Column(name = "lastActivityAt", columnDefinition = "timestamptz", nullable = true)
  private OffsetDateTime lastActivityAt;

  @ManyToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "user_id", nullable = false)
  private User user;

  public String getToken() {
    return this.token;
  }

  public void setToken(String token) {
    this.token = token;
  }

  public String getDeviceName() {
    return this.deviceName;
  }

  public void setDeviceName(String deviceName) {
    this.deviceName = deviceName;
  }

  public String getIpAddress() {
    return this.ipAddress;
  }

  public void setIpAddress(String ipAddress) {
    this.ipAddress = ipAddress;
  }

  public String getUserAgent() {
    return this.userAgent;
  }

  public void setUserAgent(String userAgent) {
    this.userAgent = userAgent;
  }

  public OffsetDateTime getExpiresAt() {
    return this.expiresAt;
  }

  public void setExpiresAt(OffsetDateTime expiresAt) {
    this.expiresAt = expiresAt;
  }

  public OffsetDateTime getLastActivityAt() {
    return this.lastActivityAt;
  }

  public void setLastActivityAt(OffsetDateTime lastActivityAt) {
    this.lastActivityAt = lastActivityAt;
  }

  public User getUser() {
    return this.user;
  }

  public void setUser(User user) {
    this.user = user;
  }

  /** Computed: (OffsetDateTime.now().compareTo(this.getExpiresAt()) > 0) */
  @Transient
  public Boolean getIsExpired() {
    return (OffsetDateTime.now().compareTo(this.getExpiresAt()) > 0);
  }

  /** Computed: (!this.getIsExpired() && (!Objects.equals(this.getLastActivityAt(), null))) */
  @Transient
  public Boolean getIsActive() {
    return (!this.getIsExpired() && (!Objects.equals(this.getLastActivityAt(), null)));
  }

  /**
   * Build a new {@link UserSession} from a DSL object-literal map, setting only the keys present in
   * {@code data} (a key absent from {@code data} leaves the corresponding field at its normal Java
   * default -- {@code null} unless the field declares its own initializer above). Keys are the
   * AS-AUTHORED DSL field name (Java's own object-literal emission, {@code _datrixOrderedMap(...)},
   * preserves author-declared spelling -- no case conversion), which for a camelCase-authored DSL
   * field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.UserSession.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new UserSession(...)`.
   */
  public static UserSession fromMap(java.util.Map<String, Object> data) {
    UserSession result = new UserSession();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link UserSession}
   * instance (a key absent from {@code data} leaves the corresponding field at its CURRENT value,
   * unlike {@link #fromMap} which starts from a freshly constructed instance). Same key convention
   * and per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.UserSession.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(UserSession target, java.util.Map<String, Object> data) {
    if (data.containsKey("token")) {
      Object __v = data.get("token");
      target.setToken((String) __v);
    }
    if (data.containsKey("deviceName")) {
      Object __v = data.get("deviceName");
      target.setDeviceName((String) __v);
    }
    if (data.containsKey("ipAddress")) {
      Object __v = data.get("ipAddress");
      target.setIpAddress((String) __v);
    }
    if (data.containsKey("userAgent")) {
      Object __v = data.get("userAgent");
      target.setUserAgent((String) __v);
    }
    if (data.containsKey("expiresAt")) {
      Object __v = data.get("expiresAt");
      target.setExpiresAt(
          __v instanceof OffsetDateTime __n
              ? __n
              : ((java.time.LocalDateTime) __v).atOffset(java.time.ZoneOffset.UTC));
    }
    if (data.containsKey("lastActivityAt")) {
      Object __v = data.get("lastActivityAt");
      target.setLastActivityAt(
          __v instanceof OffsetDateTime __n
              ? __n
              : ((java.time.LocalDateTime) __v).atOffset(java.time.ZoneOffset.UTC));
    }
  }
}
