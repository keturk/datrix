package ecommerce_user_service.pubsub.mq;

import ecommerce_user_service.enums.UserStatus;
import ecommerce_user_service.errors.ContractViolationError;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.UUID;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Event contract validation functions, auto-generated from {@code ensure} clauses in event
 * declarations. Each function validates preconditions before publication.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public final class MqPubsubContracts {

  private static final Logger logger = LoggerFactory.getLogger(MqPubsubContracts.class);

  private MqPubsubContracts() {}

  /** Validate contracts for the UserRegistered event. */
  public static void validateUserRegistered(UUID userId, String email, String fullName) {
    if (!((fullName.length() > 0))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("fullName", fullName);
      logger.error(
          "contract_violation event={} clause={} actual={}", "UserRegistered", "ensure_0", actual);
      throw new ContractViolationError("UserRegistered", "ensure_0", actual);
    }
  }

  /** Validate contracts for the UserStatusChanged event. */
  public static void validateUserStatusChanged(
      UUID userId, UserStatus oldStatus, UserStatus newStatus) {
    if (!((!Objects.equals(oldStatus, newStatus)))) {
      Map<String, Object> actual = new LinkedHashMap<>();
      actual.put("newStatus", newStatus);
      actual.put("oldStatus", oldStatus);
      logger.error(
          "contract_violation event={} clause={} actual={}",
          "UserStatusChanged",
          "ensure_0",
          actual);
      throw new ContractViolationError("UserStatusChanged", "ensure_0", actual);
    }
  }
}
