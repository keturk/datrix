package ecommerce_user_service.model.user_db;

import com.fasterxml.jackson.databind.JsonNode;
import jakarta.persistence.CheckConstraint;
import jakarta.persistence.Column;
import jakarta.persistence.Entity;
import jakarta.persistence.FetchType;
import jakarta.persistence.JoinColumn;
import jakarta.persistence.OneToOne;
import jakarta.persistence.Table;
import org.hibernate.annotations.JdbcTypeCode;
import org.hibernate.type.SqlTypes;

/**
 * UserPreferences JPA entity.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Entity
@Table(
    name = "user_preferences",
    indexes = {},
    uniqueConstraints = {},
    check = {
      @CheckConstraint(
          name = "ck_user_preferences_language_max_length",
          constraint = "length(language) <= 10"),
      @CheckConstraint(
          name = "ck_user_preferences_timezone_max_length",
          constraint = "length(timezone) <= 50")
    })
public class UserPreferences extends BaseEntity {

  @Column(name = "language", columnDefinition = "varchar(10)", nullable = false)
  private String language = "en";

  @Column(name = "timezone", columnDefinition = "varchar(50)", nullable = false)
  private String timezone = "UTC";

  @Column(name = "emailNotifications", columnDefinition = "boolean", nullable = false)
  private Boolean emailNotifications = true;

  @Column(name = "smsNotifications", columnDefinition = "boolean", nullable = false)
  private Boolean smsNotifications = false;

  // JSON type for schemaless data
  @JdbcTypeCode(SqlTypes.JSON)
  @Column(name = "preferences", nullable = false)
  private JsonNode preferences;

  @OneToOne(fetch = FetchType.LAZY)
  @JoinColumn(name = "user_id", nullable = false)
  private User user;

  public String getLanguage() {
    return this.language;
  }

  public void setLanguage(String language) {
    this.language = language;
  }

  public String getTimezone() {
    return this.timezone;
  }

  public void setTimezone(String timezone) {
    this.timezone = timezone;
  }

  public Boolean getEmailNotifications() {
    return this.emailNotifications;
  }

  public void setEmailNotifications(Boolean emailNotifications) {
    this.emailNotifications = emailNotifications;
  }

  public Boolean getSmsNotifications() {
    return this.smsNotifications;
  }

  public void setSmsNotifications(Boolean smsNotifications) {
    this.smsNotifications = smsNotifications;
  }

  public JsonNode getPreferences() {
    return this.preferences;
  }

  public void setPreferences(JsonNode preferences) {
    this.preferences = preferences;
  }

  public User getUser() {
    return this.user;
  }

  public void setUser(User user) {
    this.user = user;
  }

  /**
   * Build a new {@link UserPreferences} from a DSL object-literal map, setting only the keys
   * present in {@code data} (a key absent from {@code data} leaves the corresponding field at its
   * normal Java default -- {@code null} unless the field declares its own initializer above). Keys
   * are the AS-AUTHORED DSL field name (Java's own object-literal emission, {@code
   * _datrixOrderedMap(...)}, preserves author-declared spelling -- no case conversion), which for a
   * camelCase-authored DSL field is identical to this entity's own camelCase Java field name.
   *
   * <p>Backs {@code db.UserPreferences.create({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_create`) -- entities have no `Map`-consuming
   * constructor, so the transpiled object literal is fed through this static factory instead of
   * `new UserPreferences(...)`.
   */
  public static UserPreferences fromMap(java.util.Map<String, Object> data) {
    UserPreferences result = new UserPreferences();
    applyMap(result, data);
    return result;
  }

  /**
   * Apply the keys present in a DSL object-literal map onto an EXISTING {@link UserPreferences}
   * instance (a key absent from {@code data} leaves the corresponding field at its CURRENT value,
   * unlike {@link #fromMap} which starts from a freshly constructed instance). Same key convention
   * and per-type coercion as {@link #fromMap}, which delegates here.
   *
   * <p>Backs {@code db.UserPreferences.batchUpdate({...})} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), which loads the existing row by id
   * and merges the update payload onto it rather than discarding fields the payload did not carry.
   */
  public static void applyMap(UserPreferences target, java.util.Map<String, Object> data) {
    if (data.containsKey("language")) {
      Object __v = data.get("language");
      target.setLanguage((String) __v);
    }
    if (data.containsKey("timezone")) {
      Object __v = data.get("timezone");
      target.setTimezone((String) __v);
    }
    if (data.containsKey("emailNotifications")) {
      Object __v = data.get("emailNotifications");
      target.setEmailNotifications((Boolean) __v);
    }
    if (data.containsKey("smsNotifications")) {
      Object __v = data.get("smsNotifications");
      target.setSmsNotifications((Boolean) __v);
    }
    if (data.containsKey("preferences")) {
      Object __v = data.get("preferences");
      target.setPreferences((JsonNode) __v);
    }
  }
}
