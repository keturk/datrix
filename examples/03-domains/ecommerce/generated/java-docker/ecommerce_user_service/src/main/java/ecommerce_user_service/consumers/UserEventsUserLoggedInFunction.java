package ecommerce_user_service.consumers;

import java.time.OffsetDateTime;
import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: user_events_user_logged_in (subscription 'UserEvents', event
 * 'UserLoggedIn').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersUserEventsUserLoggedIn")
public class UserEventsUserLoggedInFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(UserEventsUserLoggedInFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "user_events_user_logged_in",
        "UserLoggedIn");
    UUID userId = (UUID) input.get("userId");
    OffsetDateTime loginAt = (OffsetDateTime) input.get("loginAt");
    String ipAddress = (String) input.get("ipAddress");
    try {
      logger.info("User logged in");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "user_events_user_logged_in",
          "UserLoggedIn");
    }
  }
}
