package ecommerce_user_service.consumers;

import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: user_events_user_registered (subscription 'UserEvents', event
 * 'UserRegistered').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersUserEventsUserRegistered")
public class UserEventsUserRegisteredFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(UserEventsUserRegisteredFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "user_events_user_registered",
        "UserRegistered");
    UUID userId = (UUID) input.get("userId");
    String email = (String) input.get("email");
    String fullName = (String) input.get("fullName");
    try {
      logger.info("User registered");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "user_events_user_registered",
          "UserRegistered");
    }
  }
}
