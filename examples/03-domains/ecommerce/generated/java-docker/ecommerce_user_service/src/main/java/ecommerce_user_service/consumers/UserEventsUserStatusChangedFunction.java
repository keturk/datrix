package ecommerce_user_service.consumers;

import ecommerce_user_service.enums.UserStatus;
import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: user_events_user_status_changed (subscription 'UserEvents',
 * event 'UserStatusChanged').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersUserEventsUserStatusChanged")
public class UserEventsUserStatusChangedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(UserEventsUserStatusChangedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "user_events_user_status_changed",
        "UserStatusChanged");
    UUID userId = (UUID) input.get("userId");
    UserStatus oldStatus = (UserStatus) input.get("oldStatus");
    UserStatus newStatus = (UserStatus) input.get("newStatus");
    try {
      logger.info("User status changed");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "user_events_user_status_changed",
          "UserStatusChanged");
    }
  }
}
