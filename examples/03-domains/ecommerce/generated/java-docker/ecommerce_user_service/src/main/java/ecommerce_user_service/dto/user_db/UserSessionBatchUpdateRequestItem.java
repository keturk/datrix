package ecommerce_user_service.dto.user_db;

import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * UserSession batch-update-request DTO -- carries the row's own {@code id} alongside every field
 * {@link UserSessionUpdateRequest} allows (server-managed and immutable fields excluded
 * identically), so a batch-update payload item can identify which row to update without widening
 * what a client may change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record UserSessionBatchUpdateRequestItem(
    @NotNull UUID id,
    @Size(max = 255) String token,
    @Size(max = 500) String deviceName,
    @Pattern(regexp = "^(\\d{1,3}\\.){3}\\d{1,3}$|^([0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}$")
        String ipAddress,
    @Size(max = 255) String userAgent,
    OffsetDateTime expiresAt,
    OffsetDateTime lastActivityAt,
    UUID userId) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.UserSession.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link
   * UserSession#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("token", token());
    __m.put("deviceName", deviceName());
    __m.put("ipAddress", ipAddress());
    __m.put("userAgent", userAgent());
    __m.put("expiresAt", expiresAt());
    __m.put("lastActivityAt", lastActivityAt());
    __m.put("userId", userId());
    return __m;
  }
}
