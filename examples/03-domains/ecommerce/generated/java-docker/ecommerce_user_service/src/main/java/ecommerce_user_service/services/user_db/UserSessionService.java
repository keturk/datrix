package ecommerce_user_service.services.user_db;

import ecommerce_user_service.model.user_db.User;
import ecommerce_user_service.model.user_db.UserSession;
import ecommerce_user_service.repositories.user_db.UserSessionRepository;
import ecommerce_user_service.services.ServiceBase;
import jakarta.persistence.EntityManager;
import jakarta.persistence.PersistenceContext;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

/**
 * UserSession service -- request-scoped unit-of-work wrapper over {@link UserSessionRepository }.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Service
public class UserSessionService extends ServiceBase {

  private final UserSessionRepository userSessionRepository;

  @PersistenceContext(unitName = "user_db")
  private EntityManager entityManager;

  public UserSessionService(UserSessionRepository userSessionRepository) {
    this.userSessionRepository = userSessionRepository;
  }

  public Optional<UserSession> get(UUID id) {
    return this.userSessionRepository.findById(id);
  }

  public List<UserSession> list() {
    return this.userSessionRepository.findAll();
  }

  public Optional<UserSession> getByToken(String token) {
    return this.userSessionRepository.findByToken(token);
  }

  public List<UserSession> getByUserId(UUID userId) {
    return this.userSessionRepository.findByUserId(userId);
  }

  public List<UserSession> getByUserIdAndExpiresAt(UUID userId, OffsetDateTime expiresAt) {
    return this.userSessionRepository.findByUserIdAndExpiresAt(userId, expiresAt);
  }

  public Page<UserSession> listByUser(UUID userId, Pageable pageable) {
    return this.userSessionRepository.findByUserId(userId, pageable);
  }

  public User getUserReference(UUID id) {
    return this.entityManager.getReference(User.class, id);
  }

  @Transactional
  public UserSession create(UserSession entity) {
    return this.userSessionRepository.save(entity);
  }

  @Transactional
  public UserSession update(UserSession entity) {
    return this.userSessionRepository.save(entity);
  }

  @Transactional
  public void delete(UUID id) {
    this.userSessionRepository.deleteById(id);
  }
}
