package ecommerce_user_service.dto.user_db;

import ecommerce_user_service.enums.UserRole;
import ecommerce_user_service.enums.UserStatus;
import ecommerce_user_service.schemas.Address;
import jakarta.validation.constraints.Email;
import jakarta.validation.constraints.NotNull;
import jakarta.validation.constraints.Pattern;
import jakarta.validation.constraints.Size;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * User batch-update-request DTO -- carries the row's own {@code id} alongside every field {@link
 * UserUpdateRequest} allows (server-managed and immutable fields excluded identically), so a
 * batch-update payload item can identify which row to update without widening what a client may
 * change relative to the single-item update endpoint.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public record UserBatchUpdateRequestItem(
    @NotNull UUID id,
    @Email String email,
    String passwordHash,
    @Size(max = 100) @Pattern(regexp = "^[A-Za-z][A-Za-z0-9\\\\s'-]{0,99}$") String firstName,
    @Size(max = 100) String lastName,
    @Size(min = 1, max = 20) @Pattern(regexp = "^\\+?[1-9]\\d{1,14}$") String phoneNumber,
    UserRole role,
    UserStatus status,
    OffsetDateTime lastLoginAt,
    OffsetDateTime emailVerifiedAt,
    String emailVerificationToken,
    String passwordResetToken,
    OffsetDateTime passwordResetExpiry,
    Address shippingAddress,
    Address billingAddress) {
  /**
   * Convert the UPDATE-eligible fields (excluding {@code id}) to a DSL object-literal-shaped map,
   * keyed by field name -- backs {@code db.User.batchUpdate(items)} lowering (`transpiler/
   * _entity_query_dispatch.py::transpile_entity_batch_update`), fed through {@link User#applyMap}.
   */
  public java.util.Map<String, Object> toMap() {
    java.util.LinkedHashMap<String, Object> __m = new java.util.LinkedHashMap<>();
    __m.put("email", email());
    __m.put("passwordHash", passwordHash());
    __m.put("firstName", firstName());
    __m.put("lastName", lastName());
    __m.put("phoneNumber", phoneNumber());
    __m.put("role", role());
    __m.put("status", status());
    __m.put("lastLoginAt", lastLoginAt());
    __m.put("emailVerifiedAt", emailVerifiedAt());
    __m.put("emailVerificationToken", emailVerificationToken());
    __m.put("passwordResetToken", passwordResetToken());
    __m.put("passwordResetExpiry", passwordResetExpiry());
    __m.put("shippingAddress", shippingAddress());
    __m.put("billingAddress", billingAddress());
    return __m;
  }
}
