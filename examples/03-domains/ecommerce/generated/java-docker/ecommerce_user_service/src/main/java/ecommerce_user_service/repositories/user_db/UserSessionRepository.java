package ecommerce_user_service.repositories.user_db;

import ecommerce_user_service.model.user_db.UserSession;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import java.util.UUID;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

/**
 * UserSession Spring Data JPA repository.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
public interface UserSessionRepository
    extends JpaRepository<UserSession, UUID>, JpaSpecificationExecutor<UserSession> {

  Optional<UserSession> findByToken(String token);

  List<UserSession> findByUserId(UUID userId);

  List<UserSession> findByUserIdAndExpiresAt(UUID userId, OffsetDateTime expiresAt);

  Page<UserSession> findByUserId(UUID userId, Pageable pageable);
}
