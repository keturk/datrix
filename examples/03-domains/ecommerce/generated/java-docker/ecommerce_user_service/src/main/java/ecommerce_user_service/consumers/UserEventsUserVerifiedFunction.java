package ecommerce_user_service.consumers;

import java.util.Map;
import java.util.UUID;
import java.util.function.Function;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;

/**
 * Serverless subscription handler: user_events_user_verified (subscription 'UserEvents', event
 * 'UserVerified').
 *
 * <p>Platform-agnostic {@code Function<I,O>} bean; the input map carries the event payload fields
 * by name.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
@Component("consumersUserEventsUserVerified")
public class UserEventsUserVerifiedFunction implements Function<Map<String, Object>, Object> {

  private static final Logger logger =
      LoggerFactory.getLogger(UserEventsUserVerifiedFunction.class);

  @Override
  @SuppressWarnings("unchecked")
  public Object apply(Map<String, Object> input) {
    logger.debug(
        "subscription_handler_start handler={} event={}",
        "user_events_user_verified",
        "UserVerified");
    UUID userId = (UUID) input.get("userId");
    String email = (String) input.get("email");
    try {
      logger.info("User verified");
      return null;
    } finally {
      logger.debug(
          "subscription_handler_complete handler={} event={}",
          "user_events_user_verified",
          "UserVerified");
    }
  }
}
