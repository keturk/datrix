package ecommerce_user_service.schemas;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

/**
 * Generated tests for PaginationResult: canonical-constructor round-trip and (when applicable)
 * computed-accessor assertions.
 *
 * <p>Auto-generated by datrix-codegen-java. Do not edit manually.
 */
class PaginationResultTest {

  @Test
  void canonicalConstructorRoundTripsEveryComponent() {
    Integer page = 1;
    Integer perPage = 1;
    Integer total = 1;
    Integer totalPages = 1;
    Boolean hasNext = true;
    Boolean hasPrev = true;
    PaginationResult __datrixInstance =
        new PaginationResult(page, perPage, total, totalPages, hasNext, hasPrev);
    assertEquals(page, __datrixInstance.page());
    assertEquals(perPage, __datrixInstance.perPage());
    assertEquals(total, __datrixInstance.total());
    assertEquals(totalPages, __datrixInstance.totalPages());
    assertEquals(hasNext, __datrixInstance.hasNext());
    assertEquals(hasPrev, __datrixInstance.hasPrev());
  }
}
