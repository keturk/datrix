test:
  tags: [api, catalog, inventory, v1]

  meta: { team: "catalog" }

  healthCheck: { type: http, path: /health, interval: 10s }

development:
  tags: [api, catalog, inventory, v1]

  meta: { team: "catalog" }

  healthCheck: { type: http, path: /health, interval: 10s }

production:
  tags: [api, catalog, inventory, v1]

  meta: { team: "catalog" }

  healthCheck: { type: http, path: /health, interval: 10s }
