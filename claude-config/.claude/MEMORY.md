# MEMORY

## Decision Protocol
Before each action: verify or stop; check or stop; never violate explicit rules; unsure → stop and ask.

## Session Rules
1. Don't assume — look up, read code, verify
2. Check project code state (current + target) before creating tasks
3. Uncertain? Stop and ask — don't proceed
4. Verify grammar constraints — don't assume valid syntax
5. No backward compat — delete old code, don't wrap
